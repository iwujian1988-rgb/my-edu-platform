# 问题解决思路复盘

## 为什么失败

**根本原因：** 没有科学的调试方法，靠猜谜而不是系统性分析。

## 核心问题清单

### ❌ 1. 没有信息收集就动手

**错误做法：**
- 看到错误就开始改代码
- 没有先问清楚：什么时候出现？什么情况下出现？持续多久？
- 没有收集完整信息：本地环境、生产环境、日志、截图

**正确做法：**
```
1. 先问清楚问题：
   - 什么时候开始出现的？
   - 影响范围多大？
   - 持续多久了？
   - 复现步骤是什么？

2. 收集对比数据：
   - 本地环境 vs 生产环境
   - 正常的页面 vs 不正常的页面
   - 能工作的操作 vs 不能工作的操作

3. 再动手分析
```

### ❌ 2. 猜谜式排查，而不是科学排查

**错误做法：**
- 看到 401 → 认为是 cookies 问题 → 修改 cookie 配置
- 看到 cookies 没了 → 认为是被清除 → 找清除代码
- 看到首页正常 → 认为是单词列表页特殊 → 检查单词列表页代码

**正确做法：**
```
1. 列出所有假设
2. 为每个假设设计验证方法
3. 用实验证明或推翻假设
4. 逐步缩小范围
```

### ❌ 3. 没有控制变量法

**错误做法：**
- 一次改很多东西
- 不知道哪个修改影响了什么
- 改了之后没测试就继续改下一个

**正确做法：**
```
1. 确定变量：本地 vs 生产
2. 只改一个变量，保持其他不变
3. 观察结果
4. 用二分法缩小范围
```

### ❌ 4. 没有理解现象，就急于解决

**错误做法：**
- 用户说"刷新就丢"，我就改刷新相关的代码
- 用户说"翻页就丢"，我就改翻页相关的代码
- 没有理解："进去就没了"是什么意思

**正确做法：**
```
1. 先理解现象：
   - "进去就没了" = 页面加载时？
   - 还是加载后某个操作？
   - 还是过了一段时间？

2. 询问细节：
   - 能看到页面内容吗？
   - cookies 是什么时候消失的？
   - F12 Network 有失败的请求吗？

3. 再定位问题
```

### ❌ 5. 没有画图/写流程

**错误做法：**
- 在脑子里想象调用链
- 看一段代码就下结论
- 没有画出完整的数据流

**正确做法：**
```
1. 画出调用链：
   登录 → 设置cookies → 首页 → 单词列表页

2. 标记出可疑点：
   - 首页正常 → 首页的调用链没问题
   - 单词列表页异常 → 这个调用链有什么不同？

3. 对比每一个环节
```

### ❌ 6. 没有最小化复现

**错误做法：**
- 一上来就改生产环境的代码
- 没有在本地先验证问题
- 没有隔离问题

**正确做法：**
```
1. 能在本地复现吗？
2. 最小化复现步骤
3. 去掉无关的因素
4. 找到最小可复现的用例
```

### ❌ 7. 缺乏日志和监控

**错误做法：**
- 不知道在哪个环节出问题
- 没有添加日志来追踪
- 只看代码，不看运行时行为

**正确做法：**
```
1. 关键位置添加日志
2. 追踪 cookies 的设置和读取
3. 记录 API 调用链
4. 用数据说话
```

### ❌ 8. 没有阶段性成果

**错误做法：**
- 追求一次解决
- 没有分阶段验证
- 改了很多，但不知道哪步有效

**正确做法：**
```
1. 目标分解：
   - 第一阶段：确认 cookies 设置是否成功
   - 第二阶段：确认首页是否正常
   - 第三阶段：确认单词列表页哪里不同

2. 每个阶段验证后再进入下一阶段
```

## 科学的解决问题流程

### 第一步：信息收集（必须做，不能跳过）

**必须收集的信息：**
- ✅ 问题什么时候出现的？（最近才开始？一直存在？）
- ✅ 影响范围多大？（所有页面？部分页面？）
- ✅ 复现步骤是什么？
- ✅ 本地环境能复现吗？
- ✅ 有日志吗？有错误信息吗？
- ✅ 生产环境 vs 本地环境的差异？

### 第二步：现象理解（必须搞清楚）

**必须理解清楚：**
- ✅ 正确理解用户描述的每一个词
- ✅ 问清楚细节和边界情况
- ✅ 观察：F12 Network, Console, Application
- ✅ 确认：是在哪个时刻出现问题的？

### 第三步：建立假设（可以有多个）

**可以有多个假设，但必须能被验证：**
- 假设A：登录时 cookies 没设置好
- 假设B：某个操作清除了 cookies
- 假设C：环境配置不同导致行为不同

**不要说"应该是"，要说"可能是"**

### 第四步：设计实验验证假设

**每个假设都要设计验证方法：**
- 实验1：登录后检查 cookies（验证假设A）
- 实验2：进入单词列表页时监控 cookies（验证假设B）
- 实验3：对比本地和生产（验证假设C）

**用数据证明或推翻，不要猜**

### 第五步：缩小范围

**使用科学方法：**
- ✅ 二分法：注释掉一半功能，看问题还在吗
- ✅ 控制变量：只改一个条件，保持其他不变
- ✅ 逐步缩小到最小可复现用例

### 第六步：定位根因

**找到具体位置：**
- 具体哪行代码
- 具体哪个操作
- 具体哪个时刻

**用日志、断点、监控数据证明**

### 第七步：验证修复

**不要急着提交，先验证：**
- 修复后，验证问题解决
- 检查是否引入新问题
- 在本地验证，再到生产验证

## 下次必须遵守的规则

### ❌ 绝对不要做的事

1. **不要过早说"找到问题了"**
   - 除非你能用数据证明
   - 除非你的修复经过了验证

2. **不要连续尝试超过3次同一个方向**
   - 如果 API route 不行，Server Action 也不行，客户端也不行
   - 说明这个方向可能根本就是错的
   - 应该停下来，重新分析问题本质

3. **不要忽略用户说的关键信息**
   - 用户说"本地能工作" → 这是最重要的线索
   - 用户说"你再思考下" → 说明我的分析有问题
   - 用户质疑你的方案 → 认真考虑，不要辩解

4. **不要假装在思考，实际还是在老路上**
   - 用户能看出来
   - 要真的换个角度，或者承认需要帮助

5. **不要说"应该是"、"肯定是"**
   - 这些词意味着盲目自信
   - 要说"可能是"、"根据XXX，很可能"

6. **不要一次改很多东西**
   - 你不知道哪个修改导致了什么
   - 要小步迭代，每次改一点点

7. **不要连续失败3次还继续**
   - 应该停下来
   - 承认需要不同视角
   - 不要浪费用户时间

### ✅ 必须要做的事

1. **先收集完整信息，再分析**
   - 列出所有已知事实
   - 画出调用链/数据流
   - 标记出可疑点

2. **用数据说话，不要凭感觉**
   - 日志、截图、代码、实验结果
   - 不是"我觉得"、"应该"

3. **验证假设，不要假设**
   - 为每个假设设计验证方法
   - 用实验证明或推翻

4. **分阶段解决问题**
   - 第一阶段：复现问题
   - 第二阶段：定位问题
   - 第三阶段：修复问题
   - 每个阶段验证后再进入下一阶段

5. **及时止损**
   - 用户第一次说"你再思考下" → 停下来，重新分析
   - 用户质疑你的方案 → 认真考虑
   - 连续失败3次 → 建议寻求不同视角

6. **画图、列表、写日志**
   - 可视化调用链
   - 列出差异点
   - 添加追踪日志

7. **阶段性验证**
   - 改一点，测试一点
   - 确认这一步有效再进行下一步
   - 不要改一堆后再测试

## 沟通用语

### 避免说的话
- ❌ "找到问题了"（除非验证过）
- ❌ "应该是XXX"
- ❌ "肯定是XXX"
- ❌ "我觉得XXX"（除非标注"需要验证"）

### 应该说的话
- ✅ "可能是XXX，需要验证"
- ✅ "根据日志YYY，很可能是ZZZ"
- ✅ "我需要收集更多信息"
- ✅ "让我设计一个实验来验证这个假设"
- ✅ "连续失败3次了，我需要换个角度"

## 检查清单

动手修改代码前，先确认：
- [ ] 收集了所有必要信息（日志、截图、复现步骤）
- [ ] 理解了问题现象（问清楚细节）
- [ ] 列出了所有可能的假设
- [ ] 设计了验证假设的实验
- [ ] 画出了调用链/数据流
- [ ] 确认了本地和生产的差异点
- [ ] 确定了最小复现步骤

修改代码后：
- [ ] 只改了一个变量
- [ ] 在本地验证了
- [ ] 确认没有引入新问题
- [ ] 记录了修改内容和结果

---

## 核心原则

**用科学方法，而不是猜谜。**

**用数据说话，而不是凭感觉。**

**分阶段验证，而不是一次解决。**

**用户的耐心和时间 > 我的面子。**

# 自定义词库功能 - 冲突分析与整合方案

**日期**: 2026-01-15
**目的**: 分析现有 PRD 与新设计的冲突，提供最小开发工作量的整合方案

---

## 📊 现有 PRD 功能清单（第7章 L4）

### 1. 创建自定义词库
```
创建流：输入名称 -> 创建空书
```
**当前实现**: `src/app/library/new/page.tsx` + `NewBookClient.tsx`
✅ 已完成，无需修改

### 2. 智能录入
```
- API：调用第三方API自动匹配释义、音标
- 配额限制：单用户每日 500 词
- 超额提示："今日额度已用完，请手动输入"
```
**当前实现**: `NewBookClient.tsx` 步骤2
✅ 已完成，无需修改

### 3. 章节管理（第1245-1247行）
```
章节管理：
- 新增：点击添加章节
- 重命名：支持双击章节名修改
- 删除词库：3次确认流程
```
**当前状态**: ⚠️ **未实现**
- 缺少：章节管理 UI 入口
- 缺少：新建章节对话框
- 缺少：章节管理功能

### 4. 单词编辑
**当前 PRD**: ❌ **没有明确描述**
- 只提到"智能录入"批量导入
- 没有提到单个单词编辑
- 没有提到批量编辑
- 没有提到删除单词

### 5. 数据结构（第9.1节）
```
- Chapter 字段：可选
- words.chapter_id：允许为 NULL（默认章节）
- 支持有章节和无章节模式
```
✅ 完全兼容

---

## 🆕 新设计功能清单

### 1. 视图模式切换
```
- 学习模式：单词卡片列表（现有）
- 编辑模式：表格编辑视图（新增）
```
**优先级**: 🔴 高

### 2. 表格编辑视图
```
- 7列：选择、章节、单词、音标、词性、中文释义、操作
- Inline 编辑（直接在表格里修改）
- 分页：50词/页
```
**优先级**: 🔴 高

### 3. 章节管理对话框
```
- 章节列表展示
- 新建章节
- 编辑章节（重命名、排序）
- 删除章节
```
**优先级**: 🔴 高

### 4. 批量操作
```
- 批量删除单词
- 批量移动章节
- 批量添加单词
```
**优先级**: 🟡 中

### 5. 键盘快捷键
```
- ↑↓ 切换行
- Enter 保存
- Esc 取消
```
**优先级**: 🟢 低

---

## ⚡ 冲突点分析

### 冲突 1: 章节管理入口

**现有 PRD**:
- 只提到"点击添加章节"
- 没说明在哪里点击

**新设计**:
- 在表格编辑视图顶部有"管理章节"按钮

**整合方案**:
```
方案A（推荐）：
1. 在词库详情页顶部添加"管理章节"按钮（与筛选器并排）
2. 位置：[主题筛选] [场景筛选] [章节筛选] [📁 管理章节] ← 新增
3. 好处：任何视图模式下都可以管理章节

方案B：
1. 只在表格编辑视图顶部显示"管理章节"按钮
2. 好处：编辑模式专属功能
3. 缺点：学习模式下无法快速管理章节
```

**推荐**: 方案A（更灵活）

---

### 冲突 2: 章节重命名方式

**现有 PRD**:
- "支持双击章节名修改"

**新设计**:
- 点击"编辑"按钮，弹出对话框

**整合方案**:
```
同时支持两种方式：
1. 双击章节名：直接变为输入框，快速重命名
2. 点击编辑按钮：打开对话框（可同时修改名称和排序）
```

**无需冲突**：两种方式互补

---

### 冲突 3: 删除词库 vs 删除章节

**现有 PRD**:
- 删除词库：3次确认流程

**新设计**:
- 删除章节：2次确认流程
- 删除单词：2次确认流程

**整合方案**:
```
保持不同的确认级别：
- 删除词库：3次确认（最危险操作）
- 删除章节：2次确认（影响多个单词）
- 删除单词：2次确认（单个删除）
- 批量删除单词：2次确认（批量操作）
```

**无需冲突**：合理的分级设计

---

### 冲突 4: 单词编辑方式

**现有 PRD**:
- ❌ 没有提到单词编辑功能

**新设计**:
- 提供表格编辑视图
- 提供 Inline 编辑

**整合方案**:
```
新设计是现有 PRD 的补充，不是冲突：
- 现有：批量导入（智能录入）
- 新增：单个/批量编辑（表格视图）
```

**无需冲突**：功能扩展

---

## 🎯 最小开发工作量整合方案

### Phase 1: 章节管理（核心功能）

**为什么先做章节？**
- 现有 PRD 已经提到章节管理（未实现）
- 表格编辑依赖章节功能
- 数据库需要先创建章节表

**开发内容**:

#### 1.1 数据库迁移（0.5天）
```sql
-- 创建章节表
CREATE TABLE chapters (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  book_id UUID NOT NULL REFERENCES books(id) ON DELETE CASCADE,
  title VARCHAR(50) NOT NULL,
  order_index INTEGER NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(book_id, order_index)
);

-- 添加索引
CREATE INDEX idx_chapters_book_id ON chapters(book_id);

-- 添加 RLS 策略
ALTER TABLE chapters ENABLE ROW LEVEL SECURITY;

-- 章节列表关联到 books 表
ALTER TABLE books ADD COLUMN total_chapters INTEGER DEFAULT 0;
```

#### 1.2 章节管理对话框（1-2天）
**组件**: `src/components/ChapterManagementDialog.tsx`

**UI**:
```
┌─────────────────────────────────┐
│  📚 章节管理          [×]       │
├─────────────────────────────────┤
│  [+ 新建章节]                   │
│                                 │
│  📖 默认章节    120词  [编辑]   │
│  📖 第1章       50词  [编辑]   │
│  📖 第2章       80词  [编辑]   │
│                                 │
│  总计：250个单词 / 3个章节      │
└─────────────────────────────────┘
```

**功能**:
- 显示章节列表
- 新建章节
- 编辑章节（弹对话框）
- 删除章节（2次确认）
- 双击重命名（快捷方式）

#### 1.3 章节管理入口（0.5天）
**位置**: 词库详情页顶部

**代码**:
```tsx
// src/components/BookDetailPageClient.tsx
<div className="filter-bar">
  <ThemeFilter />
  <ScenarioFilter />
  <ChapterFilter />

  {/* 新增：管理章节按钮 */}
  {book.is_official === false && book.created_by === user.id && (
    <button onClick={openChapterManagement}>
      📁 管理章节
    </button>
  )}

  <StatusFilter />
</div>
```

#### 1.4 章节管理 API（1天）
**文件**: `src/app/api/chapters/route.ts`

**接口**:
- `GET /api/books/{bookId}/chapters` - 获取章节列表
- `POST /api/chapters` - 创建章节
- `PUT /api/chapters/{id}` - 更新章节
- `DELETE /api/chapters/{id}` - 删除章节

**Phase 1 总工作量**: 3-4天

---

### Phase 2: 表格编辑视图（核心功能）

**为什么后做表格？**
- 依赖章节功能
- 是新增功能，不影响现有流程
- 可以逐步迁移用户

**开发内容**:

#### 2.1 视图模式切换（0.5天）
**组件**: `src/components/ViewModeToggle.tsx`

**UI**:
```
[📖 学习模式] [📊 编辑模式]
```

**功能**:
- URL 参数：`?view=learn` 或 `?view=edit`
- 切换时不刷新页面
- 根据模式渲染不同组件

#### 2.2 表格编辑视图（2-3天）
**组件**: `src/components/WordEditTable.tsx`

**UI**: 见原型 HTML

**功能**:
- 7列表格
- 分页：50词/页
- Inline 编辑（失去焦点保存）
- 视觉反馈（绿色边框闪烁）

#### 2.3 表格数据 API（1天）
**文件**: `src/app/api/books/{bookId}/words/route.ts`

**接口**:
- `GET /api/books/{bookId}/words?page=1&limit=50&view=edit`

**响应格式**:
```json
{
  "success": true,
  "data": {
    "words": [...],
    "pagination": {
      "page": 1,
      "limit": 50,
      "total": 500,
      "totalPages": 10
    }
  }
}
```

#### 2.4 单词更新 API（0.5天）
**文件**: `src/app/api/words/{wordId}/route.ts`

**接口**:
- `PUT /api/words/{wordId}` - 更新单词
- `DELETE /api/words/{wordId}` - 删除单词

**Phase 2 总工作量**: 4-5天

---

### Phase 3: 批量操作（增强功能）

**开发内容**:

#### 3.1 批量选择（0.5天）
- 表头全选 checkbox
- 每行 checkbox
- 选中行高亮

#### 3.2 批量删除（0.5天）
- 批量操作栏（显示已选数量）
- 批量删除按钮
- 2次确认

#### 3.3 批量移动章节（0.5天）
- 批量移动按钮
- 章节选择对话框
- API: `PUT /api/words/batch`

#### 3.4 批量添加单词（1天）
- 复用现有智能录入逻辑
- 添加章节选择

**Phase 3 总工作量**: 2.5天

---

### Phase 4: 优化功能（可选）

**开发内容**:

#### 4.1 键盘快捷键（0.5天）
- ↑↓ 切换行
- Enter 保存
- Esc 取消

#### 4.2 搜索功能（0.5天）
- 搜索框
- API 搜索支持

#### 4.3 移动端适配（1天）
- 小屏幕切换到卡片视图
- 响应式布局

**Phase 4 总工作量**: 2天

---

## 📋 整合后的完整功能流程

### 流程 1: 创建自定义词库（已有）
```
1. 用户点击"新建词库"
2. 输入标题、描述
3. 点击创建 ✅
4. 跳转到步骤2（智能录入）✅
```
**无需修改**

### 流程 2: 添加单词（已有）
```
1. 输入单词列表（每行一个）
2. 点击"智能识别并导入" ✅
3. 自动获取释义、音标 ✅
4. 导入到词库 ✅
```
**无需修改**

### 流程 3: 管理章节（新增）
```
1. 用户在词库详情页点击"📁 管理章节"（新增）
2. 打开章节管理对话框
3. 点击"+ 新建章节"
4. 输入章节名称，选择排序位置
5. 点击创建
6. 章节列表刷新
```
**新增功能**

### 流程 4: 编辑单词（新增）
```
方式A - 表格编辑：
1. 用户在词库详情页切换到"📊 编辑模式"（新增）
2. 显示表格视图
3. 点击单元格，直接修改
4. 失去焦点自动保存

方式B - 学习模式浏览：
1. 用户在"📖 学习模式"下浏览单词
2. 可以查看单词卡片
3. 可以标记单词状态
```
**新增功能**

---

## 🎨 最终 UI 布局整合

### 词库详情页顶部
```
┌─────────────────────────────────────────────────────────────────┐
│  词库详情页                                                      │
├─────────────────────────────────────────────────────────────────┤
│  [主题筛选] [场景筛选] [章节筛选] [📁 管理章节] [状态筛选]     │
│  ↑                              ↑ 新增按钮                     │
├─────────────────────────────────────────────────────────────────┤
│  [📖 学习模式] [📊 编辑模式]  ← 新增模式切换                  │
└─────────────────────────────────────────────────────────────────┘
```

**关键点**：
1. **"📁 管理章节"按钮**：在任何视图模式下都显示，方便快速管理章节
2. **模式切换**：学习模式（卡片） vs 编辑模式（表格）
3. **筛选栏保持不变**：章节筛选仍然存在，可以选择查看特定章节的单词

---

## 💡 设计决策总结

### 决策 1: 章节管理入口位置
✅ **放在筛选栏**（方案A）
- 原因：任何视图模式下都可以访问
- 好处：灵活，不影响学习流程

### 决策 2: 章节重命名方式
✅ **同时支持双击和编辑按钮**
- 双击：快速重命名
- 按钮：完整编辑（名称+排序）
- 好处：满足不同场景需求

### 决策 3: 删除确认级别
✅ **分级确认**
- 删除词库：3次确认
- 删除章节：2次确认
- 删除单词：2次确认
- 好处：合理的风险控制

### 决策 4: 表格 vs 卡片
✅ **共存，通过模式切换**
- 学习模式：卡片列表（学习状态）
- 编辑模式：表格视图（管理数据）
- 好处：不破坏现有体验

---

## 📊 开发工作量对比

### 方案 A: 完全重构（不推荐）
- 废弃现有卡片列表
- 全部改用表格视图
- 工作量：20-25天
- 风险：破坏现有用户体验

### 方案 B: 共存模式（推荐）✅
- 保留卡片列表（学习模式）
- 新增表格视图（编辑模式）
- 工作量：11-16天
- 风险：低，不影响现有功能

**推荐**: 方案 B

---

## 🚀 实施建议

### 第一阶段（MVP）: 3-4天
1. 创建章节表
2. 实现章节管理对话框
3. 添加"管理章节"按钮
4. 实现章节 API

**交付物**:
- ✅ 用户可以创建/编辑/删除章节
- ✅ 章节筛选器正常工作

### 第二阶段（核心）: 4-5天
1. 实现视图模式切换
2. 实现表格编辑视图
3. 实现 Inline 编辑
4. 实现单词更新 API

**交付物**:
- ✅ 用户可以在表格中编辑单词
- ✅ 支持分页、筛选

### 第三阶段（增强）: 2-3天
1. 实现批量操作
2. 实现批量添加
3. 优化交互反馈

**交付物**:
- ✅ 支持批量删除/移动
- ✅ 提升用户体验

**总工作量**: 9-12天

---

## ✅ 兼容性检查

### 与现有 PRD 完全兼容
- ✅ 创建流程：不变
- ✅ 智能录入：不变
- ✅ 章节筛选：不变
- ✅ 数据结构：不变
- ✅ 学习模式：不变

### 新增功能不冲突
- ✅ 表格编辑视图：新增模式
- ✅ 章节管理对话框：补充实现
- ✅ 批量操作：功能增强

---

## 📝 待确认问题

1. **"管理章节"按钮位置**：筛选栏 or 表格顶部？
   - 推荐：筛选栏（更灵活）

2. **是否需要"编辑模式"**：还是直接在卡片上添加编辑按钮？
   - 推荐：模式切换（职责分离）

3. **默认视图模式**：默认进入学习模式还是编辑模式？
   - 推荐：学习模式（学习优先）

4. **移动端表格**：小屏幕是否支持表格视图？
   - 推荐：切换到卡片视图（响应式）

---

**文档版本**: v1.0
**最后更新**: 2026-01-15
**总结**: 无重大冲突，通过视图模式切换可以完美整合，总工作量 9-12天

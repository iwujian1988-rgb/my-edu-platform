# 学习计划模块需求文档 v4.0

> **版本**: v4.0 (简化版)
> **更新日期**: 2026-01-30
> **核心改进**: 简化参数设置 + 优化学习体验

---

## 📋 目录

1. [核心改进点](#核心改进点)
2. [用户设置界面](#用户设置界面)
3. [每日任务生成逻辑](#每日任务生成逻辑)
4. [词的筛选规则](#词的筛选规则)
5. [未完成任务处理](#未完成任务处理)
6. [结束时间计算](#结束时间计算)
7. [用户完整流程](#用户完整流程)

---

## 核心改进点

### ✨ v4.0 的重大改进

| 改进项 | v3.0（旧版） | v4.0（新版） |
|-------|------------|------------|
| **用户参数** | 2个（每天新学、每天最多） | 2个（每天新学、复习比例） ✨ |
| **学习逻辑** | 复习词多时新学词为0 | 每天都有新学词 ✨ |
| **未完成任务** | 不会重复出现 | 优先作为明天复习词 ✨ |
| **"学过"判断** | 只要有记录就算学过 | 只有"认识"才算学过 ✨ |
| **结束时间** | 静态计算 | 动态更新 ✨ |

---

## 用户设置界面

### 简化后的设置页面

```
┌─────────────────────────────────────────┐
│  创建学习计划                            │
│  CET-4 核心词汇                         │
├─────────────────────────────────────────┤
│  📚 每天新学单词                         │
│  ┌─────┐ ┌──┐ ┌──┐                     │
│  │ 20  │ │-5│ │+5│ 个                  │
│  └─────┘ └──┘ └──┘                     │
│  💡 建议：15-30个/天                     │
├─────────────────────────────────────────┤
│  🔄 复习比例                             │
│  ○ 1:1  新学20，复习20，共40个          │
│  ○ 1:2  新学20，复习40，共60个          │
│  ● 1:3  新学20，复习60，共80个 (推荐)    │
│  ○ 1:4  新学20，复习80，共100个         │
├─────────────────────────────────────────┤
│  📊 预计完成时间                         │
│  ┌─────┐    理想：225 天                │
│  │ 338 │ 天  估计：338 天               │
│  └─────┘    预计：2026年12月3日         │
├─────────────────────────────────────────┤
│  [    开始学习计划 →    ]               │
└─────────────────────────────────────────┘
```

### 参数说明

**1. 每天新学单词（daily_new_words）**
- 范围：1-100个
- 默认：20个
- 作用：决定每天学习多少个新词

**2. 复习比例（review_ratio）**
- 选项：1:1 / 1:2 / 1:3 / 1:4
- 默认：1:3（推荐）
- 作用：决定复习词数量 = 新学词 × 比例

**计算公式**：
```
新学词 = daily_new_words 个
复习词 = min(可用的复习词, daily_new_words × review_ratio) 个
总词数 = 新学词 + 复习词
```

**举例**：
```
设置：新学20个，比例1:3
新学词 = 20个
复习词 = 60个（如果有足够的话）
总词数 = 80个
```

---

## 每日任务生成逻辑

### 核心算法（简化版）

```javascript
// 每天生成今日任务时执行
function 生成今日任务(用户, 单词书) {
  计划 = 获取学习计划(用户, 单词书)

  // 步骤1：获取新学词（固定的数量）
  新学词 = 获取新学词(用户, 单词书, 计划.daily_new_words)
  // 从"从未标记过认识"的词中随机抽取

  // 步骤2：查询到期需要复习的词
  到期的复习词 = 查询复习计划表({
    用户: 用户,
    单词书: 单词书,
    复习日期 <= 今天
  })

  // 步骤3：查询昨天未完成的词（优先复习）
  昨天未完成的词 = 查询昨日未完成任务(用户, 单词书)

  // 步骤4：合并复习词（昨天未完成的优先）
  所有复习词 = [...昨天未完成的词, ...到期的复习词]

  // 步骤5：根据比例计算需要多少复习词
  期望复习词数量 = 计划.daily_new_words × 计划.review_ratio
  复习词 = 所有复习词.slice(0, 期望复习词数量)

  // 步骤6：创建今日任务记录
  今日任务 = {
    日期: 今天,
    新学词: 新学词,
    复习词: 复习词,
    已完成: [],
    全部完成: false
  }

  return 今日任务
}
```

### 关键改进点

**✅ 改进1：每天都有新学词**
```
旧逻辑：
  剩余名额 = 每天最多学习 - 复习词数量
  新学词 = min(每天新学, 剩余名额)
  ❌ 复习词过多时，新学词 = 0

新逻辑：
  新学词 = 每天新学（固定数量）
  复习词 = min(可用的复习词, 每天新学 × 比例)
  ✅ 每天都有新学词
```

**✅ 改进2：未完成的词优先复习**
```
旧逻辑：
  昨天未完成的词 → 不会再出现

新逻辑：
  昨天未完成的词 → 优先作为明天的复习词
  ✅ 确保每个词都学会
```

---

## 词的筛选规则

### "新学词"的判断标准

**🔴 v3.0 旧逻辑（有问题）**：
```javascript
// 查询 word_progress 表
已学过的词 = 查询({
  where: { user_id, book_id }
  // 只要有记录，就算学过
})
未学过的词 = 所有词.filter(词 => !已学过的词.includes(词))
```

**问题**：
- 只要标记过（known/fuzzy/unknown），就算"学过"
- 在单词列表随意标记会永久影响学习计划

**✅ v4.0 新逻辑（修复）**：
```javascript
// 只查询标记为"认识"的词
已学过的词 = 查询({
  where: {
    user_id,
    book_id,
    status: 'known'  // ⭐ 只有"认识"才算学过
  }
})
未学过的词 = 所有词.filter(词 => !已学过的词.includes(词))
```

**好处**：
- 标记"模糊/不认识"的词还可以作为"新学词"再次出现
- 避免误标记永久影响
- 更符合学习规律

### "复习词"的判断标准

```javascript
// 查询 review_schedule 表
到期需要复习的词 = 查询({
  where: {
    user_id,
    book_id,
    next_review_date <= 今天
  },
  orderBy: [
    { next_review_date: 'asc' }  // 过期的优先
  ]
})
```

**复习间隔（艾宾浩斯遗忘曲线）**：
- 第1次标记"认识" → 7天后复习
- 第2次标记"认识" → 15天后复习
- 第3次标记"认识" → 30天后复习
- 标记"模糊/不认识" → 重置为7天后

---

## 未完成任务处理

### 问题描述

**场景**：
```
第1天：任务20个词，用户只学了15个
第2天：那5个未完成的词怎么办？
```

**v3.0 旧逻辑**：
```
❌ 未完成的词不会自动出现
❌ 用户可能永远学不到这些词
```

**v4.0 新逻辑**：
```
✅ 未完成的词优先作为明天的复习词
```

### 实现逻辑

```javascript
// 查询昨天的任务
昨天的任务 = 查询({
  where: {
    user_id,
    book_id,
    task_date: 昨天
  }
})

// 找出未完成的词
所有词 = [...昨天的任务.新学词, ...昨天的任务.复习词]
已完成的词 = 昨天的任务.已完成 || []
昨天未完成的词 = 所有词.filter(词 => !已完成的词.includes(词))

// 今天的复习词 = 昨天未完成的 + 到期需要复习的
今天的复习词 = [
  ...昨天未完成的词,  // 优先
  ...到期需要复习的词
].slice(0, 期望复习词数量)
```

### 举例

```
第1天：
  新学词：20个 (ability, abnormal, about...)
  用户学完：15个
  未完成：5个 (abnormal, about, abroad, absence, absorb)

第2天（假设比例1:3，期望复习60个）：
  新学词：20个 (新的20个)
  复习词：
    - 5个 = 昨天未完成的 (优先) ✨
    - 8个 = 7天前标记的词（到期复习）
    - 47个 = 15天前的词（到期复习）
  总计：20个新学 + 60个复习 = 80个词
```

**好处**：
- ✅ 确保每个词都学会
- ✅ 不会积压太多
- ✅ 学习更连贯

---

## 结束时间计算

### v3.0 旧逻辑（静态）

```javascript
// 创建计划时计算一次，之后不再更新
理想天数 = total_words / daily_new_words
估计天数 = 理想天数 × 1.5
结束日期 = 今天 + 估计天数
```

**问题**：
- 不考虑实际进度
- 用户中断学习后不准确

### v4.0 新逻辑（动态）

```javascript
// 每次查询时动态计算
function 计算结束时间(用户, 单词书) {
  // 查询实际进度
  已完成天数 = 查询({
    where: {
      user_id,
      book_id,
      all_completed: true
    },
    count: true
  })

  已学单词数 = 查询已学单词(用户, 单词书)
  剩余单词数 = total_words - 已学单词数

  // 计划设置
  计划 = 获取学习计划(用户, 单词书)
  每天新学 = 计划.daily_new_words

  // 计算剩余时间
  剩余理想天数 = 剩余单词数 / 每天新学
  剩余估计天数 = 剩余理想天数 × 1.5

  // 更新结束日期
  新的结束日期 = 今天 + 剩余估计天数

  return {
    已完成天数,
    剩余天数: 剩余估计天数,
    结束日期: 新的结束日期,
    进度百分比: 已学单词数 / total_words
  }
}
```

### 显示给用户

```
📊 学习进度
━━━━━━━━━━━━━━━━━━━━━━
已学习：15 天
剩余天数：323 天
预计完成：2026年12月3日
进度：15% (675/4500)

💡 结束时间会根据实际学习进度动态调整
```

---

## 用户完整流程

### 场景1：首次创建学习计划

```
1. 用户在首页点击"开始学习计划"
   ↓
2. 进入创建计划页面
   ↓
3. 设置：
   - 每天新学：20个
   - 复习比例：1:3
   ↓
4. 点击"开始学习计划"
   ↓
5. 创建计划记录（learning_plans 表）
   ↓
6. 跳转到今日任务页
   ↓
7. 自动生成今日任务（20个新学词）
```

### 场景2：每日学习

```
1. 用户打开应用 → 首页显示"今日学习任务"
   ↓
2. 显示：
   - 第15天 / 共338天
   - 今日单词：80个 (20新学 + 60复习)
   - 已完成：0/80
   ↓
3. 点击"开始学习" → 进入卡片/听写模式
   ↓
4. 开始学习：
   - 显示单词
   - 标记"认识" → 移出队列
   - 标记"不认识" → 移到队尾，稍后再出现
   ↓
5. 全部完成 → 跳转到完成页
```

### 场景3：某天没学完

```
第1天：任务80个词，用户只学了60个
   ↓
第2天：打开应用
   ↓
显示今日任务：80个词
  - 20个 = 新词
  - 20个 = 昨天未完成的 ✨
  - 40个 = 到期需要复习的
   ↓
用户继续学习...
```

### 场景4：在单词列表标记词

```
用户在今日任务中学了10个词（标记"认识"）
   ↓
中途退出，去单词列表逛
   ↓
看到第11个词，标记"认识"
   ↓
今日任务进度：11/80 ✅ (自动更新)
   ↓
返回今日任务，从第12个词继续
```

---

## 数据库变更

### 表1: learning_plans（学习计划表）

**字段变更**：
```sql
-- 删除字段
ALTER TABLE learning_plans DROP COLUMN daily_max_words;

-- 新增字段
ALTER TABLE learning_plans ADD COLUMN review_ratio INTEGER NOT NULL DEFAULT 3;
-- review_ratio: 1=1:1, 2=1:2, 3=1:3, 4=1:4

-- 添加约束
ALTER TABLE learning_plans ADD CONSTRAINT check_review_ratio
  CHECK (review_ratio IN (1, 2, 3, 4));

-- 添加注释
COMMENT ON COLUMN learning_plans.review_ratio IS
'复习比例：1=1:1, 2=1:2, 3=1:3, 4=1:4';
COMMENT ON COLUMN learning_plans.daily_new_words IS
'每天新学单词数量（1-100）';
```

**最终字段列表**：
```sql
learning_plans {
  id
  user_id
  book_id
  daily_new_words       -- 每天新学数量（1-100）
  review_ratio          -- 复习比例（1/2/3/4） ✨ 新增
  total_words
  start_date
  estimated_end_date    -- 动态更新 ✨ 改进
  actual_end_date
  status: 'active' | 'paused' | 'completed' | 'delayed'
  created_at
  updated_at
}
```

### 表2: daily_task_records（每日任务记录表）

**新增字段**：
```sql
ALTER TABLE daily_task_records ADD COLUMN uncompleted_words JSONB DEFAULT '[]';
-- 昨天未完成的词ID数组（用于次日优先复习）

COMMENT ON COLUMN daily_task_records.uncompleted_words IS
'未完成的词ID数组（次日优先复习）';
```

---

## API 接口变更

### 创建/更新学习计划

**旧接口（v3.0）**：
```json
POST /api/v3/learning-plan
{
  "bookId": "xxx",
  "dailyNewWords": 20,
  "dailyMaxWords": 50  ← 删除
}
```

**新接口（v4.0）**：
```json
POST /api/v3/learning-plan
{
  "bookId": "xxx",
  "dailyNewWords": 20,
  "reviewRatio": 3      ← 新增（1/2/3/4）
}
```

### 查询学习计划进度

**新接口（v4.0）**：
```json
GET /api/v3/learning-plan/progress?bookId=xxx

Response:
{
  "success": true,
  "data": {
    "completedDays": 15,          // 已完成天数 ✨ 新增
    "remainingDays": 323,          // 剩余天数 ✨ 新增
    "learnedWords": 675,           // 已学单词数 ✨ 新增
    "totalWords": 4500,
    "progressPercentage": 15.0,    // 进度百分比 ✨ 新增
    "estimatedEndDate": "2026-12-03" // 动态计算的结束日期 ✨ 改进
  }
}
```

---

## 迁移方案

### 从 v3.0 升级到 v4.0

**步骤1：数据库迁移**
```sql
-- 1. 删除旧字段
ALTER TABLE learning_plans DROP COLUMN IF EXISTS daily_max_words;

-- 2. 添加新字段
ALTER TABLE learning_plans ADD COLUMN review_ratio INTEGER NOT NULL DEFAULT 3;

-- 3. 添加约束
ALTER TABLE learning_plans ADD CONSTRAINT check_review_ratio
  CHECK (review_ratio IN (1, 2, 3, 4));

-- 4. 为现有数据设置默认值
UPDATE learning_plans SET review_ratio = 3 WHERE review_ratio IS NULL;
```

**步骤2：API 向后兼容**
```typescript
// 旧接口仍然支持，但转换为新的逻辑
function convertV3ToV4(oldRequest: {
  dailyNewWords: number
  dailyMaxWords: number  // 旧字段
}): {
  dailyNewWords: number
  reviewRatio: number     // 新字段
} {
  // 根据 dailyMaxWords 推算 review_ratio
  const ratio = Math.round(oldRequest.dailyMaxWords / oldRequest.dailyNewWords)
  return {
    dailyNewWords: oldRequest.dailyNewWords,
    reviewRatio: Math.min(Math.max(ratio, 1), 4) // 限制在1-4
  }
}
```

**步骤3：前端UI更新**
- 删除"每天最多学习"滑块
- 添加"复习比例"单选按钮
- 更新进度显示（动态计算）

---

## 风险评估

| 风险项 | 风险等级 | 缓解措施 |
|-------|---------|---------|
| 现有用户数据迁移 | 🟡 中 | 提供数据库迁移脚本，保留旧数据 |
| 用户习惯改变 | 🟢 低 | 清晰的引导文案，帮助文档 |
| 每天任务量增加 | 🟡 中 | 显示"预计用时"，允许调整比例 |
| 复习词积压 | 🟢 低 | 限制复习词数量，优先处理昨天的 |

---

## 开发优先级

### P0（必须实现）
1. ✅ 数据库迁移脚本
2. ✅ 修改创建/更新学习计划 API
3. ✅ 修改每日任务生成逻辑（新学词固定数量）
4. ✅ 实现"昨天未完成的词"逻辑
5. ✅ 修改"新学词"筛选逻辑（只看"认识"状态）

### P1（重要）
6. ✅ 动态结束时间计算
7. ✅ 前端UI更新（复习比例选择）
8. ✅ 进度页面改进

### P2（可选）
9. ⭕ 预计用时显示
10. ⭕ 学习统计图表

---

## 文档结束

> **下一步行动**:
> 1. 技术评审会议
> 2. 确认数据库迁移方案
> 3. 开始 P0 功能开发
> 4. 编写生产级代码

# 自定义词库管理功能 - 产品需求文档

**版本**: v3.5.0
**日期**: 2026-01-15
**模块**: 自定义词库管理增强
**目标**: 为自定义词库添加高效的单词管理和章节管理功能

---

## 📋 功能背景

### 现有功能回顾

**功能1：新建自定义词库**（已实现）
- 用户输入词库名称和描述
- 创建空词库
- 进入智能录入流程

**功能2：智能录入**（已实现）
- 用户输入单词列表（每行一个）
- 系统自动调用第三方API获取完整数据
  - 单词拼写
  - 音标
  - 中文释义
  - 英文释义
  - 词性
  - 搭配
  - 例句
- 自动创建"默认章节"（如果没有章节）
- 批量插入到词库
- 有每日500词的配额限制

**功能3：词库详情页**（已实现）
- 顶部筛选栏：主题、场景、状态、章节
- 练习模式卡片：卡片背单词、听写模式、消消乐
- 单词卡片列表：展示所有单词，支持标记状态、发音、隐藏中文

### 用户痛点

**痛点1：无法修正错误的单词数据**
- 智能录入时，API返回的数据可能有误
- 例如：音标错误、释义不准确
- 用户无法修正这些错误

**痛点2：无法管理单词的章节归属**
- 智能录入时，所有单词都归入"默认章节"
- 用户无法将单词分配到不同章节
- 无法创建有结构的词库（如：基础词汇、进阶词汇）

**痛点3：无法批量管理单词**
- 想要删除一批单词，必须一个一个删除
- 想要调整单词的章节，必须一个一个编辑
- 缺少高效的管理工具

**痛点4：缺少章节管理功能**
- 现有PRD提到"章节管理"（点击添加章节、双击重命名）
- 但没有具体的交互设计
- 用户不知道在哪里操作

---

## 🎯 核心功能设计

### 功能1：视图模式切换

#### 功能描述
在词库详情页添加两种视图模式：
- **学习模式**：用于浏览单词、标记学习状态、进入练习
- **编辑模式**：用于管理单词数据、调整章节归属、批量操作

#### 用户场景

**场景A：用户想要学习单词**
1. 用户打开词库详情页
2. 默认进入"学习模式"
3. 看到练习模式卡片（卡片背单词、听写模式、消消乐）
4. 看到单词卡片列表
5. 可以标记单词状态（认识/模糊/不认识）
6. 可以点击发音、隐藏中文

**场景B：用户想要管理单词**
1. 用户点击顶部的"📊 编辑模式"按钮
2. 页面切换到表格视图
3. 看到所有单词的详细数据
4. 可以直接在表格里修改单词、音标、释义
5. 可以批量选择单词进行删除、移动章节
6. 完成后切换回"学习模式"

#### 交互设计

**模式切换按钮位置**：
- 词库详情页顶部，筛选栏下方
- 两个按钮并排显示：
  - **📖 学习模式**：默认选中
  - **📊 编辑模式**：未选中时为灰色，选中时为蓝色

**切换规则**：
- 点击"编辑模式"按钮：
  - 隐藏练习模式卡片
  - 隐藏单词卡片列表
  - 显示表格编辑视图
  - 保留当前筛选条件（主题、场景、状态、章节）

- 点击"学习模式"按钮：
  - 显示练习模式卡片
  - 显示单词卡片列表
  - 隐藏表格编辑视图
  - 保留当前筛选条件

**URL状态**：
- 学习模式：`/library/{bookId}` 或 `/library/{bookId}?view=learn`
- 编辑模式：`/library/{bookId}?view=edit`
- 刷新页面后，根据URL参数自动进入对应模式

**默认模式**：
- 用户首次打开词库详情页：默认进入"学习模式"
- 用户切换到"编辑模式"后：刷新页面保持在"编辑模式"
- 用户切换回"学习模式"后：刷新页面保持在"学习模式"

#### 数据流转

**学习模式**：
```
用户打开词库详情页
  ↓
系统读取单词数据（包含章节、状态等信息）
  ↓
渲染单词卡片列表
  ↓
用户可以：
- 标记单词状态（认识/模糊/不认识）
- 点击发音
- 隐藏中文
- 进入练习模式
```

**编辑模式**：
```
用户点击"编辑模式"按钮
  ↓
系统读取相同的单词数据
  ↓
渲染表格视图
  ↓
用户可以：
- 修改单词字段（单词、音标、词性、释义）
- 修改单词所属章节
- 批量选择单词
- 批量删除单词
- 批量移动章节
  ↓
系统实时更新数据库
  ↓
切换回学习模式后，可以看到最新的数据
```

**数据一致性保证**：
- 两种模式操作同一个数据源（words表）
- 编辑模式的修改会立即反映到学习模式
- 学习模式的标记状态也会反映到编辑模式
- 筛选条件在两种模式下保持一致

---

### 功能2：表格编辑视图

#### 功能描述
提供一个类似Excel的表格视图，让用户可以：
- 一次看到多个单词的多个字段
- 直接在表格里编辑（inline editing）
- 批量选择和操作

#### 用户场景

**场景A：修正API返回的错误数据**
1. 用户通过智能录入了单词 "agenda"
2. API返回的音标是错误的：`/ˈædʒendə/`（应该是 `/əˈdʒendə/`）
3. 用户切换到"编辑模式"
4. 在表格中找到"agenda"这一行
5. 点击"音标"这一列的输入框
6. 修改为正确的音标
7. 点击表格外部（或按回车），自动保存
8. 系统显示绿色边框闪烁（保存成功的视觉反馈）
9. 切换回"学习模式"，可以看到修正后的音标

**场景B：批量调整章节归属**
1. 用户之前导入的100个单词都在"默认章节"
2. 用户创建了"基础词汇"和"进阶词汇"两个章节
3. 用户切换到"编辑模式"
4. 在表格中勾选50个单词（基础词汇）
5. 点击"批量移动到章节"按钮
6. 选择"基础词汇"章节
7. 系统将这50个单词移动到"基础词汇"章节
8. 表格中的"章节"列自动更新为"基础词汇"
9. 切换回"学习模式"，章节筛选器可以看到"基础词汇"章节

#### 表格布局

**表格列定义**：
| 列名 | 说明 | 可编辑 | 交互方式 |
|------|------|--------|---------|
| 选择 | 复选框 | ❌ | 点击多选 |
| 章节 | 单词所属章节 | ✅ | 下拉选择 |
| 单词 | 英文单词 | ✅ | 文本输入 |
| 音标 | 国际音标 | ✅ | 文本输入 |
| 词性 | 单词词性 | ✅ | 下拉选择 |
| 中文释义 | 中文释义 | ✅ | 文本输入 |
| 操作 | 编辑/删除按钮 | ❌ | 点击操作 |

**表格操作**：
- **全选**：点击表头的复选框，选中当前页的所有单词
- **单选**：点击每行的复选框，选中/取消选中该单词
- **批量操作栏**：选中单词后，底部显示批量操作按钮
  - "已选 X 个单词"
  - "批量删除"按钮
  - "移动到章节..."按钮

#### Inline 编辑交互

**❗重要说明：表格编辑不调用第三方API**

| 功能 | 是否调用API | 说明 |
|------|------------|------|
| **表格Inline编辑** | ❌ 不调用 | 纯手动编辑，只保存用户输入的内容 |
| **批量添加单词** | ✅ 调用 | 复用现有智能导入API，调用有道词典API |
| **现有智能导入** | ✅ 调用 | 创建词库时调用有道词典API |

**设计原因**：
- 表格编辑用于**修正错误**或**补充信息**，用户手动输入更准确
- 批量添加用于**快速导入**新单词，调用API可以自动获取完整数据
- 两种方式互为补充，满足不同场景需求

**编辑流程**：
1. 用户点击表格中的输入框
2. 输入框获得焦点，边框变蓝色
3. 用户修改内容
4. 用户执行以下操作之一：
   - **按回车**：保存当前字段，焦点跳到下一行
   - **按Tab**：跳到下一个可编辑字段
   - **点击外部**：失去焦点，自动保存
   - **按Esc**：取消修改，恢复原值
5. 系统显示视觉反馈：
   - 保存中：显示loading图标（可选）
   - 保存成功：输入框绿色边框闪烁（200毫秒）
   - 保存失败：输入框红色边框，显示错误提示

**字段验证**：
- **必填字段**：单词、中文释义不能为空
- **可选字段**：音标、词性可以为空
- **错误提示**：
  - 必填字段为空：红色边框 + "不能为空"
  - 格式错误：红色边框 + 错误说明

**章节下拉选择**：
- 选项：当前词库的所有章节
- 格式：章节名称
- 选择后自动保存，并更新章节统计

**词性下拉选择**：
- 选项：n名词、v动词、adj形容词、adv副词、pron代词、prep介词、conj连词
- 选择后自动保存

#### 视觉设计

**表格样式**：
- 背景：白色
- 边框：灰色细线
- 表头：浅灰色背景，加粗文字
- 行高：固定高度，保持整齐
- 选中行：淡蓝色背景
- hover行：浅灰色背景

**输入框样式**：
- 默认：灰色边框
- focus：蓝色边框 + 蓝色阴影
- 保存成功：绿色边框闪烁
- 保存失败：红色边框

**按钮样式**：
- "批量删除"：红色文字 + 删除图标
- "移动到章节"：蓝色文字 + 文件夹图标
- hover：背景色加深

#### 分页设计

**分页控件**：
- 位置：表格底部
- 显示："上一页" | "第 X / Y 页" | "下一页"
- 显示总数："共 Z 个单词"
- 默认每页50个单词
- 可选：10词/页、20词/页、50词/页、100词/页

**筛选保持**：
- 翻页时保持当前筛选条件（主题、场景、状态、章节）
- 切换筛选条件时，重置到第1页

#### 键盘快捷键（可选）

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `↑` / `↓` | 上一行/下一行 | 焦点在表格时可用 |
| `Enter` | 保存当前字段，跳到下一行 | 提升编辑效率 |
| `Tab` | 跳到下一个可编辑字段 | 快速导航 |
| `Esc` | 取消编辑，恢复原值 | 误操作时使用 |
| `Space` | 选中/取消当前行 | 快速选择 |

---

### 功能3：章节管理

#### 功能描述
提供完整的章节管理功能，让用户可以：
- 创建新章节
- 编辑章节（重命名、调整排序）
- 删除章节
- 查看每个章节的单词数量

#### 用户场景

**场景A：创建有结构的词库**
1. 用户创建了"大学英语四级"词库
2. 导入了500个单词，都在"默认章节"
3. 用户点击顶部的"📁 管理章节"按钮
4. 打开章节管理对话框
5. 点击"+ 新建章节"
6. 输入章节名称："第1章 基础词汇"
7. 选择排序位置："作为第1章"
8. 点击"创建"
9. 章节列表显示"第1章 基础词汇"
10. 重复步骤5-9，创建"第2章 核心词汇"、"第3章 高级词汇"
11. 关闭对话框
12. 切换到"编辑模式"，将单词分配到对应章节

**场景B：重命名章节**
**方式1：双击重命名（快捷）**
1. 用户打开章节管理对话框
2. 双击章节名称"第1章 基础词汇"
3. 章节名称变为输入框
4. 用户修改为"第一章 入门词汇"
5. 按回车保存
6. 章节列表更新，筛选器也更新

**方式2：编辑按钮重命名（完整）**
1. 用户打开章节管理对话框
2. 点击章节的"编辑"按钮
3. 打开编辑对话框
4. 同时修改章节名称和排序位置
5. 点击"保存"
6. 章节列表更新

**场景C：删除章节**
1. 用户打开章节管理对话框
2. 点击"第3章 高级词汇"的"删除"按钮
3. 系统弹出第一次确认："确定删除此章节？"
4. 系统显示：该章节有30个单词
5. 用户点击"确认"
6. 系统弹出第二次确认："章节中的单词将移到默认章节，确定继续？"
7. 用户点击"确认删除"
8. 章节删除
9. 该章节的30个单词自动移到"默认章节"
10. 默认章节的单词数量从120增加到150

#### 章节管理入口

**入口位置**：
- 词库详情页顶部，筛选栏右侧
- 图标：文件夹 📁
- 文字："管理章节"
- 显示条件：
  - 只有自定义词库显示
  - 只有词库创建者可以看到
- 样式：按钮样式，与其他筛选器并排

**为什么放在筛选栏？**
- 任何视图模式下都能访问（学习模式和编辑模式）
- 与筛选功能相关（章节筛选）
- 用户可以随时管理章节，不需要切换视图

#### 章节管理对话框

**对话框结构**：
```
┌─────────────────────────────────┐
│  📚 章节管理          [×] 关闭   │
├─────────────────────────────────┤
│                                 │
│  [+ 新建章节]                   │
│                                 │
│  章节列表：                     │
│  📖 默认章节      120词  [编辑] │
│  📖 第1章 基础词汇  50词  [编辑]│
│  📖 第2章 进阶词汇  80词  [编辑]│
│  📖 第3章 高级词汇  30词  [编辑]│
│                                 │
│  总计：280个单词 / 4个章节      │
└─────────────────────────────────┘
```

**交互细节**：
- **新建章节按钮**：全宽显示，醒目的绿色按钮
- **章节列表**：垂直排列，每个章节一行
- **章节信息**：
  - 左侧：章节图标 📖 + 章节名称
  - 右侧：单词数量 + 编辑按钮 + 删除按钮
- **默认章节**：不显示删除按钮（或禁用），提示"默认章节不能删除"
- **统计信息**：底部显示总单词数和总章节数

#### 新建章节对话框

**对话框结构**：
```
┌─────────────────────────────────┐
│  📖 新建章节          [×] 关闭   │
├─────────────────────────────────┤
│                                 │
│  章节名称 *                     │
│  ┌───────────────────────────┐  │
│  │ 第4章 高频词汇           │  │
│  └───────────────────────────┘  │
│                                 │
│  排序位置                       │
│  ┌───────────────────────────┐  │
│  │ 在第3章之后 ▼           │  │
│  └───────────────────────────┘  │
│                                 │
│  [取消]          [创建]        │
└─────────────────────────────────┘
```

**字段说明**：
- **章节名称**：
  - 必填，1-50个字符
  - 不能与现有章节重复
  - 示例："第1章 基础词汇"、"Unit 1"、"动词篇"

- **排序位置**：
  - 必选
  - 选项：
    - "作为第1章"
    - "在第1章之后"
    - "在第2章之后"
    - ...
    - "作为最后一章"
  - 选择后，系统自动计算 `order_index`

**创建逻辑**：
1. 用户填写章节名称
2. 用户选择排序位置
3. 点击"创建"
4. 系统验证：
   - 章节名称不能为空
   - 章节名称不能重复
   - 词库的章节数不能超过100个
5. 系统插入章节记录
6. 对话框关闭
7. 章节列表刷新
8. 词库详情页的章节筛选器刷新

#### 编辑章节对话框

**对话框结构**：
```
┌─────────────────────────────────┐
│  ✏️ 编辑章节          [×] 关闭   │
├─────────────────────────────────┤
│                                 │
│  章节名称 *                     │
│  ┌───────────────────────────┐  │
│  │ 第1章 入门词汇           │  │
│  └───────────────────────────┘  │
│                                 │
│  排序位置                       │
│  ┌───────────────────────────┐  │
│  │ 作为第1章 ▼             │  │
│  └───────────────────────────┘  │
│                                 │
│  [取消]          [保存]        │
└─────────────────────────────────┘
```

**编辑逻辑**：
1. 用户修改章节名称或排序位置
2. 点击"保存"
3. 系统验证：
   - 章节名称不能为空
   - 章节名称不能与其他章节重复
   - 如果修改排序位置，需要重新计算所有章节的排序
4. 系统更新章节记录
5. 对话框关闭
6. 章节列表刷新
7. 词库详情页的章节筛选器刷新
8. 表格视图中的"章节"列刷新

#### 删除章节逻辑

**为什么两次确认？**
- 第一次：确认删除操作
- 第二次：告知用户单词会移到默认章节（避免误操作）

**删除流程**：
1. 用户点击章节的"删除"按钮
2. **第一次确认**：
   - 弹窗："确定删除此章节？"
   - 显示章节名称和单词数量
   - 按钮：[取消] [确认]
3. 用户点击"确认"
4. **第二次确认**：
   - 弹窗："章节中的单词将移到默认章节，确定继续？"
   - 显示：将有多少个单词被移动
   - 按钮：[取消] [确认删除]
5. 用户点击"确认删除"
6. 系统删除章节记录
7. 系统将该章节的所有单词的 `chapter_id` 设置为 `null`（移到默认章节）
8. 系统更新"默认章节"的 `word_count`
9. 系统更新词库的 `total_chapters`（减1）
10. 对话框关闭
11. 章节列表刷新
12. 词库详情页的章节筛选器刷新

**限制**：
- 默认章节不能删除
- 如果是最后一个非默认章节，允许删除（不影响默认章节）
- 删除操作不可撤销

---

### 功能4：批量操作

#### 功能描述
提供批量操作功能，让用户可以：
- 批量选择单词
- 批量删除单词
- 批量移动单词到指定章节
- 批量添加单词

#### 批量选择

**交互方式**：
1. **单选**：点击每行的复选框
2. **全选**：点击表头的复选框，选中当前页的所有单词
3. **取消全选**：再次点击表头的复选框

**选中状态**：
- 选中的行：淡蓝色背景
- 底部显示批量操作栏："已选 X 个单词"
- 切换筛选条件时，清空选中状态

**视觉反馈**：
- 复选框：选中时打勾
- 行背景：淡蓝色 `bg-blue-50`
- 批量操作栏：动态显示/隐藏

#### 批量删除

**用户场景**：
1. 用户在表格视图中筛选出状态为"认识"的单词
2. 共50个单词
3. 用户点击表头的复选框，全选这50个单词
4. 底部显示："已选 50 个单词"
5. 用户点击"批量删除"按钮
6. **第一次确认**：
   - 弹窗："确定批量删除？"
   - 显示：即将删除 50 个单词
7. 用户点击"确认"
8. **第二次确认**：
   - 弹窗："数据不可恢复！"
9. 用户点击"确认删除"
10. 系统删除这50个单词
11. 系统更新词库的 `total_words`（减50）
12. 系统更新相关章节的 `word_count`
13. 从表格中移除这50行（淡出动画）
14. 显示成功提示："已删除 50 个单词"

#### 批量移动章节

**用户场景**：
1. 用户在表格视图中选中100个单词
2. 底部显示："已选 100 个单词"
3. 用户点击"移动到章节..."按钮
4. 打开章节选择对话框
5. 对话框显示：
   - "已选 100 个单词，请选择目标章节："
   - 章节下拉列表
6. 用户选择"第2章 进阶词汇"
7. 点击"确认移动"
8. 系统将这100个单词的 `chapter_id` 更新为"第2章"的ID
9. 系统更新旧章节的 `word_count`（减100）
10. 系统更新新章节的 `word_count`（加100）
11. 表格中的"章节"列实时更新为"第2章 进阶词汇"
12. 显示成功提示："已移动 100 个单词到「第2章 进阶词汇」"

#### 批量添加单词

**入口**：
- 表格编辑视图顶部："批量添加单词"按钮
- 绿色按钮，醒目

**用户场景**：
1. 用户点击"批量添加单词"按钮
2. 打开批量添加对话框
3. 选择章节（下拉列表）
4. 输入单词列表（每行一个）
5. 点击"智能识别并导入"
6. 系统调用智能导入API（复用现有逻辑）
7. 导入成功后：
   - 关闭对话框
   - 刷新表格数据
   - 显示成功提示："已导入 X 个单词"

**✅ 此功能完全复用现有智能导入API**

| 功能点 | 说明 |
|--------|------|
| **API调用** | ✅ 调用现有的 `POST /api/smart-import` |
| **第三方API** | ✅ 调用相同的有道词典API |
| **数据字段** | ✅ 获取完整的8个字段（单词、音标、中英文释义、词性、搭配、例句） |
| **配额系统** | ✅ 共享每日500词的限制 |
| **唯一区别** | 🔸 **增加了章节选择功能**（现有功能：自动归入"默认章节"；新功能：用户可选择章节） |
| **其他逻辑** | ✅ 完全一致（错误处理、进度提示、数据验证等） |

**为什么这样设计？**
- **最小开发成本**：复用已有的、经过测试的导入逻辑
- **数据一致性**：两种导入方式使用相同的数据源和处理流程
- **用户体验一致**：用户在使用批量添加时，体验与创建词库时的智能导入完全相同

---

## 📊 数据流转设计

### 核心原则
1. **单一数据源**：所有功能都操作同一个 `words` 表
2. **实时同步**：编辑模式的修改立即反映到学习模式
3. **统计一致性**：单词数量、章节统计保持准确

### 三种数据修改方式对比

| 方式 | 调用第三方API | 数据来源 | 使用场景 |
|------|--------------|---------|---------|
| **现有智能导入** | ✅ 调用有道API | 自动获取8个字段 | 创建词库时批量导入新单词 |
| **表格Inline编辑** | ❌ 不调用 | 用户手动输入 | 修正API返回的错误数据 |
| **批量添加单词** | ✅ 调用有道API | 自动获取8个字段 | 编辑模式下继续添加新单词 |

### 智能导入流程（现有）

```
用户创建词库 → 输入单词列表
  ↓
调用第三方API获取完整数据
  ↓
检查/创建"默认章节"
  ↓
批量插入到 words 表
  ↓
更新 books 表统计（total_words, total_chapters）
  ↓
更新配额表
  ↓
跳转到词库详情页（学习模式）
```

### 表格编辑流程（新增）

```
用户切换到"编辑模式"
  ↓
从 words 表读取数据
  ↓
渲染表格视图
  ↓
用户修改单元格（如：修改音标）
  ↓
失去焦点时，更新 words 表
  ↓
触发器自动更新章节统计（如果章节改变）
  ↓
显示保存成功提示（绿色边框闪烁）
```

### 批量操作流程（新增）

```
用户选中多个单词
  ↓
点击"批量删除"或"批量移动章节"
  ↓
确认操作（2次确认）
  ↓
批量更新 words 表
  ↓
更新 books 表统计
  ↓
触发器自动更新章节统计
  ↓
刷新表格数据
  ↓
显示成功提示
```

### 章节管理流程（新增）

```
用户点击"管理章节"按钮
  ↓
打开章节管理对话框
  ↓
显示章节列表（从 chapters 表读取）
  ↓
用户点击"新建章节"
  ↓
填写章节名称、选择排序位置
  ↓
插入到 chapters 表
  ↓
刷新章节列表
  ↓
关闭对话框
  ↓
章节筛选器自动更新
```

### 数据一致性保证

#### 1. 单词数据一致性
- **学习模式**和**编辑模式**操作同一个 `words` 表
- 在编辑模式修改后，切换回学习模式，可以看到最新数据
- 在学习模式标记状态后，切换到编辑模式，可以看到最新状态

#### 2. 章节数据一致性
- 创建章节时，自动更新章节筛选器
- 删除章节时，单词自动移到默认章节
- 移动单词到其他章节时，章节统计自动更新

#### 3. 统计数据一致性
- **词库总词数**（`books.total_words`）：
  - 智能导入时：手动更新
  - 批量删除时：手动更新
  - 批量添加时：手动更新
  - 单个编辑时：不更新（数量不变）

- **章节单词数**（`chapters.word_count`）：
  - 使用数据库触发器自动更新
  - 当单词的 `chapter_id` 改变时，自动更新新旧章节的计数

#### 4. 筛选条件一致性
- 从学习模式切换到编辑模式时，保持筛选条件
- 从编辑模式切换到学习模式时，保持筛选条件
- 切换章节后，其他筛选条件保持不变

---

## 🎨 用户体验设计

### 视觉一致性

**颜色规范**：
- 学习模式：使用现有配色（绿色、黄色、红色表示状态）
- 编辑模式：使用蓝色系（编辑、管理）
- 批量操作：使用红色（删除警告）、蓝色（信息）

**图标规范**：
- 学习模式：📖 书本图标
- 编辑模式：📊 表格图标
- 管理章节：📁 文件夹图标
- 批量操作：🗑️ 删除、📦 移动

**按钮样式**：
- 主操作：绿色背景（如："新建章节"、"智能识别并导入"）
- 次要操作：白色背景，灰色边框（如："取消"、"关闭"）
- 危险操作：红色背景或红色文字（如："批量删除"）

### 交互反馈

**保存反馈**：
- 成功：输入框绿色边框闪烁（200毫秒）
- 失败：输入框红色边框 + 错误提示文字
- 保存中：显示loading图标（可选）

**删除反馈**：
- 删除前：2次确认对话框
- 删除中：显示loading遮罩
- 删除后：淡出动画（400毫秒）
- 成功：Toast提示："已删除 X 个单词"

**移动反馈**：
- 移动前：章节选择对话框
- 移动中：显示loading遮罩
- 移动后：表格中的"章节"列实时更新
- 成功：Toast提示："已移动 X 个单词到「章节名」"

### 错误处理

**必填字段验证**：
- 章节名称不能为空：红色边框 + "章节名称不能为空"
- 单词不能为空：红色边框 + "单词不能为空"
- 中文释义不能为空：红色边框 + "中文释义不能为空"

**权限错误**：
- 用户无权限：Toast提示"您只能管理自己创建的词库"
- 官方词库：不显示"管理章节"和"编辑模式"按钮

**网络错误**：
- 保存失败：Toast提示"保存失败，请检查网络连接"
- 提供重试按钮

---

## 🔄 与现有功能的整合

### 入口整合

**词库详情页顶部布局**：
```
┌─────────────────────────────────────────────────────────────────┐
│  词库详情页                                                      │
├─────────────────────────────────────────────────────────────────┤
│  [主题筛选] [场景筛选] [章节筛选] [📁 管理章节] [状态筛选] │
│  ↑ 现有                    ↑ 新增                            ↑ 现有│
├─────────────────────────────────────────────────────────────────┤
│  [📖 学习模式] [📊 编辑模式]  ← 新增模式切换                  │
├─────────────────────────────────────────────────────────────────┤
│  学习模式内容：                                                  │
│  - 练习模式卡片（卡片背单词、听写模式、消消乐）                  │
│  - 单词卡片列表                                                  │
│  或                                                               │
│  编辑模式内容：                                                  │
│  - 表格编辑视图                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**关键点**：
1. **"📁 管理章节"按钮**放在筛选栏，任何模式下都可访问
2. **模式切换按钮**放在筛选栏下方，两种模式并排
3. **学习模式**显示现有内容（练习卡片 + 单词卡片）
4. **编辑模式**显示表格视图

### 交互整合

**从学习模式到编辑模式**：
1. 用户点击"📊 编辑模式"按钮
2. 页面不刷新，只切换视图
3. 练习模式卡片淡出（300毫秒）
4. 表格编辑视图淡入（300毫秒）
5. 当前筛选条件保持（主题、场景、状态、章节）

**从编辑模式到学习模式**：
1. 用户点击"📖 学习模式"按钮
2. 页面不刷新，只切换视图
3. 表格编辑视图淡出（300毫秒）
4. 练习模式卡片淡入（300毫秒）
5. 当前筛选条件保持

**打开章节管理**：
1. 用户点击"📁 管理章节"按钮
2. 打开模态对话框（背景遮罩）
3. 不影响当前视图模式（无论是学习模式还是编辑模式）
4. 关闭对话框后，当前视图保持不变

### 数据流整合

**学习模式 → 编辑模式**：
```
用户在学习模式标记了50个单词为"认识"
  ↓
用户切换到"编辑模式"
  ↓
表格视图读取这50个单词的状态（status = "known"）
  ↓
用户在编辑模式中修改了某个单词的中文释义
  ↓
用户切换回"学习模式"
  ↓
单词卡片显示修改后的中文释义
  ↓
标记状态仍然是"认识"（没有被重置）
```

**智能导入 → 章节管理 → 表格编辑**：
```
用户通过智能导入了100个单词
  ↓
系统自动创建"默认章节"，所有单词归入该章节
  ↓
用户点击"管理章节"，创建"第1章"和"第2章"
  ↓
用户切换到"编辑模式"
  ↓
用户在表格中选中50个单词，批量移动到"第1章"
  ↓
用户切换回"学习模式"
  ↓
用户使用章节筛选器，选择"第1章"
  ↓
只显示这50个单词
```

**表格编辑 → 练习模式**：
```
用户在"编辑模式"中修正了某个单词的音标
  ↓
用户切换到"学习模式"
  ↓
用户点击单词卡片上的"播放"按钮
  ↓
播放修正后的音标
```

---

## 📝 用户操作流程完整示例

### 完整流程1：创建词库并管理章节

```
步骤1：创建词库
用户访问首页 → 点击"新建词库" → 输入名称"我的英语词库" → 点击"创建"

步骤2：智能导入
自动跳转到智能录入页面 → 输入单词列表 → 点击"智能识别并导入" → 导入成功 → 点击"前往词库"

步骤3：管理章节
打开词库详情页 → 点击"📁 管理章节" → 点击"+ 新建章节" → 输入"第1章 基础词汇" → 点击"创建" → 重复创建"第2章"、"第3章" → 关闭对话框

步骤4：切换到编辑模式
点击"📊 编辑模式"按钮 → 页面切换到表格视图

步骤5：分配单词到章节
在表格中选中100个单词 → 点击"移动到章节..." → 选择"第1章 基础词汇" → 点击"确认移动" → 表格刷新

步骤6：切换回学习模式
点击"📖 学习模式"按钮 → 页面切换回卡片视图 → 使用章节筛选器选择"第1章 基础词汇" → 显示这100个单词

步骤7：开始学习
点击"卡片背单词" → 选择"不认识的"范围 → 开始学习
```

### 完整流程2：修正错误的单词数据

```
步骤1：发现问题
用户在"学习模式"下查看单词卡片 → 发现某个单词的音标是错误的

步骤2：切换到编辑模式
点击"📊 编辑模式"按钮 → 页面切换到表格视图

步骤3：定位单词
使用筛选功能快速找到该单词（或直接在表格中找到）

步骤4：修正数据
点击"音标"列的输入框 → 修改为正确的音标 → 点击表格外部（自动保存）

步骤5：验证修正
切换回"📖 学习模式" → 打开该单词的卡片 → 点击"播放"按钮 → 听到修正后的音标
```

---

## 🎯 功能优先级

### P0（必须）
- ✅ 视图模式切换（学习/编辑）
- ✅ 表格编辑视图基础功能
- ✅ Inline 编辑（失去焦点保存）
- ✅ 章节管理对话框（新建、编辑、删除）

### P1（重要）
- ✅ 批量选择
- ✅ 批量删除
- ✅ 批量移动章节
- ✅ 章节筛选器实时更新

### P2（可选）
- ⏸️ 键盘快捷键
- ⏸️ 搜索功能
- ⏸️ 批量添加单词（复用现有智能导入）
- ⏸️ 移动端适配（小屏幕切换到卡片视图）

---

## 📋 功能清单总结

| 功能 | 优先级 | 状态 | 说明 |
|------|--------|------|------|
| 视图模式切换 | P0 | 新增 | 学习模式 ↔ 编辑模式 |
| 表格编辑视图 | P0 | 新增 | 7列表格，支持筛选和分页 |
| Inline 编辑 | P0 | 新增 | 失去焦点自动保存 |
| 章节管理对话框 | P0 | 新增 | 新建、编辑、删除章节 |
| 章节管理入口 | P0 | 新增 | 筛选栏的"📁 管理章节"按钮 |
| 批量选择 | P1 | 新增 | 多选checkbox |
| 批量删除 | P1 | 新增 | 2次确认 |
| 批量移动章节 | P1 | 新增 | 选择章节，自动移动 |
| 批量添加单词 | P2 | 新增 | 复用现有智能导入API |
| 键盘快捷键 | P2 | 新增 | ↑↓ Enter Esc Tab |
| 搜索功能 | P2 | 新增 | 搜索单词、释义 |

---

**文档版本**: v2.0
**最后更新**: 2026-01-15
**主要变更**: 移除所有技术实现细节，专注于功能设计、用户体验和数据流转

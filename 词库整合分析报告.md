# 📊 词库整合分析报告

**创建时间**: 2026-01-14
**项目**: my-edu-platform 词库整合
**目标**: 将新词库（22,503词）整合到现有系统中

---

## 🔍 现状分析

### 现有系统词库
| 词库名 | 词汇量 | 备注 |
|--------|--------|------|
| Core 200 | 200词 | 基础词汇 |
| Core 300 | 300词 | 进阶词汇 |
| Core 520 | 520词 | 完整核心词 |
| KET | ~1000词 | 剑桥少儿英语 |

**现有总计**: ~2,000词

### 新词库
| 词库名 | 词汇量 | 类型 |
|--------|--------|------|
| Master Vocabulary 2026 | 10,827词 | 总库 ⭐ |
| CET-4 | 3,849词 | 考试 |
| CET-6 | 1,956词 | 考试 |
| IELTS | 1,833词 | 考试 |
| TOEFL | 2,940词 | 考试 |
| US K-12 | 413词 | 基础 |
| Livestream Pro | 500词 | 场景 |
| Nail Salon Pro | 185词 | 场景 |

**新增总计**: 22,503词

### 重复情况分析

**重要发现**: 新词库的`tag`字段显示单词属于多个词库

示例：
```json
{
  "word": "abandon",
  "tag": "gk cet4 cet6 ky toefl gre"
}
```

这说明：
- ✅ CET4的单词在CET6、TOEFL、GRE中都有
- ✅ Master Pool是所有词库的合集（Single Source of Truth）
- ❌ 如果独立导入会有大量重复

---

## 🎯 整合策略

### 方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **方案1: Master Pool主表** | 数据统一，避免重复 | 需要大改造 | ⭐⭐⭐⭐⭐ |
| 方案2: 独立词库+关联 | 保留原始结构 | 系统复杂 | ⭐⭐⭐ |
| 方案3: 完全独立 | 实施简单 | 重复单词，体验差 | ⭐⭐ |

---

## 🏆 推荐方案：Master Pool主表 + 多维度标签系统

### 核心思想

**Master Pool作为唯一数据源**，其他词库通过标签筛选实现。

### 数据模型

```sql
-- 单词主表（合并后）
words (
  id UUID PRIMARY KEY,
  word TEXT UNIQUE,
  phonetic TEXT,
  definition TEXT,
  translation TEXT,
  part_of_speech TEXT,
  example_sentence TEXT,
  example_sentence_en TEXT,
  -- 多维度标签
  level TEXT[],              -- 难度等级 [1,2,3,4,5]
  frequency_rank INTEGER,    -- 词频等级 [1-10]
  exam_tags TEXT[],          -- 考试标签 [cet4, cet6, ielts, toefl]
  theme_tags TEXT[],         -- 主题标签
  scene_tags TEXT[],         -- 场景标签 [livestream, nail_salon]
  bnc_frequency INTEGER,     -- BNC语料库频率
  collins_level INTEGER,     -- 柯林斯等级
  -- 元数据
  source_books TEXT[],       -- 来源词库
  created_at TIMESTAMP,
  updated_at TIMESTAMP
)

-- 词库表（虚拟视图，不存储数据）
word_books (
  id UUID PRIMARY KEY,
  title TEXT,
  total_words INTEGER,
  filter_condition TEXT,     -- JSON筛选条件
  -- 例如: {"exam_tags": ["cet4", "cet6"]}
)
```

### 筛选逻辑

```javascript
// 获取CET4词汇
const cet4Words = await db.words.findMany({
  where: {
    exam_tags: { has: 'cet4' }
  }
})

// 获取CET6词汇
const cet6Words = await db.words.findMany({
  where: {
    exam_tags: { has: 'cet6' }
  }
})

// 获取Livestream词汇
const livestreamWords = await db.words.findMany({
  where: {
    scene_tags: { has: 'livestream' }
  }
})
```

---

## 📋 实施计划

### Phase 1: 数据准备（1-2天）

#### 1.1 分析现有单词
```bash
# 提取现有系统所有单词ID
SELECT DISTINCT word FROM words;

# 对比新词库，找出：
# - 新增单词（在新词库但不在现有系统）
# - 重复单词（两边都有）
# - 更完善的单词数据（新词库数据更完整）
```

#### 1.2 数据清洗
- [ ] 统一ID格式（使用UUID或保持自增ID）
- [ ] 合并重复单词的释义
- [ ] 补充缺失字段
- [ ] 标准化音标格式
- [ ] 补充例句

#### 1.3 标签映射
```javascript
// 现有词库 → 新标签系统
{
  "ket": { level: [1,2], exam_tags: ["ket"] },
  "core200": { level: [1], frequency_rank: [1,2,3] },
  "core300": { level: [2], frequency_rank: [2,3,4] },
  "core520": { level: [2,3], frequency_rank: [3,4,5] }
}
```

---

### Phase 2: 数据库迁移（3-5天）

#### 2.1 创建新表结构
```sql
-- 1. 创建新words表
CREATE TABLE words_new (
  -- ... (见上面数据模型)
);

-- 2. 迁移现有单词
INSERT INTO words_new (
  word, phonetic, definition, part_of_speech,
  level, frequency_rank, source_books
)
SELECT
  word, phonetic, definition, part_of_speech,
  ARRAY[1]::INT[], -- 默认难度
  5::INT,          -- 默认词频
  ARRAY['legacy']::TEXT[]
FROM words;

-- 3. 导入Master Pool
-- (处理重复：ON CONFLICT DO UPDATE)
```

#### 2.2 处理重复单词
```sql
-- 重复单词处理策略
INSERT INTO words_new (word, phonetic, definition, ...)
VALUES (...)
ON CONFLICT (word) DO UPDATE SET
  phonetic = EXCLUDED.phonetic,
  definition = CASE
    WHEN words_new.definition IS NULL OR words_new.definition = ''
    THEN EXCLUDED.definition
    ELSE words_new.definition
  END,
  exam_tags = array_distinct(
    words_new.exam_tags || EXCLUDED.exam_tags
  ),
  source_books = array_distinct(
    words_new.source_books || EXCLUDED.source_books
  );
```

#### 2.3 导入其他词库标签
```sql
-- 标记CET4单词
UPDATE words_new
SET exam_tags = array_append(exam_tags, 'cet4')
WHERE word IN (
  SELECT word FROM cet4_words_import
);

-- 标记场景词库
UPDATE words_new
SET scene_tags = array_append(scene_tags, 'livestream')
WHERE word IN (
  SELECT word FROM livestream_import
);
```

---

### Phase 3: 学习进度迁移（1天）

#### 3.1 保护用户进度
```sql
-- 创建进度映射表
CREATE TABLE word_progress_migration (
  old_word_id INTEGER,
  new_word_id UUID,
  user_id UUID,
  status VARCHAR,
  migrated_at TIMESTAMP
);

-- 迁移逻辑
-- 1. 按word匹配（不按ID）
-- 2. 如果单词内容相似度>90%，认为是同一个单词
-- 3. 迁移学习状态
```

#### 3.2 回滚计划
```sql
-- 备份现有进度
CREATE TABLE word_progress_backup AS
SELECT * FROM word_progress;

-- 如果出现问题，可以快速回滚
```

---

### Phase 4: API和前端更新（2-3天）

#### 4.1 API更新
```typescript
// 获取词库单词（新逻辑）
async function getWordsByBook(bookId: string) {
  const book = await getBook(bookId);

  // 根据bookId的filter_condition筛选
  const filter = JSON.parse(book.filter_condition);

  return await db.words.findMany({
    where: buildWhereClause(filter)
  });
}

// 支持多维度筛选
async function getWordsByFilter(filter: {
  level?: number[],
  exam_tags?: string[],
  scene_tags?: string[]
}) {
  return await db.words.findMany({
    where: {
      AND: [
        filter.level ? { level: { in: filter.level } } : {},
        filter.exam_tags ? { exam_tags: { hasEvery: filter.exam_tags } } : {},
        filter.scene_tags ? { scene_tags: { hasEvery: filter.scene_tags } } : {}
      ]
    }
  });
}
```

#### 4.2 前端更新
```typescript
// 词库配置
const books = [
  {
    id: 'book_master',
    title: 'Master Vocabulary 2026',
    filter: { /* 无筛选，显示所有 */ },
    totalWords: 10827
  },
  {
    id: 'book_cet4',
    title: 'CET-4 Core Vocabulary',
    filter: { exam_tags: ['cet4'] },
    totalWords: 3849
  },
  {
    id: 'book_livestream',
    title: 'Livestream Shopping Pro',
    filter: { scene_tags: ['livestream'] },
    totalWords: 500
  }
  // ... 其他词库
];
```

---

### Phase 5: 测试和验证（2天）

#### 5.1 数据完整性测试
```bash
# 测试脚本
1. 检查重复单词数量
2. 检查孤儿单词（有进度但单词不存在）
3. 检查标签一致性
4. 检查学习进度完整性
```

#### 5.2 性能测试
```bash
# 测试查询性能
1. 获取CET4单词（3,849词）< 100ms
2. 获取Master Pool（10,827词）< 200ms
3. 多维度筛选 < 50ms
```

#### 5.3 用户体验测试
```bash
# 真实场景测试
1. 用户从KET切换到CET4，进度是否保留？
2. 搜索"abandon"，是否显示所有相关词库？
3. 学习CET4时，是否只显示CET4单词？
```

---

## ⚠️ 风险点和应对

### 风险1: 学习进度丢失
**风险**: 🔴 高
**影响**: 用户学习进度清零
**应对**:
- ✅ 完整备份`word_progress`表
- ✅ 按word而不是ID映射进度
- ✅ 迁移后逐个用户验证

### 风险2: 数据重复
**风险**: 🟡 中
**影响**: 用户看到重复单词
**应对**:
- ✅ Master Pool作为唯一数据源
- ✅ 其他词库通过标签筛选
- ✅ 前端显示"此词也出现在CET6"

### 风险3: 性能下降
**风险**: 🟡 中
**影响**: 查询变慢
**应对**:
- ✅ 添加索引：`exam_tags`, `scene_tags`, `level`
- ✅ 使用PostgreSQL的数组索引
- ✅ 考虑Redis缓存热门词库

### 风险4: ID冲突
**风险**: 🟢 低
**影响**: 数据导入失败
**应对**:
- ✅ 使用UUID而不是自增ID
- ✅ 或添加前缀：`cet4_123`, `master_456`

---

## 📊 预期效果

### 整合后词库结构

```
Master Vocabulary 2026 (10,827词) - 主表
├── 按难度筛选
│   ├── Level 1 (基础): ~2,000词
│   ├── Level 2 (进阶): ~3,000词
│   ├── Level 3 (中级): ~2,500词
│   ├── Level 4 (高级): ~2,000词
│   └── Level 5 (专业): ~1,327词
├── 按考试筛选
│   ├── KET: ~500词
│   ├── CET4: 3,849词
│   ├── CET6: 1,956词
│   ├── IELTS: 1,833词
│   └── TOEFL: 2,940词
└── 按场景筛选
    ├── Livestream: 500词
    └── Nail Salon: 185词
```

### 商业价值

| 词库组合 | 价格 | 目标用户 | 市场规模 |
|---------|------|----------|----------|
| 基础包 (Level 1-2) | ¥9.9/月 | 初高中生 | 500万 |
| 考试包 (CET4/6) | ¥19.9/月 | 大学生 | 800万 |
| 留学包 (IELTS/TOEFL) | ¥39.9/月 | 留学生 | 100万 |
| 场景包 (Livestream) | ¥99/次 | 直播主播 | 50万 |
| 全通包 (所有词库) | ¥59.9/月 | 终身学习者 | 50万 |

---

## 🎯 下一步行动

### 立即可做
1. ✅ 备份现有数据库
2. ✅ 分析现有单词数据
3. ✅ 设计新表结构

### 本周可做
4. ⏳ 创建数据迁移脚本
5. ⏳ 在测试环境验证
6. ⏳ 准备回滚方案

### 下周可做
7. ⏳ 执行数据迁移
8. ⏳ 更新API和前端
9. ⏳ 用户测试

---

## 📞 需要讨论的问题

1. **ID策略**:
   - 选项A: UUID (推荐)
   - 选项B: 带前缀自增ID (如 `cet4_001`)

2. **学习进度迁移**:
   - 选项A: 按word匹配 (推荐)
   - 选项B: 手动重新学习

3. **现有词库**:
   - 选项A: 保留作为历史记录
   - 选项B: 合并到Master Pool

4. **实施时间**:
   - 选项A: 一次性迁移 (风险高，快)
   - 选项B: 分阶段迁移 (风险低，慢)

---

**请告诉我你的想法，我会根据你的选择调整实施方案！** 🚀

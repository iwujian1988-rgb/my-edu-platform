# Flashcard功能更新 - 测试计划

**测试日期**: 2026-01-12
**测试环境**: Development

---

## 📋 测试前准备

### 1. 执行数据库迁移
在Supabase控制台执行:
```sql
ALTER TABLE user_book_preferences
ADD COLUMN IF NOT EXISTS preferences JSONB DEFAULT '{}'::jsonb;

CREATE INDEX IF NOT EXISTS idx_user_book_preferences_preferences_gin
ON user_book_preferences USING GIN (preferences);
```

### 2. 启动开发服务器
```bash
npm run dev
```

---

## 🧪 功能测试用例

### 测试用例1: API性能优化
**目标**: 验证API响应时间改善

**步骤**:
1. 打开浏览器开发者工具 (F12)
2. 切换到 Network 标签
3. 访问 `/study/{bookId}/flashcards?scope=unknown&shuffle=true`
4. 观察 `/api/words` 请求的响应时间

**预期结果**:
- ✅ 响应时间 < 1秒
- ✅ 只看到2个主要API请求（words和word-progress并行）

**实际结果**: _________

---

### 测试用例2: 单词乱序
**目标**: 验证单词以乱序呈现

**步骤**:
1. 选择"不认识的"范围开始学习
2. 记录前5个单词的顺序
3. 刷新页面
4. 验证单词顺序是否一致

**预期结果**:
- ✅ 单词顺序是乱序的（不是按字母或ID排序）
- ✅ 刷新后顺序保持一致（稳定随机）

**实际结果**: _________

---

### 测试用例3: 范围独立进度
**目标**: 验证每个范围独立记录进度

**步骤**:
1. 选择"不认识的"范围，学习到第5个单词
2. 返回首页
3. 点击统计色块切换到"模糊的"范围
4. 学习到第3个单词
5. 返回首页点击"继续学习"

**预期结果**:
- ✅ 继续学习跳转到"不认识的"范围第5个单词
- ✅ 两个范围的进度互不影响

**实际结果**: _________

---

### 测试用例4: 统计色块切换
**目标**: 验证色块切换功能

**步骤**:
1. 在flashcards页面观察顶部统计色块
2. 点击"认识"色块
3. 确认对话框中点击"确认切换"
4. 验证是否跳转到"认识"范围

**预期结果**:
- ✅ 色块显示各状态单词分布
- ✅ 点击后弹出确认对话框
- ✅ 确认后跳转到新范围
- ✅ 进度条更新为新范围的进度

**实际结果**: _________

---

### 测试用例5: 进度保存和恢复
**目标**: 验证学习进度正确保存

**步骤**:
1. 选择"不认识的"范围学习
2. 标记10个单词后关闭浏览器标签
3. 重新打开首页
4. 点击"继续学习"

**预期结果**:
- ✅ 直接跳转到"不认识的"范围
- ✅ 从第11个单词开始
- ✅ URL包含正确的scope和index参数

**实际结果**: _________

---

### 测试用例6: 例句TTS播放
**目标**: 验证例句朗读功能

**步骤**:
1. 翻转卡片查看背面
2. 点击英文例句的播放按钮
3. 验证是否播放英文例句

**预期结果**:
- ✅ 每个例句右侧有播放按钮
- ✅ 点击播放对应的例句
- ✅ 不影响卡片的翻转状态

**实际结果**: _________

---

### 测试用例7: 空范围处理
**目标**: 验证空范围的显示

**步骤**:
1. 选择一个新词库（没有学习记录）
2. 尝试选择"认识的"范围

**预期结果**:
- ✅ 显示"无单词"提示
- ✅ 按钮禁用状态
- ✅ 无法选择空范围

**实际结果**: _________

---

### 测试用例8: 性能对比
**目标**: 对比优化前后的加载速度

**步骤**:
1. 清除浏览器缓存
2. 访问flashcards页面
3. 观察Network面板的DOMContentLoaded时间
4. 记录总加载时间

**预期结果**:
- ✅ 总加载时间 < 2秒
- ✅ 无明显的阻塞等待

**实际结果**: _________

---

## 🐛 已知问题

### 问题1: 测试文件错误（非关键）
**文件**: `tests/diagnostics/check-invitation-codes.ts`
**错误**: 未闭合的字符串
**影响**: 不影响主功能
**修复**: 需要单独修复测试文件

---

## ✅ 测试检查清单

### API测试
- [ ] `/api/flashcard-progress` GET 正常返回
- [ ] `/api/flashcard-progress` POST 正常保存
- [ ] `/api/words?status=xxx` 正确筛选
- [ ] `/api/words?shuffle=true` 返回乱序结果

### 组件测试
- [ ] FlashcardScopeDialog 正确显示
- [ ] FlashcardStatsBar 色块可点击
- [ ] 确认对话框正常工作

### 集成测试
- [ ] flashcards页面正常加载
- [ ] 范围切换正常工作
- [ ] 进度正确保存和恢复
- [ ] 首页继续学习正确跳转

### 性能测试
- [ ] API响应时间 < 1秒
- [ ] 页面加载时间 < 2秒
- [ ] 无明显卡顿

---

## 📊 测试结果汇总

| 测试项 | 通过 | 失败 | 备注 |
|--------|------|------|------|
| API性能优化 | | | |
| 单词乱序 | | | |
| 范围独立进度 | | | |
| 统计色块切换 | | | |
| 进度保存恢复 | | | |
| 例句TTS播放 | | | |
| 空范围处理 | | | |
| 性能对比 | | | |

**测试人**: _________
**测试日期**: _________
**总体评价**: _________

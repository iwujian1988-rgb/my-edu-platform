# 自定义词库管理系统 - 完整 PRD

**版本**: v3.5.0
**日期**: 2026-01-15
**模块**: 自定义词库管理
**作者**: AI Assistant

---

## 📋 文档概述

本文档整合了两个子功能的完整需求：
1. **表格编辑功能** - 类似 Excel 的单词管理界面
2. **章节管理功能** - 创建、编辑、删除章节

---

## 1. 功能架构

```
自定义词库管理系统
│
├── 【视图模式切换】
│   ├── 学习模式（原有）
│   │   ├── 练习模式卡片
│   │   └── 单词卡片列表
│   └── 编辑模式（新增）
│       ├── 表格编辑视图
│       └── 章节管理对话框
│
├── 【表格编辑功能】
│   ├── Inline 编辑（直接在表格里修改）
│   ├── 批量操作（删除、移动章节）
│   ├── 筛选和搜索
│   └── 分页
│
└── 【章节管理功能】
    ├── 新建章节
    ├── 编辑章节（重命名、排序）
    ├── 删除章节
    └── 单词分配到章节
```

---

## 2. 视图模式切换

### 2.1 入口设计

**位置**：词库详情页顶部，筛选栏下方

**UI 设计**：
```
┌─────────────────────────────────────────────────────────────────┐
│  词库详情页                                                      │
├─────────────────────────────────────────────────────────────────┤
│  [筛选栏：主题 | 场景 | 状态 | 章节]                             │
│                                                                  │
│  [📖 学习模式] [📊 编辑模式]  ← 模式切换按钮                    │
│                                                                  │
│  学习模式时显示：                                                 │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                        │
│  │卡片背单词│  │听写模式 │  │消消乐   │                        │
│  └─────────┘  └─────────┘  └─────────┘                        │
│  单词卡片列表                                                     │
│                                                                  │
│  编辑模式时显示：                                                 │
│  表格编辑视图                                                     │
└─────────────────────────────────────────────────────────────────┘
```

**切换规则**：
- ✅ 自定义词库：显示两个模式切换按钮
- ❌ 官方词库：只显示学习模式，隐藏编辑模式按钮

---

## 3. 表格编辑视图

### 3.1 整体布局

```
┌─────────────────────────────────────────────────────────────────┐
│  词库：我的自定义词库                           [📊 编辑模式]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  【筛选栏】                                                      │
│  [全部章节 ▼] [全部状态 ▼] [50词/页 ▼]                         │
│  🔍 搜索单词...                                    [搜索]        │
│                                                                  │
│  【操作栏】                                                      │
│  [ + 批量添加单词]  [ 📁 管理章节]  [ 💾 保存所有]  [ ↻ 刷新]  │
│                                                                  │
│  【数据表格】                                                    │
│  ┌────┬──────────┬────────┬────┬────────────┬──────────┬─────┐│
│  │ ☑️ │ 章节     │ 单词   │音标│ 词性       │ 中文释义 │ 操作││
│  ├────┼──────────┼────────┼────┼────────────┼──────────┼─────┤│
│  │ ☑️ │ 默认章节 │ agenda │/ə││ n名词      │ 议程，日 │ ✏️ 🗑️││
│  │    │          │        │dʒ  │            │ 程表     │     ││
│  ├────┼──────────┼────────┼────┼────────────┼──────────┼─────┤│
│  │ ☑️ │ 第1章    │compromise│/│ v动词     │ 妥协，   │ ✏️ 🗑️││
│  │    │          │        │ˈkɒm│            │ 折中     │     ││
│  │    │          │        │mprə│            │          │     ││
│  │    │          │        │maɪz│            │          │     ││
│  │    │          │        │/   │            │          │     ││
│  ├────┼──────────┼────────┼────┼────────────┼──────────┼─────┤│
│  │ ☐  │ 第2章    │ abbreviate │ /ə  │ v动词  │ 缩写，   │ ✏️ 🗑️││
│  │    │          │        │ˈbri│            │ 缩短     │     ││
│  │    │          │        │:vie│            │          │     ││
│  │    │          │        │ɪt/ │            │          │     ││
│  └────┴──────────┴────────┴────┴────────────┴──────────┴─────┘│
│                                                                  │
│  【批量操作栏】                                                  │
│  已选 2 个单词    [🗑️ 批量删除]    [📁 移动到章节...]          │
│                                                                  │
│  【分页】                                                        │
│  ← 上一页    第 1 / 10 页    下一页 →                          │
│  共 500 个单词                                                   │
│                                                                  │
│  💡 快捷键：↑↓切换行 | Enter保存 | Esc取消                       │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 表格列定义

| 列名 | 字段 | 宽度 | 对齐 | 控件类型 | 必填 | 说明 |
|------|------|------|------|---------|------|------|
| 选择 | - | 50px | 居中 | Checkbox | - | 多选框 |
| 章节 | `chapter` | 120px | 居左 | 下拉选择 | ❌ | 动态加载章节列表 |
| 单词 | `word` | 150px | 居左 | 文本输入 | ✅ | 英文单词 |
| 音标 | `phonetic` | 150px | 居左 | 文本输入 | ❌ | IPA音标，等宽字体 |
| 词性 | `part_of_speech` | 100px | 居中 | 下拉选择 | ❌ | 预定义词性列表 |
| 中文释义 | `definition` | 200px | 居左 | 文本输入 | ✅ | 中文释义 |
| 操作 | - | 100px | 居中 | 按钮 | - | 编辑/删除 |

### 3.3 Inline 编辑交互

**编辑流程**：
1. 用户点击单元格中的输入框
2. 输入框获得焦点，显示蓝色边框
3. 用户修改内容
4. 用户执行以下操作之一：
   - **按 Enter**：保存当前字段，跳到下一行
   - **按 Tab**：跳到下一个可编辑字段
   - **点击外部**：失去焦点，自动保存
   - **按 Esc**：取消修改，恢复原值
5. 系统显示视觉反馈：
   - 保存中：loading 图标
   - 保存成功：绿色边框闪烁（200ms）
   - 保存失败：红色边框 + 错误提示

**特殊字段**：

**章节字段**：
```
┌─────────────────────────────┐
│ 默认章节               ▼   │ ← 下拉选择器
└─────────────────────────────┘
```

**词性字段**：
```
┌─────────────────────────────┐
│ n名词                 ▼   │
└─────────────────────────────┘
```
选项：n名词、v动词、adj形容词、adv副词、pron代词、prep介词、conj连词、art冠词

**中文释义字段**：
```
┌─────────────────────────────┐
│ 议程，日程表          8/500│ ← 字符计数
└─────────────────────────────┘
```

### 3.4 批量操作功能

**多选**：
- 表头checkbox：全选/取消全选当前页
- 每行checkbox：选中/取消选中该行
- 选中行背景：淡蓝色 `bg-blue-50`

**批量删除**：
1. 选中多个单词
2. 点击"批量删除"按钮
3. 弹出确认对话框（2次确认）
4. 调用 API：`DELETE /api/words/batch`
5. 删除成功：从表格移除，显示成功提示

**批量移动章节**：
1. 选中多个单词
2. 点击"移动到章节..."
3. 弹出章节选择对话框
4. 选择目标章节，确认
5. 调用 API：`PUT /api/words/batch`
6. 更新成功：刷新表格，显示成功提示

---

## 4. 章节管理功能

### 4.1 章节管理对话框

**入口**：表格编辑视图顶部的"管理章节"按钮

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│  📚 章节管理                      [×] 关闭   │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  + 新建章节                         │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  【章节列表】                               │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 📖 默认章节              120词      │   │
│  │                      [编辑] [删除]  │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 📖 第1章 基础词汇        50词      │   │
│  │                      [编辑] [删除]  │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 📖 第2章 进阶词汇        80词      │   │
│  │                      [编辑] [删除]  │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 📖 第3章 高级词汇        30词      │   │
│  │                      [编辑] [删除]  │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  总计：280个单词 / 4个章节                  │
│                                             │
└─────────────────────────────────────────────┘
```

### 4.2 新建章节

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│  📖 新建章节                      [×] 关闭   │
├─────────────────────────────────────────────┤
│                                             │
│  章节名称 *                                 │
│  ┌─────────────────────────────────────┐   │
│  │ 第4章                               │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  排序位置                                   │
│  ┌─────────────────────────────────────┐   │
│  │ 在第3章之后 ▼                       │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌───────────┐  ┌───────────┐              │
│  │   取消    │  │   创建    │              │
│  └───────────┘  └───────────┘              │
└─────────────────────────────────────────────┘
```

**排序位置选项**：
- 作为第1章
- 在第1章之后
- 在第2章之后
- 在第3章之后
- 作为最后一章

### 4.3 编辑章节

**功能**：重命名章节、调整排序位置

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│  ✏️ 编辑章节                      [×] 关闭   │
├─────────────────────────────────────────────┤
│                                             │
│  章节名称 *                                 │
│  ┌─────────────────────────────────────┐   │
│  │ 第1章 入门词汇                      │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  排序位置                                   │
│  ┌─────────────────────────────────────┐   │
│  │ 作为第1章 ▼                         │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌───────────┐  ┌───────────┐              │
│  │   取消    │  │   保存    │              │
│  └───────────┘  └───────────┘              │
└─────────────────────────────────────────────┘
```

### 4.4 删除章节

**流程**：
1. 点击章节的"删除"按钮
2. 第一次确认：`alert("确定删除此章节？")`
3. 第二次确认：`alert("章节中的单词将移到默认章节，确定继续？")`
4. 确认后，调用 API：`DELETE /api/chapters/{chapterId}`
5. 删除成功：
   - 从章节列表移除
   - 该章节所有单词的 `chapter_id` 设置为 NULL
   - 刷新表格和筛选器

**限制**：
- 默认章节不允许删除
- 删除按钮置灰，提示："默认章节不能删除"

---

## 5. API 接口设计

### 5.1 获取单词列表（分页）

**接口**：`GET /api/books/{bookId}/words`

**查询参数**：
```
?page=1
&limit=50
&chapter=chapter-id
&status=known
&search=agenda
&view=edit  // 新增：指定返回格式（表格视图）
```

**响应**：
```json
{
  "success": true,
  "data": {
    "words": [
      {
        "id": "word-uuid-1",
        "word": "agenda",
        "phonetic": "/əˈdʒendə/",
        "part_of_speech": "n名词",
        "definition": "议程，日程表",
        "definition_en": "A list of items to be discussed at a meeting",
        "chapter": "默认章节",
        "chapter_id": null,
        "status": "new"
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 50,
      "total": 500,
      "totalPages": 10
    }
  }
}
```

### 5.2 更新单词

**接口**：`PUT /api/words/{wordId}`

**请求体**：
```json
{
  "word": "agenda",
  "phonetic": "/əˈdʒendə/",
  "part_of_speech": "n名词",
  "definition": "议程，日程表",
  "definition_en": "A list of items to be discussed at a meeting",
  "collocation": "",
  "collocation_en": "",
  "example_sentence": "",
  "example_sentence_en": "",
  "chapter_id": null
}
```

### 5.3 批量更新单词

**接口**：`PUT /api/words/batch`

**请求体**：
```json
{
  "word_ids": ["uuid-1", "uuid-2", "uuid-3"],
  "updates": {
    "chapter_id": "chapter-uuid",
    "part_of_speech": "n名词"
  }
}
```

### 5.4 批量删除单词

**接口**：`DELETE /api/words/batch`

**请求体**：
```json
{
  "word_ids": ["uuid-1", "uuid-2", "uuid-3"]
}
```

### 5.5 获取章节列表

**接口**：`GET /api/books/{bookId}/chapters`

**响应**：
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid-1",
      "title": "默认章节",
      "order_index": 0,
      "word_count": 120
    },
    {
      "id": "uuid-2",
      "title": "第1章 基础词汇",
      "order_index": 1,
      "word_count": 50
    }
  ]
}
```

### 5.6 创建章节

**接口**：`POST /api/chapters`

**请求体**：
```json
{
  "book_id": "book-uuid",
  "title": "第4章 高级词汇",
  "order_index": 4
}
```

### 5.7 更新章节

**接口**：`PUT /api/chapters/{chapterId}`

**请求体**：
```json
{
  "title": "第1章 入门词汇",
  "order_index": 1
}
```

### 5.8 删除章节

**接口**：`DELETE /api/chapters/{chapterId}`

---

## 6. 数据库设计

### 6.1 章节表结构

**表名**：`chapters`

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | UUID | PRIMARY KEY | 章节ID |
| `book_id` | UUID | NOT NULL, FK | 词库ID |
| `title` | VARCHAR(50) | NOT NULL | 章节标题 |
| `order_index` | INTEGER | NOT NULL, UNIQUE | 排序索引 |
| `created_at` | TIMESTAMPTZ | DEFAULT NOW() | 创建时间 |
| `updated_at` | TIMESTAMPTZ | DEFAULT NOW() | 更新时间 |

**索引**：
- `PRIMARY KEY (id)`
- `INDEX (book_id)`
- `UNIQUE INDEX (book_id, order_index)`

### 6.2 单词表关联

**现有 `words` 表字段**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `chapter_id` | UUID | 章节ID（外键，可为NULL） |
| `word` | VARCHAR(100) | 单词拼写 |
| `phonetic` | VARCHAR(100) | 音标 |
| `part_of_speech` | VARCHAR(50) | 词性 |
| `definition` | TEXT | 中文释义 |
| `definition_en` | TEXT | 英文释义 |
| `collocation` | TEXT | 中文搭配 |
| `collocation_en` | TEXT | 英文搭配 |
| `example_sentence` | TEXT | 中文例句 |
| `example_sentence_en` | TEXT | 英文例句 |

---

## 7. 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `↑` / `↓` | 移动到上一行/下一行 |
| `←` / `→` | 移动到上一列/下一列 |
| `Enter` | 保存当前字段，跳到下一行 |
| `Tab` | 跳到下一个可编辑字段 |
| `Shift + Tab` | 跳到上一个可编辑字段 |
| `Esc` | 取消编辑，恢复原值 |
| `Space` | 选中/取消当前行 |
| `Ctrl/Cmd + S` | 保存当前行的所有修改 |

---

## 8. 开发工作量评估

| 模块 | 工作量 | 优先级 |
|------|--------|--------|
| **数据库迁移** | 0.5天 | 🔴 高 |
| **章节管理 MVP** | 2-3天 | 🔴 高 |
| **表格编辑视图 MVP** | 3-4天 | 🔴 高 |
| **Inline 编辑** | 2-3天 | 🔴 高 |
| **批量操作** | 2-3天 | 🟡 中 |
| **筛选和搜索** | 1-2天 | 🟡 中 |
| **键盘快捷键** | 1天 | 🟢 低 |
| **移动端适配** | 2-3天 | 🟢 低 |
| **总计** | **14-20天** | - |

---

## 9. 相关文件

### 前端组件

| 文件 | 说明 |
|------|------|
| `src/components/ViewModeToggle.tsx` | 视图模式切换按钮 |
| `src/components/WordEditTable.tsx` | 表格编辑视图主组件 |
| `src/components/WordTableRow.tsx` | 表格行组件 |
| `src/components/WordTableCell.tsx` | 可编辑单元格组件 |
| `src/components/BatchOperationBar.tsx` | 批量操作栏 |
| `src/components/ChapterManagementDialog.tsx` | 章节管理对话框 |
| `src/components/ChapterFormDialog.tsx` | 新建/编辑章节表单 |
| `src/components/BatchAddWordsDialog.tsx` | 批量添加单词对话框 |

### API 路由

| 文件 | 说明 |
|------|------|
| `src/app/api/books/[bookId]/words/route.ts` | 获取单词列表（分页） |
| `src/app/api/words/[wordId]/route.ts` | 更新/删除单个单词 |
| `src/app/api/words/batch/route.ts` | 批量更新/删除单词 |
| `src/app/api/books/[bookId]/chapters/route.ts` | 获取章节列表 |
| `src/app/api/chapters/route.ts` | 创建/更新/删除章节 |

### 数据库

| 文件 | 说明 |
|------|------|
| `supabase/migrations/20260115_create_chapters_table.sql` | 创建章节表 |
| `supabase/migrations/20260115_add_chapters_rls.sql` | 章节表 RLS 策略 |

---

## 10. 未来扩展方向

- 📤 **导出为 Excel**：将词库导出为 .xlsx 文件
- 📥 **从 Excel 导入**：支持从 Excel 文件批量导入
- 📜 **版本历史**：记录单词的修改历史，支持回滚
- 🤖 **AI 辅助补全**：自动补全释义、例句
- 🔍 **单词去重**：检测并处理重复单词
- 🎯 **拖拽排序**：拖拽行调整单词顺序
- 📊 **统计图表**：章节学习进度统计

---

**文档版本**：v1.0 (整合版)
**最后更新**：2026-01-15

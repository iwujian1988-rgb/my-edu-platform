# AI 编程防护指南

> 最后更新: 2026-03-16
> 适用范围: 所有 AI 辅助编写的代码

---

## 一、AI 编程常见问题

| # | 问题 | 表现 | 危害 |
|---|------|------|------|
| 1 | **碎片化修复** | 每次只修一个点，叠加多个 `🔧 FIX` 注释 | 代码越来越乱，难以维护 |
| 2 | **魔法数字** | `3000`, `15`, `50` 等硬编码常量 | 修改困难，语义不明 |
| 3 | **边界忽略** | 只测 happy path，不测边界情况 | 生产环境出 bug |
| 4 | **多路径逻辑** | 同一功能有 4+ 条不同的代码路径 | 行为不一致，难以测试 |
| 5 | **防御性编程错位** | 客户端做去重，而不是 API 保证一致性 | 掩盖根本问题 |
| 6 | **类型滥用** | 大量使用 `any` 类型 | 类型检查失效 |
| 7 | **错误处理不完整** | 空 catch 或静默失败 | 问题难以排查 |
| 8 | **状态不一致** | 前端 count 和后端不同 | 用户体验混乱 |
| 9 | **过早宣布完成** ⚠️ | 修复一个报错就说"可以测试"，没检查所有相关界面 | 用户信任受损，浪费时间 |
| 10 | **重复定义** | 同一类型/函数在多个文件中定义 | 维护困难，行为不一致 |

---

## 二、代码规范

### 2.1 常量提取（禁止魔法数字）

```typescript
// ❌ 禁止
queryPageSize = 3000
maxIterations: 15
if (words.length > 50) { ... }

// ✅ 必须
/**
 * 单词查询配置
 */
export const WORDS_CONFIG = {
  /** 每页单词数量 */
  PAGE_SIZE: 50,

  /** 乱序模式最大支持的单词数量 */
  MAX_WORDS_FOR_SHUFFLE: 10000,

  /** 数据库查询最大迭代次数（防止无限循环） */
  MAX_DB_QUERY_ITERATIONS: 200,

  /** 单次数据库查询超时时间（毫秒） */
  DB_QUERY_TIMEOUT_MS: 10000,

  /** 进度保存防抖延迟（毫秒） */
  PROGRESS_DEBOUNCE_MS: 1000,
} as const
```

### 2.2 类型约束（禁止 any）

```typescript
// ❌ 禁止
const words: any[] = []
function processWord(word: any) { ... }
const result = await supabase.rpc(...)  // 无类型

// ✅ 必须
interface Word {
  id: string
  word: string
  phonetic: string | null
  uk_phonetic: string | null
  us_phonetic: string | null
  definition: string
  definition_en: string | null
  example_sentence: string | null
  example_sentence_en: string | null
  collocation: string | null
  collocation_en: string | null
  part_of_speech: string | null
  chapter_id: string
  audio_url: string | null
  status?: WordStatus
}

type WordStatus = 'new' | 'unknown' | 'fuzzy' | 'known'

interface GetWordsResult {
  success: boolean
  data: Word[]
  page: number
  pageSize: number
  count: number
  total: number
}
```

### 2.3 错误处理规范

```typescript
// ❌ 禁止
try {
  // ...
} catch (error) {
  console.error('Error:', error)
}

// ✅ 必须
// 定义错误码枚举
enum ErrorCode {
  UNAUTHORIZED = 'UNAUTHORIZED',
  BOOK_NOT_FOUND = 'BOOK_NOT_FOUND',
  WORDS_FETCH_ERROR = 'WORDS_FETCH_ERROR',
  INVALID_PARAMS = 'INVALID_PARAMS',
  DATABASE_ERROR = 'DATABASE_ERROR',
}

try {
  // ...
} catch (error) {
  const errorMessage = error instanceof Error ? error.message : 'Unknown error'
  const errorStack = error instanceof Error ? error.stack : undefined

  // 结构化日志
  logger.error('[getWords] Database query failed', {
    code: ErrorCode.WORDS_FETCH_ERROR,
    bookId,
    status,
    page,
    errorMessage,
    errorStack,
  })

  // 结构化响应
  return NextResponse.json({
    success: false,
    error: '获取单词失败，请稍后重试',
    code: ErrorCode.WORDS_FETCH_ERROR,
  }, { status: 500 })
}
```

---

## 三、单一数据流设计

### 3.1 问题示例（多路径）

```
if (RPC 可用 && status === 'all' && !shuffle)
  → 路径 A: RPC 查询

else if (status === 'new')
  → 路径 B: 特殊处理

else if (status !== 'all')
  → 路径 C: 另一种处理

else if (shuffle)
  → 路径 D: 又一种处理

❌ 问题：每条路径有不同的分页、筛选、性能特征
```

### 3.2 解决方案（统一数据流）

```typescript
/**
 * 统一的单词分页查询函数
 * 所有 status、shuffle 组合都走同一逻辑
 */
async function getWordsPaginated(params: GetWordsParams): Promise<GetWordsResult> {
  const { bookId, status, shuffle, page, pageSize } = params

  // Step 1: 获取所有匹配的 word_id（轻量查询）
  const allIds = await getMatchingWordIds(bookId, status, userId)

  // Step 2: 可选乱序（统一处理）
  const orderedIds = shuffle
    ? seededShuffle(allIds, `${bookId}-${status}`)
    : allIds

  // Step 3: 分页切片（统一处理）
  const startIndex = (page - 1) * pageSize
  const pageIds = orderedIds.slice(startIndex, startIndex + pageSize)

  // Step 4: 获取完整单词数据（统一处理）
  const words = await getWordsByIds(pageIds)

  // Step 5: 返回统一格式
  return {
    success: true,
    data: words,
    page,
    pageSize,
    count: orderedIds.length,  // 匹配条件的总数
    total: bookTotalWords,     // 整本书总数
  }
}
```

---

## 四、边界条件检查清单

每次编写或修改代码时，必须检查以下边界条件：

| # | 边界条件 | 测试方法 | 期望行为 |
|---|---------|---------|---------|
| 1 | 空数据 | 新用户/空词书 | 返回 `data: [], count: 0` |
| 2 | 页码为 0 | `page=0` | 返回第一页或空数组 |
| 3 | 页码超出范围 | `page=1000` | 返回空数组 `data: []` |
| 4 | 无效 bookId | `bookId='invalid'` | 返回 400 或 404 |
| 5 | 未授权访问 | 无 token | 返回 401 |
| 6 | 大数据量 | 10000+ 词的词书 | 响应时间 < 2s |
| 7 | 并发请求 | 同时请求多页 | 每页返回正确数据 |
| 8 | 网络超时 | 模拟慢查询 | 超时后优雅降级 |

---

## 五、开发流程约束

```
┌─────────────────────────────────────────────────────────────────────┐
│  强制开发流程（AI 必须遵循）                                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  Step 1: 先写常量定义                                                │
│  ─────────────────────────────────────────────────────────────────  │
│  • 所有数字、字符串必须有命名                                         │
│  • 必须有注释说明用途                                                 │
│  • 文件: constants.ts                                                │
│                                                                      │
│  Step 2: 先写类型定义                                                │
│  ─────────────────────────────────────────────────────────────────  │
│  • 所有接口必须完整                                                   │
│  • 禁止使用 any                                                      │
│  • 文件: types.ts                                                    │
│                                                                      │
│  Step 3: 先写函数签名和注释                                          │
│  ─────────────────────────────────────────────────────────────────  │
│  • 每个函数必须有：                                                   │
│    - 输入参数类型                                                    │
│    - 返回值类型                                                      │
│    - 边界处理说明                                                    │
│    - 调用示例                                                        │
│                                                                      │
│  Step 4: 再写实现                                                    │
│  ─────────────────────────────────────────────────────────────────  │
│  • 严格按设计实现                                                    │
│  • 不随意添加新逻辑                                                  │
│  • 不引入新的魔法数字                                                │
│                                                                      │
│  Step 5: 写完立即验证                                                │
│  ─────────────────────────────────────────────────────────────────  │
│  • 对照边界条件检查清单                                              │
│  • 运行相关测试                                                      │
│  • 手动测试关键路径                                                  │
│                                                                      │
│  Step 6: 代码审查                                                    │
│  ─────────────────────────────────────────────────────────────────  │
│  • 对照本文档检查                                                    │
│  • 确认无 any 类型                                                   │
│  • 确认无魔法数字                                                    │
│  • 确认单一数据流                                                    │
│  • 确认无重复定义（类型/函数）                                        │
│                                                                      │
│  Step 7: 完整性验证 ⚠️ 强制                                          │
│  ─────────────────────────────────────────────────────────────────  │
│  • 列出所有受影响的界面/功能点                                        │
│  • 逐个检查每个界面的实现状态                                         │
│  • 记录：✅ 已完成 | ⚠️ 部分完成 | ❌ 未完成                          │
│  • 只有全部 ✅ 才能说"可以测试"                                       │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 六、文件结构规范

```
src/app/api/[feature]/
├── route.ts                 # 入口（精简，只做参数校验和调用）
├── [feature]-service.ts     # 核心业务逻辑
├── constants.ts             # 常量定义
├── types.ts                 # 类型定义
└── __tests__/
    └── [feature].test.ts    # 单元测试
```

---

## 七、自检清单

每次提交代码前，对照此清单检查：

| 检查项 | 通过标准 |
|--------|---------|
| [ ] 无魔法数字 | 所有数字都有命名常量 |
| [ ] 无 any 类型 | 所有类型都有明确定义 |
| [ ] 单一数据流 | 同类操作走同一代码路径 |
| [ ] 边界处理 | 所有边界条件都有处理 |
| [ ] 错误处理 | 所有异常都有结构化处理 |
| [ ] 日志完整 | 关键操作有结构化日志 |
| [ ] 注释清晰 | 复杂逻辑有解释 |
| [ ] 测试通过 | 边界测试用例通过 |
| [ ] **无重复定义** ⚠️ | 类型/函数没有在多文件重复定义 |
| [ ] **完整性验证** ⚠️ | 所有相关界面都已检查，不是只修一个 |

---

## 八、历史教训

### 案例 1: 乱序分页失效

**问题**: 使用 `Date.now()` 作为随机种子，每次请求返回不同的单词顺序
**根因**: 没有考虑分页一致性需求
**教训**: 设计时必须考虑"同一会话内一致性"需求

### 案例 2: 3000 词上限

**问题**: 硬编码 `queryPageSize = 3000`，超过 3000 词的词书数据丢失
**根因**: 魔法数字 + 未考虑大数据量场景
**教训**: 所有数字必须是命名常量，必须考虑边界情况

### 案例 3: 客户端去重

**问题**: API 返回重复单词，客户端被迫做去重
**根因**: API 设计缺陷，防御性编程位置错误
**教训**: API 必须保证数据一致性，不应该让客户端补救

### 案例 4: 过早宣布完成 ⚠️ NEW

**问题**: 用户问"法语可以正常测试了吗？"，AI 回答"可以开始测试了"，但实际上只修复了 TTS 报错，6 个界面还没支持法语 gender 显示

**根因**:
1. 只关注用户提到的报错（TTS 500），没有主动检查所有相关界面
2. 没有系统性验证就说"可以测试"
3. 假设"修复一个 = 全部完成"

**教训**:
```
┌─────────────────────────────────────────────────────────────────────┐
│  禁止在没有完整验证的情况下说"可以测试"或"完成了"                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  正确做法：                                                          │
│                                                                      │
│  1. 列出所有相关界面/功能点                                          │
│     例：法语适配涉及 12 个界面，不是 1 个                              │
│                                                                      │
│  2. 逐个验证并记录状态                                               │
│     ✅ 已完成 | ⚠️ 部分完成 | ❌ 未完成                                │
│                                                                      │
│  3. 明确告知用户当前状态                                             │
│     "TTS 已修复，但 6 个界面还没支持法语 gender，需要继续吗？"           │
│                                                                      │
│  4. 全部确认后才说"可以测试"                                         │
│     而不是修完一个就说"好了"                                          │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 案例 5: 重复定义类型 ⚠️ NEW

**问题**: `SupportedLanguage` 类型和 `getWordLanguage()` 函数在 6 个文件中重复定义

**根因**: 修改时直接在当前文件定义，没有先检查是否已存在统一定义

**教训**:
1. 类型/工具函数应统一定义在 `types.ts` 或 `utils.ts`
2. 修改前先搜索是否已有定义
3. 使用 `import { } from '@/types/xxx'` 而不是复制粘贴

---

## 九、宣布完成前的强制检查 ⚠️ NEW

在说"可以测试"或"完成了"之前，**必须**执行以下检查：

### 9.1 功能完整性检查

```
□ 列出所有相关界面/功能点
□ 逐个验证每个界面的实现状态
□ 记录哪些完成、哪些未完成
□ 明确告知用户当前状态
```

### 9.2 界面检查模板

当用户问"X 功能可以测试了吗？"时，AI 必须回复：

```markdown
## X 功能当前状态

| 界面 | 状态 | 说明 |
|-----|------|------|
| 界面1 | ✅ 已完成 | ... |
| 界面2 | ⚠️ 部分完成 | 缺少 Y 功能 |
| 界面3 | ❌ 未完成 | ... |

**结论**: 还不能完整测试，需要先完成界面2和界面3
```

### 9.3 禁止的回复模式

| ❌ 禁止 | ✅ 正确 |
|--------|--------|
| "可以测试了！" | "核心功能已完成，但还有 3 个界面未支持" |
| "完成了！" | "已完成 8/12 个界面，还需要..." |
| "应该可以了" | "已验证 X、Y、Z，W 还需要测试" |
| "修好了，你试试" | "修复了报错，但其他界面可能还有问题" |

---

## 十、数据处理防护（避免 AI 幻觉）⚠️ NEW

### 10.1 核心原则：数据来源必须权威可验证

```
┌─────────────────────────────────────────────────────────────────────┐
│  数据处理三原则                                                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  1. 权威来源                                                         │
│  ─────────────────────────────────────────────────────────────────  │
│  • 不使用 AI 生成的数据作为最终数据源                                  │
│  • 使用学术项目、权威词典、官方数据库                                   │
│  • 例：Lexique 3.0 (CNRS), kaikki.org (Wiktionary), MeCab 等         │
│                                                                      │
│  2. 可追溯性                                                         │
│  ─────────────────────────────────────────────────────────────────  │
│  • 记录每条数据的来源 URL、版本号、日期                                 │
│  • 脚本中包含数据源校验（MD5、行数、格式检查）                           │
│  • 保存原始数据和处理日志                                              │
│                                                                      │
│  3. 分阶段验证                                                       │
│  ─────────────────────────────────────────────────────────────────  │
│  • Phase 1: 下载数据 → 验证完整性                                     │
│  • Phase 2: 构建索引 → 抽样检查                                       │
│  • Phase 3: 生成修复 → 人工审核                                       │
│  • Phase 4: 执行更新 → 验证结果                                       │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 10.2 AI 数据处理常见幻觉

| 幻觉类型 | 表现 | 危害 | 防护措施 |
|---------|------|------|---------|
| **变位推断错误** | AI 猜测 `mange` → `manger` | 可能推断错误 | 使用权威词形还原表 |
| **词性猜测** | AI 根据"经验"标注词性 | 系统性错误 | 使用 Lexique 语法类别 |
| **释义幻觉** | AI 生成"看起来对"的释义 | 误导学习者 | 只用权威词典释义 |
| **频率幻觉** | AI 说"这个词很常用" | 不可信 | 使用 Lexique 频率数据 |
| **阴阳性猜测** | AI 根据"规律"猜阴阳性 | 50% 错误率 | 使用 Lexique gender 字段 |

### 10.3 典型错误案例

```javascript
// ❌ 禁止：AI 推断变位形式
const CONJUGATION_PATTERNS = {
  'mange': 'manger',  // AI "觉得"这是对的
  'parle': 'parler',  // 但可能出错！
}

// ❌ 禁止：AI 生成释义
const AI_GENERATED_DEFINITIONS = {
  'bonjour': '你好',  // AI 生成的，不可信
  'merci': '谢谢',
}

// ✅ 正确：从权威数据源获取
const LEXIQUE_DATA = await fetch('http://www.lexique.org/databases/_json/Lexique383.json')
const wordIndex = buildIndexFromLexique(LEXIQUE_DATA)
// lemme 字段提供权威词元，无需推断
```

### 10.4 数据处理工作流模板

```
┌─────────────────────────────────────────────────────────────────────┐
│  Phase 1: 下载权威数据                                               │
│  ─────────────────────────────────────────────────────────────────  │
│  • 明确数据源 URL 和许可证                                            │
│  • 下载后验证文件大小/行数/格式                                        │
│  • 保存原始文件，不修改                                               │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Phase 2: 构建本地索引                                               │
│  ─────────────────────────────────────────────────────────────────  │
│  • 从权威数据提取需要的字段                                            │
│  • 抽样检查（手动验证 50-100 条）                                      │
│  • 记录映射规则和异常情况                                              │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Phase 3: 诊断现有数据                                               │
│  ─────────────────────────────────────────────────────────────────  │
│  • 对比权威数据，标记问题                                             │
│  • 生成诊断报告（JSON 格式，可追溯）                                   │
│  • 统计问题数量和类型                                                 │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Phase 4: 生成修复命令                                               │
│  ─────────────────────────────────────────────────────────────────  │
│  • 生成 SQL 更新语句（不自动执行！）                                   │
│  • 分类：高置信度（自动）/ 低置信度（需审核）                           │
│  • 每条语句包含修复原因和数据来源                                      │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Phase 5: 人工审核                                                   │
│  ─────────────────────────────────────────────────────────────────  │
│  • 审核低置信度修复                                                   │
│  • 抽样检查高置信度修复                                               │
│  • 批准或拒绝每条修复                                                 │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Phase 6: 执行更新                                                   │
│  ─────────────────────────────────────────────────────────────────  │
│  • 在事务中执行 SQL                                                  │
│  • 验证更新后的数据                                                   │
│  • 更新进度文档                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 10.5 数据源验证清单

在采用任何数据源之前，必须验证：

| 检查项 | 要求 | 验证方法 |
|-------|------|---------|
| **许可证** | 允许非商业使用 | 查看官网/License 文件 |
| **数据质量** | 学术/权威来源 | 查看项目背景、引用论文 |
| **下载可用** | URL 可访问 | 实际下载测试 |
| **格式清晰** | 有文档说明字段 | 阅读 README/Schema |
| **更新频率** | 有维护更新 | 查看最后更新日期 |
| **数据量** | 覆盖目标词汇 | 检查词数/覆盖范围 |

### 10.6 已验证的权威数据源

| 数据源 | 语言 | 许可证 | 状态 | 用途 |
|-------|------|-------|------|------|
| Lexique 3.0 | 法语 | CC BY-NC | ✅ 已验证 | 词形、词元、阴阳性、频率 |
| kaikki.org | 多语言 | CC-BY-SA | ✅ 已验证 | 释义、例句 |
| WordNet | 英语 | MIT-like | ✅ 已验证 | 同义词、定义 |
| Unihan | 中文 | Unicode | ✅ 已验证 | 拼音、部首 |
| MeCab ipadic | 日语 | BSD | ✅ 已验证 | 词性、读音 |

### 10.7 数据处理脚本自检清单

每次编写数据处理脚本时，检查：

| 检查项 | 要求 |
|-------|------|
| [ ] 数据源 URL 已验证可访问 | |
| [ ] 数据源许可证已确认允许使用 | |
| [ ] 脚本包含数据完整性验证 | |
| [ ] 脚本不依赖 AI 推断（使用查表） | |
| [ ] 生成修复命令而非直接执行 | |
| [ ] 修复命令包含来源说明 | |
| [ ] 有抽样检查步骤 | |
| [ ] 有回滚方案 | |

---

## 十一、参考资源

- [CLAUDE.md](./CLAUDE.md) - 项目代码规范
- [DICTATION_MODULE_DESIGN.md](./DICTATION_MODULE_DESIGN.md) - 听写模块设计文档
- [MULTILINGUAL_ADAPTATION_PLAN.md](./MULTILINGUAL_ADAPTATION_PLAN.md) - 多语言适配方案
- [FRENCH_DATA_FIX_PLAN.md](./FRENCH_DATA_FIX_PLAN.md) - 法语数据修复方案（含权威数据源）

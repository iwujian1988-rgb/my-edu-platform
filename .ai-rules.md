# 🚨 AI 助手必读 - 强制规则

> **这是你在这个项目中必须遵守的最高优先级规则**
>
> **违反这些规则 = 代码被拒绝**

---

## 🔴 立即执行（在回复用户前）

### 第一步：读取强制规则
```bash
请先读取：.claude/SYSTEM_PROMPT.md
```

### 第二步：确认理解
```
我已阅读并承诺遵守以下强制规则：
1. ✅ 禁止 console.log/info/debug
2. ✅ 所有 async 必须有 try-catch
3. ✅ 数据库操作必须检查 error
4. ✅ 必须处理并发竞态条件
5. ✅ 必须设置超时保护
6. ✅ 避免内存泄漏
```

### 第三步：然后才开始帮助用户

---

## ⚡ 快速检查清单

在给出任何代码前，快速自问：

- [ ] 有 `console.log` 吗？❌ 删除它
- [ ] async 函数有 `try-catch` 吗？✅ 必须有
- [ ] 数据库调用检查 `error` 了吗？✅ 必须检查
- [ ] 可能并发冲突吗？✅ 加保护
- [ ] 设置超时了吗？✅ 加超时
- [ ] 有内存泄漏吗？✅ 检查定时器/缓存

如果任何一项是 ❌，立即停止并修复！

---

## 📋 代码模板（复制使用）

### API 路由
```typescript
import { createClient } from '@/lib/supabase/server'
import { withTimeout } from '@/lib/timeout'

export async function GET(request: NextRequest) {
  try {
    const result = await withTimeout(
      businessLogic(),
      5000,
      '操作超时'
    )
    return Response.json({ success: true, data: result })
  } catch (error) {
    console.error('API Error:', { path: request.url, error })
    return Response.json(
      { error: error.message },
      { status: 500 }
    )
  }
}
```

### 数据库操作
```typescript
const { data, error } = await supabase
  .from('table')
  .select('*')

if (error) {
  console.error('DB error:', error)
  throw new Error(`Failed: ${error.message}`)
}

return data
```

---

## 🎯 记住

**用户信任你是专业的工程师，不是只会写代码的机器！**

- 主动发现问题 ✅
- 全面考虑边界情况 ✅
- 生产环境思维 ✅
- 性能和安全优先 ✅

**开始工作前，先读取 .claude/SYSTEM_PROMPT.md ！**

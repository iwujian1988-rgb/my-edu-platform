# 自定义词库 - 章节管理功能 PRD

**版本**: v3.5.0
**日期**: 2026-01-15
**作者**: AI Assistant

---

## 1. 功能概述

为自定义词库添加章节管理功能，允许用户创建、重命名、删除章节，以及将单词分配到不同章节。

**核心价值**：
- 用户可以将单词按章节组织（类似教材结构）
- 提升自定义词库的结构化程度
- 方便用户按章节学习和筛选

**适用范围**：
- ✅ **自定义词库** (`is_official = false`)：完全支持章节管理
- ❌ **官方词库** (`is_official = true`)：只读，不支持章节管理

---

## 2. 章节管理入口

### 2.1 在词库详情页

**入口位置**：
- 词库详情页的筛选栏区域
- 与"主题"、"场景"、"状态"筛选器并列
- 图标：List (lucide-react)
- 标签："章节"下拉选择器

**UI 设计**：
```tsx
// 筛选栏布局
<div className="flex items-center gap-3 flex-wrap">
  {/* 主题筛选器 */}
  <ThemeFilter />

  {/* 场景筛选器 */}
  <ScenarioFilter />

  {/* 章节筛选器 */}
  {book.is_official === false && book.created_by === user.id && (
    <ChapterFilter
      chapters={chapters}
      selectedChapter={selectedChapter}
      onFilterChange={handleChapterFilterChange}
      onManageChapters={handleOpenChapterManagement}
    />
  )}

  {/* 状态筛选器 */}
  <StatusFilter />
}
```

**章节筛选器**：
- 默认状态：显示"全部章节"
- 点击后：显示章节列表 + "管理章节"选项（底部）
- 选择章节：筛选显示该章节的单词
- 选择"管理章节"：打开章节管理对话框

### 2.2 在新建词库流程中

**位置**：步骤2（智能录入）之前或之后

**选项A - 在创建词库后**：
```
步骤1：创建词库（输入标题、描述）
↓
步骤1.5：管理章节（可选，新增步骤）
↓
步骤2：智能录入单词
↓
步骤3：成功页面
```

**选项B - 在导入单词后**：
```
步骤1：创建词库
↓
步骤2：智能录入单词
↓
步骤2.5：分配章节（可选，新增步骤）
↓
步骤3：成功页面
```

**推荐**：选项A（在创建词库后立即管理章节）

---

## 3. 章节管理界面

### 3.1 章节管理对话框

**UI 结构**：
```
┌─────────────────────────────────────────────┐
│  📚 章节管理                      [×] 关闭   │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  + 新建章节                         │   │ ← 主操作按钮
│  └─────────────────────────────────────┘   │
│                                             │
│  【章节列表】                               │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 📖 默认章节              120词      │   │ ← 章节卡片
│  │                      [编辑] [删除]  │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 📖 第1章 基础词汇        50词      │   │
│  │                      [编辑] [删除]  │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 📖 第2章 进阶词汇        80词      │   │
│  │                      [编辑] [删除]  │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 📖 第3章 高级词汇        30词      │   │
│  │                      [编辑] [删除]  │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  总计：280个单词 / 4个章节                  │
│                                             │
└─────────────────────────────────────────────┘
```

**样式规范**：
- 对话框宽度：PC端 `max-w-xl`（512px），移动端全屏
- 背景：白色 `bg-white`
- 边框：3px 黑色边框 `border-[3px] border-black`
- 阴影：Neo-Brutalism 阴影 `shadow-[4px_4px_0px_0px_#000]`
- 圆角：`rounded-xl`
- 内边距：`p-6`

**标题栏**：
- 左侧：图标 + "章节管理"文字
- 右侧：关闭按钮（×）

**新建章节按钮**：
- 背景：酸性绿 `bg-[#B4F416]`
- 文字：`text-black font-bold`
- 边框：`border-2 border-black`
- 图标：Plus (lucide-react)
- hover：`hover:shadow-[2px_2px_0px_0px_#000] hover:-translate-y-0.5`
- 布局：全宽 `w-full`

**章节列表**：
- 布局：垂直列表，间距 `space-y-3`
- 最大高度：`max-h-[400px]`
- 滚动：`overflow-y-auto`

**章节卡片**：
- 背景：白色 `bg-white`
- 边框：`border-2 border-gray-200`
- 圆角：`rounded-lg`
- 内边距：`p-4`
- hover：`hover:border-gray-300 hover:shadow-sm`
- 布局：Flexbox，左右布局
  - 左侧：章节图标 + 章节名称 + 单词数量
  - 右侧：编辑按钮 + 删除按钮

**章节信息**：
```
📖 {chapter_title}       {word_count}词
```

**操作按钮**：
- 编辑按钮：
  - 图标：Edit (lucide-react)
  - 样式：`text-gray-400 hover:text-blue-500`
  - 提示：hover显示"编辑章节"
- 删除按钮：
  - 图标：Trash2 (lucide-react)
  - 样式：`text-gray-400 hover:text-red-500`
  - 提示：hover显示"删除章节"

**统计信息**：
- 位置：对话框底部
- 样式：`text-sm text-gray-500 text-center`
- 格式："总计：{total_words}个单词 / {total_chapters}个章节"

---

## 4. 新建章节功能

### 4.1 新建章节对话框

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│  📖 新建章节                      [×] 关闭   │
├─────────────────────────────────────────────┤
│                                             │
│  章节名称 *                                 │
│  ┌─────────────────────────────────────┐   │
│  │ 第4章                               │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  排序位置                                   │
│  ┌─────────────────────────────────────┐   │
│  │ 在第3章之后 ▼                       │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌───────────┐  ┌───────────┐              │
│  │   取消    │  │   创建    │              │
│  └───────────┘  └───────────┘              │
└─────────────────────────────────────────────┘
```

**表单字段**：

| 字段 | 类型 | 必填 | 限制 | 说明 |
|------|------|------|------|------|
| 章节名称 | 文本 | ✅ | 1-50字符 | 章节标题，如"第1章 基础词汇" |
| 排序位置 | 下拉选择 | ❌ | 枚举值 | 选择新章节的插入位置 |

**排序位置选项**：
```
- 作为第1章
- 在第1章之后
- 在第2章之后
- 在第3章之后
- ...
- 作为最后一章
```

**数据结构**：
```typescript
interface Chapter {
  id: string          // UUID
  book_id: string     // 词库ID
  title: string       // 章节标题
  order_index: number // 排序索引（整数）
  created_at: string  // 创建时间
  updated_at: string  // 更新时间
}
```

**创建逻辑**：
1. 用户填写章节名称
2. 选择排序位置（默认：作为最后一章）
3. 点击"创建"按钮
4. 系统生成UUID作为章节ID
5. 系统计算 `order_index`：
   - 如果是第1章：`order_index = 1`
   - 如果在第N章之后：`order_index = N + 1`
   - 如果是最后一章：`order_index = max(order_index) + 1`
6. 系统插入章节记录到数据库
7. 系统刷新章节列表
8. 关闭对话框，显示成功提示

**边界情况处理**：
- 章节名称为空：显示错误提示"章节名称不能为空"
- 章节名称重复：显示错误提示"章节名称已存在"
- 章节数量超限：显示错误提示"章节数量不能超过100个"

---

## 5. 编辑章节功能

### 5.1 编辑章节对话框

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│  ✏️ 编辑章节                      [×] 关闭   │
├─────────────────────────────────────────────┤
│                                             │
│  章节名称 *                                 │
│  ┌─────────────────────────────────────┐   │
│  │ 第1章 入门词汇                      │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  排序位置                                   │
│  ┌─────────────────────────────────────┐   │
│  │ 作为第1章 ▼                         │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌───────────┐  ┌───────────┐              │
│  │   取消    │  │   保存    │              │
│  └───────────┘  └───────────┘              │
└─────────────────────────────────────────────┘
```

**表单字段**：
- 章节名称：预填充当前章节名称
- 排序位置：预填充当前位置

**更新逻辑**：
1. 用户修改章节名称或排序位置
2. 点击"保存"按钮
3. 系统验证字段：
   - 章节名称不能为空
   - 章节名称不能与其他章节重复
4. 系统更新数据库记录
5. 如果排序位置变化，需要重新计算所有章节的 `order_index`
6. 系统刷新章节列表和筛选器
7. 关闭对话框，显示成功提示

**排序更新算法**：
```typescript
// 示例：将第3章移动到第1章位置
// 原始：第1章(1) 第2章(2) 第3章(3) 第4章(4)
// 目标：第3章(1) 第1章(2) 第2章(3) 第4章(4)

async function updateChapterOrder(chapterId: string, newIndex: number) {
  // 1. 获取要移动的章节
  const chapter = await getChapter(chapterId)

  // 2. 获取所有章节
  const allChapters = await getChapters(bookId)

  // 3. 从列表中移除该章节
  const filtered = allChapters.filter(c => c.id !== chapterId)

  // 4. 在新位置插入
  filtered.splice(newIndex - 1, 0, chapter)

  // 5. 重新分配 order_index
  for (let i = 0; i < filtered.length; i++) {
    await updateChapter(filtered[i].id, {
      order_index: i + 1
    })
  }
}
```

---

## 6. 删除章节功能

### 6.1 删除确认流程

**步骤**：
1. 用户点击章节卡片的"删除"按钮
2. 弹出确认对话框（2次确认）：
   - 第一次：`alert("确定删除此章节？")`
   - 第二次：`alert("章节中的单词将移到默认章节，确定继续？")`
3. 用户确认后，调用 API：`DELETE /api/chapters/{chapterId}`
4. 删除成功：
   - 从章节列表中移除
   - 该章节的所有单词的 `chapter_id` 设置为 NULL（移到默认章节）
   - 刷新页面显示
5. 显示成功提示："章节已删除"

**删除确认对话框设计**：
```
┌─────────────────────────────────┐
│  ⚠️ 确认删除章节？               │
│                                 │
│  章节名称：第1章 基础词汇       │
│  单词数量：50个                 │
│                                 │
│  删除后，这些单词将移到"默认章节"│
│                                 │
│  [取消]        [确认删除]        │
└─────────────────────────────────┘
```

**二次确认设计**：
```
┌─────────────────────────────────┐
│  ⚠️ 最后确认！                   │
│                                 │
│  章节删除后不可恢复              │
│  单词将移到默认章节              │
│                                 │
│  [取消]        [确认删除]        │
└─────────────────────────────────┘
```

**边界情况处理**：
- **默认章节**：不允许删除
  - 删除按钮置灰 `opacity-50 cursor-not-allowed`
  - 提示："默认章节不能删除"
- **章节包含单词**：提示单词数量，确认后删除
- **最后一个非默认章节**：允许删除，不影响默认章节

---

## 7. API 接口设计

### 7.1 获取章节列表

**接口**：`GET /api/books/{bookId}/chapters`

**响应**：
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid-1",
      "title": "默认章节",
      "order_index": 0,
      "word_count": 120
    },
    {
      "id": "uuid-2",
      "title": "第1章 基础词汇",
      "order_index": 1,
      "word_count": 50
    },
    {
      "id": "uuid-3",
      "title": "第2章 进阶词汇",
      "order_index": 2,
      "word_count": 80
    }
  ]
}
```

### 7.2 创建章节

**接口**：`POST /api/chapters`

**请求体**：
```json
{
  "book_id": "book-uuid",
  "title": "第4章 高级词汇",
  "order_index": 4
}
```

**响应**：
```json
{
  "success": true,
  "data": {
    "id": "new-uuid",
    "book_id": "book-uuid",
    "title": "第4章 高级词汇",
    "order_index": 4,
    "created_at": "2026-01-15T10:30:00Z"
  }
}
```

### 7.3 更新章节

**接口**：`PUT /api/chapters/{chapterId}`

**请求体**：
```json
{
  "title": "第1章 入门词汇",
  "order_index": 1
}
```

**响应**：
```json
{
  "success": true,
  "data": {
    "id": "uuid",
    "title": "第1章 入门词汇",
    "order_index": 1,
    "updated_at": "2026-01-15T10:35:00Z"
  }
}
```

### 7.4 删除章节

**接口**：`DELETE /api/chapters/{chapterId}`

**响应**：
```json
{
  "success": true,
  "message": "章节已删除"
}
```

**副作用**：
- 删除章节记录
- 将该章节所有单词的 `chapter_id` 设置为 NULL
- 更新词库的 `total_chapters` 字段（减1）

---

## 8. 数据库设计

### 8.1 章节表结构

**表名**：`chapters`

**字段**：

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | UUID | PRIMARY KEY | 章节ID |
| `book_id` | UUID | NOT NULL, FOREIGN KEY | 词库ID |
| `title` | VARCHAR(50) | NOT NULL | 章节标题 |
| `order_index` | INTEGER | NOT NULL, UNIQUE | 排序索引（同一词库内唯一） |
| `created_at` | TIMESTAMPTZ | DEFAULT NOW() | 创建时间 |
| `updated_at` | TIMESTAMPTZ | DEFAULT NOW() | 更新时间 |

**索引**：
- 主键索引：`id`
- 外键索引：`book_id`
- 唯一索引：`(book_id, order_index)`
- 普通索引：`created_at`

**RLS 策略**：
```sql
-- 用户可以查看自己创建的词库的章节
CREATE POLICY "Users can view chapters of their books"
ON chapters FOR SELECT
USING (
  book_id IN (
    SELECT id FROM books WHERE created_by = auth.uid()
  )
);

-- 用户可以创建章节（在自己的词库中）
CREATE POLICY "Users can create chapters in their books"
ON chapters FOR INSERT
WITH CHECK (
  book_id IN (
    SELECT id FROM books WHERE created_by = auth.uid()
  )
);

-- 用户可以更新章节（在自己的词库中）
CREATE POLICY "Users can update chapters in their books"
ON chapters FOR UPDATE
USING (
  book_id IN (
    SELECT id FROM books WHERE created_by = auth.uid()
  )
);

-- 用户可以删除章节（在自己的词库中）
CREATE POLICY "Users can delete chapters in their books"
ON chapters FOR DELETE
USING (
  book_id IN (
    SELECT id FROM books WHERE created_by = auth.uid()
  )
);
```

### 8.2 单词表关联

**现有 `words` 表字段**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `chapter_id` | UUID | 章节ID（外键，可为NULL） |

**注意**：
- `chapter_id` 为 NULL 表示单词属于"默认章节"
- 删除章节时，将单词的 `chapter_id` 设置为 NULL

---

## 9. 单词分配到章节

### 9.1 在导入时分配章节

**场景**：用户在新建词库时导入单词

**UI 选项**：
```
┌─────────────────────────────────────────────┐
│  📝 智能录入单词                            │
├─────────────────────────────────────────────┤
│                                             │
│  选择章节 *                                 │
│  ┌─────────────────────────────────────┐   │
│  │ 默认章节 ▼                         │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  输入单词（每行一个）                      │
│  ┌─────────────────────────────────────┐   │
│  │ apple                               │   │
│  │ banana                              │   │
│  │ ...                                 │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  [ + 添加单词 ]                            │
│                                             │
└─────────────────────────────────────────────┘
```

**逻辑**：
1. 用户先创建章节（可选）
2. 用户在导入前选择章节
3. 导入的单词自动分配到选中的章节
4. 如果未选择章节，默认分配到"默认章节"

### 9.2 在编辑单词时修改章节

**位置**：单词编辑对话框（见前一个 PRD）

**字段**：
```
章节 [第1章 基础词汇 ▼]
```

**选项列表**：
```
- 默认章节
- 第1章 基础词汇
- 第2章 进阶词汇
- 第3章 高级词汇
```

**逻辑**：
1. 用户打开单词编辑对话框
2. 修改章节下拉选择
3. 点击保存
4. 单词的 `chapter_id` 更新为新章节的ID
5. 如果选择"默认章节"，`chapter_id` 设置为 NULL

### 9.3 批量移动单词到章节（可选）

**功能**：选择多个单词，批量移动到指定章节

**入口**：
- 词库详情页顶部
- "批量操作"按钮

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│  📦 批量操作（已选10个单词）    [×] 关闭    │
├─────────────────────────────────────────────┤
│                                             │
│  选择目标章节 *                             │
│  ┌─────────────────────────────────────┐   │
│  │ 第2章 进阶词汇 ▼                   │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │         确认移动                    │   │
│  └─────────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 10. 用户体验优化

### 10.1 拖拽排序章节（可选）

**功能**：用户可以拖拽章节卡片调整排序

**实现方式**：
- 使用 `react-beautiful-dnd` 或 `dnd-kit` 库
- 拖拽时实时更新 `order_index`
- 拖拽完成后保存到数据库

**视觉反馈**：
- 拖拽时：章节卡片半透明
- 占位符：显示插入位置的虚线框
- 完成后：显示保存提示"排序已更新"

### 10.2 章节重命名快捷方式

**功能**：双击章节名称直接进入编辑模式

**交互**：
1. 用户双击章节卡片上的章节名称
2. 章节名称变为输入框
3. 用户修改名称
4. 按回车或点击外部保存
5. 按Esc取消

**样式**：
- 输入框：无边框，下划线样式
- focus：蓝色下划线
- 字体：与原章节名称一致

### 10.3 章节快捷菜单（可选）

**功能**：右键点击章节卡片显示快捷菜单

**菜单选项**：
```
┌─────────────────┐
│  ✏️ 编辑章节    │
│  🔄 重命名      │
│  📊 查看统计    │
│  ❌ 删除章节    │
└─────────────────┘
```

---

## 11. 测试用例

### 11.1 功能测试

| 用例 | 步骤 | 预期结果 |
|------|------|---------|
| 创建章节 | 1. 打开章节管理对话框<br>2. 点击"新建章节"<br>3. 输入章节名称<br>4. 点击创建 | 章节创建成功，出现在列表中 |
| 重命名章节 | 1. 点击章节的"编辑"按钮<br>2. 修改章节名称<br>3. 点击保存 | 章节名称更新，筛选器同步更新 |
| 删除章节 | 1. 点击章节的"删除"按钮<br>2. 确认2次 | 章节删除，单词移到默认章节 |
| 排序章节 | 1. 编辑章节<br>2. 修改排序位置<br>3. 点击保存 | 章节顺序调整，列表重新排序 |
| 分配单词到章节 | 1. 编辑单词<br>2. 修改章节<br>3. 点击保存 | 单词移动到新章节，筛选显示正确 |
| 删除默认章节 | 尝试删除"默认章节" | 删除按钮置灰，不能删除 |
| 官方词库 | 打开官方词库，查看章节筛选器 | 不显示"管理章节"选项 |

---

## 12. 开发计划

### 12.1 Phase 1: 基础章节管理（MVP）

- ✅ 数据库表结构：`chapters` 表
- ✅ 获取章节列表 API
- ✅ 创建章节 API
- ✅ 章节管理对话框 UI
- ✅ 章节列表显示

**工作量**：约2-3天

### 12.2 Phase 2: 编辑和删除

- ✅ 更新章节 API
- ✅ 删除章节 API
- ✅ 编辑章节对话框
- ✅ 删除确认流程

**工作量**：约1-2天

### 12.3 Phase 3: 单词分配

- ✅ 导入时选择章节
- ✅ 编辑单词时修改章节
- ⏸️ 批量移动单词到章节

**工作量**：约1-2天

### 12.4 Phase 4: 用户体验优化

- ⏸️ 拖拽排序
- ⏸️ 双击重命名
- ⏸️ 右键快捷菜单

**工作量**：约1-2天

**总计**：5-9天

---

## 13. 相关文件

### 前端文件

| 文件 | 说明 |
|------|------|
| `src/components/ChapterManagementDialog.tsx` | 新建：章节管理对话框 |
| `src/components/ChapterFilter.tsx` | 新建：章节筛选器 |
| `src/components/NewBookClient.tsx` | 修改：添加章节选择步骤 |
| `src/components/EditWordDialog.tsx` | 修改：添加章节选择字段 |
| `src/app/api/books/[bookId]/chapters/route.ts` | 新建：获取章节列表 API |
| `src/app/api/chapters/route.ts` | 新建：创建/更新/删除章节 API |

### 数据库

| 文件 | 说明 |
|------|------|
| `supabase/migrations/20260115_create_chapters_table.sql` | 新建：章节表 |

---

## 14. 未来扩展方向

- 章节描述：为每个章节添加详细说明
- 章节图标：上传章节封面图标
- 章节进度：统计每个章节的学习进度
- 章节导出：单独导出某个章节的单词
- 章节模板：提供常用章节模板（如"基础"、"进阶"、"高级"）

---

**文档版本**：v1.0
**最后更新**：2026-01-15

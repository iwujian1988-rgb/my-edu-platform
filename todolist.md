# 学习中心优化 - 待办事项

**创建时间**：2026-01-09
**状态**：⏸️ 搁置（等待 1.0 版本完成后处理）
**优先级**：P1（用户体验改进）

---

## 📋 最初的问题和困惑

### 问题描述

听写、卡片、消消乐三个学习模式存在用户体验问题：

1. **入口在单词列表页**
2. **游戏范围依赖列表页的筛选条件**（主题、场景、状态）
3. **筛选条件不持久化**：
   - 用户设置筛选（如"不认识"+"会议场景"）
   - 点击进入游戏学习
   - 返回列表页 → **筛选条件重置为"全部"**
   - 用户需要重新手动设置才能继续学相同范围的单词
4. **用户需要记住上次的筛选条件**，否则每次都是一样的单词范围，游戏体验下降

### 用户痛点

> "因为筛选条件是每次用户离开就变了，那么下次用户还得记得上次的筛选条件是啥然后再继续玩，否则每次都是一样的单词范围导致游戏没意思"

---

## 💡 讨论过的解决方案

### 方案1：智能快捷筛选按钮

在列表页顶部添加常用筛选组合的快捷按钮：
- 🎯 仅不认识
- 📚 全部未掌握（不认识+模糊+未标注）
- 🔄 需巩固（模糊）
- ⏸️ 继续上次

**优点**：一键切换常用场景
**缺点**：还是需要用户主动选择

---

### 方案2：记住上次游戏范围

在游戏入口按钮上显示上次玩的范围：
```
[卡片背单词]
 上次: 不认识 - 会议场景 (15个单词)
```

提供"继续上次"功能，一键恢复。

**优点**：符合用户习惯（类似视频网站的"继续观看"）
**缺点**：需要用户看到提示后主动点击

---

### 方案3：默认"学习优先"模式

将筛选条件默认值改为"未掌握的单词"（unknown+fuzzy+new），排除 known。

**优点**：一次设置，长期有效
**缺点**：可能不符合所有用户需求

---

### 方案4：游戏范围持久化

每次玩游戏时保存筛选条件，下次自动恢复到 localStorage。

**优点**：用户无需任何操作，自动恢复
**缺点**：可能不符合用户当前需求

---

### 方案5：统一"开始学习"按钮

**核心理念**：把三个游戏模式合并为一个"学习流程"，用户只关心"学什么"，不关心"怎么学"。

```
旧流程（多步）：
列表页 → 选择筛选 → 选择游戏模式 → 开始学习

新流程（一步）：
列表页 → 点击"开始学习" → 直接开始！
```

点击后系统智能决策：
- 优先"不认识"的单词 → 听写模式
- 其次"模糊"的单词 → 消消乐
- 都没有 → 弹出"学习计划"选择

**优点**：一步到位，降低启动门槛
**缺点**：可能过于简化，失去灵活性

---

### 方案6：学习任务系统

将学习内容抽象为"任务"，类似健身APP的训练计划。

```typescript
interface LearningTask {
  id: string
  name: string              // 任务名称：如"每日复习"
  wordIds: string[]         // 包含的单词ID列表
  totalWords: number
  completedWords: number
  gameMode: 'flashcards' | 'dictation' | 'match-game' | 'auto'
  schedule: {
    frequency: 'daily' | 'weekly' | 'once'
    time?: string
  }
  createdAt: string
  lastPlayedAt?: string
}
```

创建任务页面 `/study/[bookId]/tasks`，提供：
- 推荐任务（每日复习、新词学习）
- 我的任务列表
- 创建新任务（自定义筛选+游戏模式+计划）

**优点**：专业化，符合教育产品定位
**缺点**：实现成本高，需要大量开发工作

---

### 方案7：智能快捷入口 ⭐ 推荐

结合方案2和方案4的优点，在列表页顶部添加"学习中心"区域。

```
┌─────────────────────────────────────┐
│  📚 学习中心                         │
│  ╔═══════╗ ╔═══════╗ ╔═══════╗      │
│  ║待复习  ║ ║待巩固  ║ ║新词学习║      │
│  ║ 23个  ║ ║ 45个  ║ ║ 88个  ║      │
│  ╚═══════╝ ╚═══════╝ ╚═══════╝      │
│  [▶️ 继续上次] [⚙️ 自定义筛选]       │
└─────────────────────────────────────┘
```

**核心功能**：
1. **3个智能推荐卡片**（待复习、待巩固、新词学习）
2. 点击卡片 → 自动筛选单词 → 直接进入游戏
3. "继续上次学习"按钮 → 一键恢复上次的学习状态
4. 保留"自定义筛选"功能 → 高级用户仍然可以自由组合

**实现要点**：
- 使用 sessionStorage 保存快速学习的单词ID列表
- 游戏页面支持 `scope=quick` 参数
- 保存学习记录到 localStorage（用于"继续上次"功能）

**优点**：
- ✅ 零配置：用户无需设置筛选条件
- ✅ 一目了然：直接看到需要学习的内容
- ✅ 灵活性强：保留自定义选项
- ✅ 实现简单：只需改造列表页和游戏页面入口

---

## 🆕 新用户场景问题

### 问题描述

如果用户对所有单词都没标记（都是 `new` 状态）：

```
┌─────────────────────────────────────┐
│  ╔═══════╗ ╔═══════╗ ╔═══════╗      │
│  ║待复习  ║ ║待巩固  ║ ║新词学习║      │
│  ║  0个  ║ ║  0个  ║ ║ 156个 ║      │ ← 问题在这里！
│  ╚═══════╝ ╚═══════╝ ╚═══════╝      │
│   空卡片    空卡片      太多了！      │
└─────────────────────────────────────┘
```

**问题**：
1. 两个空卡片，视觉上很怪
2. "新词学习"156个，用户压力巨大
3. 新用户不知道从何开始

### 改进方案：智能检测新用户

```typescript
// 检测用户状态
const isNewUser = (unknownCount + fuzzyCount) === 0 && newCount > 50
```

**新用户界面**：

```
┌─────────────────────────────────────────────┐
│  📚 学习中心                                 │
│                                             │
│  ╔═════════════════════════════════════╗   │
│  ║   🌟 欢迎开始学习！                   ║   │
│  ║                                       ║   │
│  ║   这本书有 156 个单词，建议你：       ║   │
│  ║                                       ║   │
│  ║   ✅ 每天学习 20 个新词               ║   │
│  ║   ✅ 先完成第 1-2 章                  ║   │
│  ║   ✅ 大约需要 8 天完成本书             ║   │
│  ╚═════════════════════════════════════╝   │
│                                             │
│  ╔═══════════╗ ╔═══════════╗ ╔═══════════╗│
│  ║📖 每日20词║ ║🎯 第1章   ║ ║🚀 全部挑战║│
│  ║           ║ ║           ║ ║           ║│
│  ║  推荐模式  ║ ║  25个单词  ║ ║  156个   ║│
│  ║           ║ ║           ║ ║           ║│
│  ║ [开始学习]→║ ║ [开始学习]→║ ║ [开始学习]→║│
│  ╚═══════════╝ ╚═══════════╝ ╚═══════════╝│
└─────────────────────────────────────────────┘
```

---

## 🧠 艾宾浩斯记忆曲线系统

### 核心理念

基于艾宾浩斯遗忘曲线，科学安排复习时间：

```
记忆保持率
100% |●_________
     |  ●_______
     |    ●_____
     |      ●__
     |        ●_
     |          ●_
     |____________●_____ 时间
       20m 1h  9h  1d  2d  6d  31d
```

**关键复习点**：20分钟 → 1小时 → 9小时 → 1天 → 2天 → 6天 → 31天

### 实现方案

#### 阶段1：轻量级实现（推荐先用这个）

**数据字段**（已有，无需新增）：
- `last_practiced_at`：最后练习时间
- `status`：new | known | fuzzy | unknown
- `practice_count`：练习次数

**计算逻辑**：

```typescript
// 判断单词是否需要复习
const needsReview = (word: WordProgress): boolean => {
  const now = Date.now()
  const lastPracticed = new Date(word.last_practiced_at).getTime()
  const hoursSince = (now - lastPracticed) / (1000 * 60 * 60)

  switch (word.status) {
    case 'unknown':
      // 不认识的单词：1小时后就应该复习
      return hoursSince > 1

    case 'fuzzy':
      // 模糊的单词：1天后应该复习
      return hoursSince > 24

    case 'known':
      // 已认识的单词：根据练习次数决定
      const reviewInterval = Math.min(2 ** word.practice_count, 30 * 24)
      return hoursSince > reviewInterval

    default:
      return false
  }
}
```

**学习中心显示**：

```
┌─────────────────────────────────────────────┐
│  📚 智能学习中心                             │
│  基于艾宾浩斯记忆曲线，科学安排复习          │
├─────────────────────────────────────────────┤
│                                             │
│  🔥 急需复习 (8个)                           │
│     遗忘临界点！立即复习                     │
│     [立即复习 →]                            │
│                                             │
│  ─────────────────────────────────────────   │
│                                             │
│  📅 今日复习 (15个)                          │
│  ✨ 新词学习 (20个推荐)                      │
│  💪 强化训练 (23个)                          │
└─────────────────────────────────────────────┘
```

#### 阶段2：完整记忆等级系统（进阶版）

**新增数据库字段**：

```sql
ALTER TABLE word_progress ADD COLUMN IF NOT EXISTS memory_level INTEGER DEFAULT 0;
ALTER TABLE word_progress ADD COLUMN IF NOT EXISTS next_review_at TIMESTAMP;
ALTER TABLE word_progress ADD COLUMN IF NOT EXISTS review_count INTEGER DEFAULT 0;
ALTER TABLE word_progress ADD COLUMN IF NOT EXISTS correct_streak INTEGER DEFAULT 0;

COMMENT ON COLUMN word_progress.memory_level IS '记忆等级 0-5，越高表示记忆越牢固';
COMMENT ON COLUMN word_progress.next_review_at IS '下次复习时间（根据艾宾浩斯曲线计算）';
```

**记忆等级定义**：

| 等级 | 名称 | 复习间隔 | 说明 |
|------|------|----------|------|
| 0 | 陌生 | 1小时 | 刚开始学习 |
| 1 | 熟悉 | 9小时 | 初步认识 |
| 2 | 掌握 | 1天 | 基本掌握 |
| 3 | 巩固 | 3天 | 记忆较好 |
| 4 | 熟练 | 1周 | 记忆牢固 |
| 5 | 精通 | 1月 | 完全掌握 |

**复习算法**：

```typescript
// 更新单词记忆等级
const updateMemoryLevel = (
  currentLevel: MemoryLevel,
  isCorrect: boolean
): { newLevel: MemoryLevel, nextReviewAt: Date } => {

  if (isCorrect) {
    // 答对了：升级记忆等级
    const newLevel = Math.min(currentLevel + 1, 5) as MemoryLevel
    const intervalHours = MEMORY_LEVELS[newLevel].reviewInterval
    const nextReviewAt = new Date(Date.now() + intervalHours * 60 * 60 * 1000)
    return { newLevel, nextReviewAt }
  } else {
    // 答错了：降级记忆等级
    const newLevel = Math.max(currentLevel - 1, 0) as MemoryLevel
    const nextReviewAt = new Date(Date.now() + 1 * 60 * 60 * 1000) // 1小时后复习
    return { newLevel, nextReviewAt }
  }
}
```

---

## 🎮 三种游戏模式的区别与整合

### 核心问题

三种游戏的**学习深度**不同：
- **听写模式**：深度学习（需要拼写、音频识别）→ 最强记忆效果
- **卡片背单词**：中等深度（看英文想中文）→ 中等记忆效果
- **消消乐**：浅层复习（匹配游戏）→ 较弱记忆效果

**如果完全不区别** → 用户用消消乐"玩"了10次，记忆等级升到"精通"，但实际可能根本不会拼写

**如果完全区别** → 每个单词有3个记忆等级，数据太复杂，用户困惑

### 解决方案：权重系数 + 互补关系

**核心思路**：
1. **三种游戏共用一个记忆等级**（不重复）
2. **根据游戏模式给不同的权重**（区分）
3. **推荐最佳学习路径**（引导）

#### 游戏模式配置

```typescript
interface GameModeConfig {
  mode: GameMode
  name: string
  depth: 'shallow' | 'medium' | 'deep'
  weight: number           // 记忆提升权重系数
  canIncreaseLevel: boolean // 是否能提升记忆等级
  description: string
}

const GAME_MODES: Record<GameMode, GameModeConfig> = {
  flashcards: {
    mode: 'flashcards',
    name: '卡片背单词',
    depth: 'medium',
    weight: 0.5,          // 卡片模式正确 = 0.5次有效学习
    canIncreaseLevel: true,
    description: '快速浏览，建立初步印象'
  },

  dictation: {
    mode: 'dictation',
    name: '听写模式',
    depth: 'deep',
    weight: 1.0,          // 听写模式正确 = 1次有效学习（标准）
    canIncreaseLevel: true,
    description: '深度学习，强化记忆'
  },

  'match-game': {
    mode: 'match-game',
    name: '消消乐',
    depth: 'shallow',
    weight: 0.3,          // 消消乐正确 = 0.3次有效学习
    canIncreaseLevel: false,  // 仅用于复习，不能提升记忆等级
    description: '轻松复习，保持记忆活跃'
  }
}
```

#### 学习路径建议

```
新词 → 卡片模式（建立印象）→ 听写模式（深度记忆）→ 消消乐（巩固复习）
```

#### 界面设计

```
┌─────────────────────────────────────────────┐
│  💡 学习建议                                │
│     根据记忆科学研究，不同游戏模式适合不同   │
│     学习阶段：                              │
│                                             │
│   📇 新词学习 → 🎧 深度记忆 → 🎮 轻松复习   │
│   (卡片模式)    (听写模式)     (消消乐)      │
│   建立印象      强化拼写       保持活跃      │
└─────────────────────────────────────────────┘
```

---

## 🎨 界面原型设计

### 方案7原型（带记忆曲线）

```
┌─────────────────────────────────────────────┐
│  📚 智能学习中心            今日: 15/30     │
│     基于艾宾浩斯记忆曲线，科学安排复习      │
├─────────────────────────────────────────────┤
│                                             │
│  🔥 急需复习 (8个) - [立即复习 →]          │
│                                             │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐│
│  │ 📅 今日复习 │  │ ✨ 新词学习 │  │ 💪 强化训练 ││
│  │            │  │            │  │            ││
│  │   15个     │  │   20个     │  │   23个     ││
│  │            │  │            │  │            ││
│  │[开始学习→] │  │[开始学习→] │  │[开始学习→] ││
│  └────────────┘  └────────────┘  └────────────┘│
│                                             │
│  📈 记忆状态分布              查看详细报告 → │
│  ─────────────────────────────────────────   │
│  陌生  熟悉  掌握  巩固  熟练  精通          │
│  [15]  [23]  [38]  [25]  [18]  [12]          │
│                                             │
│  整体记忆稳固度: 2.8 / 5.0                  │
│  ████████░░░░░░░░░ 56%                      │
└─────────────────────────────────────────────┘
```

### 极简版本原型

**用户反馈**：原方案太复杂，用户理解困难

**简化方案**：只显示"今天该学啥"

```
┌─────────────────────────────────────────────┐
│  📚 今日学习计划                15/30 完成  │
│     根据艾宾浩斯记忆曲线为你安排            │
├─────────────────────────────────────────────┤
│                                             │
│         ╔═══════════════╗                  │
│         ║               ║                  │
│         ║  ▶️ 开始学习  ║                  │
│         ║               ║                  │
│         ╚═══════════════╝                  │
│                                             │
│   今天你需要学习 15 个单词，预计 10 分钟    │
│                                             │
│   📝 复习: 8 个    ✨ 新词: 7 个            │
│                                             │
│  [🎮 换个模式]    [⚙️ 自定义]              │
│                                             │
└─────────────────────────────────────────────┘
```

**点击"开始学习"后**：
- 系统自动选择最合适的游戏模式
- 用户不需要理解背后的逻辑
- 学完显示结果，然后问"继续学习吗？"

---

## ❓ 讨论中的问题

### 问题1：用户理解难度

**用户反馈**：
> "用户理解起来有难度吧"

**问题分析**：
- 记忆等级（0-5）太复杂
- 三种游戏的区别（权重、升级条件）需要用户理解
- 学习路径建议又要用户记
- 总体来说：认知负担太重

**简化方向**：
- 完全隐藏记忆曲线逻辑，只显示"今日学习"
- 简化为3个状态（需要学习 / 需要复习 / 已掌握）
- 只保留一个"开始学习"按钮，系统自动决策

---

## 📝 最终决定

**状态**：⏸️ **搁置**

**原因**：
1. 当前优先完成 1.0 版本的核心功能
2. 学习中心优化属于体验改进，可以在后续版本迭代
3. 需要更多时间打磨交互细节，避免用户理解困难

**后续计划**：
- ✅ 完成所有核心功能（听写、卡片、消消乐、词库管理等）
- ✅ 修复已知 bug
- ✅ 完善测试覆盖
- ⏸️ 学习中心优化（1.0 之后）
  - 先实现简化版本（只保留"开始学习"大按钮）
  - 逐步引入记忆曲线功能
  - 根据用户反馈调整交互细节

---

## 🎯 推荐实现路径（搁置后的参考）

### 第一步（1-2小时）：轻量级实现
- ✅ 实现"急需复习"功能
- ✅ 基于 `last_practiced_at` 简单计算
- ✅ 在学习中心显示

### 第二步（半天）：完整记忆等级系统
- ✅ 添加数据库字段（`memory_level`, `next_review_at`）
- ✅ 实现完整的记忆等级系统
- ✅ 游戏结束后更新记忆等级

### 第三步（可选）：增强功能
- ✅ 添加复习提醒通知
- ✅ 详细的记忆分析报告
- ✅ 学习统计数据可视化

---

## 📚 参考资料

### 艾宾浩斯遗忘曲线
- 关键复习点：20分钟 → 1小时 → 9小时 → 1天 → 2天 → 6天 → 31天
- 遗忘速度：先快后慢
- 在关键点复习 → 记忆保持率回升到 100%

### 记忆科学研究
- **主动回忆**（Active Recall）：比被动阅读效果好
- **间隔重复**（Spaced Repetition）：在不同时间点复习
- **深度加工**（Deep Processing）：听写比卡片效果好

### 产品参考
- **Anki**：经典的间隔重复记忆软件
- **Duolingo**：游戏化学习，每日目标
- **Quizlet**：多种学习模式，灵活切换

---

**最后更新**：2026-01-09
**下次review时间**：1.0 版本完成后

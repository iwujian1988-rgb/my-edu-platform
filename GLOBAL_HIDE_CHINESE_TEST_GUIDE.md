# 全局"隐藏中文"功能 - 测试指南

## 功能概述

新增全局"隐藏中文"按钮，点击后对当前用户+当前单词书生效，设置持久化保存到数据库。

## 前置步骤

### 1. 执行数据库迁移

参考 `USER_PREFERENCES_SETUP_GUIDE.md` 执行数据库迁移：

```bash
# 在 Supabase SQL Editor 中执行
supabase/migrations/add_user_preferences.sql
```

### 2. 重启开发服务器

```bash
# 停止当前服务器 (Ctrl+C)
# 重新启动
npm run dev
```

## 测试步骤

### Test 1: 基本功能测试

1. **访问单词书详情页**
   ```
   http://localhost:3000/library/[任意book-id]
   ```

2. **观察初始状态**
   - 右上角应显示"隐藏中文"按钮（默认未激活）
   - 所有单词卡片应显示中文和英文

3. **点击全局"隐藏中文"按钮**
   - 按钮变为"显示中文"状态
   - 按钮右侧显示"全局生效"紫色徽章
   - **所有单词卡片**的中文内容应同时隐藏：
     - ✅ 释义中文隐藏
     - ✅ 搭配中文隐藏
     - ✅ 例句中文隐藏
   - **功能标签始终显示**：
     - ✅ "💡 搭配:" 标签可见
     - ✅ "📝 例句:" 标签可见
   - **英文内容始终显示**：
     - ✅ 释义英文可见
     - ✅ 搭配英文可见
     - ✅ 例句英文可见

### Test 2: 持久化测试

4. **刷新页面**
   ```
   按 F5 或 Ctrl+R
   ```
   - 页面刷新后，"显示中文"状态应该保留
   - 所有单词卡片的中文内容仍然隐藏
   - 数据库中已保存该设置

5. **打开浏览器控制台验证**
   ```
   F12 -> Console 标签
   ```
   - 应该看到日志：`✅ 全局隐藏中文设置已开启`

### Test 3: 恢复显示测试

6. **点击"显示中文"按钮**
   - 按钮变回"隐藏中文"状态
   - "全局生效"徽章消失
   - **所有单词卡片**的中文内容恢复显示

7. **再次刷新页面**
   - 设置应该保留（中文持续显示）

### Test 4: 多用户隔离测试

8. **测试用户隔离**
   - 使用用户 A 登录，设置"隐藏中文"
   - 退出登录
   - 使用用户 B 登录，访问同一本书
   - 用户 B 应该看到默认状态（显示中文）
   - 用户 B 的设置不影响用户 A

### Test 5: 多书籍隔离测试

9. **测试书籍隔离**
   - 在"书籍 A"设置"隐藏中文"
   - 切换到"书籍 B"
   - "书籍 B"应该有独立的设置（默认显示中文）
   - 回到"书籍 A"，设置应该保留

### Test 6: 本地按钮 vs 全局按钮测试

10. **本地按钮优先级测试**
    - 开启全局"隐藏中文"
    - 单词卡片上的本地"隐藏中文"按钮仍可点击
    - 但本地按钮仅影响单个卡片
    - 全局按钮覆盖所有卡片

## 预期结果

### 成功标志
- ✅ 全局按钮点击后，所有卡片中文内容同步隐藏
- ✅ 刷新页面设置保留
- ✅ 按钮显示"全局生效"徽章
- ✅ 功能标签（"💡 搭配:"、"📝 例句:"）始终显示
- ✅ 不同用户设置独立
- ✅ 不同书籍设置独立

### 失败标志
- ❌ 点击后部分卡片未隐藏
- ❌ 刷新后设置丢失
- ❌ 功能标签被隐藏
- ❌ 英文内容也被隐藏

## 故障排查

### 问题 1: 点击后没有反应
**可能原因：** 数据库表未创建
**解决方案：** 检查 `user_book_preferences` 表是否存在

### 问题 2: 刷新后设置丢失
**可能原因：** API 调用失败
**解决方案：** 打开浏览器控制台查看错误信息

### 问题 3: 所有卡片都隐藏了
**可能原因：** 英文内容也被错误隐藏
**解决方案：** 检查 WordCard.tsx 中的 `shouldShowChinese` 逻辑

## 数据库验证

### 查询用户偏好设置

```sql
-- 查看所有用户的偏好设置
SELECT
  u.email,
  bp.book_id,
  bp.hide_chinese,
  bp.created_at,
  bp.updated_at
FROM user_book_preferences bp
JOIN auth.users u ON bp.user_id = u.id
ORDER BY bp.updated_at DESC;
```

### 手动重置设置

```sql
-- 删除某个用户对某本书的偏好设置
DELETE FROM user_book_preferences
WHERE user_id = 'user-uuid'
  AND book_id = 'book-uuid';
```

## API 测试

### 获取偏好设置

```bash
curl -X GET \
  "http://localhost:3000/api/user-preferences?book_id=test-book-id" \
  -H "Cookie: your-session-cookie"
```

### 保存偏好设置

```bash
curl -X POST \
  "http://localhost:3000/api/user-preferences" \
  -H "Content-Type: application/json" \
  -H "Cookie: your-session-cookie" \
  -d '{
    "book_id": "test-book-id",
    "hide_chinese": true
  }'
```

## 完成标准

- [ ] 数据库表创建成功
- [ ] 全局按钮功能正常
- [ ] 设置持久化保存
- [ ] 刷新后设置保留
- [ ] 多用户隔离正常
- [ ] 多书籍隔离正常
- [ ] 功能标签始终显示
- [ ] 英文内容始终显示

# 自定义词库 - 单词编辑功能 PRD

**版本**: v3.5.0
**日期**: 2026-01-15
**作者**: AI Assistant

---

## 1. 功能概述

为自定义词库添加单词编辑功能，允许用户修改已添加单词的所有内容字段，包括拼写、音标、释义、搭配、例句等。

**核心价值**：
- 用户可以修正导入错误的单词数据
- 用户可以补充或完善单词信息
- 提升自定义词库的灵活性和可用性

**适用范围**：
- ✅ **自定义词库** (`is_official = false`)：完全支持编辑
- ❌ **官方词库** (`is_official = true`)：只读，不支持编辑

---

## 2. 编辑模式入口

### 2.1 在单词列表页面

**触发条件**：
- 词库为自定义词库 (`book.is_official === false`)
- 用户有权限 (`book.created_by === user.id`)

**入口位置**：
- 每个单词卡片的右上角（与"隐藏中文"按钮并排）
- 图标：Edit (lucide-react)
- 样式：极低调设计，无边框、无背景，仅显示图标
- 图标尺寸：12px × 12px (w-3 h-3)
- 图标颜色：`text-gray-300`（极淡灰色）
- hover颜色：`text-blue-500`（蓝色，提示可编辑）
- 交互：hover时图标略微放大（scale-110）

**权限判断**：
```typescript
// 官方词库：不显示编辑按钮
if (book.is_official === true) {
  return null // 不渲染编辑按钮
}

// 自定义词库：只对创建者显示编辑按钮
if (book.created_by === user.id) {
  return <EditButton />
}
```

### 2.2 在智能导入预览页面

**功能**：在批量导入前，允许用户预览和编辑即将导入的单词

**入口位置**：
- 智能导入结果页面
- 每个单词卡片的右上角
- 批量编辑按钮（顶部操作栏）

---

## 3. 编辑界面设计

### 3.1 编辑对话框

**UI 结构**：
```
┌─────────────────────────────────────────────┐
│  ✏️ 编辑单词                      [×] 关闭   │
├─────────────────────────────────────────────┤
│                                             │
│  【基本信息】                                │
│  ┌─────────────────────────────────────┐   │
│  │ 单词 *                               │   │
│  │ ┌─────────────────────────────────┐ │   │
│  │ │ agenda                          │ │   │
│  │ └─────────────────────────────────┘ │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 音标                                 │   │
│  │ ┌─────────────────────────────────┐ │   │
│  │ │ /əˈdʒendə/                      │ │   │
│  └─────────────────────────────────┘ │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 词性                                 │   │
│  │ ┌─────────────────────────────────┐ │   │
│  │ │ n名词 ▼                         │ │   │
│  └─────────────────────────────────┘ │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  【释义】                                   │
│  ┌─────────────────────────────────────┐   │
│  │ 英文释义                             │   │
│  │ ┌─────────────────────────────────┐ │   │
│  │ │ A list of items to be discussed│ │   │
│  │ │ at a meeting                    │ │   │
│  │ │                                 │ │   │
│  └─────────────────────────────────┘ │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 中文释义 *                           │   │
│  │ ┌─────────────────────────────────┐ │   │
│  │ │ 议程，日程表                    │ │   │
│  └─────────────────────────────────┘ │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  【搭配】                                   │
│  ┌─────────────────────────────────────┐   │
│  │ 英文搭配                             │   │
│  │ ┌─────────────────────────────────┐ │   │
│  │ │                                 │ │   │
│  └─────────────────────────────────┘ │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 中文搭配                             │   │
│  │ ┌─────────────────────────────────┐ │   │
│  │ │                                 │ │   │
│  └─────────────────────────────────┘ │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  【例句】                                   │
│  ┌─────────────────────────────────────┐   │
│  │ 英文例句                             │   │
│  │ ┌─────────────────────────────────┐ │   │
│  │ │                                 │ │   │
│  └─────────────────────────────────┘ │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 中文例句                             │   │
│  │ ┌─────────────────────────────────┐ │   │
│  │ │                                 │ │   │
│  └─────────────────────────────────┘ │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  【其他信息】                               │
│  ┌─────────────────────────────────────┐   │
│  │ 章节                                 │   │
│  │ ┌─────────────────────────────────┐ │   │
│  │ │ 第1章 ▼                         │ │   │
│  └─────────────────────────────────┘ │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌───────┐  ┌───────┐                    │
│  │ 取消  │  │ 保存  │                    │
│  └───────┘  └───────┘                    │
└─────────────────────────────────────────────┘
```

**样式规范**：
- 对话框宽度：PC端 `max-w-2xl`（672px），移动端全屏
- 背景：白色 `bg-white`
- 边框：3px 黑色边框 `border-[3px] border-black`
- 阴影：Neo-Brutalism 阴影 `shadow-[4px_4px_0px_0px_#000]`
- 圆角：`rounded-xl`
- 内边距：`p-6`

**标题栏**：
- 左侧：图标 + "编辑单词"文字
- 右侧：关闭按钮（×）
- 样式：clay-card，居中显示

**分组标题**：
- 字体：`font-bold text-sm`
- 颜色：`text-gray-600`
- 上边距：`mt-6 mb-3`
- 图标：可加 emoji 图标增强识别

**表单字段**：
- 标签：`font-semibold text-sm text-gray-700`
- 必填标记：`*` 红色
- 输入框：clay-input 样式
  - 背景：白色 `bg-white`
  - 边框：`border-2 border-gray-300`
  - focus边框：`focus:border-blue-500`
  - 圆角：`rounded-lg`
  - 内边距：`px-4 py-2`
  - 字体：`text-base`
- 文本域：
  - 最小高度：`min-h-[80px]`
  - 可自动扩展高度（可选）

**底部按钮**：
- 布局：Flexbox，右侧对齐
- 间距：`gap-3`
- 取消按钮：
  - 背景：白色 `bg-white`
  - 文字：`text-gray-700`
  - 边框：`border-2 border-gray-300`
- 保存按钮：
  - 背景：酸性绿 `bg-[#B4F416]`
  - 文字：`text-black font-bold`
  - 边框：`border-2 border-black`
  - hover：`hover:shadow-[2px_2px_0px_0px_#000] hover:-translate-y-0.5`

---

## 4. 可编辑字段

### 4.1 基本信息区

| 字段 | 数据库字段 | 必填 | 类型 | 限制 | 说明 |
|------|-----------|------|------|------|------|
| 单词 | `word` | ✅ | 文本 | 1-100字符 | 英文单词，支持中英文、数字、符号 |
| 音标 | `phonetic` | ❌ | 文本 | 0-100字符 | IPA音标，如 /əˈdʒendə/ |
| 词性 | `part_of_speech` | ❌ | 下拉选择 | 枚举值 | 见下方词性选项表 |

**词性选项**：
```
n名词
v动词
adj形容词
adv副词
pron代词
prep介词
conj连词
art冠词
aux助动词
modal情态动词
vt及物动词
vi不及物动词
```

### 4.2 释义区

| 字段 | 数据库字段 | 必填 | 类型 | 限制 | 说明 |
|------|-----------|------|------|------|------|
| 英文释义 | `definition_en` | ❌ | 文本域 | 0-500字符 | 英文解释 |
| 中文释义 | `definition` | ✅ | 文本域 | 1-500字符 | 中文释义，不要加词性标记 |

### 4.3 搭配区

| 字段 | 数据库字段 | 必填 | 类型 | 限制 | 说明 |
|------|-----------|------|------|------|------|
| 英文搭配 | `collocation_en` | ❌ | 文本域 | 0-500字符 | 英文搭配 |
| 中文搭配 | `collocation` | ❌ | 文本域 | 0-500字符 | 中文搭配 |

### 4.4 例句区

| 字段 | 数据库字段 | 必填 | 类型 | 限制 | 说明 |
|------|-----------|------|------|------|------|
| 英文例句 | `example_sentence_en` | ❌ | 文本域 | 0-500字符 | 英文例句 |
| 中文例句 | `example_sentence` | ❌ | 文本域 | 0-500字符 | 中文例句 |

### 4.5 其他信息

| 字段 | 数据库字段 | 必填 | 类型 | 限制 | 说明 |
|------|-----------|------|------|------|------|
| 章节 | `chapter` | ❌ | 下拉选择 | 枚举值 | 当前词库的所有章节列表 |

**章节选项**：
- 动态加载当前词库的所有章节
- 选项格式：`{order_index}. {chapter_title}`
- 默认值：当前单词所属章节
- 特殊选项："默认章节"（对应 chapter = NULL 或空字符串）

---

## 5. 交互流程

### 5.1 打开编辑对话框

**步骤**：
1. 用户点击单词卡片右上角的"编辑"图标
2. 系统检查权限：
   - ❌ 如果是官方词库：不显示按钮
   - ❌ 如果非创建者：不显示按钮
   - ✅ 如果是创建者的自定义词库：继续
3. 系统加载单词数据到表单
4. 显示编辑对话框（模态弹窗）
5. 背景添加遮罩层 `bg-black/50 backdrop-blur-sm`

**动画效果**：
- 对话框淡入：`opacity-0` → `opacity-100`
- 对话框缩放：`scale-95` → `scale-100`
- 动画时长：200ms
- 缓动函数：`ease-out`

### 5.2 编辑单词

**用户操作**：
1. 用户修改表单字段
2. 系统实时验证输入
3. 如果有错误，显示红色边框和错误提示

**字段验证规则**：

```typescript
// 单词（必填）
- 不能为空
- 长度：1-100字符
- 允许字符：字母、数字、常见符号、中文

// 音标（可选）
- 长度：0-100字符
- 允许字符：字母、数字、符号（/ˈˌ.ː-）

// 词性（可选）
- 下拉选择，从预定义列表选择

// 中文释义（必填）
- 不能为空
- 长度：1-500字符

// 其他文本域
- 长度：0-500字符
```

**错误提示样式**：
- 输入框边框：`border-red-500`
- 错误文字：`text-red-500 text-xs`
- 位置：输入框下方
- 示例："单词不能为空" / "中文释义不能为空"

### 5.3 保存修改

**步骤**：
1. 用户点击"保存"按钮
2. 系统验证所有必填字段：
   - ❌ 如果有错误：显示错误提示，停留在对话框
   - ✅ 如果验证通过：继续
3. 系统显示保存中状态：
   - 按钮文字："保存中..."
   - 按钮禁用：`opacity-50 cursor-not-allowed`
   - 加载图标：可选（Spinner）
4. 系统调用 API：`PUT /api/words/{wordId}`
5. API 返回成功：
   - 关闭对话框
   - 显示成功提示："单词已更新"
   - 更新单词卡片显示（乐观更新）
   - 如果章节变化，刷新单词列表
6. API 返回失败：
   - 显示错误提示："保存失败，请重试"
   - 关闭对话框（或停留在对话框）

**乐观更新策略**：
```typescript
// 1. 前端立即更新 UI
setWord(updatedWord)

// 2. 后台调用 API
const response = await fetch(`/api/words/${wordId}`, {
  method: 'PUT',
  body: JSON.stringify(updatedData)
})

// 3. 如果失败，回滚并提示
if (!response.ok) {
  setWord(originalWord)
  showToast('保存失败，请重试', 'error')
}
```

### 5.4 取消编辑

**步骤**：
1. 用户点击"取消"按钮或"×"关闭按钮
2. 系统检查是否有未保存的修改：
   - 如果有：弹出确认对话框
   - 如果没有：直接关闭编辑对话框

**确认对话框设计**：
```
┌─────────────────────────────────┐
│  ⚠️ 确认取消？                   │
│                                 │
│  您有未保存的修改，确定要放弃吗？ │
│                                 │
│  [继续编辑]    [确认取消]        │
└─────────────────────────────────┘
```

**关闭方式**：
- 点击"取消"按钮
- 点击"×"关闭按钮
- 点击遮罩层（可选，建议不关闭，防止误操作）
- 按 ESC 键

### 5.5 删除单词（扩展功能）

**入口位置**：
- 编辑对话框底部，左侧
- 红色文字按钮："删除此单词"
- 图标：Trash2 (lucide-react)

**删除流程**：
1. 用户点击"删除此单词"按钮
2. 弹出确认对话框（3次确认）：
   - 第一次：`alert("确定删除此单词？")`
   - 第二次：`alert("数据不可恢复！")`
   - 第三次：`alert("最后确认！")`
3. 用户确认后，调用 API：`DELETE /api/words/{wordId}`
4. 删除成功：
   - 关闭编辑对话框
   - 从单词列表中移除该单词（动画效果）
   - 显示成功提示："单词已删除"
   - 更新词库的单词总数

**删除动画**：
- 单词卡片向右滑出：`translate-x-full`
- 淡出效果：`opacity-0`
- 缩小效果：`scale-95`
- 动画时长：400ms
- 缓动函数：`ease-out`

---

## 6. API 接口设计

### 6.1 更新单词

**接口**：`PUT /api/words/{wordId}`

**请求头**：
```
Content-Type: application/json
Authorization: Bearer {access_token}
```

**请求体**：
```json
{
  "word": "agenda",
  "phonetic": "/əˈdʒendə/",
  "part_of_speech": "n名词",
  "definition_en": "A list of items to be discussed at a meeting",
  "definition": "议程，日程表",
  "collocation_en": "place on the agenda",
  "collocation": "列入议程",
  "example_sentence_en": "We need to add this item to the agenda.",
  "example_sentence": "我们需要把这一项列入议程。",
  "chapter": "第1章"
}
```

**响应**：
- 成功（200 OK）：
```json
{
  "success": true,
  "data": {
    "id": "word-uuid",
    "word": "agenda",
    "phonetic": "/əˈdʒendə/",
    "part_of_speech": "n名词",
    "definition_en": "A list of items to be discussed at a meeting",
    "definition": "议程，日程表",
    "collocation_en": "place on the agenda",
    "collocation": "列入议程",
    "example_sentence_en": "We need to add this item to the agenda.",
    "example_sentence": "我们需要把这一项列入议程。",
    "chapter": "第1章",
    "updated_at": "2026-01-15T10:30:00Z"
  }
}
```

- 失败（400 Bad Request）：
```json
{
  "success": false,
  "error": "验证失败",
  "details": {
    "word": "单词不能为空",
    "definition": "中文释义不能为空"
  }
}
```

- 失败（401 Unauthorized）：
```json
{
  "success": false,
  "error": "未登录"
}
```

- 失败（403 Forbidden）：
```json
{
  "success": false,
  "error": "无权限编辑此单词"
}
```

- 失败（404 Not Found）：
```json
{
  "success": false,
  "error": "单词不存在"
}
```

**权限验证**：
```typescript
// 1. 用户必须登录
if (!user) {
  return 401, '未登录'
}

// 2. 单词必须存在
const word = await getWord(wordId)
if (!word) {
  return 404, '单词不存在'
}

// 3. 单词所属的词库必须是自定义词库
const book = await getBook(word.book_id)
if (book.is_official === true) {
  return 403, '官方词库不支持编辑'
}

// 4. 用户必须是词库的创建者
if (book.created_by !== user.id) {
  return 403, '无权限编辑此单词'
}

// 5. 验证通过，允许更新
```

### 6.2 删除单词

**接口**：`DELETE /api/words/{wordId}`

**请求头**：
```
Authorization: Bearer {access_token}
```

**响应**：
- 成功（200 OK）：
```json
{
  "success": true,
  "message": "单词已删除"
}
```

- 失败（403 Forbidden）：
```json
{
  "success": false,
  "error": "官方词库不支持删除"
}
```

**副作用**：
- 删除单词后，需要更新词库的 `total_words` 字段（减1）
- 删除相关的学习进度记录（`word_progress` 表）
- 如果是章节最后一个单词，章节保留（不删除）

---

## 7. 用户体验优化

### 7.1 自动保存（可选）

**功能**：用户编辑时，每隔30秒自动保存一次到草稿

**触发条件**：
- 用户修改了任意字段
- 距离上次保存超过30秒

**保存位置**：
- LocalStorage：`word_edit_draft_{wordId}`

**恢复逻辑**：
- 用户重新打开编辑对话框时，检查是否有草稿
- 如果有，提示："检测到未保存的草稿，是否恢复？"

**草稿数据结构**：
```typescript
{
  wordId: string,
  editedData: WordData,
  savedAt: timestamp
}
```

### 7.2 撤销功能（可选）

**功能**：保存后提供"撤销"功能

**实现方式**：
- 保存成功后，显示 Toast 提示
- 提示包含"撤销"按钮
- 点击撤销，恢复到修改前的数据
- 再次调用 API 更新回原数据

**Toast 样式**：
```
┌─────────────────────────────────┐
│ ✓ 单词已更新        [撤销]      │
└─────────────────────────────────┘
```

**撤销有效期**：
- 5秒后自动消失
- 5秒内点击"撤销"按钮有效

### 7.3 快捷键支持

**快捷键**：
- `Esc`：关闭对话框
- `Ctrl/Cmd + S`：保存并关闭
- `Ctrl/Cmd + Enter`：保存并关闭

**提示**：
- 在对话框底部显示快捷键提示
- 样式：小字号灰色文字

---

## 8. 边界情况处理

### 8.1 并发编辑

**场景**：用户在两个浏览器标签页中同时编辑同一个单词

**处理策略**：
- 使用乐观锁（updated_at 字段）
- API 验证：如果客户端的 `updated_at` < 服务端的 `updated_at`，拒绝更新
- 提示用户："此单词已被其他设备修改，请刷新后重试"

### 8.2 网络错误

**场景**：保存时网络断开

**处理策略**：
1. 检测到网络错误
2. 将修改保存到 LocalStorage（离线队列）
3. 显示提示："网络已断开，将在恢复连接后自动保存"
4. 网络恢复后，自动重试保存
5. 最多重试3次

### 8.3 章节变化

**场景**：用户修改了单词的章节

**处理策略**：
1. 保存成功后，刷新单词列表
2. 如果当前在筛选状态下（比如只显示"第1章"），提示："单词已移到其他章节"
3. 自动切换到"全部章节"筛选，显示移动后的单词

### 8.4 删除最后一个单词

**场景**：词库只剩最后一个单词，用户删除它

**处理策略**：
- 允许删除
- 删除后，词库变为空（`total_words = 0`）
- 显示空状态插画和文案

---

## 9. 响应式设计

### 9.1 PC端（>768px）

- 对话框宽度：`max-w-2xl`（672px）
- 居中显示
- 两列布局：左侧标签，右侧输入框（可选）

### 9.2 平板端（768px - 1024px）

- 对话框宽度：`max-w-xl`（512px）
- 居中显示
- 单列布局

### 9.3 移动端（<768px）

- 对话框宽度：`w-full`（全屏宽度）
- 侧边距：`mx-4`
- 单列布局
- 底部按钮固定在屏幕底部
- 可滚动内容区域（表单字段）

---

## 10. 测试用例

### 10.1 功能测试

| 用例 | 步骤 | 预期结果 |
|------|------|---------|
| 打开编辑对话框 | 1. 打开自定义词库<br>2. 点击单词卡片编辑按钮 | 显示编辑对话框，预填充单词数据 |
| 编辑单词 | 1. 修改单词字段<br>2. 点击保存 | 单词更新成功，卡片显示新数据 |
| 验证必填字段 | 1. 清空"单词"字段<br>2. 点击保存 | 显示错误提示："单词不能为空" |
| 修改章节 | 1. 修改单词章节<br>2. 点击保存 | 单词移动到新章节，列表刷新 |
| 取消编辑 | 1. 修改字段<br>2. 点击取消 | 对话框关闭，数据不变 |
| 删除单词 | 1. 点击删除<br>2. 确认3次 | 单词删除，从列表移除 |
| 官方词库 | 打开官方词库，查看单词卡片 | 不显示编辑按钮 |
| 非创建者 | 用非创建者账号打开自定义词库 | 不显示编辑按钮 |

### 10.2 性能测试

| 指标 | 目标 | 测试方法 |
|------|------|---------|
| 对话框打开速度 | <200ms | Performance API |
| 保存响应时间 | <500ms | API 响应时间 |
| 列表刷新时间 | <100ms | 乐观更新 |
| 动画帧率 | 60fps | Chrome DevTools |

---

## 11. 开发计划

### 11.1 Phase 1: 基础编辑功能（MVP）

- ✅ 编辑对话框 UI
- ✅ 基本字段编辑（单词、音标、释义）
- ✅ 保存 API
- ✅ 权限验证
- ✅ 乐观更新

**工作量**：约2-3天

### 11.2 Phase 2: 完整编辑功能

- ✅ 所有字段编辑（搭配、例句、章节）
- ✅ 删除单词功能
- ✅ 字段验证
- ✅ 错误处理

**工作量**：约1-2天

### 11.3 Phase 3: 用户体验优化

- ⏸️ 自动保存（草稿功能）
- ⏸️ 撤销功能
- ⏸️ 快捷键支持
- ⏸️ 离线队列

**工作量**：约1-2天

**总计**：4-7天

---

## 12. 相关文件

### 前端文件

| 文件 | 说明 |
|------|------|
| `src/components/WordCard.tsx` | 添加编辑按钮 |
| `src/components/EditWordDialog.tsx` | 新建：编辑对话框组件 |
| `src/app/api/words/[wordId]/route.ts` | 新建：更新/删除单词 API |
| `src/lib/wordEdit.ts` | 新建：编辑相关工具函数 |

### 数据库

无需修改表结构，使用现有的 `words` 表字段。

---

## 13. 未来扩展方向

- 批量编辑：选择多个单词，批量修改章节或词性
- 导出单词：将自定义词库导出为 Excel
- 导入更新：从 Excel 批量更新现有单词
- 单词历史：记录单词的修改历史，支持版本回溯
- AI 辅助：集成 AI 自动补全释义、搭配、例句

---

**文档版本**：v1.0
**最后更新**：2026-01-15

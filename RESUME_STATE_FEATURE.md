# 学习进度恢复功能

## 功能说明

用户在浏览单词列表时，系统会自动保存：
- 当前页码
- 筛选条件（状态、主题、场景、章节）

下次用户访问同一个词库时：
1. 如果检测到最近24小时内的学习进度
2. 显示对话框询问用户是否"继续学习"还是"从头开始"
3. 用户可以选择：

## 功能实现

### 1. 状态保存（自动）

**触发时机：**
- 页码改变
- 筛选条件改变
- 使用100ms防抖，避免频繁保存

**保存内容：**
```typescript
{
  bookId: '词库ID',
  mode: 'word-list',
  updatedAt: 时间戳,
  context: {
    filters: {
      status: 'new',      // 筛选状态
      theme: '购物',       // 主题
      scenario: '餐厅',    // 场景
      chapter: 'chapter-1' // 章节
    },
    page: 3  // 当前页码
  }
}
```

### 2. 状态恢复（用户选择）

**显示条件：**
- 同一个词库
- 同一个模式（word-list）
- 24小时内的学习进度
- 页码 > 1（如果只是第1页，没必要询问）
- 不是通过URL参数访问的（URL参数优先级更高）

**对话框内容：**
```
┌─────────────────────────────────┐
│  🔄 继续上次的学习进度？          │
│  上次学习到第 3 页               │
│                                  │
│  上次的学习设置：                 │
│  • 筛选：未标注                   │
│  • 主题：购物                     │
│  • 章节：第1章                   │
│                                  │
│  [从头开始]    [继续学习]         │
└─────────────────────────────────┘
```

### 3. 用户选择

#### 从头开始
- 不恢复任何状态
- 使用默认的第1页
- 使用默认的筛选条件（全部）
- 对话框关闭

#### 继续学习
- 恢复保存的页码（比如第3页）
- 恢复保存的筛选条件
- 自动加载对应页的数据
- 对话框关闭

## 技术细节

### 依赖关系
```
用户操作
  ↓
saveCurrentState() 被调用（防抖100ms）
  ↓
保存到数据库（user_book_preferences.last_resume_state）
  ↓
下次访问
  ↓
getResumeState() 检查数据库
  ↓
符合条件 → 显示对话框
  ↓
用户选择 → 恢复状态或不恢复
```

### API端点

**保存状态：**
```
POST /api/user-preferences
Body: {
  book_id: string,
  last_resume_state: ResumeState
}
```

**获取状态：**
```
GET /api/user-preferences?book_id=xxx
Response: {
  last_resume_state: ResumeState
}
```

### 优先级

1. **URL参数**（最高优先级）
   - 如果URL带有 ?theme=xxx&page=3
   - 直接从URL恢复
   - 不显示对话框

2. **保存的状态**（次优先级）
   - 检查数据库中是否有保存的状态
   - 如果有且符合条件，显示对话框

3. **默认状态**（最低优先级）
   - 第1页
   - 全部筛选条件

## 使用场景

### 场景1: 用户学到第3页后离开
```
1. 用户浏览到第3页
2. 点击浏览器返回按钮
3. 系统自动保存状态（第3页，筛选条件）
4. 用户再次访问该词库
5. 显示对话框："上次学习到第 3 页"
6. 用户选择"继续学习"
7. 自动跳转到第3页，恢复筛选条件
```

### 场景2: 用户筛选"未标注"后离开
```
1. 用户筛选"未标注"，浏览到第5页
2. 关闭浏览器
3. 第二天打开，访问该词库
4. 显示对话框："上次学习到第 5 页，筛选：未标注"
5. 用户选择"继续学习"
6. 自动跳转到第5页，显示未标注的单词
```

### 场景3: 用户想从头开始
```
1. 用户上次学到第10页
2. 今天再次访问
3. 显示对话框："上次学习到第 10 页"
4. 用户选择"从头开始"
5. 显示第1页，使用默认筛选条件
```

### 场景4: 从首页"继续学习"按钮访问
```
1. 用户在首页点击"继续学习"
2. 系统生成URL: /library/xxx?theme=购物&page=3
3. 页面直接从URL恢复状态
4. 不显示对话框（URL优先级更高）
```

## 已知限制

1. **24小时限制**
   - 只有24小时内的学习进度会提示
   - 超过24小时的不提示（避免打扰）

2. **只在第2页及以后提示**
   - 如果用户只看了第1页，不提示
   - 避免不必要的打扰

3. **同一词库同一模式**
   - 不同词库之间不恢复
   - 不同模式之间不恢复（word-list vs flashcards）

4. **筛选保存是客户端的**
   - 页面、状态筛选保存
   - 但单词列表本身不保存
   - 恢复时重新从API获取数据

## 用户体验优化

### 为什么不自动恢复？
- 用户可能想从头开始
- 给用户选择权更友好
- 避免强制恢复造成困惑

### 为什么用对话框？
- 清晰的用户提示
- 用户可以选择
- 显示详细信息（页码、筛选条件）

### 为什么有24小时限制？
- 太久之前的学习进度可能不相关
- 避免频繁打扰用户
- 可以后续添加设置让用户自定义

## 未来优化

1. **设置页面**
   - 让用户选择是否自动恢复
   - 让用户自定义时间限制
   - 让用户选择是否显示对话框

2. **多个保存点**
   - 保存多个书签（比如第3页、第7页）
   - 让用户选择跳转到哪个保存点

3. **进度条**
   - 显示总学习进度
   - "你已学习 60%（353/586）"

4. **学习时间统计**
   - "你今天学习了30分钟"
   - "连续学习7天"

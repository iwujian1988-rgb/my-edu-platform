# PM2 零停机重启指南

## 为什么不会影响用户？

### 1. 集群模式 + 滚动重启

当前配置使用 **集群模式**，会启动多个Node.js实例（根据CPU核心数）：

```bash
# 假设有4核CPU，会启动4个实例
pm2 list
┌───────┬──────────────┬──────────┐
│ id    │ name         │ status   │
├───────┼──────────────┼──────────┤
│ 0     │ my-edu       │ online  │  ← 实例1
│ 1     │ my-edu       │ online  │  ← 实例2
│ 2     │ my-edu       │ online  │  ← 实例3
│ 3     │ my-edu       │ online  │  ← 实例4
└───────┴──────────────┴──────────┘
```

### 2. 重启流程

执行 `pm2 reload` 或内存超限触发重启时：

```
时刻1: [实例1:重启] [实例2:运行] [实例3:运行] [实例4:运行]
       用户请求 → 实例2,3,4 处理

时刻2: [实例1:完成] [实例2:重启] [实例3:运行] [实例4:运行]
       用户请求 → 实例1,3,4 处理

时刻3: [实例1:运行] [实例2:完成] [实例3:重启] [实例4:运行]
       用户请求 → 实例1,2,4 处理

时刻4: [实例1:运行] [实例2:运行] [实例3:完成] [实例4:重启]
       用户请求 → 实例1,2,3 处理

时刻5: [实例1:运行] [实例2:运行] [实例3:运行] [实例4:完成]
       全部完成，所有实例健康运行
```

**关键点**：
- ✅ 总有3/4的实例在运行（75%容量）
- ✅ 用户请求自动路由到健康实例
- ✅ 每个实例独立重启，互不影响

### 3. 优雅关闭机制

配置了 `kill_timeout: 8000`（8秒）：

```javascript
// 进程收到关闭信号
process.on('SIGTERM', () => {
  // 1. 停止接收新请求
  server.close(() => {
    // 2. 等待当前请求完成（最多8秒）
    console.log('所有请求已处理完成')
    process.exit(0)
  })

  // 3. 8秒后强制退出（防止挂起）
  setTimeout(() => {
    console.log('强制退出')
    process.exit(1)
  }, 8000)
})
```

---

## 实际测试

### 测试零停机重启

```bash
# 1. 启动应用
npm run pm2:start

# 2. 查看实例状态
pm2 list

# 3. 持续监控（另一个终端）
watch -n 1 'pm2 list'

# 4. 执行零停机重启
pm2 reload ecosystem.config.js

# 观察到：
# - 实例逐个重启（status: 'stopping' → 'launching' → 'online'）
# - 请求不会中断
```

### 模拟用户请求测试

```bash
# 在重启期间持续发送请求
while true; do
  curl -s http://localhost:3000 > /dev/null
  if [ $? -eq 0 ]; then
    echo "✅ 请求成功"
  else
    echo "❌ 请求失败"
  fi
  sleep 0.5
done
```

**预期结果**：即使执行 `pm2 reload`，请求也应该全部成功 ✅

---

## 手动重启命令对比

| 命令 | 停机时间 | 说明 |
|------|---------|------|
| `pm2 reload` | **零停机** | ✅ 推荐，逐个重启实例 |
| `pm2 restart` | **短暂停机** | ⚠️ 所有实例同时重启 |
| `pm2 stop && pm2 start` | **长时间停机** | ❌ 不推荐，完全停止服务 |

---

## 内存超限自动重启

### 触发条件

当单个实例内存超过 1.5GB 时：

```javascript
max_memory_restart: '1.5G'
```

### 自动重启行为

```
实例1内存: 1.6GB → 触发重启
[实例1:重启中] [实例2:运行] [实例3:运行] [实例4:运行]
    ↓
[实例1:健康]   [实例2:运行] [实例3:运行] [实例4:运行]
```

**用户体验**：
- ✅ 无感知
- ✅ 请求不中断
- ✅ 自动恢复

---

## 监控建议

### 1. 实时监控

```bash
# 实时查看所有实例
npm run pm2:monit
```

### 2. 设置告警

当某个实例频繁重启时，需要排查问题：

```bash
# 查看重启次数
pm2 show my-edu-platform | grep "restart time"

# 如果某个实例重启超过10次/小时，说明有内存泄漏
```

### 3. 日志分析

```bash
# 查看重启日志
pm2 logs my-edu-platform --err

# 查看内存使用趋势
pm2 show my-edu-platform | grep "memory"
```

---

## 常见问题

### Q: 如果所有实例同时崩溃怎么办？

**A**: PM2会立即重启所有实例：

```javascript
max_restarts: 10  // 最多重启10次
restart_delay: 5000  // 每次间隔5秒
```

**影响**：短暂中断（5-10秒），然后恢复正常

### Q: 重启期间的用户数据会丢失吗？

**A**: 不会，因为：

1. **HTTP请求**：正在处理的请求会等待完成（最多8秒）
2. **数据库**：Supabase自动处理连接
3. **进度数据**：已保存到数据库，不受重启影响

### Q: 能否完全禁用自动重启？

**A**: 不建议，但可以调整：

```javascript
max_memory_restart: '3G'  // 提高到3GB（不推荐）
// 或
max_memory_restart: false  // 完全禁用（危险）
```

---

## 最佳实践

1. **生产环境必须使用集群模式** ✅
2. **保留至少1个空闲实例** ✅
3. **设置合理的内存阈值** ✅（1.5GB）
4. **定期监控日志** ✅
5. **及时发现频繁重启** ⚠️

---

## 总结

✅ **零停机重启已配置**
✅ **用户请求不会中断**
✅ **正在处理的请求会等待完成**
✅ **多个实例保证高可用**

**安全系数**：
- 假设4核CPU = 4个实例
- 1个重启时仍有3个在运行（75%容量）
- 即使1个实例崩溃，其他3个继续服务
- PM2自动重启崩溃的实例

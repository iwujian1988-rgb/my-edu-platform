# 继续学习功能产品需求文档 (PRD)

## 文档信息
- **产品名称**: 喵喵笔记 - 智能英语学习平台
- **功能模块**: 继续学习功能
- **文档版本**: v1.0
- **创建日期**: 2026-01-17
- **文档状态**: 已实现

---

## 1. 功能概述

### 1.1 功能描述
"继续学习"功能帮助用户快速恢复上次的学���状态，避免重复学习，提高学习效率。该功能覆盖所有学习模式，包括单词列表、卡片背单词、听写和打字游戏。

### 1.2 用户价值
- **快速恢复**: 一键回到上次学习的位置
- **进度可视化**: 清晰显示上次学习的范围和进度
- **多场景支持**: 支持不同学习模式的断点续做
- **智能推荐**: 优先显示最近学习的词库

---

## 2. 功能架构

### 2.1 触发入口

继续学习功能有多个触发入口：

1. **首页"最近学习"区域**
   - 显示位置：首页顶部
   - 显示数量：最多3个词库卡片
   - 触发方式：点击卡片直接进入学习

2. **词库详情页**
   - 显示位置：词库详情页顶部
   - 显示条件：有学习进度时显示
   - 触发方式：点击"继续学习"按钮

3. **学习模式选择对话框**
   - 显示位置：进入学习模式时的弹窗
   - 显示条件：有上次学习记录时显示
   - 触发方式：点击"继续"按钮

### 2.2 覆盖的学习模式

| 学习模式 | 支持状态 | 优先级 |
|---------|---------|-------|
| 单词列表 | ✅ 完整支持 | P0 |
| 卡片背单词 | ✅ 完整支持 | P0 |
| 听写模式 | ✅ 完整支持 | P0 |
| 打字游戏 | ⚠️ 部分支持 | P1 |

---

## 3. 首页"最近学习"功能

### 3.1 功能说明

在首页顶部显示用户最近学习过的3个词库卡片，每个卡片显示学习进度和模式，点击后直接跳转到上次学习的位置继续学习。

### 3.2 数据来源

系统按以下优先级获取"最近学习"数据：

**优先级1**: `user_book_preferences` 表
- 数据来源：打字练习模式更新
- 判断依据：最后访问时间（last_accessed_at）
- 数据完整性：包含完整的学习记录

**优先级2**: `typing_recent_practice` 表
- 数据来源：打字练习的专门记录
- 判断依据：最后练习时间（last_practice_at）
- 数据完整性：包含词库和范围

**优先级3**: `word_progress` 表
- 数据来源：所有学习模式（卡片、听写等）
- 判断依据：进度更新时间（updated_at）
- 数据完整性：需要去重处理

**兜底策略**: 如果以上3个数据源都为空，显示"空空如也"状态

### 3.3 卡片内容设计

每个"最近学习"卡片显示以下信息：

#### 3.3.1 卡片头部信息

**位置**：卡片左上角

**显示内容**：
1. **书名**
   - 字体：加粗、黑色
   - 大小：16px（移动端）/ 18px（桌面端）
   - 限制：单行显示，超长截断

2. **学习模式信息**
   - 显示格式：`图标 + 模式名称 • 范围名称`
   - 示例：
     - `🎯 卡片 • 不认识的`
     - `✍️ 听写 • 模糊的`
     - `⌨️ 打字 • 未标注的`
   - 颜色：灰色文字（text-gray-600）
   - 字体：加粗、小字号（12px）

**图标说明**：
- 📚 单词列表：`BookOpen` 图标，蓝色
- 🎯 卡片背单词：`Target` 图标，紫色
- ✍️ 听写模式：`PenTool` 图标，绿色
- ⌨️ 打字游戏：`Keyboard` 图标，橙色

#### 3.3.2 卡片右上角图标

**位置**：卡片右上角

**显示内容**：
- 方形圆角图标（40x40px）
- 图标：对应学习模式的图标（白色）
- 背景色：对应学习模式的主题色
  - 单词列表：蓝色背景
  - 卡片背单词：紫色背景
  - 听写模式：绿色背景
  - 打字游戏：橙色背景
- 边框：2px 黑色边框
- 交互效果：悬停时图标放大 1.1 倍

#### 3.3.3 学习进度信息

**位置**：卡片中部

**显示内容**：

1. **进度百分比**（大字显示）
   - 字体：特大号、加粗、黑色
   - 大小：36px（移动端）/ 48px（桌面端）
   - 显示格式：`数字 + %`
   - 示例：`0%`、`15%`、`85%`
   - 对齐：左对齐

2. **当前位置信息**（小字显示）
   - 字体：小号、加粗、灰色
   - 大小：12px
   - 显示格式：`当前位置/总数 • 最后学习时间`
   - 示例：
     - `5/50 • 5分钟前`
     - `127/10101 • 2小时前`
     - `1/100 • 刚刚`
   - 对齐：左对齐

#### 3.3.4 完整卡片示例

**示例1：卡片背单词 - 不认识的范围**
```
┌─────────────────────────────────┐
│ GRE                      [图标] │
│ 🎯 卡片 • 不认识的              │
│                                 │
│ 0%                              │
│ 5/50 • 5分钟前                  │
└─────────────────────────────────┘
```

**示例2：听写模式 - 模糊的范围**
```
┌─────────────────────────────────┐
│ PETS3                   [图标] │
│ ✍️ 听写 • 模糊的                │
│                                 │
│ 15%                             │
│ 127/850 • 2小时前               │
└─────────────────────────────────┘
```

**示例3：打字游戏 - 未标注的范围**
```
┌─────────────────────────────────┐
│ 外研社初中英语         [图标] │
│ ⌨️ 打字 • 未标注的              │
│                                 │
│ 0%                              │
│ 1/2000 • 刚刚                  │
└─────────────────────────────────┘
```

#### 3.3.5 数据计算逻辑

**进度百分比计算**：
```
已学习数量 = 认识数量 + 模糊数量
学习进度 = (已学习数量 ÷ 总单词数) × 100%
向下取整到整数
```

**示例计算**：
- 总单词数：10101
- 认识数量：1
- 模糊数量：0
- 已学习数量：1 + 0 = 1
- 学习进度：(1 ÷ 10101) × 100% = 0.009% → 0%

**当前位置显示**：
```
当前位置 = 当前索引 + 1
（索引从0开始，显示时从1开始）
总数量 = 该范围内的总单词数
```

**时间标签格式化**：
- 1分钟内："刚刚"
- 1小时内："X分钟前"（如：5分钟前、30分钟前）
- 24小时内："X小时前"（如：2小时前、10小时前）
- 24小时~7天："X天前"（如：2天前、5天前）
- 7天以上："X月X日"（如：1月15日、12月25日）

#### 3.3.6 视觉设计规范

**卡片样式**：
- 背景：白色
- 边框：3px 黑色实线
- 圆角：12px
- 阴影：3px 3px 0px 0px 黑色
- 内边距：16px
- 卡片高度：自适应（约120px）

**响应式布局**：
- 移动端（< 1024px）：2列网格
- 桌面端（≥ 1024px）：4列网格
- 卡片间距：16px（移动端）/ 24px（桌面端）

**交互动画**：
1. **悬停效果**：
   - 整体卡片向上平移 2px
   - 阴影减小为 2px 2px 0px 0px
   - 平滑过渡（transition: 0.2s）

2. **点击反馈**：
   - 卡片向下移动到原位
   - 显示点击态样式
   - 页面跳转

3. **图标动画**：
   - 右上角图标悬停时放大 1.1 倍
   - 平滑过渡（transition: 0.2s）

#### 3.3.7 空状态设计

当用户没有学习记录时，显示空状态卡片：

**显示位置**：与正常卡片相同位置

**显示内容**：
```
┌─────────────────────────────────┐
│                                 │
│            👻                   │
│                                 │
│         空空如也                 │
│    还没有访问过任何词库         │
│                                 │
└─────────────────────────────────┘
```

**文案说明**：
- 标题："空空如也"
- 副标题："还没有访问过任何词库"
- 图标：Ghost 图标（40x40px）
- 颜色：灰色
- 居中对齐

**替代方案**：
如果不显示空状态卡片，该位置显示"新建词库"卡片，引导用户创建或导入词库。

### 3.4 点击跳转逻辑

点击卡片后，系统执行以下操作：

1. **读取断点数据**
   - 学习模式（flashcards/dictation/typing/word-list）
   - 学习范围（unknown/fuzzy/known/new/all）
   - 当前位置（索引或页码）

2. **智能范围验证**
   - 检查上次学习的范围是否有单词
   - 如果范围为空（0个单词），自动切换到"全部单词"
   - 避免跳转到空页面

3. **生成跳转链接**
   - 单词列表：跳转到词库详情页，带筛选条件和页码
   - 卡片模式：跳转到学习页，带范围和索引
   - 听写模式：跳转到学习页，带范围和索引
   - 打字游戏：跳转到练习页，带范围

4. **执行跳转**
   - 在新标签页或当前页打开学习页面
   - 自动定位到上次学习的位置

### 3.5 空状态设计

当用户没有任何学习记录时，显示空状态：

**文案内容**：
- 标题："空空如也"
- 提示："还没有访问过任何词库"

**视觉设计**：
- 居中显示
- 灰色图标（Ghost 图标）
- 引导按钮："开始学习"

---

## 4. 单词列表模式继续学习

### 4.1 功能说明

用户在单词列表页面浏览单词时，系统会记录用户的筛选条件、页码等信息。下次进入该词库时，系统提示用户是否继续上次的学习。

### 4.2 数据记录时机

系统在以下时机保存学习记录：

1. **切换筛选条件时**
   - 状态筛选（不认识的/模糊的/认识的/未标注的）
   - 主题筛选
   - 场景筛选

2. **翻页时**
   - 记录当前页码

3. **离开页面时**
   - 保存最后的浏览状态

### 4.3 恢复提示设计

当用户重新进入词库详情页时：

**触发条件**：
- 存在上次的学习记录（last_reading_progress）

**提示位置**：
- 页面顶部
- 浮层对话框形式

**提示内容**：
```
┌──────────────────────────────────┐
│  📚 继续上次学习                 │
│                                  │
│  您上次在学习"不认识的 - 未标注" │
│  （第2页），是否继续？           │
│                                  │
│    [取消]        [继续学习]      │
└──────────────────────────────────┘
```

**文案规则**：
- 有筛选条件且页码>1：显示"您上次在学习'筛选条件'（第X页），是否继续？"
- 有筛选条件且页码=1：显示"您上次在学习'筛选条件'，是否继续？"
- 无筛选条件且页码>1：显示"您上次学习到第X页，是否继续学习？"
- 无筛选条件且页码=1：显示"您上次有学习进度，是否继续？"

### 4.4 用户操作流程

**场景1：选择继续学习**
```
1. 用户进入词库详情页
   ↓
2. 系统检测到学习记录
   ↓
3. 显示恢复提示对话框
   ↓
4. 用户点击"继续学习"
   ↓
5. 系统执行以下操作：
   - 应用上次的筛选条件
   - 跳转到上次的页码
   - 滚动到页面顶部
   ↓
6. 用户继续浏览
```

**场景2：选择不继续**
```
1. 用户进入词库详情页
   ↓
2. 系统检测到学习记录
   ↓
3. 显示恢复提示对话框
   ↓
4. 用户点击"取消"或点击对话框外部
   ↓
5. 对话框关闭
   ↓
6. 用户可以：
   - 重新选择筛选条件
   - 从第1页开始浏览
```

### 4.5 特殊处理

**筛选条件缺失**：
- 如果上次的主题/场景在当前词库中不存在
- 忽略该筛选条件，使用默认值（all）

**页码超限**：
- 如果上次页码超过当前总页数
- 自动跳转到最后一页

**数据清除**：
- 用户刷新页面后，提示不再显示
- 用户选择"继续学习"后，提示不再显示

---

## 5. 卡片背单词模式继续学习

### 5.1 功能说明

用户在卡片背单词模式下学习时，系统会记录学习的范围（不认识的/模糊的等）和当前学到的位置（第几个单词）。下次进入时，系统提供"继续上次学习"的快速入口。

### 5.2 数据记录时机

系统在以下时机保存学习记录：

1. **切换范围时**
   - 记录选择的学习范围（scope）
   - 记录范围内的总单词数

2. **切换单词时**
   - 每次点击"下一个"或"上一个"
   - 实时更新当前索引

3. **完成学习时**
   - 学习完当前范围的所有单词
   - 标记完成状态

### 5.3 恢复提示设计

**触发时机**：
用户点击词库的"卡片背单词"入口时

**提示位置**：
- 范围选择对话框的顶部
- 高亮显示，使用渐变绿色背景

**提示内容**：
```
┌──────────────────────────────────────┐
│ ✨ 继续上次学习                      │
│ ⏰ 5分钟前                           │
│ 📍 不认识的，第 5 题 / 共 50 题      │
│                          [继续] 按钮 │
└──────────────────────────────────────┘
```

**信息要素**：
1. **时间显示**
   - 1分钟内："刚刚"
   - 1小时内："X分钟前"
   - 24小时内："X小时前"
   - 7天内："X天前"
   - 7天以上："X月X日"

2. **进度显示**
   - 范围名称（不认识的/模糊的/认识的/未标注的/全部单词）
   - 当前位置（第 X 题）
   - 总数量（共 Y 题）

### 5.4 用户操作流程

**场景1：从继续学习卡片进入**
```
1. 用户打开范围选择对话框
   ↓
2. 看到顶部的"继续上次学习"卡片
   ↓
3. 点击"继续"按钮
   ↓
4. 系统执行以下操作：
   - 加载上次的学习范围
   - 跳转到上次的位置（第5个单词）
   - 显示该单词的卡片
   - 隐藏范围选择对话框
   ↓
5. 用户继续学习
```

**场景2：选择新的学习范围**
```
1. 用户打开范围选择对话框
   ↓
2. 看到顶部的"继续上次学习"卡片
   ↓
3. 忽略该卡片，向下滚动
   ↓
4. 选择其他学习范围
   ↓
5. 开始新的学习
   ↓
6. 系统覆盖旧的断点记录
```

### 5.5 首页卡片快速进入

**入口位置**：首页"最近学习"区域

**点击流程**：
```
1. 用户在首页看到"最近学习"卡片
   例如："GRE - 卡片模式 (0%)"
   ↓
2. 点击卡片
   ↓
3. 系统直接跳转到断点位置
   - 不显示范围选择对话框
   - 自动加载上次的学习范围
   - 自动定位到上次的单词
   ↓
4. 用户立即继续学习
```

**跳转URL格式**：
```
/study/{词库ID}/flashcards?scope={范围}#word-{索引}

示例：
/study/20b0873a.../flashcards?scope=unknown#word-4
```

### 5.6 特殊处理

**单词数为0**：
- 如果上次选择的范围现在没有单词
- 首页生成链接时自动切换到"全部单词"
- 提示用户："上次学习的范围已无单词，已切换到全部单词"

**索引超限**：
- 如果上次的索引超过当前范围的总单词数
- 自动调整到最后一题

**进度重置**：
- 用户完成当前范围后
- 索引重置为0
- 下次显示为"第 1 题"

---

## 6. 听写模式继续学习

### 6.1 功能说明

听写模式的"继续学习"功能与卡片模式类似，记录用户的学习范围和当前位置，下次进入时提供快速恢复入口。

### 6.2 数据记录时机

系统在以下时机保存学习记录：

1. **开始听写时**
   - 记录选择的学习范围
   - 记录起始索引

2. **切换单词时**
   - 用户提交当前单词的听写答案后
   - 更新当前索引

3. **暂停/退出时**
   - 用户点击暂停或关闭页面
   - 保存当前位置

### 6.3 恢复提示设计

**触发时机**：
用户点击词库的"听写模式"入口时

**提示位置**：
- 范围选择对话框的顶部
- 高亮显示，使用渐变绿色背景

**提示内容**：
```
┌──────────────────────────────────────┐
│ ✨ 继续上次学习                      │
│ ⏰ 10分钟前                          │
│ 📍 不认识的，第 7 题 / 共 100 题     │
│                          [继续] 按钮 │
└──────────────────────────────────────┘
```

**信息要素**：与卡片模式相同

### 6.4 用户操作流程

**场景1：从继续学习卡片进入**
```
1. 用户打开范围选择对话框
   ↓
2. 看到顶部的"继续上次学习"卡片
   ↓
3. 点击"继续"按钮
   ↓
4. 系统执行以下操作：
   - 加载上次的学习范围
   - 跳转到上次的位置（第7个单词）
   - 自动播放该单词的发音
   - 显示输入框
   - 隐藏范围选择对话框
   ↓
5. 用户继续听写
```

**场景2：从首页卡片快速进入**
```
1. 用户在首页看到"最近学习"卡片
   例如："GRE - 听写模式 (15%)"
   ↓
2. 点击卡片
   ↓
3. 系统直接跳转到断点位置
   - 不显示范围选择对话框
   - 自动加载上次的学习范围
   - 自动定位到上次的单词
   - 自动播放发音
   ↓
4. 用户立即开始听写
```

**跳转URL格式**：
```
/study/{词库ID}/dictation?scope={范围}#word-{索引}

示例：
/study/20b0873a.../dictation?scope=unknown#word-6
```

### 6.5 特殊处理

**单词数为0**：
- 如果上次选择的范围现在没有单词
- 首页生成链接时自动切换到"全部单词"

**发音自动播放**：
- 从断点继续时，自动播放单词发音
- 提示用户开始听写

**重试机制**：
- 如果上次用户答错了某个单词
- 从断点继续后，重新播放该单词

---

## 7. 打字游戏模式继续学习

### 7.1 功能说明

打字游戏模式目前仅支持"最近学习"记录，暂不支持完整的断点续做功能。

### 7.2 当前实现

**已实现**：
- ✅ 记录最近练习的词库和范围
- ✅ 在首页显示"最近学习"记录
- ✅ 点击后快速进入该词库+范围

**未实现**：
- ❌ 记录当前练习到第几个单词
- ❌ 提供断点续做选项
- ❌ 从断点位置继续练习

### 7.3 "最近学习"显示

**显示位置**：
- 首页"最近学习"卡片
- 或：打字游戏开始时的"最近学习"区域

**显示内容**：
```
最近学习
  📕 GRE
     📊 不认识的
     → 点击跳转
```

**点击行为**：
- 快速进入该词库的学习范围
- 从第0个单词开始（重新开始）

### 7.4 用户体验

**当前流程**：
```
1. 用户在首页看到"最近学习"
   例如："GRE - 未标注"
   ↓
2. 点击卡片
   ↓
3. 进入打字游戏
   ↓
4. 从第0个单词开始练习
   （即使上次学到第10个单词）
```

**改进空间**：
- 显示"上次练习到第X个单词"
- 提供"继续练习"和"重新开始"两个选项
- 支持"断点续做"

---

## 8. 跨模式断点管理

### 8.1 数据存储结构

所有学习模式的断点数据统一存储在 `user_book_preferences` 表中：

**字段1**: `last_reading_progress`
- 用途：单词列表模式的阅读进度
- 包含：页码、筛选条件

**字段2**: `last_resume_state`
- 用途：学习模式的断点数据
- 包含：模式、范围、索引、总单词数

### 8.2 数据覆盖规则

**同一词库，不同模式**：
- 每个学习模式独立记录断点
- 互不干扰
- 例如：卡片模式学到第5题，听写模式学到第7题，各自记录

**同一词库，同一模式**：
- 新的学习记录覆盖旧记录
- 例如：上次在"不认识的"范围学到第5题，本次切换到"模糊的"范围，则覆盖为新的范围

### 8.3 数据优先级

首页获取"最近学习"时，按以下优先级：

1. **user_book_preferences** (最高优先级)
   - 最完整的学习记录
   - 包含学习模式和位置

2. **typing_recent_practice** (中等优先级)
   - 打字游戏的专门记录
   - 仅包含词库和范围

3. **word_progress** (最低优先级)
   - 所有学习模式的进度记录
   - 需要提取最新学习的词库

---

## 9. 异常场景处理

### 9.1 数据丢失

**场景**：用户的断点数据被意外删除

**处理**：
- 不显示恢复提示
- 用户需要重新开始学习
- 不影响新学习记录的保存

### 9.2 数据不一致

**场景**：上次学习的范围已修改（单词数变化）

**处理**：
- 自动调整索引到有效范围
- 如果索引超限，调整到最后一题/页
- 显示提示："上次的学习范围已更新，已调整到最新位置"

### 9.3 词库删除

**场景**：上次学习的词库被删除

**处理**：
- 首页不显示该词库的卡片
- 不显示恢复提示
- 用户只能选择其他词库

### 9.4 权限变更

**场景**：用户失去某词库的访问权限

**处理**：
- 首页不显示该词库的卡片
- 不显示恢复提示
- 用户只能选择有权限的词库

---

## 10. 性能要求

### 10.1 响应时间

**首页加载**：
- 数据查询时间：< 2秒
- 页面渲染时间：< 1秒
- 总加载时间：< 3秒

**断点恢复**：
- 数据读取时间：< 500ms
- 页面跳转时间：< 200ms
- 总恢复时间：< 1秒

### 10.2 数据同步

**实时保存**：
- 切换单词时立即保存
- 翻页时立即保存
- 切换筛选时立即保存

**可靠性**：
- 保存成功率：> 99.9%
- 数据一致性：强一致性
- 容错机制：自动重试

---

## 11. 用户反馈机制

### 11.1 视觉反馈

**成功提示**：
- 数据保存成功：不显示（静默保存）
- 断点恢复成功：页面跳转，自然过渡

**失败提示**：
- 数据保存失败：显示错误提示
- 断点恢复失败：显示错误提示，提供重试选项

### 11.2 引导提示

**首次使用**：
- 不显示引导（功能自解释）
- 通过界面文案自然引导

**功能发现**：
- 首页"最近学习"区域
- 对话框中的"继续学习"卡片
- 高亮显示，绿色背景

---

## 12. 数据统计

### 12.1 关键指标

**功能使用率**：
- 首页"最近学习"卡片点击率
- "继续学习"按钮点击率
- 断点恢复成功率

**用户行为**：
- 用户更倾向于使用哪个模式的继续学习
- 用户通常学习多少分钟后返回
- 用户是否完成整个学习范围

### 12.2 数据埋点

**记录事件**：
1. 首页"最近学习"卡片曝光
2. 首页"最近学习"卡片点击
3. 断点恢复提示显示
4. "继续学习"按钮点击
5. "重新开始"按钮点击
6. 断点恢复成功/失败

---

## 13. 附录

### 13.1 术语表

- **断点续做**：记录用户的学习位置，下次可以继续
- **学习范围**：用户选择学习的单词类型（不认识的/模糊的等）
- **索引**：当前学到第几个单词（从0开始计数）
- **恢复提示**：系统提示用户是否继续上次学习的对话框

### 13.2 相关文档

- 《继续学习功能完整性报告.md》：技术实现细节
- 《首页卡片断点跳转问题分析.md》：问题分析报告
- 《PRD.md》：产品总体需求文档

---

## 14. 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2026-01-17 | 初始版本，完整描述所有继续学习功能 | Claude |

---

**文档状态**: ✅ 已完成
**最后更新**: 2026-01-17

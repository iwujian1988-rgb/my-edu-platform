# 听写模式重构技术评审报告

**评审人**：技术评审专家
**评审日期**：2026-01-14
**评审对象**：《听写模式重构技术文档》v1.0
**评审结论**：✅ **有条件通过**（需修改7个问题）

---

## 📊 评审总览

| 评审维度 | 评分 | 结论 |
|---------|------|------|
| 架构设计 | ⭐⭐⭐⭐☆ | 优秀 |
| 数据库设计 | ⭐⭐⭐☆☆ | 需改进 |
| API设计 | ⭐⭐⭐⭐☆ | 优秀 |
| 前端架构 | ⭐⭐⭐⭐⭐ | 优秀 |
| 测试策略 | ⭐⭐⭐⭐☆ | 良好 |
| 性能与安全 | ⭐⭐⭐☆☆ | 需改进 |
| 实施可行性 | ⭐⭐⭐⭐☆ | 良好 |

**总体评分**：⭐⭐⭐⭐☆ (4.0/5.0)

---

## 🔴 严重问题（P0 - 必须修复）

### 问题1：数据库表设计过度设计 ❌

**问题描述**：
技术文档提议创建新的`dictation_progress`表，但系统已有`user_book_preferences.preferences` JSONB字段用于存储flashcard进度，存在架构不一致。

**现有实现**：
```sql
-- user_book_preferences 表 (已存在)
CREATE TABLE user_book_preferences (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  book_id UUID NOT NULL,
  preferences JSONB DEFAULT '{}'::jsonb,  -- ✅ 已有JSONB字段
  -- ...
);

-- flashcard-progress 使用方式
progressKey = `flashcard_progress_${bookId}_${scopeType}`
preferences[progressKey] = { currentIndex, totalWords, lastStudyTime, scopeType }
```

**技术文档提议**：
```sql
-- ❌ 新建独立的dictation_progress表
CREATE TABLE dictation_progress (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  book_id TEXT NOT NULL,
  scope_type TEXT NOT NULL,
  current_index INTEGER NOT NULL,
  total_words INTEGER NOT NULL,
  -- ...
);
```

**问题分析**：
1. ❌ **架构不一致**：flashcard用JSONB，dictation用独立表，增加维护成本
2. ❌ **过度设计**：听写进度和flashcard进度结构完全相同，不需要两套存储方案
3. ❌ **迁移复杂**：新建表需要额外的迁移脚本和索引维护
4. ❌ **查询效率**：独立表需要JOIN查询，JSONB字段一次查询搞定

**改进建议**：
```sql
-- ✅ 推荐：复用user_book_preferences.preferences
-- dictation进度键名
progressKey = `dictation_progress_${bookId}_${scopeType}`

-- 存储格式
preferences[progressKey] = {
  currentIndex: 15,
  totalWords: 30,
  lastStudyTime: 1705228800000,
  scopeType: "unknown"
}

-- API复用
GET /api/flashcard-progress?bookId=xxx&scopeType=xxx&mode=dictation
POST /api/flashcard-progress
{
  "bookId": "xxx",
  "mode": "dictation",  // 新增mode字段区分
  "scopeType": "unknown",
  "currentIndex": 15,
  "totalWords": 30
}
```

**优点**：
- ✅ 架构统一：两种模式使用相同的存储方案
- ✅ 代码复用：直接复用flashcard-progress API
- ✅ 查询高效：单表查询，无需JOIN
- ✅ 维护简单：只需维护一个表结构

**影响范围**：
- 需要修改技术文档第3.1节（数据库设计）
- 需要修改第4.1.2节（听写进度API）
- 无需新建表，无需迁移脚本

---

### 问题2：API设计缺少Zod验证 ⚠️

**问题描述**：
技术文档中提到"使用Zod schema参数验证"，但API实现示例中没有实际使用Zod进行严格的输入验证。

**技术文档示例**：
```typescript
// 文档中说"使用Zod schema参数验证"
const GetStatsSchema = z.object({
  bookId: z.string().min(1, 'bookId不能为空')
})

// ❌ 但后续代码中没有正确使用错误处理
const validationResult = GetStatsSchema.safeParse({ bookId })
if (!validationResult.success) {
  return NextResponse.json({
    success: false,
    error: validationResult.error.errors[0].message,  // ❌ 只返回第一个错误
    code: 'INVALID_PARAMS'
  }, { status: 400 })
}
```

**改进建议**：
```typescript
// ✅ 完整的Zod验证实现
import { z } from 'zod'
import { fromZodError } from 'zod-validation-error'

const GetStatsSchema = z.object({
  bookId: z.string().min(1, 'bookId不能为空').uuid('bookId格式错误'),  // 增加UUID验证
  scopeType: z.enum(['all', 'unknown', 'fuzzy', 'known', 'new']).optional()
})

export async function GET(request: NextRequest) {
  try {
    const searchParams = Object.fromEntries(request.nextUrl.searchParams)

    // ✅ 使用zod的parse而不是safeParse，自动抛出详细错误
    const { bookId, scopeType } = GetStatsSchema.parse(searchParams)

    // 业务逻辑...

  } catch (error) {
    if (error instanceof z.ZodError) {
      // ✅ 返回所有验证错误，而不是只有第一个
      return NextResponse.json({
        success: false,
        error: fromZodError(error).message,  // 格式化所有错误
        code: 'INVALID_PARAMS',
        details: error.errors  // 返回详细错误列表
      }, { status: 400 })
    }

    console.error('❌ [Stats API] 服务器错误:', error)
    return NextResponse.json({
      success: false,
      error: '服务器内部错误',
      code: 'INTERNAL_ERROR'
    }, { status: 500 })
  }
}
```

**影响范围**：
- 需要修改技术文档第4.1节（API设计）的所有示例代码
- 需要安装zod-validation-error依赖

---

### 问题3：缺少缓存策略设计 ⚠️

**问题描述**：
技术文档在多处提到"缓存策略"，但没有统一的缓存方案设计，存在缓存击穿风险。

**文档中的缓存提及**：
```typescript
// 第7.2节：监控指标
// await cache.set(`stats:${user.id}:${bookId}`, statusCount, 300)

// ❌ 但没有定义：
// 1. cache的实现（Redis？Memcached？In-memory？）
// 2. 缓存失效策略
// 3. 缓存预热机制
// 4. 缓存击穿保护
```

**风险分析**：
- ❌ **缓存击穿**：高并发时，大量用户同时查询同一个stats，可能导致数据库压力骤增
- ❌ **缓存雪崩**：如果所有缓存同时失效（如同时设置5分钟过期），会导致数据库请求暴增
- ❌ **数据一致性**：用户更新word_progress后，缓存没有及时失效

**改进建议**：
```typescript
// ✅ 统一缓存服务
// lib/cache/redis.ts

import { Redis } from 'ioredis'

class CacheService {
  private redis: Redis
  private readonly DEFAULT_TTL = 300 // 5分钟

  constructor() {
    this.redis = new Redis(process.env.REDIS_URL!)
  }

  /**
   * 获取统计数据（带缓存击穿保护）
   */
  async getStats(userId: string, bookId: string): Promise<DictationStats | null> {
    const cacheKey = `stats:${userId}:${bookId}`

    // 1. 尝试从缓存读取
    const cached = await this.redis.get(cacheKey)
    if (cached) {
      return JSON.parse(cached)
    }

    // 2. 使用SET NX实现缓存击穿保护
    const lockKey = `lock:${cacheKey}`
    const lockAcquired = await this.redis.set(lockKey, '1', 'NX', 'EX', 10)

    if (!lockAcquired) {
      // 锁已被占用，等待100ms后重试
      await new Promise(resolve => setTimeout(resolve, 100))
      return this.getStats(userId, bookId)  // 递归重试
    }

    try {
      // 3. 从数据库查询
      const stats = await this.fetchStatsFromDB(userId, bookId)

      // 4. 写入缓存（添加随机过期时间，避免雪崩）
      const randomTTL = this.DEFAULT_TTL + Math.random() * 60  // 300-360秒
      await this.redis.setex(cacheKey, randomTTL, JSON.stringify(stats))

      return stats
    } finally {
      // 5. 释放锁
      await this.redis.del(lockKey)
    }
  }

  /**
   * 失效缓存（当word_progress更新时调用）
   */
  async invalidateStats(userId: string, bookId: string): Promise<void> {
    const cacheKey = `stats:${userId}:${bookId}`
    await this.redis.del(cacheKey)
  }
}

export const cacheService = new CacheService()
```

**使用示例**：
```typescript
// /api/words/stats/route.ts

export async function POST(request: NextRequest) {
  const { word_id, book_id, status } = await request.json()

  // 1. 更新word_progress
  await supabase.from('word_progress').upsert({ ... })

  // 2. 失效相关缓存
  await cacheService.invalidateStats(user.id, book_id)

  return NextResponse.json({ success: true })
}
```

**影响范围**：
- 需要在技术文档中新增"缓存架构设计"章节
- 需要安装Redis/ioredis依赖
- 需要在所有word_progress更新处添加缓存失效逻辑

---

## 🟡 重要问题（P1 - 建议修复）

### 问题4：Service层缺少重试机制 ⚠️

**问题描述**：
Service层的API调用没有实现重试机制，网络抖动时用户体验差。

**当前实现**：
```typescript
// services/dictationService.ts

async saveProgress(...): Promise<void> {
  try {
    const response = await fetch(...)  // ❌ 失败就直接抛出错误
    // ...
  } catch (error) {
    console.error('❌ 保存失败:', error)
    throw error  // ❌ 没有重试
  }
}
```

**改进建议**：
```typescript
// ✅ 添加重试机制
import { retry } from '@lifeomic/attempt'

class DictationService {
  private readonly MAX_RETRIES = 3
  private readonly RETRY_DELAY = 1000  // 1秒

  async saveProgress(...): Promise<void> {
    return retry(
      async () => {
        const response = await fetch(...)
        if (!response.ok) {
          throw new Error(`HTTP ${response.status}`)
        }
        return response.json()
      },
      {
        maxAttempts: this.MAX_RETRIES,
        delay: this.RETRY_DELAY,
        handleError: (error, attempt) => {
          console.warn(`⚠️ 保存失败，第${attempt}次重试...`, error.message)
          return true  // 继续重试
        }
      }
    )
  }
}
```

**影响范围**：
- 需要修改技术文档第5.3节（Service层设计）
- 需要安装@lifeomic/attempt依赖

---

### 问题5：前端进度保存缺少防抖优化 ⚠️

**问题描述**：
技术文档中提到"批量保存优化"，但Hook实现中没有防抖，可能导致频繁API调用。

**当前实现**：
```typescript
// hooks/useDictationProgress.ts

const saveProgress = useCallback(async (currentIndex: number) => {
  // ❌ 每次调用都立即发送API请求
  await dictationService.saveProgress(...)
}, [bookId, scopeType, totalWords])

// 如果用户快速切题，会触发多次API调用
moveToNext() {
  saveProgress(currentIndex + 1)  // ❌ 第1次API调用
  setCurrentIndex(currentIndex + 1)
}
```

**改进建议**：
```typescript
// ✅ 使用防抖 + 批量保存
import { debounce } from 'lodash'

class DictationProgressManager {
  private pendingSaves = new Map<string, number>()
  private saveTimer: NodeJS.Timeout | null = null
  private readonly DEBOUNCE_DELAY = 1000  // 1秒防抖

  saveProgress(bookId: string, scopeType: string, currentIndex: number) {
    const key = `${bookId}:${scopeType}`

    // 1. 累积待保存的进度
    this.pendingSaves.set(key, currentIndex)

    // 2. 清除之前的定时器
    if (this.saveTimer) {
      clearTimeout(this.saveTimer)
    }

    // 3. 设置新的防抖定时器
    this.saveTimer = setTimeout(async () => {
      // 4. 批量保存所有待保存的进度
      for (const [key, index] of this.pendingSaves.entries()) {
        const [bookId, scopeType] = key.split(':')
        await dictationService.saveProgress(bookId, scopeType, index, totalWords)
      }

      // 5. 清空待保存队列
      this.pendingSaves.clear()
      this.saveTimer = null
    }, this.DEBOUNCE_DELAY)
  }

  // 立即保存（用于页面卸载等关键场景）
  async flush() {
    if (this.saveTimer) {
      clearTimeout(this.saveTimer)
      this.saveTimer = null
    }

    for (const [key, index] of this.pendingSaves.entries()) {
      const [bookId, scopeType] = key.split(':')
      await dictationService.saveProgress(bookId, scopeType, index, totalWords)
    }

    this.pendingSaves.clear()
  }
}

export const progressManager = new DictationProgressManager()

// 在组件中使用
useEffect(() => {
  return () => {
    // 组件卸载时，确保保存所有待保存的进度
    progressManager.flush()
  }
}, [])
```

**影响范围**：
- 需要修改技术文档第5.4.2节（useDictationProgress Hook）
- 需要安装lodash依赖

---

### 问题6：缺少并发控制机制 ⚠️

**问题描述**：
技术文档提到"并发操作处理"，但只说了"使用AbortController取消请求"，没有完整的并发控制方案。

**问题场景**：
```typescript
// 场景1：用户快速切换范围
handleScopeChange('unknown')  // 请求1
handleScopeChange('fuzzy')    // 请求2
handleScopeChange('known')    // 请求3
// ❌ 可能导致：请求3先返回，请求1/2后返回，最终状态错误

// 场景2：用户切题时进度保存还没完成
handleNext() {
  saveProgress(currentIndex)  // 异步保存
  setCurrentIndex(currentIndex + 1)  // 立即更新UI
  // ❌ 如果保存失败，UI已经更新，用户以为保存成功了
}
```

**改进建议**：
```typescript
// ✅ 完整的并发控制方案

// 1. 使用AbortController取消请求
class DictationService {
  private abortControllers = new Map<string, AbortController>()

  async getWords(
    bookId: string,
    scopeType: DictationScopeType,
    shuffle: boolean = false
  ): Promise<any[]> {
    // 取消之前的请求
    const key = `${bookId}:${scopeType}`
    const prevController = this.abortControllers.get(key)
    if (prevController) {
      prevController.abort()
    }

    // 创建新的AbortController
    const controller = new AbortController()
    this.abortControllers.set(key, controller)

    try {
      const response = await fetch(..., {
        signal: controller.signal
      })

      if (!response.ok) {
        throw new Error(...)
      }

      return response.json()
    } finally {
      this.abortControllers.delete(key)
    }
  }
}

// 2. 使用状态机管理UI状态
type PageState = 'idle' | 'loading' | 'saving' | 'switching'

function useDictationPage() {
  const [pageState, setPageState] = useState<PageState>('idle')

  const handleNext = async () => {
    // ❌ 阻止操作：正在保存或切换中
    if (pageState === 'saving' || pageState === 'switching') {
      return
    }

    setPageState('saving')

    try {
      await saveProgress(currentIndex)
      setCurrentIndex(currentIndex + 1)
    } catch (error) {
      console.error('保存失败:', error)
      // 回滚UI状态
      setCurrentIndex(currentIndex)  // 保持原位置
    } finally {
      setPageState('idle')
    }
  }

  const handleScopeChange = async (newScope: string) => {
    // ❌ 阻止操作：正在保存中
    if (pageState === 'saving') {
      return
    }

    setPageState('switching')
    setShowScopeSelectDialog(false)

    try {
      // 取消当前请求
      abortCurrentRequest()

      // 切换范围
      await switchScope(newScope)
    } finally {
      setPageState('idle')
    }
  }

  return { pageState, handleNext, handleScopeChange }
}
```

**影响范围**：
- 需要在技术文档中新增"并发控制策略"章节
- 需要修改Service层和Hook层实现

---

### 问题7：测试用例不完整 ⚠️

**问题描述**：
技术文档的测试策略章节只提供了基础示例，缺少关键场景的测试用例。

**缺少的测试场景**：
1. ❌ 边界测试：0个单词、1个单词、10000个单词
2. ❌ 异常测试：网络超时、服务器错误、数据格式错误
3. ❌ 并发测试：快速切换范围、快速切题
4. ❌ 性能测试：大数据量下的API响应时间
5. ❌ 兼容性测试：不同浏览器的TTS支持

**改进建议**：
```typescript
// ✅ 完整的测试用例

describe('听写模式 - 边界测试', () => {
  test('应该处理0个单词的情况', async ({ page }) => {
    await page.goto('/library/empty_book')
    await page.click('text=开始听写')

    // 应该显示"单词数量太少"提示
    await expect(page.locator('text=单词数量太少')).toBeVisible()
  })

  test('应该处理1个单词的情况', async ({ page }) => {
    // 听写完成后应该显示完成对话框
    await expect(page.locator('text=🎉 太棒了！')).toBeVisible()
  })

  test('应该处理10000个单词的性能', async ({ page }) => {
    const startTime = Date.now()

    await page.goto('/library/large_book')
    await page.click('text=开始听写')

    // 等待页面加载
    await page.waitForSelector('[data-testid="dictation-card"]')

    const loadTime = Date.now() - startTime
    expect(loadTime).toBeLessThan(3000)  // 3秒内加载完成
  })
})

describe('听写模式 - 异常测试', () => {
  test('应该处理网络超时', async ({ page }) => {
    // Mock网络超时
    await page.route('**/api/dictation-progress', route => {
      route.abort()
    })

    // 应该静默失败，不阻塞用户操作
    await expect(page.locator('input[name="userInput"]')).toBeVisible()
  })

  test('应该处理服务器错误', async ({ page }) => {
    await page.route('**/api/words/stats', route => {
      route.fulfill({
        status: 500,
        body: JSON.stringify({ error: 'Internal Server Error' })
      })
    })

    // 应该显示错误提示
    await expect(page.locator('text=加载失败')).toBeVisible()
  })
})

describe('听写模式 - 性能测试', () => {
  test('API响应时间应该<500ms', async () => {
    const startTime = Date.now()

    const response = await fetch('/api/words/stats?bookId=book_123')

    const duration = Date.now() - startTime
    expect(duration).toBeLessThan(500)
    expect(response.ok).toBe(true)
  })

  test('内存占用应该<100MB', async ({ page }) => {
    // 获取初始内存
    const [initialMetrics] = await page.metrics()

    // 加载10000个单词
    await page.goto('/library/large_book')
    await page.click('text=开始听写')
    await page.waitForSelector('[data-testid="dictation-card"]')

    // 获取当前内存
    const [currentMetrics] = await page.metrics()

    const memoryIncrease = (currentMetrics.JSHeapUsedSize - initialMetrics.JSHeapUsedSize) / 1024 / 1024
    expect(memoryIncrease).toBeLessThan(100)  // <100MB
  })
})
```

**影响范围**：
- 需要扩展技术文档第6节（测试策略）
- 需要添加性能基准测试

---

## 🟢 优点与亮点

### ✅ 优点1：架构设计清晰合理

**评价**：
- ✅ 遵循SOLID原则，职责划分清晰
- ✅ Service层封装良好，便于测试和维护
- ✅ Custom Hooks设计合理，复用性强
- ✅ 数据流设计清晰，7层架构易于理解

**示例**：
```typescript
// ✅ 优秀的分层架构
UI Layer (DictationPage)
  ↓
Dialog Layer (DictationScopeDialog)
  ↓
Hook Layer (useDictationWords/Progress/Stats)
  ↓
Service Layer (DictationService)
  ↓
API Layer (/api/*)
  ↓
Data Layer (PostgreSQL)
```

---

### ✅ 优点2：类型安全设计优秀

**评价**：
- ✅ 完整的TypeScript类型定义
- ✅ 使用const assertion确保类型不可变
- ✅ API接口类型定义清晰

**示例**：
```typescript
// ✅ 优秀的类型定义
export type DictationScopeType = 'all' | 'unknown' | 'fuzzy' | 'known' | 'new'

export const DICTATION_SCOPE_LABELS: Record<DictationScopeType, string> = {
  all: '全部单词',
  unknown: '不认识的',
  fuzzy: '模糊的',
  known: '认识',
  new: '未标注'
} as const  // ✅ const assertion
```

---

### ✅ 优点3：错误处理完善

**评价**：
- ✅ 所有异步操作都有try-catch
- ✅ 错误日志记录详细
- ✅ 用户友好的错误提示

**示例**：
```typescript
// ✅ 完善的错误处理
try {
  const data = await dictationService.getStats(bookId)
  setStats(data)
} catch (err) {
  console.error('❌ [useDictationStats] 获取统计失败:', err)
  setError(err as Error)
  // ✅ 失败时使用默认值，不阻塞用户
  setStats(getDefaultStats())
} finally {
  setLoading(false)
}
```

---

### ✅ 优点4：代码可读性强

**评价**：
- ✅ 变量命名清晰（bookId、scopeType、currentIndex）
- ✅ 注释详细，包含JSDoc
- ✅ 代码格式统一，符合Prettier规范

**示例**：
```typescript
/**
 * 获取单词状态统计
 * @param bookId - 词书ID
 * @returns 统计数据
 * @throws Error 当API调用失败时
 */
async getStats(bookId: string): Promise<DictationStats> {
  // ...
}
```

---

## 📋 修改建议清单

### 必须修改（P0）

| 序号 | 问题 | 修改位置 | 工作量 |
|------|------|---------|--------|
| 1 | 复用user_book_preferences.preferences而非新建表 | 第3.1节、第4.1.2节 | 2小时 |
| 2 | 完善Zod验证，返回所有错误 | 第4.1节 | 1小时 |
| 3 | 设计完整的缓存策略 | 新增章节 | 3小时 |

### 建议修改（P1）

| 序号 | 问题 | 修改位置 | 工作量 |
|------|------|---------|--------|
| 4 | Service层添加重试机制 | 第5.3节 | 2小时 |
| 5 | 进度保存添加防抖优化 | 第5.4.2节 | 2小时 |
| 6 | 设计并发控制方案 | 新增章节 | 3小时 |
| 7 | 补充测试用例 | 第6节 | 4小时 |

**总工作量估计**：17小时（约2-3个工作日）

---

## 🎯 最终评审结论

### ✅ 通过条件

1. **必须修改所有P0问题**（问题1-3）
2. **建议至少解决3个P1问题**（优先推荐问题4、5、6）
3. **补充完整的测试用例**（问题7）
4. **修改完成并更新技术文档后，可进入开发阶段**

### 📊 评审评分细则

| 评审维度 | 评分 | 权重 | 加权分 |
|---------|------|------|--------|
| 架构设计 | 4.0 | 25% | 1.00 |
| 数据库设计 | 3.0 | 20% | 0.60 |
| API设计 | 4.0 | 15% | 0.60 |
| 前端架构 | 5.0 | 15% | 0.75 |
| 测试策略 | 4.0 | 10% | 0.40 |
| 性能与安全 | 3.0 | 10% | 0.30 |
| 实施可行性 | 4.0 | 5% | 0.20 |
| **总分** | - | **100%** | **3.85/5.0** |

**评审结论**：⭐⭐⭐⭐☆ (3.85/5.0)

---

## 📝 评审人签名

**评审人**：技术评审专家
**评审日期**：2026-01-14
**评审版本**：v1.0
**下次评审**：修改完成后进行复审

---

## 附录：详细修改示例

### 附录A：复用user_book_preferences的完整实现

```typescript
// ✅ 推荐：扩展flashcard-progress API支持mode参数

// /api/flashcard-progress/route.ts (修改)

/**
 * GET /api/flashcard-progress?bookId=xxx&scopeType=xxx&mode=flashcards|dictation
 * 获取指定范围和模式的学习进度
 */
export async function GET(request: NextRequest) {
  const user = await getCurrentUser()

  if (!user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

  try {
    const searchParams = request.nextUrl.searchParams
    const bookId = searchParams.get('bookId')
    const scopeType = searchParams.get('scopeType')
    const mode = searchParams.get('mode') || 'flashcards'  // ✅ 新增mode参数

    if (!bookId) {
      return NextResponse.json({ error: 'bookId is required' }, { status: 400 })
    }

    const supabase = await createClient()

    // 从 user_book_preferences 表获取进度
    const { data: preferences, error } = await supabase
      .from('user_book_preferences')
      .select('preferences')
      .eq('user_id', user.id)
      .eq('book_id', bookId)
      .maybeSingle()

    if (error) {
      return NextResponse.json({
        success: true,
        data: null
      })
    }

    const allPreferences = (preferences as any)?.preferences || {}

    // ✅ 根据mode构建不同的progressKey
    const progressKey = scopeType
      ? `${mode}_progress_${bookId}_${scopeType}`  // flashcard_progress_xxx_unknown 或 dictation_progress_xxx_unknown
      : null

    if (scopeType && progressKey) {
      const progress = allPreferences[progressKey] || null
      return NextResponse.json({
        success: true,
        data: progress
      })
    }

    return NextResponse.json({
      success: true,
      data: null
    })
  } catch (error) {
    console.error('Error in GET /api/flashcard-progress:', error)
    return NextResponse.json({ error: 'Internal server error' }, { status: 500 })
  }
}

/**
 * POST /api/flashcard-progress
 * 保存/更新学习进度（支持flashcards和dictation两种模式）
 */
export async function POST(request: NextRequest) {
  const user = await getCurrentUser()

  if (!user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

  try {
    const body = await request.json()
    const { bookId, scopeType, currentIndex, totalWords, mode = 'flashcards' } = body  // ✅ 新增mode参数

    if (!bookId || !scopeType || currentIndex === undefined) {
      return NextResponse.json(
        { error: 'bookId, scopeType, and currentIndex are required' },
        { status: 400 }
      )
    }

    const supabase = await createClient()

    // ✅ 根据mode构建不同的progressKey
    const progressKey = `${mode}_progress_${bookId}_${scopeType}`
    const progressData = {
      currentIndex,
      totalWords: totalWords || 0,
      lastStudyTime: Date.now(),
      scopeType
    }

    // 获取现有preferences
    const { data: existing } = await supabase
      .from('user_book_preferences')
      .select('preferences')
      .eq('user_id', user.id)
      .eq('book_id', bookId)
      .maybeSingle()

    const currentPreferences = (existing as any)?.preferences || {}

    // 更新进度
    const updatedPreferences = {
      ...currentPreferences,
      [progressKey]: progressData
    }

    // 保存到数据库
    const { error } = await (supabase.from('user_book_preferences') as any)
      .upsert({
        user_id: user.id,
        book_id: bookId,
        preferences: updatedPreferences
      }, {
        onConflict: 'user_id,book_id'
      })

    if (error) {
      console.error('Error saving progress:', error)
      return NextResponse.json({ error: 'Failed to save progress' }, { status: 500 })
    }

    return NextResponse.json({
      success: true,
      data: progressData
    })
  } catch (error) {
    console.error('Error in POST /api/flashcard-progress:', error)
    return NextResponse.json({ error: 'Internal server error' }, { status: 500 })
  }
}
```

---

**报告结束**

**下一步**：请技术团队根据评审意见修改技术文档，修改完成后提交复审。

# 监控系统故障排查指南

## 📋 目录
- [常见问题](#常见问题)
- [问题定位流程](#问题定位流程)
- [实战案例](#实战案例)
- [告警配置](#告警配置)

---

## 🚨 常见问题速查表

| 问题症状 | 可能原因 | 排查步骤 | 解决方案 |
|---------|---------|---------|---------|
| 网站无法访问 | 服务器宕机 | 1. 检查健康检查<br>2. 查看服务器日志 | 重启服务器 |
| 页面报错 | 前端代码错误 | 1. 查看 Sentry<br>2. 查看浏览器控制台 | 修复前端代码 |
| 功能异常 | API 错误 | 1. 查看后端日志<br>2. 检查 API 响应 | 修复后端代码 |
| 数据保存失败 | 数据库问题 | 1. 检查 API 日志<br>2. 检查 Supabase 日志 | 修复数据库连接/权限 |
| 页面很慢 | 性能问题 | 1. 查看 Web Vitals<br>2. 检查 API 响应时间 | 优化性能 |
| 部分用户报错 | 特定条件问题 | 1. 查看 Sentry 标签<br>2. 复现场景 | 修复特定场景 |

---

## 🔍 问题定位流程

### 场景 1: 用户报告"网站打不开"

```
第 1 步：检查健康检查端点
├─ 访问 /api/health
│  ├─ 返回 200 + status: "ok"
│  │  └─ 服务器正常，继续检查 ↓
│  │
│  └─ 返回 503 或超时
│     └─ 服务器问题 ⚠️
│        ├─ 检查服务器是否运行
│        ├─ 检查端口是否正常
│        └─ 检查内存/CPU 使用率
│
第 2 步：检查网络连接
├─ ping 域名
├─ traceroute 追踪路由
└─ 检查 DNS 解析
│
第 3 步：检查 CDN/反向代理
├─ Vercel 状态页
├─ Cloudflare 状态（如有）
└─ 阿里云负载均衡（如有）
```

**解决时间**：5-10 分钟

---

### 场景 2: 用户报告"页面报错"

```
第 1 步：检查 Sentry（前端错误）
├─ 打开 Sentry 控制板
├─ 查看最新错误
│  ├─ 有相同错误？
│  │  ├─ 查看错误堆栈
│  │  ├─ 查看影响用户数
│  │  └─ 查看浏览器/设备信息
│  │
│  └─ 无错误记录
│     └─ 可能是用户浏览器问题 ↓
│
第 2 步：检查用户环境
├─ 询问用户浏览器版本
├─ 询问是否出现错误提示
├─ 让用户打开 F12 查看控制台
└─ 检查是否有浏览器扩展冲突
│
第 3 步：分析错误类型
├─ TypeError → 变量类型错误
├─ ReferenceError → 变量未定义
├─ NetworkError → 网络请求失败
└─ SyntaxError → 代码语法错误
```

**解决时间**：10-20 分钟

---

### 场景 3: 用户报告"功能异常"

```
第 1 步：确定问题范围
├─ 所有用户都受影响？
│  └─ 是 → 后端问题 ⚠️
│
└─ 只有个别用户受影响？
   └─ 可能是：
      ├─ 用户权限问题
      ├─ 用户数据异常
      └─ 用户环境问题

第 2 步：检查 API 日志（后端）
├─ 查看对应 API 的日志
│  ├─ 有 500 错误？
│  │  └─ 后端代码错误
│  │     ├─ 查看错误堆栈
│  │     ├─ 查看请求参数
│  │     └─ 检查代码逻辑
│  │
│  ├─ 有 401/403 错误？
│  │  └─ 权限问题
│  │     ├─ 检查用户登录状态
│  │     ├─ 检查 RLS 策略
│  │     └─ 检查 API 权限配置
│  │
│  └─ 有超时错误？
│     └─ 性能问题
│        ├─ 检查数据库查询
│        ├─ 检查外部 API 调用
│        └─ 优化代码逻辑
│
第 3 步：检查数据库
├─ Supabase Dashboard
│  ├─ 查询性能 insights
│  ├─ 慢查询日志
│  └─ 数据库连接池
│
└─ 检查数据完整性
   ├─ 数据格式是否正确
   ├─ 约束是否满足
   └─ 关联数据是否存在
```

**解决时间**：15-30 分钟

---

### 场景 4: 用户报告"页面很慢"

```
第 1 步：检查前端性能（Sentry）
├─ 查看 Web Vitals
│  ├─ LCP (Largest Contentful Paint) > 2.5s
│  │  └─ 页面加载慢
│  │     ├─ 图片太大
│  │     ├─ 资源太多
│  │     └─ 网络慢
│  │
│  ├─ FID (First Input Delay) > 100ms
│  │  └─ 交互响应慢
│  │     ├─ JavaScript 执行时间太长
│  │     └─ 主线程阻塞
│  │
│  └─ CLS (Cumulative Layout Shift) > 0.1
│     └─ 布局不稳定
│        ├─ 图片尺寸未设置
│        └─ 动态内容插入
│
第 2 步：检查 API 性能
├─ 查看日志中的响应时间
│  ├─ 某个 API 特别慢？
│  │  └─ 优化该 API
│  │     ├─ 添加数据库索引
│  │     ├─ 减少 JOIN 查询
│  │     └─ 使用缓存
│  │
│  └─ 所有 API 都慢？
│     └─ 服务器问题
│        ├─ CPU 使用率
│        ├─ 内存使用率
│        └─ 网络带宽
│
第 3 步：检查数据库性能
├─ Supabase Query Performance
│  ├─ 慢查询列表
│  ├─ 查询计划分析
│  └─ 索引建议
│
└─ 数据库连接池
   ├─ 连接数
   ├─ 等待时间
   └─ 超时设置
```

**解决时间**：30-60 分钟

---

### 场景 5: 用户报告"数据不对"

```
第 1 步：确认问题
├─ 所有用户数据都不对？
│  └─ 数据问题 ⚠️
│
└─ 只有个别用户数据不对？
   └─ 用户特定问题

第 2 步：检查前端
├─ 数据是否正确显示？
│  ├─ 检查数据格式化
│  ├─ 检查数据过滤
│  └─ 检查数据排序
│
└─ 数据是否正确获取？
   ├─ 检查 API 调用
   ├─ 检查缓存策略
   └─ 检查状态管理

第 3 步：检查后端
├─ API 返回的数据
│  ├─ 查看日志中的响应数据
│  ├─ 检查数据转换逻辑
│  └─ 检查数据过滤条件
│
└─ 数据库查询
   ├─ SQL 查询是否正确
   ├─ JOIN 条件是否正确
   └─ WHERE 条件是否正确

第 4 步：检查数据库
├─ 数据完整性
│  ├─ 关联数据是否存在
│  ├─ 数据格式是否正确
│  └─ 约束是否满足
│
└─ 数据一致性
   ├─ 事务是否正确
   ├─ 并发控制
   └─ 数据同步
```

**解决时间**：20-40 分钟

---

## 💡 实战案例

### 案例 1: 练习功能突然无法保存

**用户反馈**：
> "练习完成后点击保存，一直在转圈，最后提示保存失败"

**排查过程**：

1. **检查 Sentry**（1 分钟）
   ```
   未发现前端 JS 错误
   → 不是前端代码问题
   ```

2. **检查 API 日志**（2 分钟）
   ```json
   {
     "type": "API_REQUEST_ERROR",
     "path": "/api/practice/progress",
     "error": "SupabaseError: permission denied for table word_progress",
     "status": 500
   }
   ```
   → 数据库权限问题！

3. **检查 Supabase RLS 策略**（3 分钟）
   ```
   发现 word_progress 表的 RLS 策略被意外修改
   所有用户都没有 INSERT 权限
   ```

4. **修复**（5 分钟）
   ```sql
   -- 恢复正确的 RLS 策略
   CREATE POLICY "Users can insert their own progress"
   ON word_progress
   FOR INSERT
   TO authenticated
   WITH CHECK (auth.uid() = user_id);
   ```

**总用时**：11 分钟
**根本原因**：RLS 策略配置错误

---

### 案例 2: 页面加载速度突然变慢

**用户反馈**：
> "最近网站变得很慢，打开一个页面要等 10 秒"

**排查过程**：

1. **检查 Sentry Web Vitals**（2 分钟）
   ```
   LCP 平均值: 8.5 秒（正常应该 < 2.5 秒）
   → 页面加载确实很慢
   ```

2. **检查 API 响应时间**（3 分钟）
   ```
   /api/word-books: 平均 150ms ✅
   /api/practice: 平均 200ms ✅
   /api/health: 平均 50ms ✅
   → API 响应正常，不是后端问题
   ```

3. **检查前端资源**（5 分钟）
   ```
   发现：
   - 首页加载了 50 个图片
   - 平均图片大小 2MB
   - 总资源大小 120MB
   → 资源太大！
   ```

4. **修复**（10 分钟）
   ```typescript
   // 1. 压缩图片
   image-optimizer *.jpg

   // 2. 添加图片懒加载
   <Image loading="lazy" ... />

   // 3. 使用 Next.js Image 组件
   import Image from 'next/image'
   ```

5. **验证**（5 分钟）
   ```
   优化后 LCP: 1.8 秒 ✅
   ```

**总用时**：25 分钟
**根本原因**：图片未优化，资源过大

---

### 案例 3: 部分用户无法登录

**用户反馈**：
> "登录时一直转圈，最后提示'网络错误'"

**排查过程**：

1. **检查 Sentry**（2 分钟）
   ```
   发现 15 个用户遇到同样问题
   都在同一时间段
   → 不是个别用户问题
   ```

2. **检查错误详情**（3 分钟）
   ```
   TypeError: Failed to fetch
   → 网络请求失败
   ```

3. **检查 API 日志**（2 分钟）
   ```
   API 日志中**没有**这些登录请求
   → 请求根本没有到达服务器
   ```

4. **分析用户特征**（5 分钟）
   ```
   受影响用户：
   - 都使用 Chrome 浏览器
   - 都启用了广告拦截器
   - 都在同一地区（江苏电信）

   → 可能是网络运营商或广告拦截器拦截了请求
   ```

5. **解决方案**（8 分钟）
   ```typescript
   // 1. 添加重试机制
   const retryRequest = async (url, options, retries = 3) => {
     for (let i = 0; i < retries; i++) {
       try {
         return await fetch(url, options)
       } catch (error) {
         if (i === retries - 1) throw error
         await new Promise(r => setTimeout(r, 1000 * (i + 1)))
       }
     }
   }

   // 2. 添加用户提示
   "登录失败，请检查网络连接或关闭广告拦截器后重试"
   ```

**总用时**：20 分钟
**根本原因**：网络环境或广告拦截器拦截

---

## 🔔 告警配置

### 告警级别

| 级别 | 响应时间 | 通知方式 | 示例 |
|------|---------|---------|------|
| 🔴 P0 - 紧急 | 5 分钟内 | 电话 + 短信 + 邮件 | 服务器宕机、数据库连接失败 |
| 🟠 P1 - 高 | 15 分钟内 | 短信 + 邮件 | 错误率 > 10%、核心功能失效 |
| 🟡 P2 - 中 | 1 小时内 | 邮件 | 错误率 > 5%、性能下降 |
| 🟢 P3 - 低 | 次日 | 邮件 | 少量错误、轻微性能问题 |

### Sentry 告警规则

**规则 1: 错误率突增**
```javascript
// Sentry Alert Rule
条件: error rate > 5% for 5 minutes
触发: 发送邮件
```

**规则 2: 新错误出现**
```javascript
// Sentry Alert Rule
条件: new error issue detected
触发: 发送 Slack 通知
```

**规则 3: 特定错误**
```javascript
// Sentry Alert Rule
条件: error message contains "database"
触发: 发送短信告警
```

### 健康检查告警

**脚本**: 监控健康检查端点

```bash
#!/bin/bash
# health-check-monitor.sh

while true; do
  response=$(curl -s http://your-domain.com/api/health)
  status=$(echo $response | jq -r '.status')

  if [ "$status" != "ok" ]; then
    # 发送告警
    curl -X POST "https://api.slack.com/..." \
      -d "{\"text\": \"⚠️ 健康检查失败: $response\"}"
  fi

  sleep 60  # 每分钟检查一次
done
```

---

## 📊 日志查询技巧

### Grep 搜索

```bash
# 搜索错误日志
grep "level":"error" logs/app.log

# 搜索特定用户
grep "userId":"xxx" logs/app.log

# 搜索特定 API
grep "path":"/api/practice" logs/app.log

# 搜索超时的请求
grep "duration":  logs/app.log | awk '$3 > 5000'
```

### jq 分析 JSON 日志

```bash
# 统计错误类型
cat logs/app.log | jq -r '.type' | sort | uniq -c

# 查看最慢的 10 个 API
cat logs/app.log | jq -r 'select(.duration) | "\(.duration) \(.path)"' | sort -rn | head -10

# 统计各 API 的调用次数
cat logs/app.log | jq -r '.path' | sort | uniq -c | sort -rn
```

---

## 🎯 快速参考

### 紧急故障处理流程

```
1. 确认问题范围（1 分钟）
   ├─ 多少用户受影响？
   ├─ 哪些功能受影响？
   └─ 什么时候开始的？

2. 快速诊断（5 分钟）
   ├─ 检查健康检查端点
   ├─ 查看 Sentry 最新错误
   ├─ 查看后端日志
   └─ 检查服务器资源

3. 定位问题（10 分钟）
   ├─ 确定问题层次（前端/后端/数据库）
   ├─ 找到具体错误位置
   └─ 分析根本原因

4. 修复问题（时间根据问题复杂度）
   ├─ 紧急修复（hotfix）
   ├─ 验证修复效果
   └─ 监控问题是否复发

5. 复盘总结
   ├─ 记录问题详情
   ├─ 更新监控规则
   └─ 制定预防措施
```

---

## 📞 联系支持

如果按照上述流程仍无法解决问题：

- **文档**: 查看项目 README 和技术文档
- **社区**: Supabase Discord, Next.js GitHub Discussions
- **Sentry 支持**: https://sentry.io/support/

---

**文档版本**: v1.0
**最后更新**: 2026-01-21

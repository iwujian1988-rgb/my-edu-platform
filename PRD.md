产品需求文档 (PRD): 小语笔记-英语 (Web端)
版本号：v3.2.4 (Revised)
更新日期：2026-01-06

1. 文档概览
项目名称：小语笔记-英语 (Web端)
适用终端：Web (PC), Mobile Web, Pad Web (响应式设计)
核心目标：打造一个集“高效录入”、“结构化记忆”、“数据可视化”于一体的英语学习平台。
2. 功能架构图 (Information Architecture)
[L0] 全局层

├── 身份验证 (Auth System) [手机号+密码+邀请码]
├── [新增] 多端同步机制 (WebSocket/长轮询)
├── 顶部导航栏 (Global Nav): 首页、单词库、意见反馈、[新增] 退出登录
└── 核心工作台 (Dashboard)
[L1] 单词库大厅 (Library Hub)

├── [A] 个人学习区 (My Dashboard)
├── 智能继续学习
├── 错题本 (Mistakes Book)
├── 生词日历
└── 新建自定义词库
└── [B] 词库资源列表 (All Books List)
├── 筛选栏 (Tabs: 全部/考试/场景/教材)
└── 词库卡片流
[L2] 单词书详情页 (Book Viewer)

├── 统一导航头：主题筛选、场景筛选
├── 单词列表容器 (Word List Container)
└── 底部控制栏 (Pagination & Actions)
[L3] 练习系统 (Practice System)

├── 范围选择器
├── 听写模式 (Dictation) [修订：纯键盘输入]
├── 消消乐 (Match Game)
└── 卡片背单词 (Flashcards)
3. 注册登录系统 (Auth System)
核心策略：无短信验证码，通过“邀请码强校验+行为风控”保障安全。

3.1 注册交互流程
页面加载：若URL携带 ?code=xxx，自动填入邀请码并置灰；否则允许输入。
表单填写：手机号实时校验；密码显示/隐藏切换。
人机验证：滑块拼图 (Slider Captcha)，成功后“注册”按钮激活。
提交反馈：成功跳转登录页；失败提示错误信息。
3.2 安全防刷策略
邀请码锁：单码限制使用次数。
IP/设备限流：单IP 1小时限3次；单设备24小时限1次。
蜜罐技术：拦截脚本自动填充。
防爆破：输错5次邀请码锁定24小时，接口延迟响应。
3.3 账户管理与同步 [新增章节]
修改密码：
前台不提供直接修改功能。
点击“修改密码”入口，弹窗提示：“为了您的账户安全，如需重置密码请联系官方客服，邮箱：support@xiaoyu.com”。
注销账号：v3.2版本暂不支持，后续迭代。
多端同步机制：
系统需支持多端数据实时同步（建议采用 WebSocket 或 长轮询机制）。
冲突解决：以服务器端接收到的“最后操作时间戳”为准，自动覆盖旧数据。
4. [L1] 单词库大厅 (Library Hub)
4.1 个人学习区
智能继续学习卡片：展示上次学习进度。若无数据，按钮置灰，文案：“您还未开始学习，请开始学吧！”
功能入口卡片：
错题本：显示错题总数。
生词日历：显示今日新增词数。
新建词库：显示 "+" 号大图标。
4.2 词库资源列表
筛选导航：Tabs切换，骨架屏加载。
列表展示：响应式网格布局。
词库卡片：封面、书名、单词总量、描述、状态（学习中Badge）。
5. [L2] 单词书详情页 (Book Viewer)
5.1 顶部统一导航
主题/场景选择器：下拉菜单（PC）/ 底部抽屉（Mobile）。
联动逻辑：选择主题后刷新列表；若主题下有场景，二级筛选显示对应场景。
5.2 排序与筛选
排序：默认（Excel导入顺序）、随机打乱。
状态筛选：全部、未标注、认识、不认识、模糊。
5.3 单词列表容器 (Word List)
显示内容：单词、音标、**词性**、释义、搭配、例句、操作区。

**词性显示规范** [优化]:
- 数据存储：数据库存储英文词性（如 "noun"、"verb"、"adjective" 或缩写 "n"、"v"、"adj"）
- 前端显示格式：英文缩写 + 中文（如"n名词"、"v动词"、"adj形容词"）
- 词性映射表：
  - noun / n → n名词
  - verb / v → v动词
  - adjective / adj → adj形容词
  - adverb / adv → adv副词
  - pronoun / pron → pron代词
  - preposition / prep → prep介词
  - conjunction / conj → conj连词
  - article / art → art冠词
  - auxiliary verb → aux助动词
  - modal verb → modal情态动词
  - transitive verb → vt及物动词
  - intransitive verb → vi不及物动词
  - 其他词性 → 保持原样显示（用于扩展）
- 显示样式：紫色圆角徽章，位于音标右侧
- 多词性处理：逗号分隔显示（如"n名词, v动词"）

交互细节：

**发音功能** [增强]：
- 使用 Web Speech API (TTS) 实现单词朗读
- 支持三个独立发音按钮：
  - **单词发音**：小喇叭图标紧跟单词右侧
  - **搭配发音**：小喇叭图标在搭配区域右侧
  - **例句发音**：小喇叭图标在例句区域右侧
- 发音按钮样式：16px × 16px 小图标，淡灰色（不显眼），hover时颜色加深
- 点击发音时，图标无loading状态，语音播放完毕自动停止

**中文翻译显示规范** [新增]：
- **格式要求**：
  - 必须添加"中文："前缀（如："中文：议程，日程表"）
  - **中文翻译中不包含词性标记**：不显示 n./v./adj./adv. 等前缀
  - 原因：词性已通过独立的紫色徽章显示（如"n名词"标签），避免重复
  - 错误示例：~~"中文：n. 议程，日程表"~~
  - 正确示例：✅ "中文：议程，日程表"
- **样式规范**：
  - 字号：`text-sm` (14px) - 小字号
  - 颜色：`text-gray-600` (#4B5563) - 灰色（柔和，不刺眼）
  - 字重：`font-semibold` - 半粗体
- **显示顺序**：
  - 英文释义在上方
  - "中文：xxx" 在下方
  - 示例：
    ```
    A list of items to be discussed at a meeting
    中文：议程，日程表
    ```
- **数据规范**：
  - Excel导入时的 `Definition` 字段应直接写中文释义
  - 不需要在释义前加词性标记（如"n."、"v."）
  - 词性通过独立的 `Part of Speech` 字段标识

**中文显隐功能** [优化]：
支持两种方式切换中文释义可见性（用于测试自己是否掌握）：

1. **全局隐藏（持久化）**：
   - 入口：页面顶部右上角的"隐藏中文"按钮
   - 作用范围：当前用户 + 当前单词书
   - 持久化：设置保存到数据库 `user_book_preferences` 表
   - 下次访问该书时自动应用该设置
   - 视觉反馈：激活时按钮显示"全局生效"紫色徽章

2. **本地隐藏（前端状态）**：
   - 入口：每个单词卡片顶部的"隐藏中文"按钮（眼睛图标）
   - 作用范围：仅影响当前卡片
   - 持久化：不保存，刷新页面后恢复默认
   - **优先级优化**：本地按钮可以覆盖全局设置
     - 当全局隐藏生效时，所有卡片默认不显示中文
     - 用户可通过本地按钮强制显示某个卡片的中文
     - 每个卡片的本地按钮完全独立控制

**隐藏时的视觉表现**：
- **始终显示**：
  - 单词（英文）、音标、词性中文徽章
  - 功能标签："💡 搭配:"、"📝 例句:"
  - 所有英文内容（释义、搭配、例句）
  - "中文："前缀
- **隐藏中文时显示**：
  - 释义：`中文：______________________` (灰色下划线占位符)
  - 搭配：`中文：______________________` (灰色下划线占位符)
  - 例句：`中文：______________________` (灰色下划线占位符)
  - 目的：让用户明确知道此处有中文内容，仅是被隐藏
- **显示中文时显示**：
  - 所有内容（中英文都显示）

**隐藏按钮样式** [优化]：
- 极度低调设计：
  - 无边框、无背景
  - 仅显示图标（眼睛图标）
  - 图标尺寸：12px × 12px (w-3 h-3)
  - 图标颜色：`text-gray-300` (极淡灰色) - 几乎不可见
  - hover颜色：`text-gray-500` (稍微明显)
  - padding：`px-1.5 py-0.5`
- 位置：每个卡片顶部，单词右侧
- 交互：鼠标悬停时图标略微变深，提供视觉反馈
- 按钮状态：
  - 显示中文时：闭眼图标（EyeOff）
  - 隐藏中文时：眼睛图标（Eye）

**内容折叠展开功能** [新增]：
- **目的**：当搭配或例句内容过长时，保持界面整洁
- **折叠状态**：
  - 最大高度：96px (max-h-24)
  - 超出部分隐藏 (overflow-hidden)
  - 底部显示半透明渐变遮罩（从背景色到透明）
  - 遮罩上显示"展开"按钮（仅在内容溢出时）
- **展开状态**：
  - 显示完整内容
  - 遮罩消失
  - 显示"收起"按钮（极淡灰色）
- **遮罩样式**：
  - 高度：48px (h-12)
  - 渐变：从背景色（100%不透明）到透明
  - 过渡：平滑 (transition-all duration-200)
- **展开按钮样式**：
  - 位置：叠加在遮罩层底部居中
  - 背景：白色半透明 `bg-white/80`
  - 特效：背景模糊 `backdrop-blur-sm`
  - 形状：圆角胶囊形 `rounded-full`
  - 阴影：淡阴影 `shadow-sm`
  - 文字颜色：`text-gray-400`，hover时 `text-gray-600`
  - 字号：`text-xs`
- **智能显示**：
  - 使用 useRef + useEffect 检测内容是否溢出
  - 只在内容真正溢出时显示遮罩和按钮
  - 短内容不显示展开功能，保持界面简洁

**卡片布局** [优化]：
- 布局方式：CSS Grid，响应式布局
  - 移动端：1列 (grid-cols-1)
  - 平板：2列 (md:grid-cols-2)
  - 桌面：4列 (xl:grid-cols-4)
- 卡片高度：自适应内容，不设置固定高度
- 按钮对齐：使用 Flexbox 的 `mt-auto` 固定在卡片底部
- 卡片间距：16px (gap-4)

**状态标记**：
- 点击圆点选择：认识(绿) / 模糊(黄) / 不认识(红)
- 按钮尺寸：40px × 40px 圆形按钮
- 视觉反馈：选中状态填充对应颜色，未选中状态为白底灰边

**5.4 练习模式区域** [新增关键模块]
在单词书详情页的筛选栏下方、单词列表上方，直接展示三种练习模式卡片，无需跳转到单独的选择页面。

布局结构：
- 标题卡片：显示"选择练习模式 🎯"和引导文案
- 练习模式网格：3列响应式布局（PC端横排，移动端竖排）
  1. **卡片背单词**（紫色卡片）
     - 图标：CreditCard
     - 描述：经典记忆模式，点击翻转卡片，快速记忆单词
     - 按钮：开始背诵
     - 链接：/study/[bookId]/flashcards
  2. **听写模式**（薄荷绿卡片）
     - 图标：Volume2
     - 描述：听音拼写，强化记忆，检验学习成果
     - 按钮：开始听写
     - 链接：/study/[bookId]/dictation
  3. **消消乐**（桃色卡片）
     - 图标：Gamepad2
     - 描述：趣味配对游戏，轻松学习，寓教于乐
     - 按钮：开始游戏
     - 链接：/study/[bookId]/match-game
- 学习小贴士卡片（蓝色）：提供学习建议

交互设计：
- 卡片悬停：阴影增强 + 向上位移1px
- 卡片点击：直接跳转到对应练习页面
- 响应式：PC端3列横排，平板/移动端1列竖排

设计原则：
- 降低用户操作成本：无需额外点击进入练习模式选择页
- 提升功能可见性：三种练习模式一目了然
- 保持页面流畅性：练习模式卡片位于页面中部，不干扰单词列表浏览

5.5 单词状态核心逻辑 [新增关键逻辑]
为解决同一单词在不同书籍中释义不同但状态需同步的问题，采用 “内容局部化，状态全局化” 策略：

唯一性判断：以 英文拼写 为用户维度的唯一键。
内容独立：单词的释义、例句跟随 “单词书”。
例：在《少儿英语》中 "Bank" 释义为“河岸”；在《金融英语》中释义为“银行”。
状态共享：单词的“认识/不认识”状态跟随 “用户” 全局共享。
例：用户在《少儿英语》标记 "Bank" 为认识，打开《金融英语》时，"Bank" 自动显示为认识。
冲突处理：若用户在《金融英语》将 "Bank" 改为"不认识"，则全局（包括《少儿英语》）该词状态同步变为"不认识"。

5.6 底部控制栏
分页器：当章节单词数 > 50 时显示分页；否则隐藏。

6. [L3] 练习系统 (Practice System)

**6.0 范围选择器** [新增关键功能]
用户点击练习模式卡片（卡片背单词/听写模式/消消乐）时，弹出范围选择对话框，让用户选择练习范围。

**交互流程**：
1. 用户点击任意练习模式卡片的按钮（开始背诵/开始听写/开始游戏）
2. 弹出范围选择对话框（模态框，带背景遮罩）
3. 用户选择练习范围后，点击"确认开始"进入练习页面

**对话框设计**：
- 标题："选择练习范围"
- 两个选项卡片（单选）：
  1. **当前筛选结果**
     - 显示当前筛选后的单词数量
     - 说明：仅练习当前主题/场景/状态筛选后的单词
     - 示例文案："商务主题 - 50个单词" 或 "全部单词 - 100个单词"
  2. **全书所有单词**
     - 显示本书的单词总数
     - 说明：练习本书所有单词，忽略筛选条件
     - 示例文案："全书共 100个单词"

**UI样式**：
- 对话框：clay-card 居中显示，最大宽度 500px
- 选项卡片：可点击的单选卡片，选中时显示绿色边框和对勾图标
- 底部按钮："确认开始"（主按钮，绿色）+ "取消"（次要按钮，灰色）
- 响应式：移动端全屏显示，PC端居中浮层

**边界情况处理**：
- 如果当前筛选结果为0个单词，禁用"当前筛选结果"选项，显示提示："当前筛选结果为空"
- 如果全书单词数少于3个，提示："单词数量太少，无法开始练习"

**默认选择**：
- 默认选中"当前筛选结果"（如果有单词）
- 如果当前筛选为空，默认选中"全书所有单词"

**数据传递**：
- URL参数传递范围选择：
  - 当前筛选：`/study/[bookId]/flashcards?scope=filtered`
  - 全书：`/study/[bookId]/flashcards?scope=all`
- 练习页面根据scope参数决定使用哪个单词列表

**6.1 听写模式 (Dictation)
输入方式 [修订]：
取消手写功能。
PC端：物理键盘输入。
Mobile端：自动调用系统软键盘。
交互优化：题目加载时，输入框自动获取焦点 (Auto Focus)。
流程：自动播放 -> 循环检测 -> 用户输入打断循环 -> Enter提交。
反馈：
正确：边框变绿 + "Ding"音效 -> 0.5s后切题。状态更新为“认识”。
错误：边框变红 + 震动动画 + "Buzz"音效 -> 显示正确拼写 -> 倒计时2s -> 切题。
6.2 消消乐 (Match Game)
数据范围：当前筛选范围内，非“认识”的单词。
边界值处理 [新增]：
词数 >= 10：随机抽取10对。
3 <= 词数 < 10：抽取全部单词。
词数 < 3：不进入游戏，Toast提示：“单词数量太少啦，先去背几个单词再来玩吧！”
玩法：点击卡片A -> 点击卡片B -> 匹配消除/不匹配震动。
**6.3 卡片背单词 (Flashcards)**

**页面布局**：
- 顶部导航栏：返回按钮、词书标题、进度显示
- 进度条：显示学习进度百分比（已学习/总数）
- 操作提示区：三个文字提示（← 左滑/认识、↑ 上滑/模糊、→ 右滑/不认识）
- 单词卡片：中央大卡片，占据主要视觉区域
- 完成提示：学完所有单词时显示祝贺信息

**卡片设计**：
- 样式：高级纸张质感（clay-card-xl）
  - 背景：暖米色渐变（#fffcf5 → #f9f6ec → #f5f0e3）
  - 边框：1px 实线 #e0d5c5（淡雅棕色）
  - 阴影：5层渐进阴影 + 内阴影 + 顶部高光
  - 纹理：三重纹理叠加（横向、纵向、斜向）
  - 边缘磨损效果：使用 ::after 伪元素
- 正面：
  - 单词：超大字号（text-5xl），加粗，居中
  - 音标：中字号（text-xl），灰色
  - 词性标签：紫色圆角徽章
  - 状态徽章：已标记单词显示状态（认识✓/模糊?/不认识✗）
  - 播放按钮：居中显示，无背景无边框，灰色图标（hover时变深）
  - 提示文字："点击卡片或按⬇️查看释义 👆"
- 背面：
  - 英文释义：小标签 + 正文
  - 中文释义：小标签 + 大字号加粗
  - 搭配：小标签 + 搭配内容
  - 例句：灰色背景卡片，小标签 + 例句内容

**交互设计**：

1. **自动朗读**：
   - 每次切换到新单词时，自动播放单词发音
   - 延迟500ms后播放，让用户先看到单词
   - 使用 Web Speech API (SpeechSynthesis)
   - 语言：en-US
   - 语速：0.8（稍慢于正常速度，便于学习）
   - 循环播放：每2秒重复一次，用户操作时停止

2. **卡片翻转**：
   - 点击卡片任意位置触发翻转
   - 3D CSS变换动画（rotateY 180deg）
   - 动画时长：600ms
   - 正反面使用backface-visibility: hidden

3. **键盘快捷键**：
   - ⬅️ 左方向键：标记为"认识"
   - ↑ 上方向键：标记为"模糊"
   - ➡️ 右方向键：标记为"不认识"
   - ⬇️ 下方向键：翻转卡片查看详情
   - 键盘操作触发卡片滑动动画（视觉反馈）

4. **滑动手势** [智能方向判断]：
   - 基于滑动角度判断方向（Math.atan2计算）
   - 最小滑动距离阈值：50像素
   - 小于50px视为点击，触发翻转
   - 方向划分：
     - 右滑（-45° 到 45°）：标记为"不认识"
     - 下滑（45° 到 135°）：翻转回正面
     - 左滑（135° 到 225°/-135°）：标记为"认识"
     - 上滑（-135° 到 -45°）：翻转查看详情
   - 卡片跟随手指/鼠标移动，带有倾斜效果
   - 倾斜角度：每100px位移约5度

5. **预览卡片效果**：
   - 滑动时在当前卡片后方显示下一个单词的预览
   - 预览卡片与前台卡片尺寸完全相同
   - 使用 translateZ(-50px) 营造3D深度感
   - 透明度：40%（半透明，隐约可见）
   - 平滑的淡入淡出动画（300ms ease-out）
   - 包含完整内容：单词、音标、词性、播放按钮

6. **单词状态标记**：
   - 操作方式：滑动、键盘快捷键
   - 状态类型：认识/模糊/不认识
   - 视觉反馈：
     - 认识：卡片向左滑动150px，左倾15度
     - 模糊：无位移动画
     - 不认识：卡片向右滑动150px，右倾15度
   - 动画时长：300ms
   - 动画完成后自动切换到下一张卡片
   - 更新进度条

7. **数据保存策略** [性能优化]：
   - 乐观更新：操作后立即更新本地状态，无等待
   - 批量保存：累积操作3秒后统一批量保存到服务器
   - 使用 Promise.all 并行发送多个保存请求
   - 数据安全：
     - 页面卸载时自动保存所有待保存数据
     - 页面隐藏时（用户切换标签页）自动保存
     - 关闭页面前（beforeunload）自动保存

8. **操作提示**：
   - 位置：进度条下方、卡片上方
   - 样式：纯文字，无背景无边框
   - 内容：三个简洁的文字提示
     - "← 左滑" + "认识"（绿色）
     - "↑ 上滑" + "模糊"（黄色）
     - "→ 右滑" + "不认识"（红色）
   - 字体：小号灰色文字 + 中号彩色粗体

9. **朗读功能**：
   - 位置：卡片正面中央
   - 样式：无背景无边框，只显示图标
   - 图标：Volume2，48px × 48px
   - 颜色：默认 text-gray-400，hover时 text-gray-600
   - 交互：hover时放大1.1倍
   - 点击朗读当前单词

**进度显示**：
- 进度条：渐变色（紫色到淡紫色），显示完成百分比
- 导航栏文字："卡片背单词 • {范围} • {当前序号} / {总数}"
- 示例："卡片背单词 • 全书 • 15 / 100"

**完成状态**：
- 当到达最后一张单词时，显示完成卡片：
  - 标题："🎉 太棒了！"
  - 文案："你已经完成了所有单词的学习"
  - 按钮："返回词书详情"

**范围标签显示**：
- 全书：显示"全书"
- 筛选结果：显示筛选条件，如"商务"、"商务 - 会议"、"认识"等
- 多条件组合：用" - "连接，如"商务 - 会议 - 50个单词"

**数据获取**：
- API: GET /api/words?bookId={bookId}&scope={filtered|all}&theme={theme}&scene={scene}&status={status}
- 根据scope和筛选条件获取对应的单词列表
- 支持实时筛选，无需刷新页面

**响应式设计**：
- PC端：卡片最大宽度xl（max-w-xl）
- 移动端：卡片全宽
- iPad竖屏：优化间距，确保完整显示卡片
- 布局优化：减少垂直间距，卡片高度380px
7. [L4] 自定义词库编辑器
创建流：输入名称 -> 创建空书。
智能录入 (Smart Batch Import)：
API说明：调用第三方API将用户输入的英文单词自动匹配释义、音标。
配额限制 [新增]：单用户每日限制智能识别 500词。超额提示“今日额度已用完，请手动输入”。
章节管理：
新增：点击添加章节。
重命名 [新增]：支持双击章节名修改。
删除词库 [新增]：
需执行 3次确认 流程：点击删除 -> 弹窗“确定删除？” -> 弹窗“数据不可恢复！” -> 弹窗“最后确认” -> 执行删除。
空状态：显示插画 + 文案“打造你的专属单词书” + 按钮“立即创建”。
8. [L5] 生词日历与错题本
生词日历：热力图展示每日新增生词；支持导出图片/Excel。
错题本：
数据源：标记为“不认识”或“模糊”的单词。
空状态 [新增]：
图标：奖杯插画。
文案：“太棒了！你消灭了所有错题。”
按钮：“去背新单词”。
9. 后台数据与资源管理 (Backend & Data) [新增章节]
9.1 官方词库导入规范
官方词库不开发后台UI，采用 Excel 离线导入 模式。

Excel 模板字段标准：
Chapter (章节名): 必填，用于生成目录。
Word (单词): 必填，英文原文。
Phonetic (音标): 选填。
Definition (释义): 必填，本书特定的中文释义。
Theme (主题): 选填，对应筛选标签（如：商务）。
Scene (场景): 选填，对应子标签（如：会议）。
层级处理：后端解析Excel时，自动根据 Theme 和 Scene 字段构建或关联标签树。
9.2 用户偏好设置 [新增章节]
为提供个性化学习体验，系统支持用户偏好设置的持久化存储。

9.2.1 数据库设计
表名：`user_book_preferences`

字段说明：
- `id`: UUID，主键
- `user_id`: UUID，用户ID（外键关联 auth.users）
- `book_id`: TEXT，词书ID（支持UUID或slug格式）
- `hide_chinese`: BOOLEAN，是否隐藏中文释义（默认false）
- `created_at`: TIMESTAMPTZ，创建时间
- `updated_at`: TIMESTAMPTZ，更新时间（自动更新触发器）

索引设计：
- 复合唯一索引：`(user_id, book_id)` - 确保每个用户对每本书只有一条偏好记录
- 单列索引：`user_id` 和 `book_id` - 提升查询性能

设计决策：
- `book_id` 使用 TEXT 而非 UUID：支持多种ID格式（UUID、slug、自定义字符串），提高灵活性
- 级联删除：用户删除时自动清理其偏好设置

9.2.2 API 接口设计

**GET /api/user-preferences?book_id=xxx**
- 功能：获取用户对某本书的偏好设置
- 认证：必须登录（Supabase Auth）
- 响应：
  ```json
  {
    "success": true,
    "data": {
      "hide_chinese": false
    }
  }
  ```
- 默认值：若无记录，返回 `hide_chinese: false`

**POST /api/user-preferences**
- 功能：保存/更新用户偏好设置
- 认证：必须登录
- 请求体：
  ```json
  {
    "book_id": "demo-book-1",
    "hide_chinese": true
  }
  ```
- 响应：
  ```json
  {
    "success": true,
    "data": {
      "id": "uuid",
      "user_id": "user-uuid",
      "book_id": "demo-book-1",
      "hide_chinese": true,
      "created_at": "2026-01-06T...",
      "updated_at": "2026-01-06T..."
    }
  }
  ```
- 操作：使用 UPSERT（插入或更新），确保幂等性

9.2.3 前端实现逻辑

**组件层级：**
```
BookDetailPage (Server Component)
  └── BookDetailPageClient (Client Component)
      ├── GlobalHideButton (全局按钮)
      └── WordList
          └── WordCard[] (所有卡片)
```

**状态管理：**
1. GlobalHideButton 组件加载时：
   - 调用 GET `/api/user-preferences?book_id=xxx` 获取当前设置
   - 更新按钮状态（显示"隐藏中文"或"显示中文"）

2. 用户点击全局按钮时：
   - 调用 POST `/api/user-preferences` 保存设置
   - 更新本地状态
   - 通过 `onHideChange` 回调通知父组件
   - 父组件将 `globalHideChinese` 传递给 WordList 和所有 WordCard

3. WordCard 组件：
   - 接收 `globalHideChinese` 属性
   - 计算最终显示状态：`shouldShowChinese = !globalHideChinese && showDefinition`
   - 全局设置优先于本地按钮

**优先级逻辑：**
- 全局设置 > 本地设置
- 若 `globalHideChinese = true`，所有卡片强制隐藏中文，本地按钮失效
- 若 `globalHideChinese = false`，本地按钮可单独控制每个卡片

9.2.4 用户体验优化

**视觉反馈：**
- 全局隐藏激活时：按钮显示紫色背景 + "全局生效"徽章
- 保存中状态：按钮显示"保存中..."并禁用
- 错误提示：401时弹出提示"请先登录"

**数据隔离：**
- 用户隔离：不同用户的设置互不影响
- 书籍隔离：同一用户对不同书籍的设置独立存储

**性能优化：**
- 使用 UPSERT 避免重复查询
- 索引优化查询性能
- 前端乐观更新，提升响应速度

10. 非功能性需求 (NFR) [新增章节]
10.1 浏览器兼容性
核心依赖：Web Speech API (TTS)。
兼容策略：
优先支持 Chrome, Safari, Edge。
若浏览器不支持 (如 IE)，隐藏发音按钮，并顶部提示“建议使用 Chrome 或 Safari 浏览器以获得完整体验”。
10.2 性能要求
列表渲染：当单词列表 (L2) 单页数据超过 100条 时，前端需采用 虚拟列表 (Virtual Scroll) 技术，确保滚动流畅。
加载速度：L1 大厅骨架屏展示时间不应超过 1.5秒。
10.3 UI/UX 细节补充
搜索无结果：
显示放大镜插画。
文案：“未找到相关内容”。
操作：提供“意见反馈”链接。
全局搜索：规划于 v3.3 版本上线，本期暂不开发。
产品需求文档 (PRD): 小语笔记-英语 (Web端)
版本号：v3.5.0
更新日期：2026-01-16

1. 文档概览
项目名称：小语笔记-英语 (Web端)
适用终端：Web (PC), Mobile Web, Pad Web (响应式设计)
核心目标：打造一个集“高效录入”、“结构化记忆”、“数据可视化”于一体的英语学习平台。
2. 功能架构图 (Information Architecture)
[L0] 全局层

├── 身份验证 (Auth System) [手机号+密码+邀请码]
├── [新增] 多端同步机制 (WebSocket/长轮询)
├── 顶部导航栏 (Global Nav): 首页、单词库、意见反馈、[新增] 退出登录
└── 核心工作台 (Dashboard)
[L1] 单词库大厅 (Library Hub)

├── [A] 个人学习区 (My Dashboard)
├── 继续学习
├── 错题本 (Mistakes Book)
├── 学习日历
└── 新建自定义词库
└── [B] 词库资源列表 (All Books List)
├── 筛选栏 (Tabs: 全部/考试/场景/教材)
└── 词库卡片流
[L2] 单词书详情页 (Book Viewer)

├── 统一导航头：主题筛选、场景筛选
├── 单词列表容器 (Word List Container)
└── 底部控制栏 (Pagination & Actions)
[L3] 练习系统 (Practice System)

├── 范围选择器
├── 听写模式 (Dictation) [修订：纯键盘输入]
├── 消消乐 (Match Game)
├── 卡片背单词 (Flashcards)
└── 打字练习（肌肉训练）(Typing Practice) [新增]
3. 注册登录系统 (Auth System)
核心策略：无短信验证码，通过"邀请码强校验+行为风控"保障安全。

**3.1 注册交互流程** [修订 2026-01-09]
页面加载：若URL携带 ?code=xxx，自动填入邀请码并置灰；否则允许输入。
表单填写：手机号（11位）、密码（至少6位）、邀请码。
密码显示/隐藏切换：点击眼睛图标切换明文/密文显示。
提交反馈：成功跳转首页（自动登录）；失败提示错误信息。

**注**：
- 无确认密码字段
- 无滑块拼图验证（暂时不做，后续迭代添加）
- 注册成功后自动登录并跳转首页

**3.1.1 邀请码与权限继承** [新增]
用户通过邀请码注册时，系统自动赋予对应的权限：
- **功能权限**：消消乐、卡片背单词、自定义单词书、听写训练
- **单词书权限**：可访问的单词书列表（或全部）
- **权限有效期**：从注册日期开始计算的有效时长
- **权限继承**：注册后用户立即获得邀请码对应的套餐权限

权限验证：
- 用户登录后，系统实时检查权限是否过期
- 过期后显示友好提示："您的权限已过期，请联系管理员续费"
- 权限不足的功能模块显示升级提示

**3.2 安全防刷策略**
邀请码锁：单码限制使用次数（每个邀请码仅能使用1次）。
IP/设备限流：单IP 1小时限3次；单设备24小时限1次。
蜜罐技术：拦截脚本自动填充。
防爆破：输错5次邀请码锁定24小时，接口延迟响应。
3.3 账户管理与同步 [新增章节]
修改密码：
前台不提供直接修改功能。
点击“修改密码”入口，弹窗提示：“为了您的账户安全，如需重置密码请联系官方客服，邮箱：support@xiaoyu.com”。
注销账号：v3.2版本暂不支持，后续迭代。
多端同步机制：
系统需支持多端数据实时同步（建议采用 WebSocket 或 长轮询机制）。
冲突解决：以服务器端接收到的“最后操作时间戳”为准，自动覆盖旧数据。
4. [L1] 单词库大厅 (Library Hub)
### 4.1 个人学习区

**继续学习卡片** [更新 2026-01-14]：
- 展示最近3本词书的学习进度（多进度卡片显示）
- 每张卡片显示：书名、学习进度百分比、学习模式（📚单词表/🎴卡片/📝默写/🎮消消乐）、时间信息
- 永久显示，无时间限制

**智能跳转策略** [新增 2026-01-14]：
- **从首页点击"继续学习"** → **直接跳转，不弹窗**
  - 理由：用户已明确表达"继续"意图，应快速响应
  - 跳转URL携带 `resume=true` 参数标识来源
  - 示例：`/study/book-123/flashcards?scope=unknown&shuffle=true&resume=true#word-10`
- **从书架/词书详情点击"开始学习"** → **显示选择对话框**
  - 理由：用户可能想选择新的学习范围
  - 显示"继续学习"选项但需要用户确认
  - 不携带 `resume` 参数或 `resume=false`

**跳转URL格式**：
- word-list: `/library/{bookId}?theme={theme}&page={page}`
- flashcards: `/study/{bookId}/flashcards?scope={scope}&shuffle=true&resume=true#word-{index}`
- dictation: `/study/{bookId}/dictation?scope={scope}&resume=true#word-{index}`

- 若无学习记录，显示占位状态，文案："您还未开始学习，请选择一本词书开始吧！"

**学习进度计算** [新增定义]：
- **进度百分比** = (认识单词数 + 模糊单词数) / 总单词数 × 100%
- **known（认识）**：完全掌握，计入进度
- **fuzzy（模糊）**：部分掌握，计入进度
- **unknown（不认识）**：未掌握，**不计入进度**
- **new（未标注）**：未学习，**不计入进度**
- **计算公式**：
  ```typescript
  progress = Math.round(
    ((known_count + fuzzy_count) / total_words) * 100
  )
  ```
- **示例**：某书100词，20个认识、30个模糊、50个不认识 → 进度 = 50%

**功能入口卡片**：
- **错题本**：显示错题总数。
- **生词日历**：显示今日新增词数。
- **新建词库**：显示 "+" 号大图标。
4.2 词库资源列表
筛选导航：Tabs切换（全部/热门推荐/国内考试/国外考试/K12教材/大学教材），实时更新数量统计。
列表展示：响应式网格布局。
词库卡片：封面、书名、单词总量、描述、状态（学习中Badge）。

**词库分类体系** [更新 v3.4.0]:
- **📌 热门推荐**（3个）：考研、IELTS、TOEFL（针对大学生和研究生用户最热门的考试）
- **🎓 大学教材**（2个）：CET-4、CET-6（大学英语四六级考试）
- **🇨🇳 国内考试**（3个）：专业英语四级、专业英语八级、PETS3
- **🌍 国外考试**（8个）：GRE、SAT、GMAT、BEC、FCE、PET、PTE、KET
- **📚 K12教材**（12个）：PEP小学3-6年级、PEP初中7-9年级、PEP高中英语、高中、初中、北京高中英语、外研社初中英语
- **UI交互**：
  - 顶部显示六个标签页（带emoji图标）
  - 每个标签显示对应分类的词库数量
  - 点击标签即时筛选，无需页面刷新
  - 空状态时显示友好提示
  - 词库卡片左上角显示分类标签（替代原有的"考试/场景/教材"标签）
  - 最近访问的前6个词库额外显示金黄色"最近"标签

**智能排序策略** [新增 v3.4.0]:
- **优先级1**：最近访问的词库优先（基于 `last_accessed_at` 字段）
- **优先级2**：热门词库优先（考研 > IELTS > TOEFL > CET-4 > CET-6）
- **优先级3**：其他按单词数量降序排序（词数多的在前）
- **设计理念**：结合用户行为数据和内容价值，提升用户找到目标词库的效率

**最近访问功能** [新增 v3.4.0 - 2026-01-10]:
- **功能描述**: 自动追踪用户最近访问的词库，在首页"词库资源"区域的"最近"Tab中展示
- **展示位置**: 首页 → 词库资源 → "最近"Tab（第一个Tab，默认选中）
- **显示规则**:
  - 最多显示**6个**最近访问的词库 **[统一修订 2026-01-12]**
  - 按 `last_accessed_at` 降序排列（最近访问的在前）
  - 去重：同一词库只显示最新一次访问记录
- **记录触发**: 用户点击任意词库卡片时自动记录访问时间
- **数据来源**: `user_book_preferences.last_accessed_at` 字段
- **空状态**: "还没有访问过任何词库，从'我的'或'全部'中选择一个词库开始学习吧！"
- **实现细节**:
  - 仅在切换到"最近"Tab时加载一次数据，避免过度刷新
  - 点击词库卡片异步记录访问（不阻塞跳转）
  - 使用索引 `(user_id, last_accessed_at DESC)` 优化查询性能
- **统一性保证** [新增 2026-01-12]:
  - 首页"最近学习"卡片: `limit(6)` ✅
  - 首页BookLibrary "最近"Tab: `limit(6)` ✅
  - 系统词库列表 "最近"标记: `limit(6)` ✅
  - 三处查询完全一致，确保用户体验统一
5. [L2] 单词书详情页 (Book Viewer)
5.1 顶部统一导航
主题/场景选择器：下拉菜单（PC）/ 底部抽屉（Mobile）。
联动逻辑：选择主题后刷新列表；若主题下有场景，二级筛选显示对应场景。

**章节筛选** [新增]:
- 显示条件：仅当词库包含章节时显示（有Chapter字段的单词）
- 筛选器类型：下拉菜单，与主题/场景筛选器并列
- 选项：
  - "全部章节"（默认）：显示所有单词
  - 具体章节列表：按order_index排序显示
- 联动逻辑：
  - 选择章节后刷新列表，仅显示该章节下的单词
  - 可与主题/场景/状态筛选组合使用
  - 切换章节时保持其他筛选条件不变
- 特殊处理：
  - 无章节词库：不显示章节筛选器
  - 默认章节：Chapter为空的单词归入"默认章节"

5.2 排序与筛选
排序：默认（Excel导入顺序）、随机打乱。
状态筛选：全部、未标注、认识、不认识、模糊。
5.3 单词列表容器 (Word List)
显示内容：单词、音标、**词性**、释义、搭配、例句、操作区。

**词性显示规范** [优化]:
- 数据存储：数据库存储英文词性（如 "noun"、"verb"、"adjective" 或缩写 "n"、"v"、"adj"）
- 前端显示格式：英文缩写 + 中文（如"n名词"、"v动词"、"adj形容词"）
- 词性映射表：
  - noun / n → n名词
  - verb / v → v动词
  - adjective / adj → adj形容词
  - adverb / adv → adv副词
  - pronoun / pron → pron代词
  - preposition / prep → prep介词
  - conjunction / conj → conj连词
  - article / art → art冠词
  - auxiliary verb → aux助动词
  - modal verb → modal情态动词
  - transitive verb → vt及物动词
  - intransitive verb → vi不及物动词
  - 其他词性 → 保持原样显示（用于扩展）
- 显示样式：紫色圆角徽章，位于音标右侧
- 多词性处理：逗号分隔显示（如"n名词, v动词"）

交互细节：

**发音功能** [增强]：
- 使用 Web Speech API (TTS) 实现单词朗读
- 支持三个独立发音按钮：
  - **单词发音**：小喇叭图标紧跟单词右侧
  - **搭配发音**：小喇叭图标在搭配区域右侧
  - **例句发音**：小喇叭图标在例句区域右侧
- 发音按钮样式：16px × 16px 小图标，淡灰色（不显眼），hover时颜色加深
- 点击发音时，图标无loading状态，语音播放完毕自动停止

**中文翻译显示规范** [新增]：
- **格式要求**：
  - 必须添加"中文："前缀（如："中文：议程，日程表"）
  - **中文翻译中不包含词性标记**：不显示 n./v./adj./adv. 等前缀
  - 原因：词性已通过独立的紫色徽章显示（如"n名词"标签），避免重复
  - 错误示例：~~"中文：n. 议程，日程表"~~
  - 正确示例：✅ "中文：议程，日程表"
- **样式规范**：
  - 字号：`text-sm` (14px) - 小字号
  - 颜色：`text-gray-600` (#4B5563) - 灰色（柔和，不刺眼）
  - 字重：`font-semibold` - 半粗体
- **显示顺序**：
  - 英文释义在上方
  - "中文：xxx" 在下方
  - 示例：
    ```
    A list of items to be discussed at a meeting
    中文：议程，日程表
    ```
- **数据规范**：
  - Excel导入时的 `Definition` 字段应直接写中文释义
  - 不需要在释义前加词性标记（如"n."、"v."）
  - 词性通过独立的 `Part of Speech` 字段标识

**中文显隐功能** [优化]：
支持两种方式切换中文释义可见性（用于测试自己是否掌握）：

1. **全局隐藏（持久化）**：
   - 入口：页面顶部右上角的"隐藏中文"按钮
   - 作用范围：当前用户 + 当前单词书
   - 持久化：设置保存到数据库 `user_book_preferences` 表
   - 下次访问该书时自动应用该设置
   - 视觉反馈：激活时按钮显示"全局生效"紫色徽章

2. **本地隐藏（前端状态）**：
   - 入口：每个单词卡片顶部的"隐藏中文"按钮（眼睛图标）
   - 作用范围：仅影响当前卡片
   - 持久化：不保存，刷新页面后恢复默认
   - **优先级优化**：本地按钮可以覆盖全局设置
     - 当全局隐藏生效时，所有卡片默认不显示中文
     - 用户可通过本地按钮强制显示某个卡片的中文
     - 每个卡片的本地按钮完全独立控制

**隐藏时的视觉表现**：
- **始终显示**：
  - 单词（英文）、音标、词性中文徽章
  - 功能标签："💡 搭配:"、"📝 例句:"
  - 所有英文内容（释义、搭配、例句）
  - "中文："前缀
- **隐藏中文时显示**：
  - 释义：`中文：______________________` (灰色下划线占位符)
  - 搭配：`中文：______________________` (灰色下划线占位符)
  - 例句：`中文：______________________` (灰色下划线占位符)
  - 目的：让用户明确知道此处有中文内容，仅是被隐藏
- **显示中文时显示**：
  - 所有内容（中英文都显示）

**隐藏按钮样式** [优化]：
- 极度低调设计：
  - 无边框、无背景
  - 仅显示图标（眼睛图标）
  - 图标尺寸：12px × 12px (w-3 h-3)
  - 图标颜色：`text-gray-300` (极淡灰色) - 几乎不可见
  - hover颜色：`text-gray-500` (稍微明显)
  - padding：`px-1.5 py-0.5`
- 位置：每个卡片顶部，单词右侧
- 交互：鼠标悬停时图标略微变深，提供视觉反馈
- 按钮状态：
  - 显示中文时：闭眼图标（EyeOff）
  - 隐藏中文时：眼睛图标（Eye）

**内容折叠展开功能** [新增]：
- **目的**：当搭配或例句内容过长时，保持界面整洁
- **折叠状态**：
  - 最大高度：96px (max-h-24)
  - 超出部分隐藏 (overflow-hidden)
  - 底部显示半透明渐变遮罩（从背景色到透明）
  - 遮罩上显示"展开"按钮（仅在内容溢出时）
- **展开状态**：
  - 显示完整内容
  - 遮罩消失
  - 显示"收起"按钮（极淡灰色）
- **遮罩样式**：
  - 高度：48px (h-12)
  - 渐变：从背景色（100%不透明）到透明
  - 过渡：平滑 (transition-all duration-200)
- **展开按钮样式**：
  - 位置：叠加在遮罩层底部居中
  - 背景：白色半透明 `bg-white/80`
  - 特效：背景模糊 `backdrop-blur-sm`
  - 形状：圆角胶囊形 `rounded-full`
  - 阴影：淡阴影 `shadow-sm`
  - 文字颜色：`text-gray-400`，hover时 `text-gray-600`
  - 字号：`text-xs`
- **智能显示**：
  - 使用 useRef + useEffect 检测内容是否溢出
  - 只在内容真正溢出时显示遮罩和按钮
  - 短内容不显示展开功能，保持界面简洁

**卡片布局** [优化]：
- 布局方式：CSS Grid，响应式布局
  - 移动端：1列 (grid-cols-1)
  - 平板：2列 (md:grid-cols-2)
  - 桌面：4列 (xl:grid-cols-4)
- 卡片高度：自适应内容，不设置固定高度
- 按钮对齐：使用 Flexbox 的 `mt-auto` 固定在卡片底部
- 卡片间距：16px (gap-4)

**状态标记**：
- 点击圆点选择：认识(绿) / 模糊(黄) / 不认识(红)
- 按钮尺寸：40px × 40px 圆形按钮
- 视觉反馈：选中状态填充对应颜色，未选中状态为白底灰边

**5.4 练习模式区域** [新增关键模块]
在单词书详情页的筛选栏下方、单词列表上方，直接展示四种练习模式卡片，无需跳转到单独的选择页面。

布局结构：
- 标题卡片：显示"选择练习模式 🎯"和引导文案
- 练习模式网格：4列响应式布局（PC端横排，移动端竖排）
  1. **卡片背单词**（紫色卡片）
     - 图标：CreditCard
     - 描述：经典记忆模式，点击翻转卡片，快速记忆单词
     - 按钮：开始背诵
     - 链接：/study/[bookId]/flashcards
  2. **听写模式**（薄荷绿卡片）
     - 图标：Volume2
     - 描述：听音拼写，强化记忆，检验学习成果
     - 按钮：开始听写
     - 链接：/study/[bookId]/dictation
  3. **消消乐**（桃色卡片）
     - 图标：Gamepad2
     - 描述：趣味配对游戏，轻松学习，寓教于乐
     - 按钮：开始游戏
     - 链接：/study/[bookId]/match-game
  4. **肌肉训练**（橙色卡片）[新增]
     - 图标：Dumbbell
     - 描述：打字练习背单词，通过盲打强化肌肉记忆
     - 按钮：开始练习
     - 链接：/typing/[bookId]/practice
- 学习小贴士卡片（蓝色）：提供学习建议

交互设计：
- 卡片悬停：阴影增强 + 向上位移1px
- 卡片点击：直接跳转到对应练习页面
- 响应式：PC端3列横排，平板/移动端1列竖排

设计原则：
- 降低用户操作成本：无需额外点击进入练习模式选择页
- 提升功能可见性：三种练习模式一目了然
- 保持页面流畅性：练习模式卡片位于页面中部，不干扰单词列表浏览

5.5 单词状态核心逻辑 [新增关键逻辑]
为解决同一单词在不同书籍中释义不同但状态需同步的问题，采用 "内容局部化，状态全局化" 策略：

唯一性判断：以 英文拼写 为用户维度的唯一键。
内容独立：单词的释义、例句跟随 "单词书"。
例：在《少儿英语》中 "Bank" 释义为"河岸"；在《金融英语》中释义为"银行"。
状态共享：单词的"认识/不认识"状态跟随 "用户" 全局共享。
例：用户在《少儿英语》标记 "Bank" 为认识，打开《金融英语》时，"Bank" 自动显示为认识。
冲突处理：若用户在《金融英语》将 "Bank" 改为"不认识"，则全局（包括《少儿英语》）该词状态同步变为"不认识"。

**默认状态** [修订]：
- 所有单词的初始状态为"未标注"（new）
- 用户首次查看单词书时，所有单词均显示为"未标注"状态
- 用户需主动标记单词为"认识"、"模糊"或"不认识"后，状态才会变更
- "未标注"状态的单词不参与任何练习模式的筛选

5.6 底部控制栏
分页器：当章节单词数 > 50 时显示分页；否则隐藏。

6. [L3] 练习系统 (Practice System)

**6.0 范围选择器** [新增关键功能]
用户点击练习模式卡片（卡片背单词/听写模式/消消乐）时，弹出范围选择对话框，让用户选择练习范围。

**交互流程**：
1. 用户点击任意练习模式卡片的按钮（开始背诵/开始听写/开始游戏）
2. 检查URL是否携带 `resume=true` 参数
3. **如果 `resume=true`**（从首页"继续学习"进入）：
   - 直接进入学习页面，跳过范围选择对话框
   - 从URL参数中获取范围和进度信息
   - 自动跳转到指定位置（通过hash定位）
4. **如果无 `resume` 参数或 `resume=false`**（从书架/词书详情进入）：
   - 弹出范围选择对话框（模态框，带背景遮罩）
   - 用户选择练习范围后，点击"确认开始"进入练习页面

**智能跳转逻辑** [新增 2026-01-14]：
```typescript
// 练习页面入口判断
const searchParams = new URLSearchParams(window.location.search)
const isFromHomepageResume = searchParams.get('resume') === 'true'

if (isFromHomepageResume) {
  // 从首页"继续学习"进入：直接进入，不弹窗
  const scope = searchParams.get('scope') || 'unknown'
  const hashIndex = window.location.hash // #word-10
  startLearning(scope, hashIndex)
} else {
  // 从书架/词书详情进入：显示范围选择对话框
  showScopeSelectionDialog()
}
```

**对话框设计**：
- 标题："选择练习范围"
- 顶部显示"继续上次学习"卡片（如果有断点）：
  - 显示上次学习的范围和进度
  - 显示时间信息（刚刚/2小时前/3天前）
  - 点击可直接继续上次学习
- 分隔线："—— 或选择新范围 ——"
- 范围选项卡片（5个）：
  1. **不认识的** (unknown) - 红色
  2. **未标注的** (new) - 灰色
  3. **模糊的** (fuzzy) - 黄色
  4. **认识的** (known) - 绿色
  5. **全部单词** (all) - 黑色
- 每个选项显示：范围名称、单词数量、描述文案
- 底部按钮："关闭"（已移除确认按钮，点击选项即开始）

**UI样式**：
- 对话框：clay-card 居中显示，最大宽度 500px
- 选项卡片：可点击的卡片，hover时阴影增强
- "继续上次学习"卡片：渐变背景（#ccff00 → #b8e600），更醒目
- 响应式：移动端全屏显示，PC端居中浮层

**边界情况处理**：
- 如果某范围单词数为0，该选项禁用（opacity: 0.5, cursor: not-allowed）
- 如果全书单词数少于3个，提示："单词数量太少，无法开始练习"
- 如果所有范围都没有单词，显示空状态提示

**数据传递**：
- URL参数传递：
  - `scope`: all/unknown/fuzzy/known/new
  - `shuffle`: true/false（是否乱序）
  - `resume`: true/false（是否从首页继续学习进入）
- Hash定位：
  - `#word-{index}`：跳转到指定索引的单词
  - flashcards: `#word-10` → 第11张卡片（index=10）
  - dictation: `#word-5` → 第6题（index=5）

**6.1 听写模式 (Dictation) [重构 2026-01-14]**

**功能概述**：
将听写模式从"继承单词列表页筛选"改造为"独立范围选择 + 独立进度管理"，与卡片背单词保持一致的用户体验。

**核心改动**：
- 旧逻辑：单词列表页筛选 → 点击"开始听写" → 继承筛选条件 → 听写
- 新逻辑：点击"开始听写" → 弹出范围选择对话框 → 选择范围 → 独立进度听写

**用户价值**：
1. ✅ 清晰的范围认知：进入时明确知道要听写哪些单词
2. ✅ 独立的进度追踪：每个范围的学习进度独立保存
3. ✅ 快速场景入口：一键进入"听写错词本"等高频场景
4. ✅ 与卡片背单词体验一致：降低用户学习成本

**范围选择机制** [新增核心功能]：
- **独立范围选择**：与单词列表页筛选解耦，进入听写模式时单独选择
- **可选范围**：
  - 全部单词 (all)
  - 不认识的 (unknown)
  - 模糊的 (fuzzy)
  - 认识的 (known)
  - 未标注 (new)
- **范围选择对话框**：
  - 从单词列表页点击"开始听写"后弹出
  - 显示各范围的单词数量统计
  - 用户选择范围后进入学习
- **独立进度记录**：
  - 每个范围独立记录学习进度（`bookId + scopeType`）
  - 保存当前索引位置（currentIndex）
  - 离开时自动保存（返回、浏览器返回、关闭窗口）
- **进度恢复逻辑**：
  - 从上次离开位置继续学习
  - 如果上次学习的单词已不符合当前范围条件，自动跳到下一个符合的单词
- **首页"继续学习"集成**：
  - 直接跳转到上次学习的范围和进度
  - 示例：上次在"不认识"范围学到第50个，点击"继续学习"直接进入该范围的第50个

**页面布局**：
- 顶部导航栏：返回按钮、词书标题、**当前范围标签**、进度显示
- **实时统计色块** [新增]：
  - 显示当前词书的各状态单词分布（认识/模糊/不认识/未标注）
  - 色块可点击，点击后弹出确认框切换到对应范围
  - 示例：`[认识60% | 模糊20% | 不认识15% | 未标注5%]`
  - 颜色映射：认识(绿色 #B4F416) / 模糊(黄色 #FACC15) / 不认识(红色 #FF6B6B) / 未标注(灰色 #E5E7EB)
- 进度条：显示当前范围的学习进度百分比（已学习/总数）
- 听写卡片：
  - 播放按钮：大图标按钮，点击播放单词
  - 中文释义显示区域：可切换显示/隐藏（默认显示）
  - 输入框：大字号，居中，自动聚焦
  - 提交按钮：提交答案
  - 反馈区域：显示正确/错误信息
- 控制按钮区：三个按钮（上一个、暂停播放、重新播放）

**播放逻辑** [修订]：
- 单词只播放**一遍**（自动播放）
- 用户点击播放按钮可重新播放
- 播放时显示播放状态指示器
- 播放完成后隐藏指示器

**中文释义显示** [新增]：
- 默认显示中文释义，降低难度
- 提供切换按钮："显示/隐藏"（虚线下划线样式，低调）
- 切换状态持久化到用户偏好（`user_book_preferences` 表）
  - 字段：`hide_definition`（布尔值，true=隐藏中文）
  - 作用域：用户+单词书维度的设置
  - 体验：用户在任意单词书的听写模式下切换设置，在该书的所有听写会话中都生效
- 显示样式：
  - 显示：居中大字号文本，无背景无边框
  - 隐藏：虚线下划线 + 灰色"（已隐藏）"文字
- 页面加载时自动聚焦到输入框

**听写模式辅助功能** [新增 2026-01-14]：

1. **隐藏中文纯练听力** [新增]
   - 功能描述：开关控制是否显示中文释义，开启后隐藏所有中文内容（释义、例句），仅通过听力练习拼写
   - 默认状态：**关闭**（默认显示中文释义）
   - UI位置：进度条下方，与"打错进入错题本"开关并排显示
   - 交互方式：
     - 点击按钮切换开/关状态
     - 开启状态：按钮显示"✓ 隐藏中文纯练听力"，按钮背景为酸性绿(#CCFF00)
     - 关闭状态：按钮显示"○ 隐藏中文纯练听力"，按钮背景为白色
   - 实时生效：切换后立即隐藏/显示释义区域，无需刷新页面
   - 使用场景：
     - 初学者：关闭该功能，看中文释义辅助理解
     - 进阶者：开启该功能，纯听力训练，提高听力水平
   - 持久化：该设置仅在当前学习会话中有效，不保存到用户偏好

2. **打错进入错题本** [新增]
   - 功能描述：自动将听写错误的单词标记为"不认识"(unknown)，加入错题本
   - 默认状态：**打开**（默认自动加入错题本）
   - UI位置：进度条下方，与"隐藏中文纯练听力"开关并排显示
   - 交互方式：
     - 点击按钮切换开/关状态
     - 开启状态：按钮显示"✓ 打错进入错题本"，按钮背景为酸性绿(#CCFF00)
     - 关闭状态：按钮显示"○ 打错进入错题本"，按钮背景为白色
   - 触发时机：
     - 用户提交答案后，系统判断拼写错误时
     - 如果该功能开启，自动调用API将单词状态标记为"unknown"
     - 如果该功能关闭，不自动修改单词状态
   - 实现逻辑：
     - 调用API：POST /api/word-progress
     - 请求体：`{ wordId, status: 'unknown' }`
     - 成功后，该单词会被归类到"不认识"范围，可在错题本中练习
   - 使用价值：
     - 帮助用户快速识别薄弱单词，集中复习
     - 避免手动标记，提升学习效率
     - 自动化构建个性化错题集
   - 持久化：该设置仅在当前学习会话中有效，不保存到用户偏好

3. **Tab键提示首字母** [新增]
   - 功能描述：用户按Tab键时，自动在输入框中追加当前单词的首字母
   - 实现逻辑：
     - 监听输入框的onKeyDown事件
     - 检测到Tab键时，阻止默认行为（防止失去焦点）
     - 获取当前单词的首字母（小写）
     - 追加到当前输入内容的末尾
     - 示例：当前单词"cumulative"，输入框为空，按Tab后变为"c"
   - UI提示：
     - 输入框上方显示提示文字："💡 提示：按 Tab 键可显示首字母"
     - 提示样式：小字号、灰色、居中、不抢眼
   - 使用场景：
     - 用户完全听不懂发音时，提供首字母提示
     - 降低难度，避免完全卡壳
     - 渐进式学习：从首字母推断完整拼写
   - 注意事项：
     - 该功能为提示性质，不扣除学习积分
     - 可以多次按Tab（但只会追加一次首字母）
     - 适用于所有单词，不影响学习数据

**控制按钮** [新增]：
- 样式：纯icon按钮，使用 clay-card 样式，低调不抢眼
- 布局：横向排列，间距适中
- 交互：hover时放大1.1倍，禁用时透明度30%
三个按钮布局（从左到右）：
1. **上一个**：
   - 图标：SkipBack（lucide-react）
   - 功能：返回上一个单词
   - 样式：次级按钮（蓝色）
2. **暂停/继续播放**：
   - 图标：Pause/Play（lucide-react）
   - 功能：暂停/继续当前音频播放
   - 样式：主按钮（绿色）
   - 动态切换图标和文字
3. **重新播放**：
   - 图标：RotateCcw（lucide-react）
   - 功能：重新播放当前单词
   - 样式：次级按钮（蓝色）

**输入方式**：
PC端：物理键盘输入。
Mobile端：自动调用系统软键盘。
交互优化：题目加载时，输入框自动获取焦点 (Auto Focus)。

**流程**：
1. 自动播放单词一次
2. 用户查看中文释义（如需要）
3. 用户输入拼写
4. 按Enter或点击提交
5. 反馈：
   - 正确：边框变绿 + "Ding"音效 -> 0.5s后切题。状态更新为"认识"
   - 错误：边框变红 + 晃动动画 + "滴"声 -> 显示正确拼写 -> **自动标记为"不认识"** -> 倒计时2s -> 切题

**单词状态更新机制** [新增 2026-01-14]：

**1. 答题结果与状态映射**
| 答题结果 | 单词状态更新 | API调用 | 说明 |
|---------|------------|---------|------|
| ✅ 拼写正确 | `status = 'known'`（认识） | POST /api/word-progress | 标记为已掌握，自动移出错题本 |
| ❌ 拼写错误（有输入） | `status = 'unknown'`（不认识） | POST /api/word-progress | 自动加入错题本，需开启"错题入本" |
| ⚠️ 空输入（直接回车） | 不更新状态 | 无 | 只显示错误提示，不保存 |

**2. 乐观更新策略（Optimistic Update）**
- **前端立即更新**：答对/答错后，UI立即更新，无需等待API响应
- **后台延迟保存**：切题时（500ms/3000ms后）静默调用API
- **错误处理**：API失败不影响UI，加入重试队列
- **数据不丢失**：使用LocalStorage备份 + sendBeacon

**更新流程**：
```typescript
// 步骤1：前端立即更新（乐观更新）
if (isCorrect) {
  // 立即更新UI统计
  stats.known += 1
  stats.unknown -= 1  // 如果之前是不认识
}

// 步骤2：显示反馈
setFeedback('correct')
playSound('correct')

// 步骤3：延迟后切题并保存
setTimeout(async () => {
  // 后台静默调用API
  await saveWordStatus(currentWord.id, 'known')
  await handleNext()
}, 500)
```

**3. 统计色块实时更新**
- 位置：页面右上角Header区域
- 布局：4列（不认识 | 模糊 | 认识 | 未标注）⭐ 新增"未标注"
- 颜色：不认识(红) / 模糊(黄) / 认识(绿) / 未标注(灰)
- 交互：数字实时变化，无刷新

**UI布局**：
```
┌─────────────────────────────────────┐
│ 不认识 | 模糊 | 认识 | 未标注        │
│  150  | 30  | 120  | 200          │
└─────────────────────────────────────┘
```

**4. 边界情况处理**
| 场景 | 行为 | 理由 |
|-----|------|------|
| 答对"未标注"的词 | known+1, new-1 | 新词变为认识 |
| 答对"不认识"的词 | known+1, unknown-1 | 错题移出 |
| 答错"认识"的词 | unknown+1, known-1 | 降级处理 |
| 空输入按回车 | 不更新，只提示 | 无输入不算答题 |

**5. 数据持久化策略**
- **本地备份**：LocalStorage备份进度数据
- **批量提交**：2秒批量提交一次（最多10个任务）
- **错误重试**：自动重试3次（1s/2s/5s延迟）
- **离线支持**：队列持久化，联网后自动提交

**断点续做功能** [新增 2026-01-14]：

**1. 功能概述**
- 永久记住用户的上次学习位置（无时间限制）
- 数据存储在服务端（`user_book_preferences.last_resume_state`）
- 在范围选择对话框顶部显示"继续上次学习"快速入口

**2. 数据结构**
```typescript
interface DictationResumeState {
  mode: 'dictation'
  bookId: string
  bookTitle: string
  updatedAt: number
  context: {
    scopeType: 'all' | 'unknown' | 'fuzzy' | 'known' | 'new'
    currentIndex: number
    totalWords: number
    currentWord?: {
      id: string
      word: string
    }
  }
}
```

**3. 范围选择对话框UI更新**
```
┌─────────────────────────────────┐
│     📍 选择学习范围              │
├─────────────────────────────────┤
│                                 │
│  🎯 继续上次学习                 │  ⭐ 新增
│  ┌─────────────────────────┐   │
│  │ 已认识  •  第5个词      │   │
│  │ 📖 5/200 (2%)          │   │
│  └─────────────────────────┘   │
│                                 │
│  ──────────────────────────    │
│     或选择新范围                  │
│  ──────────────────────────    │
│                                 │
│  ┌─────┐ ┌─────┐ ┌─────┐     │
│  │全部 │ │不认 │ │模糊 │     │
│  │1000│ │ 300│ │ 200│     │
│  └─────┘ └─────┘ └─────┘     │
│  ┌─────┐ ┌─────┐              │
│  │认识 │ │未标 │              │
│  │ 500│ │ 150│              │
│  └─────┘ └─────┘              │
└─────────────────────────────────┘
```

**4. 保存时机**
- 切换到下一个单词时
- 页面卸载时（beforeunload）
- 浏览器返回时（popstate）
- 页面隐藏时（visibilitychange）

**5. 数据恢复逻辑**
- 进入听写模式时，自动从服务端读取上次进度
- 如果有进度，在对话框顶部显示"继续上次学习"
- 点击后恢复到上次的学习范围和索引位置
- 无时间限制（永久保存）

**6. 边界情况处理**
| 场景 | 处理方式 |
|-----|---------|
| 上次的单词已不在范围内 | 自动跳到下一个符合的单词 |
| 用户清空缓存 | 从服务端重新读取 |
| 数据被重置 | 显示手动选择范围 |

**完成状态** [新增]：
- 当用户完成当前范围的最后一个单词时，显示完成对话框（模态弹窗）：
  - 标题："🎉 太棒了！"
  - 文案："你已经完成了'{范围名称}'范围的所有单词听写练习！"
  - 副标题："接下来你想做什么？"
  - 三个操作按钮：
    1. **🔄 重新学习这个范围** - 重置到第一个单词（index=0），可重新开始学习当前范围
    2. **📚 选择其他范围** - 打开范围选择对话框，切换到不同的学习范围
    3. **返回首页** - 返回到应用首页
- 适用范围：所有范围（all/unknown/fuzzy/known/new）
- 对话框样式：Neo-Brutalism设计，带遮罩层，点击遮罩关闭对话框

**数据获取** [更新]：
- API: GET /api/words?bookId={bookId}&status={status}
- status 参数：all | unknown | fuzzy | known | new
- 返回结果按乱序排列（使用基于 bookId + status 的随机种子）
- 支持实时切换范围，无需刷新页面

**进度保存机制** [新增]：
- 存储键：`dictation_progress_{bookId}_{scopeType}`
- 保存内容：
  - currentIndex: 当前学习到的索引位置
  - totalWords: 该范围的总单词数
  - lastStudyTime: 最后学习时间戳
  - scopeType: 范围类型（all/unknown/fuzzy/known/new）
- 保存时机：
  - 切换到下一个单词时
  - 标记单词状态时
  - 页面卸载时（beforeunload）
  - 页面隐藏时（visibilitychange）
  - 浏览器返回时

**听写模式设置** [保留功能模块]
在听写页面顶部导航栏右上角，新增设置按钮（齿轮图标），点击打开设置面板。

**设置面板设计**：
- 位置：模态框，居中显示，带背景遮罩
- 样式：clay-card-xl，最大宽度 500px
- 包含两个主要设置：

1. **🎲 练习顺序**：
   - 单选按钮组，两个选项：
     - 按顺序（默认）：按原有顺序练习单词
     - 随机乱序：使用 Fisher-Yates 算法随机打乱单词顺序
   - 实现逻辑：
     - 乱序在页面加载时应用一次（基于用户偏好）
     - 保存到 `user_book_preferences.shuffle_order` 字段
     - 客户端 JavaScript 实现随机算法

2. **✅ 答对后自动移出错词**：
   - 复选框：启用/禁用自动移除功能
   - 下拉选择器：选择连续答对次数阈值（1/2/3/5/10次，默认3次）
   - 功能说明：
     - 开启后，当用户连续答对指定次数的单词时，自动从错词本移除
     - 标记为"认识"状态
     - 仅对"未标注"、"模糊"或"不认识"状态的单词生效
   - 实现逻辑：
     - 每次答对：`consecutive_correct_count + 1`
     - 每次答错：`consecutive_correct_count` 重置为 0
     - 达到阈值：更新单词状态为"known"，从错词本移除
     - 保存到 `word_progress.consecutive_correct_count` 字段
   - 阈值选择建议：
     - 1次：激进模式，快速清空错词本
     - 3次：推荐模式，平衡学习效果
     - 5-10次：保守模式，确保真正掌握

**数据库设计变更**：

1. **user_book_preferences 表新增字段**：
   - `shuffle_order`: BOOLEAN，是否启用乱序模式（默认 false）
   - `auto_remove_from_mistakes`: BOOLEAN，是否启用自动移除（默认 false）
   - `consecutive_correct_threshold`: INTEGER，连续答对阈值（默认 3）

2. **word_progress 表新增字段**：
   - `consecutive_correct_count`: INTEGER，当前连续答对次数（默认 0）

**API 接口变更**：

- GET /api/user-preferences?book_id=xxx
  - 响应新增字段：shuffle_order, auto_remove_from_mistakes, consecutive_correct_threshold

- POST /api/user-preferences
  - 请求体新增字段：shuffle_order, auto_remove_from_mistakes, consecutive_correct_threshold

- POST /api/word-progress
  - 请求体新增字段：consecutive_correct_count（可选）

**用户体验优化**：
- 设置保存成功后关闭模态框
- 保存失败时显示错误提示
- 设置实时生效（当前会话和未来会话都应用）
- 设置按用户+书籍维度独立存储
- 乱序设置需重新进入页面才生效（避免当前练习中途打乱顺序）

**边界情况处理**：
- 如果用户在练习中途修改乱序设置：提示"设置已保存，下次练习时生效"
- 如果连续答对计数达到阈值：在控制台输出日志，不中断当前流程
- 如果启用自动移除但单词本身已经是"认识"状态：不进行特殊处理

6.2 消消乐 (Match Game) [修订]

**难度选择系统** [新增功能 - 多轮游戏]：
游戏开始前，用户可选择难度级别，不同难度对应每轮的卡片数量：

1. **轻松模式**：每轮8张卡片（4对单词）
   - 适合：碎片时间学习、快速练习
   - 预计每轮时长：3-5分钟

2. **中等模式**：每轮20张卡片（10对单词）
   - 适合：日常学习、平衡效率与难度
   - 预计每轮时长：8-10分钟

3. **困难模式**：每轮40张卡片（20对单词）
   - 适合：深度学习、高强度练习
   - 预计每轮时长：15-20分钟

**多轮游戏机制** [核心功能]：
- **自动计算轮次**：总轮次 = Math.ceil(筛选的单词数 / 选择的难度对数)
  - 例如：100个单词 + 选择中等模式(10对) = 需要玩10轮
  - 例如：40个单词 + 选择困难模式(20对) = 需要玩2轮
- **进度显示**：Header显示"第X/Y轮 • 已完成 Z/N 个单词"
- **轮次完成界面**：每轮结束后显示进度和选项
- **通关界面**：完成所有轮次后显示像素风格通关画面

**实时难度切换**：
- 游戏右上角下拉菜单可随时切换难度
- 切换后下一轮生效，不影响当前游戏
- Toast提示："难度已更改，下轮生效"

**UI设计**：
- **选择界面**：三个大卡片并排显示，不同颜色（绿色/蓝色/紫色）渐变背景
- **轮次完成界面**：显示进度条、本轮掌握单词数、"继续下一轮"/"返回难度选择"按钮
- **通关界面**：像素游戏风格
  - 深紫色渐变背景 + 黄色边框
  - 像素字体（Courier New）
  - 统计卡片：完成轮数/掌握单词/总步数
  - 成就徽章：🏅🎖️🏆
  - 按钮带阴影效果（4px box-shadow）
  - 无animate-bounce，使用静态设计

**记忆功能**：
- 使用 localStorage 记住用户选择的难度
- 下次游戏直接应用上次选择
- 按词书ID分别存储

**数据范围**：
当前筛选范围内，状态为"未标注"、"模糊"或"不认识"的单词。已标记为"认识"的单词不参与游戏。

**进度保存机制** [智能累积系统]：

**核心逻辑**：
- 不再是一次匹配就标记为"认识"，而是采用**智能累积计数**
- 每个单词维护两个计数器：
  - `match_count`：消消乐匹配成功次数
  - `fail_count`：消消乐匹配失败次数

**阈值规则**：
- 匹配成功 2 次 → 自动标记为"认识"（`known`）
- 匹配失败 3 次 → 自动标记为"不认识"（`unknown`，仅对"未标注"状态的单词生效）

**实时反馈**：
- 每次匹配成功后显示提示：
  - 第1次匹配："✅ 匹配成功！再匹配1次即可掌握"
  - 第2次匹配："🎉 已掌握'xxx'！"
- 游戏统计区域新增"✨ 本次掌握"计数器
- 游戏结束时显示本次掌握的单词列表

**UI显示**：
1. **Toast提示**（页面顶部）：
   - 匹配成功：显示还需几次掌握
   - 达到阈值：庆祝动画 + 提示

2. **统计区域**（游戏上方）：
   ```
   已配对 | 步数 | 剩余 | ✨ 本次掌握: 5
   ```

3. **胜利界面**：
   ```
   ✨ 本次掌握了 5 个单词：
   [agenda] [compromise] [abbreviate] ...
   这些单词已标记为"认识"并同步到单词列表
   ```

**与单词列表状态同步**：
- 单词列表页面从 `word_progress` 表读取真实状态
- 消消乐游戏保存的进度会实时反映在单词列表中
- 例如：消消乐匹配成功2次后，单词列表中该单词显示为"认识"状态
- 反之：单词列表中标记为"认识"的单词，不会出现在消消乐游戏中

**跨设备同步**：
- 用户在不同设备上登录，进度保持一致
- 数据存储在 `word_progress` 表，字段包括：
  - `match_count`：匹配成功计数
  - `fail_count`：匹配失败计数
  - `status`：单词状态

**边界值处理**：
- 词数 >= 20：随机抽取20对（40张卡片）
- 3 <= 词数 < 20：抽取全部单词
- 词数 < 3：不进入游戏，提示："单词数量太少啦，先去背几个单词再来玩吧！"

**单词去重逻辑** [新增]：
- 同一单词只保留一个释义（避免多义词问题）
- 如果词书中同一单词出现多次（如"bank"在不同章节有不同释义），系统只保留第一个出现的释义
- 确保游戏中不会出现重复单词，避免用户困惑

**随机算法优化** [新增]：
- 使用 Fisher-Yates 洗牌算法实现真正的均匀随机
- 替代原有的 `sort(() => Math.random() - 0.5)` 有偏差的随机方法
- 确保每个单词被选中的概率完全相同，游戏更公平

**玩法设计** [重要修订]：
- **可见性**：所有卡片初始状态即显示完整内容（单词或释义），无需翻牌
- **配对逻辑**：
  1. 用户点击第一张卡片 → 卡片高亮（紫色边框）
  2. 用户点击第二张卡片 → 检查是否匹配
  3. 匹配规则：基于**单词文本**匹配，而非ID
     - 如果是同一单词的不同释义（如"bank" - "银行" 和 "bank" - "河岸"），可以互相配对
     - 点击任意单词卡片 + 任意释义卡片（只要单词拼写相同）即可匹配
  4. 匹配成功 → 所有相同单词的卡片同时消除 + 播放成功音效
  5. 匹配失败 → 两张卡片取消选中状态 + 播放错误音效

**布局设计** [新增]：
- Flexbox自动换行布局：
  - 卡片宽度：calc(8.33% - 16px)，每行约12张卡片
  - 最小宽度：110px（小屏适配）
  - 最大宽度：150px（大屏限制）
  - 间距：16px (gap-4)
- 卡片尺寸：高度 100px，圆角 (rounded-2xl)
- 内容区域最大宽度：max-w-7xl（比其他页面更宽）
- 目标：确保所有卡片在一屏内显示，无需滚动

**视觉设计** [新增]：
- **卡片背景颜色区分**：
  - 英文单词卡片：紫色渐变背景 (bg-gradient-to-br from-purple-50 to-purple-100) + 深紫色文字 (text-purple-900)
  - 中文释义卡片：绿色渐变背景 (bg-gradient-to-br from-emerald-50 to-emerald-100) + 深绿色文字 (text-emerald-900)
  - 两种背景色柔和但明显不同，清晰区分卡片类型
- **文字样式**：
  - 英文单词：18px (text-lg)，加粗
  - 中文释义：16px (text-base)，半粗体
- **选中状态**：
  - 英文卡片选中：紫色边框 (ring-4 ring-purple-400 ring-offset-2) + 更大阴影 + 深色渐变背景
  - 中文卡片选中：绿色边框 (ring-4 ring-emerald-400 ring-offset-2) + 更大阴影 + 深色渐变背景
  - 放大效果：scale-105
- **卡片消除动画** [优化]：
  - 匹配成功时，卡片执行简单的放大+消失动画
  - 动画效果：卡片从100%放大至150%，同时透明度降至0
  - 动画时长：150ms（极快响应）
  - 性能优化：
    - 仅使用CSS transform scale + opacity变化
    - 无额外DOM元素创建
    - 无复杂计算
    - GPU加速，零卡顿
  - 视觉效果：简洁流畅的卡片消失，匹配成功后立即进入下一题

**游戏流程**：
1. 进入页面，显示所有单词和释义卡片
2. 用户点击任意卡片，卡片高亮显示
3. 用户点击第二张卡片
4. 系统判断是否匹配：
   - 匹配：卡片消除 + 步数+1 + 配对数增加
   - 不匹配：取消高亮 + 步数+1
5. 全部卡片消除后显示胜利界面

**6.3 卡片背单词 (Flashcards)**

**范围选择机制** [重大更新 2026-01-12]：
- **独立范围选择**：与单词列表页筛选解耦，进入卡片模式时单独选择
- **可选范围**：
  - 全部单词 (all)
  - 不认识的 (unknown)
  - 模糊的 (fuzzy)
  - 认识的 (known)
  - 未标注 (new)
- **范围选择对话框**：
  - 从单词列表页点击"卡片背单词"后弹出
  - 显示各范围的单词数量统计
  - 用户选择范围后进入学习
- **独立进度记录**：
  - 每个范围独立记录学习进度（`bookId + scopeType`）
  - 保存当前索引位置（currentIndex）
  - 离开时自动保存（返回、浏览器返回、关闭窗口）
- **进度恢复逻辑**（选项 A - 推荐）：
  - 从上次离开位置继续学习
  - 如果上次学习的单词已不符合当前范围条件，自动跳到下一个符合的单词
  - 避免重复学习已掌握的单词
- **首页"继续学习"集成**：
  - 直接跳转到上次学习的范围和进度
  - 示例：上次在"不认识"范围学到第50个，点击"继续学习"直接进入该范围的第50个

**页面布局**：
- 顶部导航栏：返回按钮、词书标题、当前范围标签、进度显示
- **实时统计色块** [新增]：
  - 显示当前词书的各状态单词分布（认识/模糊/不认识/未标注）
  - 色块可点击，点击后弹出确认框切换到对应范围
  - 示例：`[认识60% | 模糊20% | 不认识15% | 未标注5%]`
  - 颜色映射：认识(绿色 #B4F416) / 模糊(黄色 #FACC15) / 不认识(红色 #FF6B6B) / 未标注(灰色 #E5E7EB)
- 进度条：显示当前范围的学习进度百分比（已学习/总数）
- 操作提示区：三个文字提示（← 左滑/认识、↑ 上滑/模糊、→ 右滑/不认识）
- 单词卡片：中央大卡片，占据主要视觉区域
- 完成提示：学完当前范围所有单词时显示祝贺信息

**单词顺序** [新增]：
- **乱序模式**：单词以随机顺序呈现，避免记忆顺序而非记忆单词
- 随机种子：基于 `bookId + scopeType` 生成，保证同一范围内的顺序稳定
- 不同范围的顺序相互独立

**卡片设计**：
- 样式：高级纸张质感（clay-card-xl）
  - 背景：暖米色渐变（#fffcf5 → #f9f6ec → #f5f0e3）
  - 边框：1px 实线 #e0d5c5（淡雅棕色）
  - 阴影：5层渐进阴影 + 内阴影 + 顶部高光
  - 纹理：三重纹理叠加（横向、纵向、斜向）
  - 边缘磨损效果：使用 ::after 伪元素
- 正面：
  - 单词：超大字号（text-5xl），加粗，居中
  - 音标：中字号（text-xl），灰色
  - 词性标签：紫色圆角徽章
  - 状态徽章：已标记单词显示状态（认识✓/模糊?/不认识✗）
  - 播放按钮：居中显示，无背景无边框，灰色图标（hover时变深）
  - 提示文字："点击卡片或按⬇️查看释义 👆"
- 背面：
  - 条件渲染：仅当字段有内容时显示对应区域（字段名+内容一起显示/隐藏）
  - 英文释义：小标签 + 正文（当 definition_en 有值时显示）
  - 中文释义：小标签 + 大字号加粗（当 definition 有值时显示）
  - 英文搭配：小标签 + 搭配内容（当 collocation_en 有值时显示）
  - 搭配：小标签 + 搭配内容（当 collocation 有值时显示）
  - 英文例句：灰色背景卡片，小标签 + 例句内容 + TTS播放按钮（当 example_sentence_en 有值时显示）
  - 例句：灰色背景卡片，小标签 + 例句内容 + TTS播放按钮（当 example_sentence 有值时显示）
  - **例句朗读** [新增]：每个例句右侧有独立的播放按钮，点击播放该例句

**交互设计**：

1. **自动朗读**：
   - 每次切换到新单词时，自动播放单词发音
   - 延迟300ms后播放，确保卡片切换动画完成（优化响应速度）
   - 使用 Web Speech API (SpeechSynthesis)
   - 语言：en-US
   - 语速：0.8（稍慢于正常速度，便于学习）
   - 仅在用户首次交互后启用（浏览器自动播放策略限制）

2. **卡片翻转**：
   - 点击卡片任意位置触发翻转
   - 3D CSS变换动画（rotateY 180deg）
   - 动画时长：600ms
   - 正反面使用backface-visibility: hidden

3. **键盘快捷键**：
   - ⬅️ 左方向键：标记为"认识"
   - ↑ 上方向键：标记为"模糊"
   - ➡️ 右方向键：标记为"不认识"
   - ⬇️ 下方向键：翻转卡片查看详情
   - 键盘操作触发卡片滑动动画（视觉反馈）

4. **滑动手势** [智能方向判断]：
   - 基于滑动角度判断方向（Math.atan2计算）
   - 最小滑动距离阈值：50像素
   - 小于50px视为点击，触发翻转
   - 方向划分：
     - 右滑（-45° 到 45°）：标记为"不认识"
     - 下滑（45° 到 135°）：翻转回正面
     - 左滑（135° 到 225°/-135°）：标记为"认识"
     - 上滑（-135° 到 -45°）：翻转查看详情
   - 卡片跟随手指/鼠标移动，带有倾斜效果
   - 倾斜角度：每100px位移约5度

5. **预览卡片效果**：
   - 滑动时在当前卡片后方显示下一个单词的预览
   - 预览卡片与前台卡片尺寸完全相同
   - 使用 translateZ(-50px) 营造3D深度感
   - 透明度：40%（半透明，隐约可见）
   - 平滑的淡入淡出动画（300ms ease-out）
   - 包含完整内容：单词、音标、词性、播放按钮

6. **单词状态标记**：
   - 操作方式：滑动、键盘快捷键
   - 状态类型：认识/模糊/不认识
   - 视觉反馈：
     - 认识：卡片向左滑动150px，左倾15度
     - 模糊：无位移动画
     - 不认识：卡片向右滑动150px，右倾15度
   - 动画时长：300ms
   - 动画完成后自动切换到下一张卡片
   - 更新进度条

7. **数据保存策略** [性能优化]：
   - 乐观更新：操作后立即更新本地状态，无等待
   - 批量保存：累积操作3秒后统一批量保存到服务器
   - 使用 Promise.all 并行发送多个保存请求
   - 数据安全：
     - 页面卸载时自动保存所有待保存数据
     - 页面隐藏时（用户切换标签页）自动保存
     - 关闭页面前（beforeunload）自动保存

8. **操作提示**：
   - 位置：进度条下方、卡片上方
   - 样式：纯文字，无背景无边框
   - 内容：三个简洁的文字提示
     - "← 左滑" + "认识"（绿色）
     - "↑ 上滑" + "模糊"（黄色）
     - "→ 右滑" + "不认识"（红色）
   - 字体：小号灰色文字 + 中号彩色粗体

9. **朗读功能**：
   - 位置：卡片正面中央
   - 样式：无背景无边框，只显示图标
   - 图标：Volume2，48px × 48px
   - 颜色：默认 text-gray-400，hover时 text-gray-600
   - 交互：hover时放大1.1倍
   - 点击朗读当前单词

**进度显示** [更新]：
- 进度条：Neo-Brutalism 风格，酸绿色背景 (#B4F416)
- 显示格式："{当前序号} / {总数}" 和百分比
- 导航栏文字："卡片背单词 • {范围名称} • {当前序号} / {总数}"
- 范围名称映射：
  - all → "全部单词"
  - unknown → "不认识的"
  - fuzzy → "模糊的"
  - known → "认识"
  - new → "未标注"
- 示例："卡片背单词 • 不认识的 • 50 / 200"

**实时统计色块** [详细设计]：
- 位置：顶部导航栏下方、进度条上方
- 布局：
  - **横向进度条**：显示各状态单词的分布比例，每个颜色段的宽度按实际百分比显示
  - **图例行**：显示各状态的图标、名称和数量
- 交互：
  - 点击进度条中的颜色段或图例按钮，弹出确认对话框
  - 确认后切换到对应范围
- 确认对话框内容：
  - 标题："切换学习范围"
  - 内容："确定要切换到'{新范围}'吗？\n当前进度将被保存，可以随时返回。"
  - 按钮：[取消] [确认切换]
- 示例布局：
  ```
  ┌─────────────────────────────────────────┐
  │ ████████░░░░░░░███████░░░░░███░░░░░░░░░░░ │ ← 横向进度条
  │  绿色60%   黄色20%  红色15%   灰色5%    │
  └─────────────────────────────────────────┘
  [✓ 认识(120)] [? 模糊(40)] [✗ 不认识(30)] [+ 未标注(10)] ← 图例
  ```
- 颜色映射：
  - 认识：绿色 (#B4F416)
  - 模糊：黄色 (#FACC15)
  - 不认识：红色 (#FF6B6B)
  - 未标注：灰色 (#9CA3AF)

**完成状态** [更新]：
- 当用户标记完当前范围的最后一个单词时，显示完成对话框（模态弹窗）：
  - 标题："🎉 太棒了！"
  - 文案："你已经完成了'{范围名称}'范围的所有单词学习！"
  - 副标题："接下来你想做什么？"
  - 三个操作按钮：
    1. **🔄 重新学习这个范围** - 重置到第一个单词（index=0），可重新开始学习当前范围
    2. **📚 选择其他范围** - 打开范围选择对话框，切换到不同的学习范围
    3. **返回首页** - 返回到应用首页
- 适用范围：所有范围（all/unknown/fuzzy/known/new）
- 对话框样式：Neo-Brutalism设计，带遮罩层，点击遮罩关闭对话框

**数据获取** [更新]：
- API: GET /api/words?bookId={bookId}&status={status}
- status 参数：all | unknown | fuzzy | known | new
- 返回结果按乱序排列（使用基于 bookId + status 的随机种子）
- 支持实时切换范围，无需刷新页面

**进度保存机制** [新增]：
- 存储键：`flashcard_progress_{bookId}_{scopeType}`
- 保存内容：
  - currentIndex: 当前学习到的索引位置
  - totalWords: 该范围的总单词数
  - lastStudyTime: 最后学习时间戳
  - scopeType: 范围类型（all/unknown/fuzzy/known/new）
- 保存时机：
  - 切换到下一个单词时
  - 标记单词状态时
  - 页面卸载时（beforeunload）
  - 页面隐藏时（visibilitychange）
  - 浏览器返回时

**响应式设计**：
- PC端：卡片最大宽度xl（max-w-xl）
- 移动端：卡片全宽
- iPad竖屏：优化间距，确保完整显示卡片
- 布局优化：减少垂直间距，卡片高度380px

**6.4 打字练习（肌肉训练）** [新增功能 2026-01-16]

**功能概述**：
打字练习（肌肉训练）是平台的第四大核心学习模式，通过键盘输入单词强化肌肉记忆，加深单词拼写记忆。与卡片背单词、听写模式、消消乐并列，作为一级功能在全站首页左侧导航展示。

**核心价值**：
- **输入即学习**：通过盲打单词强化肌肉记忆，加深单词拼写记忆
- **即时反馈**：实时显示正确（绿色）/错误（红色），三次错误自动重练
- **防挫败机制**：连续错误 4 次可跳过，保护学习积极性
- **智能复习**：自动记录错题，支持错题专项练习，无缝对接主项目错题本系统

**目标用户**：
- **备考用户**：需要强化单词拼写记忆的考试备考者（四六级、雅思、托福等）
- **进阶用户**：已掌握单词基本含义，希望提升拼写准确度的学习者
- **视觉学习者**：通过打字动作强化记忆的学习者

**功能入口**：
- **首页左侧导航**：`<Dumbbell className="w-5 h-5" />` + "肌肉训练"文字
- **词书详情页**：练习模式区域第4个卡片（橙色）
- **路由结构**：
  - `/typing` - 词书列表页
  - `/typing/[bookId]` - 范围选择页
  - `/typing/[bookId]/practice?scope=xxx` - 核心游戏页

**用户旅程**：

**场景1：首次使用打字练习**
```
用户 [点击首页左侧导航"肌肉训练"]
  ↓
用户 [浏览词书列表，选择"CET-4 核心词汇"]
  ↓
用户 [查看各范围单词数量，选择"未标注"]
  ↓
用户 [开始打字练习，看到单词 "abandon"]
  ↓
用户 [键盘输入 "a-b-a-n-d-o-n"]
  ↓
系统 [实时显示绿色高亮，播放TTS发音]
  ↓
系统 [自动跳转到下一个单词]
```

**场景2：错题专项练习**
```
用户 [练习过程中连续打错 "accommodation" 3 次]
  ↓
系统 [显示红色高亮，自动删除重练]
  ↓
用户 [第4次还是打错]
  ↓
系统 [显示"跳过"按钮]
  ↓
用户 [点击"跳过"]
  ↓
系统 [自动记录到错题本]
  ↓
用户 [练习结束后，点击"错题本"]
  ↓
用户 [选择"开始错题专项练习"]
  ↓
系统 [只练习之前出错的单词]
```

**场景3：循环强化记忆**
```
用户 [打开循环设置面板]
  ↓
用户 [选择"3次循环"]
  ↓
系统 [每个单词需要打对3次才跳转]
  ↓
用户 [完成第一个单词的3次循环]
  ↓
系统 [自动跳转到下一个单词]
```

**核心功能**：

**1. 实时打字检测**
- 显示当前单词和中文释义
- 用户键盘输入实时检测
- 正确输入：绿色高亮显示
- 错误输入：红色高亮显示
- 连续错误 3 次：自动删除整词，重新开始
- 单词完成（全部正确）：自动跳转下一个单词（300ms 延迟）

**2. TTS 发音系统**
- 使用浏览器原生 Web Speech API
- 支持 3 种发音方案：
  - 美式英语 (en-US)
  - 英式英语 (en-GB)
  - 自动选择（浏览器默认）
- 单词打对后自动触发发音
- 可调节音量（0-100）和语速（0.5-2.0）

**3. 循环练习**
- 支持 5 种循环模式：
  - 不循环（1次）
  - 3次循环
  - 5次循环
  - 9次循环
  - 无限循环
- 显示当前单词完成次数（如：2/3）
- 达到循环次数后自动跳转

**4. 错题本系统**
- 实时记录错误单词到 localStorage
- 显示错误次数统计
- 连续错误 4 次显示"跳过"按钮
- 点击"跳过"自动记录到错题本
- 支持错题专项练习模式

**5. 学习模式切换**
- 默写模式 (Ctrl+M)：隐藏释义，只显示单词
- 释义显示切换 (Ctrl+Shift+V)：显示/隐藏中文释义

**6. 数据持久化**
- 自动保存学习进度到 localStorage
- 页面刷新后自动恢复：
  - 词书 ID
  - 当前范围
  - 当前单词索引
  - 用户设置（发音、循环、显示等）
- 练习结束时同步到服务器数据库：
  - 错题同步到 mistakes 表
  - 进度更新到 word_progress 表
  - 记录保存到 learning_records 表

**重要功能（P1）**：

**统计面板**
- 显示本次练习统计：
  - 练习时长
  - 输入字符数
  - WPM（每分钟单词数）
  - 正确率
  - 完成单词数
- 可查看历史统计

**设置面板**

**发音设置**：
- 单词自动发音开关
- 发音音量（0-100）
- 发音语速（0.5-2.0）
- 发音方案选择（美音/英音/自动）

**音效设置**：
- 按键音开关
- 按键音音量
- 按键音类型（机械/静音/柔和）

**显示设置**：
- 外语字体大小（20-100px）
- 中文字体大小（12-50px）
- 深色模式切换

**循环设置**：
- 循环次数选择（1/3/5/9/无限）

**快捷键支持**：
- `Enter`：开始/继续练习
- `Ctrl + M`：切换默写模式
- `Ctrl + Shift + V`：切换释义显示
- `Ctrl + J`：显示快捷键提示
- `Esc`：跳过当前单词（连续错误 4 次后）
- `Backspace`：删除上一个字符

**数据同步功能**：

练习结束或页面卸载时，自动同步数据到服务器：

**1. 错题同步**
- 调用 API 批量上传 localStorage 中的错题记录
- 服务器执行 upsert 操作

**2. 进度更新**
- 更新每个练习过的单词的：
  - `status`：根据正确率计算（≥90%: known, ≥60%: fuzzy, <60%: unknown）
  - `practice_count`：练习次数
  - `correct_count`：正确次数
  - `mastery_level`：掌握程度（0-100）

**3. 学习记录**
- 记录：
  - `book_id`：词书 ID
  - `word_ids`：练习的单词 ID 列表
  - `practice_mode`：'typing'
  - `time_spent_seconds`：练习时长
  - `action`：'typing_practice'

**UI 设计**：

**词书列表页**
- 布局：网格布局（3列），响应式
- 卡片样式：圆角、阴影、hover效果
- 显示内容：词书封面、标题、描述、单词总数

**范围选择页**
- 布局：2列网格（桌面端），1列（移动端）
- 卡片样式：
  - 未标注: 灰色背景
  - 已认识: 绿色背景
  - 模糊: 黄色背景
  - 不认识: 红色背景
  - 全部单词: 蓝色背景
- 每个选项显示范围名称和单词数量

**游戏界面**
- 单词显示区：大字体（48-72px）
- 中文释义区：中字体（16-24px）
- 输入框：隐藏，通过键盘事件监听
- 实时反馈：绿色/红色高亮
- 循环计数显示：如 2/3
- 右上角控制按钮：11个功能区

**成功指标**：

**功能指标**：
- 打字练习功能可用性：100%
- 数据同步成功率：> 95%
- 页面加载速度：< 2秒
- TTS 发音响应延迟：< 100ms

**用户指标**：
- 日活跃用户（DAU）：预期 > 100（上线1个月）
- 平均练习时长：预期 > 10分钟/次
- 用户留存率：预期 > 30%（7日留存）
- 错题练习使用率：预期 > 20%

**技术指标**：
- 页面 FCP：< 1.5s
- 页面 TTI：< 3s
- 错误率：< 0.1%

**风险与挑战**：

**技术风险**：
- TTS 兼容性问题：提供降级方案，检测浏览器支持
- 数据同步失败：localStorage 缓存 + 自动重试
- 性能问题（大量单词）：分页加载 + 虚拟滚动

**产品风险**：
- 用户接受度低：A/B 测试，收集用户反馈
- 与现有功能冲突：明确功能定位，避免重复

**后续迭代规划**：

**V1.1（上线后1个月）**：
- 添加章节选择功能
- 添加主题/场景筛选
- 优化错题本专项练习
- 添加学习报告（周报/月报）

**V1.2（上线后2-3个月）**：
- 添加 multiplayer 模式（多人竞技）
- 添加成就系统
- 添加学习提醒
- 支持自定义词库

**V2.0（长期规划）**：
- AI 智能推荐练习单词
- 自适应难度调整
- 跨设备同步（手机/平板/电脑）
- 离线应用支持

7. [L4] 自定义词库管理

**7.1 功能概述**

自定义词库管理功能为用户提供了创建个性化词库并高效管理单词的能力。用户可以：
- 创建自定义词库
- 通过智能导入批量添加单词（调用第三方API自动获取释义、音标等）
- 管理词库的章节结构
- 在表格视图中编辑单词数据
- 批量操作单词（删除、移动章节）

**7.2 创建自定义词库**

**功能描述**：
用户可以创建自己的单词书，用于收集特定主题的单词。

**用户场景**：
1. 用户从首页点击"新建词库"
2. 进入创建页面
3. 输入词库名称（必填）和描述（可选）
4. 点击"创建"按钮
5. 系统创建空词库
6. 自动跳转到智能导入页面

**表单字段**：
- 词库名称：必填，1-50个字符
- 描述：可选，最多200个字符

**7.3 智能导入单词**

**功能描述**：
用户输入单词列表（每行一个），系统自动调用第三方API获取完整的单词数据，包括音标、释义、词性、搭配、例句等8个字段。

**用户场景**：
1. 用户创建词库后，自动进入智能导入页面
2. 用户输入单词列表，每行一个单词
   示例：
   ```
   apple
   banana
   orange
   agenda
   compromise
   ```
3. 用户点击"智能识别并导入"按钮
4. 系统调用第三方API获取每个单词的完整数据
5. 系统自动创建"默认章节"（如果词库还没有章节）
6. 系统将所有单词归入"默认章节"
7. 导入完成后，显示"已导入 X 个单词"的成功提示
8. 用户点击"前往词库"按钮，进入词库详情页（学习模式）

**配额限制**：
- 每个用户每天最多智能导入500个单词
- 超出配额时，显示提示："今日额度已用完，请手动输入"
- 配额每天0点重置

**错误处理**：
- 如果某个单词在第三方API中找不到，跳过该词，显示警告："部分单词无法识别"
- 如果所有单词都无法识别，显示错误："未找到任何单词，请检查拼写"

**7.4 视图模式切换**

**功能描述**：
词库详情页提供两种视图模式：
- **学习模式**：用于浏览单词、标记学习状态、进入练习（卡片视图）
- **编辑模式**：用于管理单词数据、调整章节归属、批量操作（表格视图）

**用户场景**：

**场景A：学习单词**
1. 用户打开词库详情页
2. 默认进入"学习模式"
3. 看到单词卡片列表
4. 可以标记单词状态（认识/模糊/不认识）
5. 可以点击发音、隐藏中文

**场景B：管理单词**
1. 用户点击顶部的"📊 编辑模式"按钮
2. 页面切换到表格视图
3. 看到所有单词的详细数据（章节、单词、音标、词性、中文释义）
4. 可以直接在表格里修改单词、音标、释义
5. 可以批量选择单词进行删除、移动章节
6. 完成后切换回"学习模式"

**模式切换按钮位置**：
- 词库详情页顶部，筛选栏下方
- 两个按钮并排显示：
  - **📖 学习模式**：默认选中
  - **📊 编辑模式**：未选中时为灰色，选中时为蓝色

**切换规则**：
- 点击"编辑模式"按钮：隐藏单词卡片列表，显示表格编辑视图
- 点击"学习模式"按钮：显示单词卡片列表，隐藏表格编辑视图
- 切换时保留当前筛选条件（主题、场景、状态、章节）

**7.5 表格编辑视图**

**功能描述**：
提供一个类似Excel的表格视图，让用户可以一次看到多个单词的多个字段，并直接在表格里编辑。

**表格列定义**：

| 列名 | 说明 | 可编辑 |
|------|------|--------|
| 选择 | 复选框 | ❌ |
| 章节 | 单词所属章节 | ✅ 下拉选择 |
| 单词 | 英文单词 | ✅ 文本输入 |
| 音标 | 国际音标 | ✅ 文本输入 |
| 词性 | 单词词性 | ✅ 下拉选择 |
| 中文释义 | 中文释义 | ✅ 文本输入 |
| 操作 | 删除按钮 | ❌ |

**Inline 编辑交互**：

**编辑流程**：
1. 用户点击表格中的输入框
2. 输入框获得焦点，边框变蓝色
3. 用户修改内容
4. 用户执行以下操作之一：
   - **按回车**：保存当前字段，焦点跳到下一行
   - **按Tab**：跳到下一个可编辑字段
   - **点击外部**：失去焦点，自动保存
   - **按Esc**：取消修改，恢复原值
5. 系统显示视觉反馈：
   - 保存成功：输入框绿色边框闪烁（200毫秒）
   - 保存失败：输入框红色边框，显示错误提示

**重要说明**：
- 表格编辑**不调用第三方API**，纯手动保存用户输入的内容
- 表格编辑用于**修正错误**或**补充信息**

**字段验证**：
- **必填字段**：单词、中文释义不能为空
- **可选字段**：音标、词性可以为空
- **错误提示**：必填字段为空时，红色边框 + "不能为空"

**章节下拉选择**：
- 选项：当前词库的所有章节
- 格式：章节名称
- 选择后自动保存

**词性下拉选择**：
- 选项：n名词、v动词、adj形容词、adv副词、pron代词、prep介词、conj连词
- 选择后自动保存

**分页设计**：
- 位置：表格底部
- 显示："上一页" | "第 X / Y 页" | "下一页"
- 显示总数："共 Z 个单词"
- 默认每页50个单词
- 可选：10词/页、20词/页、50词/页、100词/页

**7.6 批量操作**

**功能描述**：
提供批量操作功能，让用户可以批量选择单词并进行批量删除或批量移动章节。

**批量选择**：
1. **单选**：点击每行的复选框
2. **全选**：点击表头的复选框，选中当前页的所有单词
3. **取消全选**：再次点击表头的复选框

**选中状态**：
- 选中的行：淡蓝色背景
- 底部显示批量操作栏："已选 X 个单词"
- 切换筛选条件时，清空选中状态

**批量删除**：

**用户场景**：
1. 用户在表格视图中筛选出状态为"认识"的单词
2. 共50个单词
3. 用户点击表头的复选框，全选这50个单词
4. 底部显示："已选 50 个单词"
5. 用户点击"批量删除"按钮
6. **第一次确认**：弹窗"确定批量删除？"，显示即将删除50个单词
7. 用户点击"确认"
8. **第二次确认**：弹窗"数据不可恢复！"
9. 用户点击"确认删除"
10. 系统删除这50个单词
11. 从表格中移除这50行（淡出动画）
12. 显示成功提示："已删除 50 个单词"

**批量移动章节**：

**用户场景**：
1. 用户在表格视图中选中100个单词
2. 底部显示："已选 100 个单词"
3. 用户点击"移动到章节..."按钮
4. 打开章节选择对话框
5. 对话框显示："已选 100 个单词，请选择目标章节："
6. 用户选择"第2章 进阶词汇"
7. 点击"确认移动"
8. 系统将这100个单词移动到"第2章 进阶词汇"
9. 表格中的"章节"列实时更新为"第2章 进阶词汇"
10. 显示成功提示："已移动 100 个单词到「第2章 进阶词汇」"

**7.7 批量添加单词（表格视图）**

**功能描述**：
在表格编辑视图顶部提供"批量添加单词"按钮，让用户可以继续添加新单词。

**入口**：
- 表格编辑视图顶部："批量添加单词"按钮（绿色按钮）

**用户场景**：
1. 用户点击"批量添加单词"按钮
2. 打开批量添加对话框
3. 选择章节（下拉列表）
4. 输入单词列表（每行一个）
5. 点击"智能识别并导入"
6. 导入成功后：
   - 关闭对话框
   - 刷新表格数据
   - 显示成功提示："已导入 X 个单词"

**功能说明**：
- 此功能**完全复用现有的智能导入功能**
- 调用相同的第三方API（有道词典）
- 获取完整的8个字段（单词、音标、中英文释义、词性、搭配、例句）
- 共享每日500词的配额限制
- **唯一区别**：增加了章节选择功能（现有功能：自动归入"默认章节"）

**7.8 章节管理**

**功能描述**：
提供完整的章节管理功能，让用户可以创建新章节、编辑章节（重命名、调整排序）、删除章节。

**章节管理入口**：
- 词库详情页顶部，筛选栏右侧
- 图标：文件夹 📁
- 文字："管理章节"
- 显示条件：只有自定义词库显示，只有词库创建者可以看到

**新建章节**：

**用户场景**：
1. 用户打开章节管理对话框
2. 点击"+ 新建章节"
3. 输入章节名称："第1章 基础词汇"
4. 选择排序位置："作为第1章"
5. 点击"创建"
6. 章节列表显示"第1章 基础词汇"

**字段说明**：
- **章节名称**：必填，1-50个字符，不能与现有章节重复
- **排序位置**：必选，选项包括"作为第1章"、"在第1章之后"、"在第2章之后"等

**编辑章节**：

**方式1：双击重命名（快捷）**
1. 用户打开章节管理对话框
2. 双击章节名称"第1章 基础词汇"
3. 章节名称变为输入框
4. 用户修改为"第一章 入门词汇"
5. 按回车保存

**方式2：编辑按钮重命名（完整）**
1. 用户打开章节管理对话框
2. 点击章节的"编辑"按钮
3. 打开编辑对话框
4. 同时修改章节名称和排序位置
5. 点击"保存"

**删除章节**：

**用户场景**：
1. 用户打开章节管理对话框
2. 点击"第3章 高级词汇"的"删除"按钮
3. **第一次确认**：弹窗"确定删除此章节？"，显示该章节有30个单词
4. 用户点击"确认"
5. **第二次确认**：弹窗"章节中的单词将移到默认章节，确定继续？"
6. 用户点击"确认删除"
7. 章节删除
8. 该章节的30个单词自动移到"默认章节"

**限制**：
- 默认章节不能删除
- 删除操作不可撤销

**7.9 数据流转设计**

**核心原则**：
1. **单一数据源**：所有功能都操作同一个单词数据
2. **实时同步**：编辑模式的修改立即反映到学习模式
3. **统计一致性**：单词数量、章节统计保持准确

**三种数据修改方式对比**：

| 方式 | 调用第三方API | 数据来源 | 使用场景 |
|------|--------------|---------|---------|
| 现有智能导入 | ✅ 调用有道API | 自动获取8个字段 | 创建词库时批量导入新单词 |
| 表格Inline编辑 | ❌ 不调用 | 用户手动输入 | 修正API返回的错误数据 |
| 批量添加单词 | ✅ 调用有道API | 自动获取8个字段 | 编辑模式下继续添加新单词 |

**数据流转示例**：

**流程1：创建词库并导入单词**
```
用户创建词库 → 输入单词列表
  ↓
系统调用第三方API获取完整数据
  ↓
自动创建"默认章节"
  ↓
所有单词归入"默认章节"
  ↓
跳转到词库详情页（学习模式）
```

**流程2：编辑模式修正单词**
```
用户切换到"编辑模式"
  ↓
从数据库读取单词数据
  ↓
渲染表格视图
  ↓
用户修改某个单词的音标
  ↓
失去焦点时，保存到数据库
  ↓
显示保存成功提示（绿色边框闪烁）
```

**流程3：管理章节并分配单词**
```
用户点击"管理章节"
  ↓
创建"第1章"和"第2章"
  ↓
切换到"编辑模式"
  ↓
选中100个单词
  ↓
批量移动到"第1章"
  ↓
表格中的"章节"列更新为"第1章"
```

**流程4：数据一致性**
```
用户在"编辑模式"修正了某个单词的音标
  ↓
切换回"学习模式"
  ↓
单词卡片显示修正后的音标
  ↓
用户点击"播放"按钮
  ↓
播放修正后的音标
```

**7.10 删除词库**

**功能描述**：
用户可以删除自己创建的自定义词库。

**删除流程**：
需执行 **3次确认** 流程：
1. 点击删除
2. 弹窗"确定删除？"
3. 弹窗"数据不可恢复！"
4. 弹窗"最后确认"
5. 执行删除

**空状态**：
显示插画 + 文案"打造你的专属单词书" + 按钮"立即创建"
8. [L5] 学习日历与错题本
**学习日历**：
- 数据源：`word_progress` 表的 `updated_at` 字段 [修订]
- 统计范围：所有状态的单词（known, fuzzy, unknown, new）
- 核心逻辑：记录用户每日学习活动，展示学习轨迹
- 功能特性：
  - GitHub 风格热力图：过去 365 天的学习记录
  - 5 级颜色梯度：根据每日学习数量动态着色（浅绿 → 深绿）
  - 统计指标：
    - **累计单词**：总共学习过的单词数
    - **连续学习**：连续有学习记录的天数
    - **今日学习**：今天有学习活动的单词数（基于 `updated_at`）

**今日新增词计算** [新增定义]：
- **统计范围**：今天有学习活动的新词（`updated_at >= 今天0点` 且 `status = 'new'`）
- **计算逻辑**：
  ```sql
  SELECT COUNT(*)
  FROM word_progress
  WHERE user_id = {user_id}
    AND updated_at >= {今天0点}
    AND status = 'new'
  ```
- **定义说明**：
  - 只统计 `status = 'new'`（新遇到，尚未标记）的单词
  - 使用 `updated_at` 而不是 `created_at`（反映今天的学习活动）
  - 如果用户今天修改了旧词的状态（new → known），不计入"今日新增"
- **示例**：
  - 昨天遇到单词A，今天标记为"认识" → 不计入今日新增 ✅
  - 今天遇到单词B，标记为"new" → 计入今日新增 ✅
  - 今天遇到单词C，标记为"fuzzy" → 不计入今日新增（不是new）✅
- 设计理念：
  - 正面激励：学习越多，格子越绿
  - 活动记录：反映真实学习活动，不限于"错词"
  - 类似 GitHub 贡献图：展示学习坚持度

**错题本**：
数据源：仅显示标记为"不认识"或"模糊"的单词（不包括"未标注"状态）。
设计理念：错题本聚焦于用户真正遇到过问题的单词，而不是所有未标记的单词。
空状态 [新增]：
图标：奖杯插画。
文案："太棒了！你消灭了所有错题。"
按钮："去背新单词"。
9. 后台数据与资源管理 (Backend & Data) [新增章节]
9.1 官方词库导入规范 [修订 2026-01-08]
官方词库采用 Excel 离线导入 模式，由管理员在后台导入。

**Excel 模板字段标准**:

| 字段名 | 中文名 | 必填 | 说明 |
|--------|--------|------|------|
| Chapter | 章节名称 | ❌ | **可选**，为空则归入"默认章节" |
| Word | 单词 | ✅ | 英文原文 |
| Phonetic | 音标 | ❌ | 国际音标 |
| Definition | 中文释义 | ✅ | 中文释义 |
| Definition EN | 英文释义 | ❌ | 英文释义 |
| Part of Speech | 词性 | ❌ | n./v./adj./adv.等 |
| Collocation | 搭配（中文） | ❌ | 搭配 |
| Collocation EN | 搭配（英文） | ❌ | 搭配 |
| Example Sentence | 例句（中文） | ❌ | 例句 |
| Example EN | 例句（英文） | ❌ | 例句 |

**重要说明**:
- **Chapter字段可选**：支持有章节模式（教材）和无章节模式（单词手册）
- **移除字段**：Audio URL、Image URL、Difficulty Score、Frequency Rank（前台不需要或未使用）
- **前台展示**：自动检测章节数量，有章节按章节展示，无章节直接展示单词列表
- **数据库**：words.chapter_id 允许为 NULL（表示无章节或默认章节）
9.2 用户偏好设置 [新增章节]
为提供个性化学习体验，系统支持用户偏好设置的持久化存储。

9.2.1 数据库设计
表名：`user_book_preferences`

字段说明：
- `id`: UUID，主键
- `user_id`: UUID，用户ID（外键关联 auth.users）
- `book_id`: TEXT，词书ID（支持UUID或slug格式）
- `hide_chinese`: BOOLEAN，是否隐藏中文释义（默认false）
- `created_at`: TIMESTAMPTZ，创建时间
- `updated_at`: TIMESTAMPTZ，更新时间（自动更新触发器）

索引设计：
- 复合唯一索引：`(user_id, book_id)` - 确保每个用户对每本书只有一条偏好记录
- 单列索引：`user_id` 和 `book_id` - 提升查询性能

设计决策：
- `book_id` 使用 TEXT 而非 UUID：支持多种ID格式（UUID、slug、自定义字符串），提高灵活性
- 级联删除：用户删除时自动清理其偏好设置

9.2.2 API 接口设计

**GET /api/user-preferences?book_id=xxx**
- 功能：获取用户对某本书的偏好设置
- 认证：必须登录（Supabase Auth）
- 响应：
  ```json
  {
    "success": true,
    "data": {
      "hide_chinese": false
    }
  }
  ```
- 默认值：若无记录，返回 `hide_chinese: false`

**POST /api/user-preferences**
- 功能：保存/更新用户偏好设置
- 认证：必须登录
- 请求体：
  ```json
  {
    "book_id": "demo-book-1",
    "hide_chinese": true
  }
  ```
- 响应：
  ```json
  {
    "success": true,
    "data": {
      "id": "uuid",
      "user_id": "user-uuid",
      "book_id": "demo-book-1",
      "hide_chinese": true,
      "created_at": "2026-01-06T...",
      "updated_at": "2026-01-06T..."
    }
  }
  ```
- 操作：使用 UPSERT（插入或更新），确保幂等性

9.2.3 前端实现逻辑

**组件层级：**
```
BookDetailPage (Server Component)
  └── BookDetailPageClient (Client Component)
      ├── GlobalHideButton (全局按钮)
      └── WordList
          └── WordCard[] (所有卡片)
```

**状态管理：**
1. GlobalHideButton 组件加载时：
   - 调用 GET `/api/user-preferences?book_id=xxx` 获取当前设置
   - 更新按钮状态（显示"隐藏中文"或"显示中文"）

2. 用户点击全局按钮时：
   - 调用 POST `/api/user-preferences` 保存设置
   - 更新本地状态
   - 通过 `onHideChange` 回调通知父组件
   - 父组件将 `globalHideChinese` 传递给 WordList 和所有 WordCard

3. WordCard 组件：
   - 接收 `globalHideChinese` 属性
   - 计算最终显示状态：`shouldShowChinese = !globalHideChinese && showDefinition`
   - 全局设置优先于本地按钮

**优先级逻辑：**
- 全局设置 > 本地设置
- 若 `globalHideChinese = true`，所有卡片强制隐藏中文，本地按钮失效
- 若 `globalHideChinese = false`，本地按钮可单独控制每个卡片

9.2.4 用户体验优化

**视觉反馈：**
- 全局隐藏激活时：按钮显示紫色背景 + "全局生效"徽章
- 保存中状态：按钮显示"保存中..."并禁用
- 错误提示：401时弹出提示"请先登录"

**数据隔离：**
- 用户隔离：不同用户的设置互不影响
- 书籍隔离：同一用户对不同书籍的设置独立存储

**性能优化：**
- 使用 UPSERT 避免重复查询
- 索引优化查询性能
- 前端乐观更新，提升响应速度

10. 非功能性需求 (NFR) [新增章节]
10.1 浏览器兼容性
核心依赖：Web Speech API (TTS)。
兼容策略：
优先支持 Chrome, Safari, Edge。
若浏览器不支持 (如 IE)，隐藏发音按钮，并顶部提示"建议使用 Chrome 或 Safari 浏览器以获得完整体验"。

10.2 性能要求
列表渲染：当单词列表 (L2) 单页数据超过 100条 时，前端需采用 虚拟列表 (Virtual Scroll) 技术，确保滚动流畅。
加载速度：L1 大厅骨架屏展示时间不应超过 1.5秒。

10.3 UI/UX细节补充
搜索无结果：
显示放大镜插画。
文案："未找到相关内容"。
操作：提供"意见反馈"链接。
全局搜索：规划于 v3.3 版本上线，本期暂不开发。

10.4 权限控制 [新增]
**功能权限验证**：
- 系统实时检查用户的功能权限
- 无权限时显示友好提示并引导联系管理员
- 权限过期后自动禁用相关功能

**单词书权限过滤**：
- 单词书列表仅显示用户有权限访问的单词书
- 练习模式仅使用用户有权限的单词
- 权限为"全部"时显示所有单词书

**权限过期处理**：
- 登录时检查权限是否过期
- 过期用户在所有页面显示提示横幅
- 提供联系管理员续费的入口

**权限提示示例**：
```
┌─────────────────────────────────┐
│ ⚠️ 您的权限已过期                │
│                                  │
│ 您的"1年基础版"套餐已于 2026-01-07 过期│
│                                  │
│ 如需继续使用，请联系管理员续费   │
│ 邮箱：support@xiaoyu.com          │
│                                  │
│          [我知道了]              │
└─────────────────────────────────┘
```
---

## 功能实现记录 (Development Log)

### 2026-01-07 - 个人学习区功能完成 ✅

#### 实现模块
1. **首页个人学习区** (`src/app/page.tsx`)
   - 继续学习：显示上次学习的词书及进度，点击进入词书详情页
   - 错题本：显示待复习单词数量
   - 学习日历：显示今日新增单词数
   - 新建自定义词库：功能开发中占位页面

2. **错题本页面** (`/mistakes`) ✅
   - 数据源：`status IN ('unknown', 'fuzzy')` **[修订]**
   - **核心逻辑说明**：
     - `unknown`：明确标记为不认识的单词
     - `fuzzy`：标记为模糊的单词
     - **不包括 `new`（未标注）状态**
   - 设计决策 [2026-01-07 修订]：
     - 原PRD设计包括 `new` 状态，目的是提醒用户学习所有未掌握的单词
     - 但根据实际需求，错题本应聚焦于"真正的错题"（用户已经标记过的单词）
     - `new` 状态的单词数量通常很多，会稀释错题本的焦点
     - 用户可以通过"练习模式"来学习未标注的单词，错题本用于复习重点难点
   - 显示：单词卡片 + 所属词书标签 + 状态标记功能
   - 空状态：奖杯插画 + "太棒了！你消灭了所有错题。"

3. **学习日历页面** (`/calendar`) ✅
   - GitHub风格热力图（过去365天）
   - 统计卡片：累计单词、连续学习天数、今日学习
   - 5级颜色梯度显示学习密度
   - 数据源：所有学习记录（不限状态）

4. **继续学习** (首页卡片) ✅
   - 显示上次学习的词书名称和进度百分比
   - 点击直接跳转到词书详情页 (`/library/${bookId}`)
   - 用户从详情页选择练习模式（听写、卡片、消消乐）
   - 无学习记录时显示占位状态

5. **自定义词库创建** (`/library/new`) 🚧
   - 占位页面，功能开发中

#### 技术实现
- 使用 Server Components 和 Server Actions
- Supabase 实时数据查询
- Claymorphism 设计风格
- 完全响应式设计

#### 相关文件
- `src/app/page.tsx` - 首页（个人学习区）
- `src/app/mistakes/page.tsx` - 错题本页面
- `src/app/calendar/page.tsx` - 生词日历页面
- `src/app/study/page.tsx` - 简化重定向页面（重定向到首页）
- `src/app/library/new/page.tsx` - 自定义词库创建
- `src/components/VocabularyCalendarClient.tsx` - 日历热力图组件

#### 错题本与练习模式的关系
- **练习模式筛选**：`status != 'new'` （只练习标记过的单词）
- **错题本筛选**：`status IN ('unknown', 'fuzzy')` （只显示错题，不包括未标注单词）**[修订]**

---

### 2026-01-07 - 错题本交互优化完成 ✅

#### 问题诊断与修复
1. **RLS 策略问题修复**
   - 问题：`vocabulary_calendar` 表的 RLS 策略阻止了单词进度保存
   - 错误：`new row violates row-level security policy for table "vocabulary_calendar"`
   - 解决：为 `vocabulary_calendar` 表添加正确的 RLS 策略（INSERT/UPDATE/SELECT/DELETE）
   - 相关文件：`supabase/migrations/FIX_VOCABULARY_CALENDAR_RLS_COMPLETE.sql`

2. **历史数据同步**
   - 问题：修复前标记的单词只保存在 localStorage，未同步到数据库
   - 解决：创建浏览器控制台同步脚本，批量上传历史数据
   - 相关文件：`SYNC_LOCALSTORAGE_TO_DB.js`

#### 交互体验优化
**错题本页面升级** (`src/app/mistakes/page.tsx` + `src/components/MistakesClient.tsx`) ✅

1. **架构重构**：
   - 从纯服务器组件改为混合架构
   - 服务器组件：数据获取（`page.tsx`）
   - 客户端组件：交互逻辑（`MistakesClient.tsx`）

2. **即时删除功能**：
   - 点击"认识"按钮后，单词立即从列表中消失
   - 无需刷新页面
   - 乐观更新 + 后台保存

3. **优雅动画效果**：
   - 删除动画：向右滑出（`translate-x-full`）
   - 淡出效果（`opacity-0`）
   - 缩小效果（`scale-95`）
   - 动画时长：400ms（`duration-300`）
   - 缓动函数：`ease-out`

4. **用户体验提升**：
   - 状态管理：使用 `useState` 管理单词列表和删除状态
   - 防止重复操作：`removingIds` Set 跟踪正在删除的单词
   - 平滑过渡：CSS transition 实现流畅动画

#### 技术实现细节
```typescript
// 客户端状态管理
const [words, setWords] = useState<MistakeWord[]>(initialWords)
const [removingIds, setRemovingIds] = useState<Set<string>>(new Set())

// 删除流程
if (status === 'known') {
  // 1. 标记为删除中（触发动画）
  setRemovingIds(prev => new Set([...prev, wordId]))

  // 2. 等待动画完成
  await new Promise(resolve => setTimeout(resolve, 400))

  // 3. 从列表中移除
  setWords(prev => prev.filter(w => w.id !== wordId))

  // 4. 后台保存到数据库
  await fetch('/api/word-progress', { ... })
}
```

#### CSS 动画类
```css
/* 正常状态 */
.opacity-100.translate-x-0.scale-100 {
  opacity: 1;
  transform: translateX(0) scale(1);
}

/* 删除状态 */
.opacity-0.translate-x-full.scale-95 {
  opacity: 0;
  transform: translateX(100%) scale(0.95);
}

/* 过渡效果 */
.transition-all.duration-300.ease-out {
  transition: all 300ms ease-out;
}
```

#### 相关文件
- `src/app/mistakes/page.tsx` - 服务器组件（数据获取）
- `src/components/MistakesClient.tsx` - 客户端组件（交互逻辑）**[新增]**
- `supabase/migrations/FIX_VOCABULARY_CALENDAR_RLS_COMPLETE.sql` - RLS 策略修复
- `SYNC_LOCALSTORAGE_TO_DB.js` - 历史数据同步脚本

#### 测试验证
- ✅ 点击"认识"后单词立即消失
- ✅ 动画流畅，无卡顿
- ✅ 数据正确保存到数据库
- ✅ 刷新页面后状态保持一致
- ✅ 错题本数量实时更新

#### 未来优化方向
- 添加撤销功能（误点"认识"后可恢复）
- 批量操作（全选标记为"认识"）
- 筛选功能（按词书、状态筛选）

---

### 2026-01-07 - "继续学习"功能简化 ✅

#### 问题背景
用户反馈"智能继续学习"功能存在问题：
- 点击后出现404错误
- 跳转到空白页面
- 首页控制台报错：`NEXT_REDIRECT` error
- 用户体验不流畅

#### 问题原因分析
原设计采用多级重定向链：
1. 首页 → `/study` (重定向页面)
2. `/study` → `/study/${bookId}` (404 - 该路由不存在)
3. 期望 → `/study/${bookId}/practice` (练习页面选择页)

问题：
- `/study/${bookId}` 路由不存在，导致404
- 复杂的重定向逻辑导致 NEXT_REDIRECT 错误
- 用户需要经过多次跳转才能开始学习

#### 解决方案：简化架构
完全移除中间重定向层，直接跳转到词书详情页：

**修改前：**
```
首页"智能继续学习" → /study → (重定向逻辑) → /study/${bookId}/practice → 404错误
```

**修改后：**
```
首页"继续学习" → /library/${bookId} → 词书详情页（已有练习模式卡片）
```

#### 技术实现

**1. 首页 (`src/app/page.tsx`)**
- 获取用户最近学习的词书及进度
- 显示词书名称和进度百分比
- 链接改为直接跳转：`href={`/library/${lastStudyBook.id}`}`
- 无学习记录时显示占位状态

```typescript
// 计算学习进度
const { data: bookProgress } = await supabase
  .from('word_progress')
  .select('status')
  .eq('user_id', user.id)
  .eq('book_id', lastBookId)

const learnedCount = bookProgress.filter(p => p.status !== 'new').length
const progress = bookData.total_words > 0
  ? Math.round((learnedCount / bookData.total_words) * 100)
  : 0

lastStudyBook = {
  id: bookData.id,
  title: bookData.title,
  progress
}
```

**2. 学习页面 (`src/app/study/page.tsx`)**
- 简化为直接重定向到首页
- 移除所有复杂逻辑和数据库查询
- 用户从首页的"继续学习"卡片进入学习

```typescript
export default async function StudyPage() {
  redirect('/') // 简单直接
}
```

#### 用户体验优化
1. **减少跳转次数**：从3次减少到1次
2. **更清晰**：用户直接进入词书详情页，可以看到所有可用练习模式
3. **更灵活**：用户可以选择听写、卡片背单词、消消乐等不同练习模式
4. **更稳定**：消除重定向链和 NEXT_REDIRECT 错误

#### 相关文件修改
- `src/app/page.tsx` - 清理重复代码，修正"继续学习"链接
- `src/app/study/page.tsx` - 简化为直接重定向
- `PRD.md` - 更新功能描述，移除"智能"前缀

#### 测试验证
- ✅ 首页加载无错误
- ✅ 点击"继续学习"直接进入词书详情页
- ✅ 进度百分比正确显示
- ✅ 无学习记录时占位状态正常
- ✅ 消除 NEXT_REDIRECT 控制台错误

---

### 2026-01-13 - 学习状态恢复对话框功能 ✅

#### 功能概述
在学习状态自动保存的基础上，增加用户选择对话框功能。当用户返回词库时，系统会询问是否"继续学习"还是"从头开始"，给用户更多控制权。

#### 实现场景

**对话框显示条件**：
- 同一个词库（bookId相同）
- 同一个模式（word-list）
- 24小时内的学习进度
- 页码 > 1（避免第1页的打扰）
- 不是通过URL参数访问的（URL参数优先级更高）

**对话框设计**：
```
┌─────────────────────────────────┐
│  🔄 继续上次的学习进度？          │
│  上次学习到第 3 页               │
│                                  │
│  上次的学习设置：                 │
│  • 筛选：未标注                   │
│  • 主题：购物                     │
│  • 章节：第1章                   │
│                                  │
│  [从头开始]    [继续学习]         │
└─────────────────────────────────┘
```

**用户选择**：

1. **从头开始**：
   - 不恢复任何状态
   - 使用默认的第1页
   - 使用默认的筛选条件（全部）
   - 对话框关闭

2. **继续学习**：
   - 恢复保存的页码（比如第3页）
   - 恢复保存的筛选条件（主题、场景、状态、章节）
   - 自动加载对应页的数据
   - 对话框关闭

#### 技术实现

**1. 新增状态变量** (`BookDetailPageClient.tsx`)
```typescript
const [showResumeDialog, setShowResumeDialog] = useState(false)
const [resumeState, setResumeState] = useState<any>(null)
const [hasRestoredState, setHasRestoredState] = useState(false)
const isRestoringRef = useRef(false)
```

**2. 检查恢复状态** (useEffect, 延迟500ms)
```typescript
useEffect(() => {
  // 如果URL参数已经恢复了状态，就不再检查保存的状态
  if (searchParams.has('theme') || searchParams.has('page')) {
    return
  }

  const checkResumeState = async () => {
    const savedState = await getResumeState(book.id, 'word-list')

    if (savedState && savedState.context) {
      // 检查状态是否是最近的（24小时内）
      const hoursSince = (Date.now() - savedState.updatedAt) / (1000 * 60 * 60)

      if (hoursSince < 24 && savedState.context.page && savedState.context.page > 1) {
        setResumeState(savedState)
        setShowResumeDialog(true)
      }
    }
  }

  // 延迟检查，避免与URL参数恢复冲突
  setTimeout(() => {
    checkResumeState()
  }, 500)
}, [book.id, searchParams])
```

**3. 恢复状态处理函数**
```typescript
const handleResume = () => {
  if (!resumeState || !resumeState.context) return

  isRestoringRef.current = true

  // 恢复筛选条件
  if (resumeState.context.filters) {
    const { theme, scenario, status, chapter } = resumeState.context.filters

    if (theme && theme !== 'all') setSelectedTheme(theme)
    if (scenario && scenario !== 'all') setSelectedScene(scenario)
    if (status && status !== 'all') setStatusFilter(status as StatusFilter)
    if (chapter && chapter !== 'all') setSelectedChapter(chapter)
  }

  // 恢复页码
  if (resumeState.context.page) {
    setCurrentPage(resumeState.context.page)
  }

  setShowResumeDialog(false)

  // 延迟标记恢复完成
  setTimeout(() => {
    isRestoringRef.current = false
    setHasRestoredState(true)
  }, 200)
}

const handleStartFresh = () => {
  setShowResumeDialog(false)
  setHasRestoredState(true)
  // 不恢复状态，使用默认的第1页
}
```

**4. 对话框UI**
```tsx
{showResumeDialog && resumeState && (
  <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/50 backdrop-blur-sm">
    <div className="bg-white rounded-2xl shadow-xl max-w-md w-full p-6">
      {/* 图标和标题 */}
      <div className="flex items-center gap-3 mb-4">
        <div className="w-12 h-12 rounded-full bg-gradient-to-r from-indigo-500 to-purple-600 flex items-center justify-center">
          <RotateCcw className="w-6 h-6 text-white" />
        </div>
        <div>
          <h3>继续上次的学习进度？</h3>
          <p>上次学习到第 {resumeState.context?.page} 页</p>
        </div>
      </div>

      {/* 显示筛选条件 */}
      {resumeState.context?.filters && (
        <div className="bg-slate-50 rounded-xl p-3 mb-4">
          <div className="font-semibold">上次的学习设置：</div>
          {filters.map(f => <div key={f.key}>• {f.label}: {f.value}</div>)}
        </div>
      )}

      {/* 按钮 */}
      <div className="flex gap-3">
        <button onClick={handleStartFresh}>从头开始</button>
        <button onClick={handleResume}>继续学习</button>
      </div>
    </div>
  </div>
)}
```

#### 优先级规则

**优先级从高到低**：
1. **URL参数**（最高优先级）
   - 如果URL带有 `?theme=xxx&page=3`
   - 直接从URL恢复，不显示对话框

2. **用户选择对话框**（次优先级）
   - 检查数据库中是否有保存的状态
   - 如果有且符合条件，显示对话框
   - 用户选择后执行对应的操作

3. **默认状态**（最低优先级）
   - 第1页
   - 全部筛选条件

#### 用户体验优化

**为什么不用自动恢复？**
- 用户可能想从头开始
- 给用户选择权更友好
- 避免强制恢复造成困惑

**为什么用对话框？**
- 清晰的用户提示
- 用户可以选择
- 显示详细信息（页码、筛选条件）

**为什么有24小时限制？**
- 太久之前的学习进度可能不相关
- 避免频繁打扰用户
- 可以后续添加设置让用户自定义

**为什么延迟500ms检查？**
- 避免与URL参数恢复冲突
- 让页面先完成URL参数的恢复
- 提升用户体验（不会立即弹出对话框）

#### 已知限制

1. **24小时限制**：只有24小时内的学习进度会提示
2. **只在第2页及以后提示**：如果用户只看了第1页，不提示
3. **同一词库同一模式**：不同词库、不同模式之间不恢复
4. **筛选保存是客户端的**：页面、状态筛选保存，但单词列表本身不保存

#### 文件修改

| 文件 | 改动内容 | 代码量 |
|------|---------|--------|
| `src/lib/resumeState.ts` | 添加 `getResumeState` 函数 | ~40行 |
| `src/components/BookDetailPageClient.tsx` | 对话框UI + 恢复逻辑 | ~100行 |
| `PRD.md` | 文档更新 | - |

**总计**：约140行新增代码

---

### 2026-01-07 - "继续学习"状态保存功能 ✅

#### 功能概述
实现智能学习状态保存和恢复，让用户可以无缝继续上次的学习进度。支持4个学习模式的状态记忆。

#### 实现场景

**1. 单词列表浏览**
- **保存时机**：
  - 用户改变筛选条件（主题、场景、状态）
  - 用户翻页
  - 用户离开页面（点击返回/关闭浏览器）
- **保存内容**：
  - 当前筛选条件（theme, scenario, status）
  - 当前页码
- **恢复逻辑**：
  - 首页"继续学习"跳转到 `/library/{bookId}?theme=xxx&scenario=xxx&page=xxx`
  - 自动应用筛选条件
  - 自动跳转到对应页码

**2. 卡片背单词**
- **保存时机**：
  - 每次切换到下一个卡片
  - 用户离开页面
- **保存内容**：
  - 当前卡片索引
  - 总单词数
- **恢复逻辑**：
  - 首页"继续学习"跳转到 `/study/{bookId}/flashcards?index=15`
  - 直接显示第15个单词
  - 提示："继续从第16个单词学习"

**3. 听写模式**（原理同卡片模式）
- **保存时机**：
  - 每次提交答案后
  - 每次跳过后
  - 用户离开页面
- **保存内容**：
  - 当前单词索引
  - 总单词数
- **恢复逻辑**：
  - 跳转到 `/study/{bookId}/dictation?index=8`
  - 从第8个词继续听写

**4. 消消乐**（Phase 2，待实现）
- 保存游戏局状态
- 保存单词列表

#### 技术实现

**数据库层**（`supabase/migrations/20260107_add_resume_state.sql`）
```sql
ALTER TABLE user_book_preferences
ADD COLUMN IF NOT EXISTS last_resume_state JSONB DEFAULT '{}'::jsonb;
```

**数据结构**
```typescript
interface ResumeState {
  mode: 'word-list' | 'flashcards' | 'dictation' | 'match-game'
  bookId: string
  bookTitle?: string
  updatedAt: number
  context?: {
    // word-list
    filters?: { theme?: string; scenario?: string; status?: string }
    page?: number

    // flashcards/dictation
    index?: number
    totalWords?: number

    // match-game
    sessionId?: string
    wordIds?: string[]
  }
}
```

**工具函数**（`src/lib/resumeState.ts`）
```typescript
// 保存学习状态
export async function saveResumeState(
  bookId: string,
  mode: ResumeMode,
  context: ResumeState['context']
)

// 读取学习状态
export async function getResumeState(bookId: string): Promise<ResumeState | null>
```

**首页智能跳转**（`src/app/page.tsx`）
```typescript
// 根据不同模式生成不同的跳转URL
if (resumeState?.mode === 'word-list') {
  // 带筛选参数和页码
  continueURL = `/library/${bookId}?theme=${theme}&page=${page}`
} else if (resumeState?.mode === 'flashcards') {
  // 带卡片索引
  continueURL = `/study/${bookId}/flashcards?index=${index}`
} else if (resumeState?.mode === 'dictation') {
  // 带单词索引
  continueURL = `/study/${bookId}/dictation?index=${index}`
}
```

**单词列表状态保存**（`src/components/BookDetailPageClient.tsx`）
```typescript
// 当筛选条件或页码改变时保存
useEffect(() => {
  const timeoutId = setTimeout(() => {
    saveResumeState(book.id, 'word-list', {
      filters: { theme: selectedTheme, scenario: selectedScene, status: statusFilter },
      page: currentPage
    })
  }, 500) // 防抖，避免频繁保存

  return () => clearTimeout(timeoutId)
}, [selectedTheme, selectedScene, statusFilter, currentPage])

// 页面卸载时保存
useEffect(() => {
  const handleBeforeUnload = () => {
    saveResumeState(book.id, 'word-list', { /* ... */ })
  }

  window.addEventListener('beforeunload', handleBeforeUnload)
  return () => window.removeEventListener('beforeunload', handleBeforeUnload)
}, [/* dependencies */])
```

**卡片背单词状态保存**（`src/app/study/[bookId]/flashcards/page.tsx`）
```typescript
// 恢复位置
const indexParam = searchParams.get('index')
if (indexParam) {
  const restoredIndex = parseInt(indexParam)
  setCurrentIndex(restoredIndex)
}

// 切换卡片时保存
setTimeout(() => {
  if (currentIndex < words.length - 1) {
    const nextIndex = currentIndex + 1
    setCurrentIndex(nextIndex)

    // 保存进度
    saveResumeState(bookId, 'flashcards', {
      index: nextIndex,
      totalWords: words.length
    })
  }
}, 200)

// 页面卸载时保存
useEffect(() => {
  const handleBeforeUnload = () => {
    saveResumeState(bookId, 'flashcards', {
      index: currentIndex,
      totalWords: words.length
    })
  }

  window.addEventListener('beforeunload', handleBeforeUnload)
  return () => window.removeEventListener('beforeunload', handleBeforeUnload)
}, [bookId, currentIndex, words.length])
```

#### 用户体验亮点

**1. 无感知保存**
- 用户正常学习过程中自动保存
- 不需要手动点击"保存"按钮
- 使用防抖技术避免频繁写入

**2. 智能恢复**
- 点击"继续学习"直接回到上次位置
- 自动应用筛选条件
- 显示友好提示

**3. 优雅降级**
- 如果单词列表变化，回到第一个单词
- 如果书籍被删除，回到首页
- 不会报错或白屏

#### 边界情况处理

**Q1: 用户快速切换怎么办？**
```typescript
// 使用防抖，500ms内只保存一次
const timeoutId = setTimeout(() => {
  saveResumeState(...)
}, 500)
```

**Q2: 用户返回后又重新进入？**
```
if (用户点击"继续学习") {
  从上次位置继续 ✅
}
else if (用户主动进入某个模式) {
  从头开始（这是用户的选择）✅
}
```

**Q3: 页面刷新时怎么办？**
```typescript
// beforeunload 事件会触发，保存当前状态
window.addEventListener('beforeunload', handleBeforeUnload)
```

#### 代码改动总结

| 模块 | 文件 | 改动内容 | 代码量 |
|------|------|---------|--------|
| **基础设施** | | | |
| 数据库迁移 | `supabase/migrations/20260107_add_resume_state.sql` | 添加 JSONB 字段 | ~10行 |
| 工具函数 | `src/lib/resumeState.ts` | saveResumeState, getResumeState | ~70行 |
| API改造 | `src/app/api/user-preferences/route.ts` | 支持 JSON 字段读写 | ~10行 |
| **各模式** | | | |
| 单词列表 | `src/components/BookDetailPageClient.tsx` | 状态保存+恢复逻辑 | ~40行 |
| 卡片模式 | `src/app/study/[bookId]/flashcards/page.tsx` | 索引保存+恢复 | ~30行 |
| 首页 | `src/app/page.tsx` | 智能跳转逻辑 | ~40行 |

**总计：**
- 代码量：~200行
- 开发时间：约4小时

#### 数据库迁移步骤

**重要：请在 Supabase SQL Editor 中执行以下SQL**

```sql
-- 添加学习状态恢复字段
ALTER TABLE user_book_preferences
ADD COLUMN IF NOT EXISTS last_resume_state JSONB DEFAULT '{}'::jsonb;

-- 添加注释
COMMENT ON COLUMN user_book_preferences.last_resume_state IS '用户最后的学习状态，用于恢复学习位置';
```

详细说明请参考：`MIGRATION_INSTRUCTIONS.md`

#### 测试验证

**测试场景1：单词列表**
1. 打开词书详情页
2. 筛选：主题=旅游，状态=未标注
3. 翻到第3页
4. 点击返回，回到首页
5. 点击"继续学习"
6. ✅ 应该自动应用筛选，显示第3页

**测试场景2：卡片背单词**
1. 进入卡片背单词模式
2. 学到第15个单词
3. 点击返回，回到首页
4. 点击"继续学习"
5. ✅ 应该直接显示第15个单词

**测试场景3：听写模式**
1. 进入听写模式
2. 听写到第8个词
3. 点击返回，回到首页
4. 点击"继续学习"
5. ✅ 应该从第8个词继续听写

#### 未来优化方向

**Phase 2 功能**（待实现）：
1. 听写模式状态保存（同卡片模式）
2. 消消乐游戏局恢复
3. 滚动位置记忆

**可选增强**：
1. 显示详细统计（正确率、用时等）
2. 智能建议（正确率低时建议先复习）
3. 历史记录查看（可以回到上周的学习状态）



### 2026-01-07 - 错题本功能完善 ✅

**需求**: 为错题本添加筛选、排序、统计功能，提高学习效率

**实现内容**:

**1. 统计卡片** (`src/components/MistakesClient.tsx`)
- ✅ 顶部显示3个统计卡片 **[修订]**
  - 待复习总数
  - 不认识单词数（红色）
  - 模糊单词数（黄色）
- 实时统计，动态更新

**2. 筛选功能**
- ✅ **按词书筛选**:
  - 下拉菜单选择词书
  - 显示每个词书的单词数量
  - 支持查看全部或单一词书
- ✅ **按状态筛选**:
  - 全部 / 不认识 / 模糊 **[修订]**
  - 显示每个状态的单词数量

**3. 排序功能**
- ✅ **默认排序**: 按添加顺序
- ✅ **按词书排序**: 词书名称字母顺序
- ✅ **按状态排序**: 不认识 → 模糊 **[修订]**

**4. UI优化**
- ✅ 筛选栏显示当前结果数
- ✅ 无结果时的友好提示
- ✅ 响应式设计，支持移动端

**文件修改**:
- `src/components/MistakesClient.tsx` - 添加筛选、排序、统计功能 (~100行)

**使用场景**:
1. 用户进入错题本，立即看到统计概览
2. 想重点复习某个词书 → 按词书筛选
3. 想先攻克"不认识"的单词 → 按状态筛选
4. 想按词书系统复习 → 按词书排序

**用户体验提升**:
- ✅ 一目了然的统计信息
- ✅ 灵活的筛选和排序
- ✅ 更高效的学习路径

---

### 2026-01-07 - 错题本数据范围修订 ✅

**背景**: 原PRD设计错题本包括 `new`（未标注）状态，但根据实际需求需要调整

**修订内容**:

**1. 数据源调整**
- **修订前**: `status IN ('unknown', 'fuzzy', 'new')`
- **修订后**: `status IN ('unknown', 'fuzzy')`
- **变更原因**:
  - 错题本应聚焦于"真正的错题"（用户明确标记过的单词）
  - `new` 状态的单词数量通常很多（可能数百个），会稀释错题本的焦点
  - 用户需要的是重点难点复习工具，而不是"所有未学单词列表"

**2. 设计理念更新**
- **定位调整**: 从"需要学习的单词" → "需要复习的错题"
- **职责分离**:
  - **练习模式**：学习未标注的单词（`new` 状态）
  - **错题本**：复习已标记的错题（`unknown` + `fuzzy`）

**3. 影响范围**
- **后端查询**: `src/app/mistakes/page.tsx:38` - 数据库查询修改
- **前端统计**: `src/components/MistakesClient.tsx:53` - 统计卡片从4个减少到3个
- **筛选选项**: 移除"未标注"筛选选项
- **排序逻辑**: 移除 `new` 状态的排序

**4. 用户价值**
- ✅ 错题本更聚焦，避免被大量未标注单词淹没
- ✅ 用户可以专注于真正的难点复习
- ✅ 符合"错题本"的字面含义和用户期望

**文件修改**:
- `PRD.md` - 更新错题本定义（第777-778行、第936-948行、第979-981行）
- `src/app/mistakes/page.tsx` - 查询条件修改
- `src/components/MistakesClient.tsx` - 统计和筛选逻辑修改

**决策记录**:
- 决策时间: 2026-01-07
- 决策理由: 用户体验优先，错题本应聚焦于真正的错题复习
- 影响评估: 正面，提升错题本的实用性和聚焦度

---

### 2026-01-07 - 权限系统设计与PRD更新 ✅

**背景**: 系统需要引入套餐化的权限管理机制，通过邀请码实现用户权限控制

**更新内容**:

**1. 注册流程权限继承** (第3.1.1节)
- 用户通过邀请码注册时自动继承套餐权限
- 权限类型：
  - 功能权限：消消乐、卡片背单词、自定义单词书、听写训练等
  - 单词书权限：可访问的单词书列表
  - 权限有效期：从注册日期开始计算
- 权限验证机制：
  - 登录时实时检查权限是否过期
  - 过期后显示友好提示
  - 权限不足的功能显示升级提示

**2. 权限控制实现** (第10.4节)
- **功能权限验证**：
  - 系统实时检查用户的功能权限
  - 无权限时显示友好提示并引导联系管理员
  - 权限过期后自动禁用相关功能

- **单词书权限过滤**：
  - 单词书列表仅显示用户有权限访问的单词书
  - 练习模式仅使用用户有权限的单词
  - 权限为"全部"时显示所有单词书

- **权限过期处理**：
  - 登录时检查权限是否过期
  - 过期用户在所有页面显示提示横幅
  - 提供联系管理员续费的入口

**3. 前端实现要点**
- 权限验证在客户端和服务器端双重验证
- 权限信息存储在 `users` 表：
  - `feature_permissions`: 功能权限数组
  - `book_permissions`: 单词书权限数组
  - `permission_expires_at`: 权限到期时间
- 前端根据权限动态显示/隐藏功能模块

**4. 用户体验优化**
- 权限不足时友好提示，不直接报错
- 过期提醒横幅样式统一
- 提供清晰的升级指引

**影响范围**:
- 注册流程：增加邀请码校验和权限继承逻辑
- 单词书列表：增加权限过滤
- 练习模式：增加权限验证
- 所有功能页面：增加权限检查

**文件更新**:
- `PRD.md` - 更新版本至 v3.3.0，添加权限控制章节

---

### 2026-01-14 - 翻页体验优化：渐进式渲染 [计划中]

**需求背景**：当前翻页时一次性渲染21个单词卡片，导致首屏渲染慢，页面卡顿。需要优化为渐进式渲染，提升用户体验。

**优化内容**：

**1. PC/Pad横屏模式**：
- ✅ 点击翻页按钮后立即显示12个骨架屏卡片
- ✅ 数据返回后（21条已加载），只渲染前12个真实卡片
- ✅ 用户向下滚动时，使用Intersection Observer逐步显示剩余9个卡片
- ✅ 每次滚动到倒数第2个卡片时，显示下一批2个卡片
- ✅ 直到显示全部21个卡片或用户看到翻页按钮

**2. 竖屏模式**：
- ✅ 一行显示2个单词（保持不变）
- ✅ 卡片高度降低约20%（优化竖屏空间）
- ✅ 点击翻页后显示6个骨架屏卡片
- ✅ 数据返回后，只渲染前6个真实卡片
- ✅ 用户下滑时，使用Intersection Observer逐步显示剩余15个卡片
- ✅ 每次滚动到倒数第2个卡片时，显示下一批2个卡片
- ✅ 直到显示全部21个卡片或用户看到翻页按钮

**3. 技术实现要点**：
- ✅ 使用Intersection Observer API（性能优于scroll事件）
- ✅ 骨架屏数量与初始渲染数量一致
- ✅ 数据已全部加载，只是延迟渲染DOM节点
- ✅ 渐进式显示时保持平滑过渡（无闪烁）

**4. 性能优势**：
- ✅ 首屏渲染快（只渲染可见卡片）
- ✅ 页面不卡顿（DOM节点分批创建）
- ✅ 滚动流畅（Intersection Observer性能）
- ✅ 用户无感知延迟（数据已预加载）

**文件修改计划**：
- `src/components/BookDetailPageClient.tsx` - 添加渐进式渲染逻辑 (~150行)
- `src/components/WordList.tsx` - 支持可控的渐进式渲染 (~50行)
- `src/components/VocabularyCard.tsx` - 降低竖屏卡片高度 (~20行)
- `PRD.md` - 更新需求文档（本节）

**总计**：
- 代码量：~220行新增
- 开发时间：约4小时

---

### 2026-01-12 - 词库分类与智能排序优化 ✅

**需求**: 优化词库分类体系，增加智能排序功能，提升大学生和研究生用户的体验

**优化内容**:

**1. 重新设计分类体系** (从2个分类扩展到6个分类)
- ✅ **📌 热门推荐**（3个）：考研、IELTS、TOEFL
  - 设计理由：针对小红书平台主要用户群（大学生和研究生）
  - 考研是国内研究生必备
  - IELTS/TOEFL是出国留学必备
- ✅ **🎓 大学教材**（2个）：CET-4、CET-6
  - 大学英语四六级考试
  - 虽然是考试，但属于大学课程体系
- ✅ **🇨🇳 国内考试**（3个）：专业英语四级、专业英语八级、PETS3
- ✅ **🌍 国外考试**（8个）：GRE、SAT、GMAT、BEC、FCE、PET、PTE、KET
- ✅ **📚 K12教材**（12个）：小学、初中、高中同步教材

**2. 智能排序策略**
- ✅ **优先级1**：最近访问优先（基于 `last_accessed_at`）
  - 用户最近访问的词库排在最前面
  - 体现"最近"可能性，快速回到常用词库
- ✅ **优先级2**：热门词库优先
  - 按热门度排序：考研 > IELTS > TOEFL > CET-4 > CET-6
  - 热门词库排在非热门词库前面
- ✅ **优先级3**：其他按单词数量降序
  - 词数多的词库排在前面（通常内容更丰富）

**3. UI/UX优化**
- ✅ 标签增加emoji图标，更直观生动
- ✅ 使用 `flex-wrap` 支持移动端换行
- ✅ 调整padding适应更多标签（`px-3 py-2`）
- ✅ 保持黑白配色简洁设计
- ✅ 词库卡片显示新分类标签（替代原有的"考试/场景/教材"）
- ✅ 最近访问的前6个词库显示金黄色"最近"标签

**4. 技术实现**
- ✅ 使用 `useMemo` 优化排序性能
- ✅ 排序函数封装为 `sortBooks`
- ✅ 热门词库列表 `HOT_BOOKS` 可配置
- ✅ 支持最近访问字段 `last_accessed_at`
- ✅ 添加 `getCategoryLabel` 函数生成分类标签
- ✅ 添加 `getRecentBookIds` 函数获取最近访问的前6个词库
- ✅ 词库卡片传递 `categoryLabel` 和 `isRecent` 属性

**文件修改**:
- `src/components/FilterableBookGrid.tsx` - 更新分类体系和排序逻辑 (~200行)
- `src/components/BookCard.tsx` - 使用新的分类标签，支持"最近"标签 (~15行修改)
- `src/app/library/page.tsx` - 添加 `last_accessed_at` 字段传递 (~1行修改)
- `PRD.md` - 更新分类和排序说明

**用户体验提升**:
- ✅ 热门词库一目了然，快速找到考研、四六级
- ✅ 最近访问的词库优先，减少重复查找
- ✅ 分类更细致（6个分类），提升查找效率
- ✅ 针对目标用户群（大学生/研究生）优化
- ✅ 词库卡片标签更准确（显示真实的分类，而非"考试/场景/教材"）
- ✅ "最近"标签让用户一眼识别常用词库

**热门词库设计理念**:
```typescript
// 针对小红书平台主要用户群（大学生和研究生）
const HOT_BOOKS = [
  '考研',      // 国内研究生必备
  'IELTS',    // 雅思（出国留学）
  'TOEFL',    // 托福（出国留学）
  'CET-4',    // 大学四级（热门度次之）
  'CET-6',    // 大学六级
]
```

**智能排序伪代码**:
```
function sortBooks(books):
  sort books by:
    1. if has last_accessed_at:
         最近访问的优先（按时间降序）
    2. else if is in HOT_BOOKS:
         热门词库优先（按热门度排序）
    3. else:
         按单词数量降序（词数多的在前）
```

**未来优化方向**:
- 根据实际访问数据动态调整热门词库列表
- 可添加搜索功能，支持模糊搜索
- 可支持自定义排序（用户选择排序方式）
- 可添加收藏功能，收藏的词库优先显示

---

### 2026-01-12 - "最近访问"功能统一性修复 ✅

**背景**: 代码审查发现"最近访问"功能在三处实现不一致，需要统一修复

**问题诊断**:
1. **首页 "最近学习" 卡片**: `limit(1)` - 只取1个
2. **首页 BookLibrary "最近" Tab**: `/api/recent-books` - `limit(8)` - 取8个
3. **系统词库列表 "最近" 标记**: `limit(6)` - 取6个

**问题影响**:
- 数据量不一致导致用户体验混乱
- 维护困难（需要同时修改3处）
- 不符合PRD设计规范

**解决方案**:
统一所有"最近访问"查询为 `limit(6)`，确保数据源和数量一致

**技术实现**:

**1. 首页 page.tsx (第60-67行)**
```diff
  // 获取用户最近访问的词库（优先使用 last_accessed_at）
  const { data: recentPrefs, error: prefsError } = await supabase
    .from('user_book_preferences')
    .select('book_id, last_accessed_at')
    .eq('user_id', user.id)
    .not('last_accessed_at', 'is', null)
    .order('last_accessed_at', { ascending: false })
-   .limit(1)
+   .limit(6)
```

**2. API /api/recent-books/route.ts (第5、22行)**
```diff
- // GET - 获取用户最近访问的词库（最多8个）
+ // GET - 获取用户最近访问的词库（最多6个）
  export async function GET() {
    ...
-   // 第一步：获取用户最近访问的8个词库ID
+   // 第一步：获取用户最近访问的6个词库ID
    const { data: recentPrefs, error: prefsError } = await supabase
      .from('user_book_preferences')
      .select('book_id, last_accessed_at')
      .eq('user_id', user.id)
      .not('last_accessed_at', 'is', null)
      .order('last_accessed_at', { ascending: false })
-     .limit(8)
+     .limit(6)
```

**3. 系统词库列表 library/page.tsx**
无需修改，已经是 `limit(6)` ✅

**测试验证**:

**测试脚本**: `test-recent-consistency.mjs`

**测试结果**:
```
📊 测试总结:
══════════════════════════════════════════════════
✅ 首页 "最近学习": limit(6)
✅ API /api/recent-books: limit(6)
✅ 系统词库列表: limit(6)
✅ 数据源一致: user_book_preferences
✅ 排序一致: last_accessed_at DESC
══════════════════════════════════════════════════
```

**一致性验证**: ✅ 三处查询结果完全一致

**统一性保证**:
- ✅ 数据源: `user_book_preferences` 表
- ✅ 排序: `last_accessed_at` DESC
- ✅ 数量: `limit(6)`
- ✅ 用户过滤: `user_id = current_user`

**用户体验提升**:
- ✅ 首页"最近学习"显示真实的最近访问（最多6个中的第1个）
- ✅ 首页"最近"Tab显示一致的6个词库
- ✅ 系统词库列表的"最近"标记显示相同的6个
- ✅ 三处数据完全同步，不会出现不一致的情况

**代码改动总结**:

| 模块 | 文件 | 改动内容 | 代码量 |
|------|------|---------|--------|
| **首页** | `src/app/page.tsx` | `limit(1)` → `limit(6)` | 1行 |
| **API** | `src/app/api/recent-books/route.ts` | `limit(8)` → `limit(6)` + 注释 | 2行 |
| **测试** | `test-recent-consistency.mjs` | 一致性测试脚本 | 新增 |

**总计**:
- 代码修改: 3行
- 测试脚本: 新增
- 开发时间: 约30分钟

**文件更新**:
- `PRD.md` - 更新"最近访问功能"描述，添加"统一性保证"说明

**未来优化方向**:
- 可考虑将limit(6)提取为环境变量或常量，便于统一修改
- 可添加数据库索引 `(user_id, last_accessed_at DESC)` 进一步优化性能

---

### 2026-01-16 - 打字练习（肌肉训练）功能新增 ✅

#### 功能概述
新增第四大核心学习模式 - 打字练习（肌肉训练），与卡片背单词、听写模式、消消乐并列，作为一级功能在全站首页左侧导航展示。

#### 核心特性

**1. 功能定位**
- **产品定位**：通过键盘输入单词强化肌肉记忆，加深单词拼写记忆
- **目标用户**：备考用户、进阶用户、视觉学习者
- **功能入口**：
  - 首页左侧导航：`<Dumbbell />` + "肌肉训练"文字
  - 词书详情页：第4个练习模式卡片（橙色）
  - 路由结构：`/typing` → `/typing/[bookId]` → `/typing/[bookId]/practice`

**2. 核心功能**

**实时打字检测**：
- 显示当前单词和中文释义
- 用户键盘输入实时检测
- 正确输入：绿色高亮显示
- 错误输入：红色高亮显示
- 连续错误 3 次：自动删除整词，重新开始
- 单词完成：自动跳转下一个单词（300ms 延迟）

**TTS 发音系统**：
- 使用浏览器原生 Web Speech API
- 支持 3 种发音方案：美式英语、英式英语、自动选择
- 单词打对后自动触发发音
- 可调节音量（0-100）和语速（0.5-2.0）

**循环练习**：
- 支持 5 种循环模式：不循环、3次、5次、9次、无限循环
- 显示当前单词完成次数（如：2/3）
- 达到循环次数后自动跳转

**错题本系统**：
- 实时记录错误单词到 localStorage
- 显示错误次数统计
- 连续错误 4 次显示"跳过"按钮
- 支持错题专项练习模式
- 无缝对接主项目错题本系统

**学习模式切换**：
- 默写模式 (Ctrl+M)：隐藏释义，只显示单词
- 释义显示切换 (Ctrl+Shift+V)：显示/隐藏中文释义

**数据持久化**：
- 自动保存学习进度到 localStorage
- 页面刷新后自动恢复所有状态
- 练习结束时同步到服务器数据库：
  - 错题同步到 mistakes 表
  - 进度更新到 word_progress 表
  - 记录保存到 learning_records 表

**3. 重要功能（P1）**

**统计面板**：
- 练习时长、输入字符数、WPM、正确率、完成单词数

**设置面板**：
- 发音设置：单词自动发音、音量、语速、发音方案
- 音效设置：按键音开关、音量、类型（机械/静音/柔和）
- 显示设置：外语字体大小、中文字体大小、深色模式
- 循环设置：循环次数选择（1/3/5/9/无限）

**快捷键支持**：
- `Enter`：开始/继续练习
- `Ctrl + M`：切换默写模式
- `Ctrl + Shift + V`：切换释义显示
- `Ctrl + J`：显示快捷键提示
- `Esc`：跳过当前单词（连续错误 4 次后）
- `Backspace`：删除上一个字符

**4. UI 设计**

**词书列表页**：
- 网格布局（3列），响应式
- 卡片样式：圆角、阴影、hover效果
- 显示内容：词书封面、标题、描述、单词总数

**范围选择页**：
- 2列网格（桌面端），1列（移动端）
- 5种范围卡片：未标注（灰）、已认识（绿）、模糊（黄）、不认识（红）、全部单词（蓝）

**游戏界面**：
- 单词显示区：大字体（48-72px）
- 中文释义区：中字体（16-24px）
- 实时反馈：绿色/红色高亮
- 循环计数显示：如 2/3
- 右上角控制按钮：11个功能区

**5. 成功指标**

**功能指标**：
- 打字练习功能可用性：100%
- 数据同步成功率：> 95%
- 页面加载速度：< 2秒
- TTS 发音响应延迟：< 100ms

**用户指标**（预期）：
- 日活跃用户（DAU）：> 100（上线1个月）
- 平均练习时长：> 10分钟/次
- 用户留存率：> 30%（7日留存）
- 错题练习使用率：> 20%

**技术指标**：
- 页面 FCP：< 1.5s
- 页面 TTI：< 3s
- 错误率：< 0.1%

**6. 文档更新**

| 文件 | 更新内容 |
|------|---------|
| `PRD.md` | 新增 6.4 章节：打字练习（肌肉训练）完整功能说明 |
| `PRD.md` | 更新功能架构图，添加打字练习到 [L3] 练习系统 |
| `PRD.md` | 更新 5.4 练习模式区域，添加第4个练习模式卡片 |
| `PRD.md` | 版本号更新：v3.4.0 → v3.5.0 |
| `TYPING_PRACTICE_PRD.md` | 独立打字练习功能PRD文档 |

**7. 后续迭代规划**

**V1.1（上线后1个月）**：
- 添加章节选择功能
- 添加主题/场景筛选
- 优化错题本专项练习
- 添加学习报告（周报/月报）

**V1.2（上线后2-3个月）**：
- 添加 multiplayer 模式（多人竞技）
- 添加成就系统
- 添加学习提醒
- 支持自定义词库

**V2.0（长期规划）**：
- AI 智能推荐练习单词
- 自适应难度调整
- 跨设备同步（手机/平板/电脑）
- 离线应用支持

**8. 风险与挑战**

**技术风险**：
- TTS 兼容性问题：提供降级方案，检测浏览器支持
- 数据同步失败：localStorage 缓存 + 自动重试
- 性能问题（大量单词）：分页加载 + 虚拟滚动

**产品风险**：
- 用户接受度低：A/B 测试，收集用户反馈
- 与现有功能冲突：明确功能定位，避免重复

**9. 开发计划**

预计开发周期：**8-12 天**

**阶段一：基础集成（2-3天）**
- 复制 SageVocab-Practice 核心代码到 `src/app/typing/`
- 创建词书列表页 (`/typing`)
- 创建范围选择页 (`/typing/[bookId]`)
- 修改 `data-loader.ts` 对接主项目 API
- 修改单词列表 API 支持 `scope` 参数

**阶段二：数据同步（2-3天）**
- 创建错题批量同步 API
- 创建进度批量更新 API
- 创建学习记录 API
- 集成数据同步逻辑
- 实现离线缓存机制

**阶段三：体验优化（2-3天）**
- 添加统计面板
- 添加设置面板
- 实现所有快捷键
- 添加加载动画
- 优化移动端体验
- 添加错误处理和用户提示

**阶段四：测试上线（2-3天）**
- 编写测试用例
- 执行功能测试
- 执行性能测试
- 修复发现的 Bug
- 编写用户文档
- 灰度发布
- 全量发布

**10. 参考资料**

- SageVocab-Practice 项目: https://github.com/alf533-real/SageVocab-Practice
- 主项目数据结构文档: 见 `单词相关数据结构总结`
- Next.js 文档: https://nextjs.org/docs
- Web Speech API 文档: https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API

---




# 自定义词库 - 表格编辑功能 PRD

**版本**: v3.5.0
**日期**: 2026-01-15
**作者**: AI Assistant

---

## 1. 功能概述

为自定义词库创建一个**专门的表格编辑视图**，类似 Excel 的管理界面，用于快速浏览、编辑、管理单词。

**核心价值**：
- 📊 **高效管理**：表格视图可以同时显示多个单词的多个字段
- ✏️ **快速编辑**：支持 inline 编辑（直接在表格里修改）
- 🚀 **批量操作**：支持批量删除、批量修改章节
- ⌨️ **键盘导航**：支持键盘快捷键，提升效率
- 👀 **清晰对比**：可以方便地对比多个单词的信息

**设计理念**：
- **分离视图和学习模式**：表格用于管理，卡片用于学习
- **像管理 Excel 一样管理单词**：熟悉、直观、高效
- **仅用于自定义词库**：官方词库只读，不支持编辑

**适用范围**：
- ✅ **自定义词库** (`is_official = false`)：完全支持表格编辑
- ❌ **官方词库** (`is_official = true`)：只显示卡片视图，不支持表格编辑

---

## 2. 视图模式切换

### 2.1 模式切换按钮

**入口位置**：
- 词库详情页顶部，筛选栏下方
- 与"选择练习模式"卡片并排（左侧）

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│  词库详情页                                  │
├─────────────────────────────────────────────┤
│  [筛选栏：主题 | 场景 | 状态 | 章节]         │
│                                             │
│  [📖 学习模式] [📊 编辑模式]  ← 模式切换    │
│                                             │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
│  │卡片背单词│  │听写模式 │  │消消乐   │    │ ← 仅学习模式显示
│  └─────────┘  └─────────┘  └─────────┘    │
└─────────────────────────────────────────────┘
```

**模式切换按钮样式**：
- **学习模式**：
  - 背景：白色 `bg-white`
  - 图标：BookOpen (lucide-react)
  - 文字："学习模式"
  - 选中：酸性绿 `bg-[#B4F416]` + 黑色边框
  - 未选中：灰色 `bg-gray-100` + 灰色边框

- **编辑模式**：
  - 背景：白色 `bg-white`
  - 图标：Table (lucide-react)
  - 文字："编辑模式"
  - 选中：蓝色 `bg-blue-500` + 白色文字
  - 未选中：灰色 `bg-gray-100` + 灰色边框

**布局**：
- 两个按钮并排，间距 `gap-2`
- 按钮大小：`px-6 py-2`
- 字体：`font-semibold text-sm`
- 圆角：`rounded-lg`
- hover效果：`hover:shadow-md`

### 2.2 视图状态管理

**状态存储**：
- URL 参数：`?view=edit` 或 `?view=learn`
- 默认值：`view=learn`（学习模式）

**切换逻辑**：
```typescript
const [viewMode, setViewMode] = useState<'learn' | 'edit'>('learn')

const handleModeChange = (mode: 'learn' | 'edit') => {
  setViewMode(mode)

  // 更新 URL（不刷新页面）
  const url = new URL(window.location.href)
  url.searchParams.set('view', mode)
  window.history.pushState({}, '', url.toString())
}

// 从 URL 读取初始状态
useEffect(() => {
  const view = searchParams.get('view') as 'learn' | 'edit' | null
  if (view === 'learn' || view === 'edit') {
    setViewMode(view)
  }
}, [searchParams])
```

**视图渲染**：
```tsx
{viewMode === 'learn' ? (
  <>
    {/* 练习模式卡片 */}
    <PracticeModeCards />

    {/* 单词卡片列表 */}
    <WordCardList />
  </>
) : (
  <>
    {/* 表格编辑视图 */}
    <WordEditTable />
  </>
)}
```

---

## 3. 表格编辑视图 UI 设计

### 3.1 整体布局

**UI 结构**：
```
┌─────────────────────────────────────────────────────────────────┐
│  词库：我的自定义词库                           [📊 编辑模式]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  [筛选栏：全部章节 ▼ | 全部状态 ▼ | 50词/页 ▼]                 │
│                                                                  │
│  [ + 批量添加单词]  [ 💾 保存所有修改]  [↻ 刷新]                │
│                                                                  │
│  ┌────┬──────────┬────────┬────┬────────────┬──────────┬─────┐│
│  │ ☑️ │ 章节     │ 单词   │音标│ 词性       │ 中文释义 │ 操作││ ← 表头
│  ├────┼──────────┼────────┼────┼────────────┼──────────┼─────┤│
│  │ ☑️ │ 默认章节 │ agenda │/ə││ n名词      │ 议程，日 │ ✏️ 🗑️││ ← 第1行
│  │    │          │        │dʒ  │            │ 程表     │     ││
│  ├────┼──────────┼────────┼────┼────────────┼──────────┼─────┤│
│  │ ☑️ │ 第1章    │compromise│/│  v动词     │ 妥协，   │ ✏️ 🗑️││ ← 第2行
│  │    │          │        │ˈkɒm│            │ 折中     │     ││
│  │    │          │        │mprə│            │          │     ││
│  │    │          │        │maɪz│            │          │     ││
│  │    │          │        │/   │            │          │     ││
│  ├────┼──────────┼────────┼────┼────────────┼──────────┼─────┤│
│  │ ☐  │ 第2章    │ abbreviate │ /ə  │ v动词  │ 缩写，   │ ✏️ 🗑️││ ← 第3行
│  │    │          │        │ˈbri│            │ 缩短     │     ││
│  │    │          │        │:vieɪt/         │          │     ││
│  └────┴──────────┴────────┴────┴────────────┴──────────┴─────┘│
│                                                                  │
│  已选 2 个单词    [🗑️ 批量删除]    [📁 移动到章节...]          │
│                                                                  │
│  ← 上一页    1 / 10    下一页 →                                  │
└─────────────────────────────────────────────────────────────────┘
```

**样式规范**：
- 表格外边框：`border-2 border-gray-300`
- 表头背景：`bg-gray-50`
- 表头字体：`font-bold text-sm text-gray-700`
- 表头边框：`border-b-2 border-gray-400`
- 单元格内边距：`px-3 py-2`
- 单元格字体：`text-sm`
- 行高：`h-16`（固定高度，保持一致性）
- 行边框：`border-b border-gray-200`
- hover效果：`hover:bg-gray-50`

### 3.2 表格列定义

| 列名 | 字段 | 宽度 | 对齐 | 是否可编辑 | 说明 |
|------|------|------|------|-----------|------|
| 选择框 | - | 50px | 居中 | ❌ | 多选checkbox |
| 章节 | `chapter` | 120px | 居左 | ✅ | 下拉选择 |
| 单词 | `word` | 150px | 居左 | ✅ | 文本输入 |
| 音标 | `phonetic` | 150px | 居左 | ✅ | 文本输入 |
| 词性 | `part_of_speech` | 100px | 居中 | ✅ | 下拉选择 |
| 中文释义 | `definition` | 200px | 居左 | ✅ | 文本域 |
| 操作 | - | 100px | 居中 | ❌ | 编辑/删除按钮 |

### 3.3 表头设计

**样式**：
```tsx
<thead className="bg-gray-50">
  <tr>
    <th className="px-3 py-3 text-left w-12">
      <input
        type="checkbox"
        checked={allSelected}
        onChange={handleSelectAll}
        className="w-4 h-4 rounded border-gray-300"
      />
    </th>
    <th className="px-3 py-3 text-left font-bold text-gray-700">章节</th>
    <th className="px-3 py-3 text-left font-bold text-gray-700">单词</th>
    <th className="px-3 py-3 text-left font-bold text-gray-700">音标</th>
    <th className="px-3 py-3 text-center font-bold text-gray-700">词性</th>
    <th className="px-3 py-3 text-left font-bold text-gray-700">中文释义</th>
    <th className="px-3 py-3 text-center font-bold text-gray-700">操作</th>
  </tr>
</thead>
```

### 3.4 表格行设计

**普通行**：
```tsx
<tr className="border-b border-gray-200 hover:bg-gray-50">
  {/* 选择框 */}
  <td className="px-3 py-2">
    <input type="checkbox" className="w-4 h-4" />
  </td>

  {/* 章节 - 可编辑 */}
  <td className="px-3 py-2">
    <select className="w-full px-2 py-1 border border-gray-300 rounded text-sm">
      <option>默认章节</option>
      <option>第1章</option>
      <option>第2章</option>
    </select>
  </td>

  {/* 单词 - 可编辑 */}
  <td className="px-3 py-2">
    <input
      type="text"
      defaultValue="agenda"
      className="w-full px-2 py-1 border border-gray-300 rounded text-sm"
    />
  </td>

  {/* 音标 - 可编辑 */}
  <td className="px-3 py-2">
    <input
      type="text"
      defaultValue="/əˈdʒendə/"
      className="w-full px-2 py-1 border border-gray-300 rounded text-sm font-mono"
    />
  </td>

  {/* 词性 - 可编辑 */}
  <td className="px-3 py-2 text-center">
    <select className="w-full px-2 py-1 border border-gray-300 rounded text-sm">
      <option>n名词</option>
      <option>v动词</option>
      <option>adj形容词</option>
    </select>
  </td>

  {/* 中文释义 - 可编辑 */}
  <td className="px-3 py-2">
    <input
      type="text"
      defaultValue="议程，日程表"
      className="w-full px-2 py-1 border border-gray-300 rounded text-sm"
    />
  </td>

  {/* 操作按钮 */}
  <td className="px-3 py-2 text-center">
    <button className="text-blue-500 hover:text-blue-700 mr-2">✏️</button>
    <button className="text-red-500 hover:text-red-700">🗑️</button>
  </td>
</tr>
```

**编辑模式行**：
```tsx
<tr className="border-b border-blue-200 bg-blue-50">
  {/* 编辑状态 - 所有字段都是输入框 */}
  <td colSpan={6}>
    {/* 完整编辑表单 */}
  </td>
  <td>
    <button className="text-green-500">✓</button>
    <button className="text-gray-500">✗</button>
  </td>
</tr>
```

### 3.5 响应式设计

**PC端（>1024px）**：
- 显示所有列（7列）
- 表格宽度：100%
- 单元格最小宽度：100px
- 横向滚动：`overflow-x-auto`（可选）

**平板端（768px - 1024px）**：
- 隐藏次要列（音标）
- 显示：选择、章节、单词、词性、中文释义、操作（6列）

**移动端（<768px）**：
- 卡片视图替代表格
- 每个单词一个卡片
- 卡片内包含所有字段

---

## 4. Inline 编辑功能

### 4.1 直接在表格里编辑

**交互方式**：
1. 用户点击表格单元格中的输入框
2. 输入框获得焦点，边框变蓝
3. 用户修改内容
4. 用户按回车或点击外部，自动保存

**保存策略**：
- **失去焦点自动保存**：用户点击外部或按Tab，自动保存该字段
- **回车保存**：按回车键，保存当前字段并跳到下一行
- **Esc取消**：按Esc，取消修改，恢复原值

**视觉反馈**：
- 编辑中：蓝色边框 `border-blue-500`
- 已保存：绿色边框闪烁（200ms）
- 保存失败：红色边框 + 错误提示

### 4.2 特殊字段编辑

**章节字段**：
- 使用下拉选择器（`<select>`）
- 选项：当前词库的所有章节
- 选择后自动保存

**词性字段**：
- 使用下拉选择器（`<select>`）
- 选项：预定义的词性列表
- 选择后自动保存

**中文释义字段**：
- 支持多行输入
- 最大长度：500字符
- 显示字符计数：`45/500`

**音标字段**：
- 使用等宽字体：`font-mono`
- 字体颜色：`text-gray-600`
- 可选字段，允许为空

### 4.3 批量编辑

**功能**：选中多个单词，批量修改某个字段

**入口**：
- 表格底部的批量操作栏
- 按钮："批量编辑"

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│  批量编辑（已选10个单词）     [×] 关闭      │
├─────────────────────────────────────────────┤
│                                             │
│  选择要修改的字段 *                         │
│  ┌─────────────────────────────────────┐   │
│  │ 章节 ▼                             │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  新值 *                                     │
│  ┌─────────────────────────────────────┐   │
│  │ 第2章                              │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │           批量修改                  │   │
│  └─────────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
```

**支持的字段**：
- 章节：批量移动到指定章节
- 词性：批量修改词性
- 状态：批量标记为"认识"/"模糊"/"不认识"（可选）

---

## 5. 批量操作功能

### 5.1 多选功能

**全选**：
- 点击表头的checkbox，全选当前页的所有单词
- 再次点击，取消全选

**单选**：
- 点击每行的checkbox，选中/取消选中该行

**选中状态**：
- 选中的行背景变为淡蓝色：`bg-blue-50`
- 表格底部显示选中数量："已选 X 个单词"

### 5.2 批量删除

**入口**：
- 表格底部的批量操作栏
- 按钮："批量删除"（红色文字）
- 条件：至少选中1个单词

**确认流程**：
1. 用户点击"批量删除"按钮
2. 弹出确认对话框：
   ```
   ┌─────────────────────────────────┐
   │  ⚠️ 确认批量删除？              │
   │                                 │
   │  即将删除 10 个单词              │
   │  此操作不可恢复！               │
   │                                 │
   │  [取消]        [确认删除]        │
   └─────────────────────────────────┘
   ```
3. 用户确认后，调用 API：`DELETE /api/words/batch`
4. 删除成功：
   - 从表格中移除已删除的行（淡出动画）
   - 显示成功提示："已删除 X 个单词"
   - 更新词库的单词总数
   - 刷新表格数据

### 5.3 批量移动章节

**入口**：
- 表格底部的批量操作栏
- 按钮："移动到章节..."（蓝色文字）
- 条件：至少选中1个单词

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│  📁 移动到章节                  [×] 关闭    │
├─────────────────────────────────────────────┤
│                                             │
│  已选 10 个单词，请选择目标章节：            │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 第2章 进阶词汇 ▼                   │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │           确认移动                  │   │
│  └─────────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
```

**逻辑**：
1. 用户选择目标章节
2. 点击"确认移动"
3. 调用 API：`PUT /api/words/batch`
4. 更新成功：
   - 刷新表格数据
   - 显示成功提示："已移动 X 个单词到「第2章」"

### 5.4 批量导入单词

**入口**：
- 表格顶部的操作栏
- 按钮："批量添加单词"（绿色背景）

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│  📥 批量添加单词                [×] 关闭    │
├─────────────────────────────────────────────┤
│                                             │
│  选择章节 *                                 │
│  ┌─────────────────────────────────────┐   │
│  │ 默认章节 ▼                         │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  输入单词（每行一个，支持逗号、空格分隔）    │
│  ┌─────────────────────────────────────┐   │
│  │ apple                               │   │
│  │ banana                              │   │
│  │ orange                              │   │
│  │                                     │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  💡 支持：英文单词，每行一个或用逗号分隔   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │    智能识别并导入                  │   │
│  └─────────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
```

**逻辑**：
1. 用户选择章节
2. 用户输入单词列表
3. 点击"智能识别并导入"
4. 调用智能导入 API：`POST /api/smart-import`
5. 导入成功：
   - 刷新表格数据
   - 显示成功提示："已导入 X 个单词"
   - 跳转到导入结果页（可选）

---

## 6. 筛选和搜索功能

### 6.1 表格筛选栏

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│ [全部章节 ▼] [全部状态 ▼] [50词/页 ▼]     │
└─────────────────────────────────────────────┘
```

**筛选器**：
1. **章节筛选**：
   - 选项：全部章节、第1章、第2章、...
   - 默认：全部章节
   - 选择后：只显示该章节的单词

2. **状态筛选**：
   - 选项：全部状态、未标注、认识、模糊、不认识
   - 默认：全部状态
   - 选择后：只显示该状态的单词

3. **每页显示数量**：
   - 选项：10词/页、20词/页、50词/页、100词/页
   - 默认：50词/页
   - 选择后：更新分页

### 6.2 搜索功能

**入口**：
- 表格顶部，筛选栏右侧
- 搜索框 + 搜索按钮

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│  🔍 搜索单词...              [搜索]         │
└─────────────────────────────────────────────┘
```

**搜索范围**：
- 单词拼写（英文）
- 中文释义
- 英文释义（可选）

**搜索逻辑**：
- 实时搜索：输入时立即搜索（debounce 300ms）
- 模糊匹配：包含关键词即可
- 大小写不敏感

**结果显示**：
- 显示匹配的单词
- 高亮关键词（可选）

---

## 7. 分页功能

### 7.1 分页控件

**UI 设计**：
```
┌─────────────────────────────────────────────┐
│  ← 上一页    第 1 / 10 页    下一页 →      │
│  共 500 个单词                               │
└─────────────────────────────────────────────┘
```

**样式**：
- 居中对齐
- 页码：`text-gray-700 font-semibold`
- 按钮：clay-button 样式
- 禁用状态：`opacity-50 cursor-not-allowed`

### 7.2 分页逻辑

**数据加载**：
- 使用 API 分页：`GET /api/books/{bookId}/words?page={page}&limit={limit}`
- 每次只加载当前页的数据
- 缓存已加载的页面数据

**页面跳转**：
- 点击"上一页"：加载上一页
- 点击"下一页"：加载下一页
- 点击页码：跳转到指定页（可选）

**总数统计**：
- 显示总单词数：`共 {total} 个单词`
- 显示当前页范围：`第 {start}-{end} 个`

---

## 8. API 接口设计

### 8.1 获取单词列表（分页）

**接口**：`GET /api/books/{bookId}/words`

**查询参数**：
```
?page=1
&limit=50
&chapter=chapter-id  // 可选
&status=known        // 可选
&search=agenda       // 可选
```

**响应**：
```json
{
  "success": true,
  "data": {
    "words": [
      {
        "id": "word-uuid-1",
        "word": "agenda",
        "phonetic": "/əˈdʒendə/",
        "part_of_speech": "n名词",
        "definition": "议程，日程表",
        "definition_en": "A list of items to be discussed at a meeting",
        "chapter": "默认章节",
        "chapter_id": null,
        "status": "new",
        "order_index": 1
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 50,
      "total": 500,
      "totalPages": 10
    }
  }
}
```

### 8.2 更新单词

**接口**：`PUT /api/words/{wordId}`

**请求体**：
```json
{
  "word": "agenda",
  "phonetic": "/əˈdʒendə/",
  "part_of_speech": "n名词",
  "definition": "议程，日程表",
  "definition_en": "A list of items to be discussed at a meeting",
  "chapter_id": null
}
```

**响应**：
```json
{
  "success": true,
  "data": {
    "id": "word-uuid",
    "word": "agenda",
    "phonetic": "/əˈdʒendə/",
    "part_of_speech": "n名词",
    "definition": "议程，日程表",
    "updated_at": "2026-01-15T10:30:00Z"
  }
}
```

### 8.3 批量更新单词

**接口**：`PUT /api/words/batch`

**请求体**：
```json
{
  "word_ids": ["uuid-1", "uuid-2", "uuid-3"],
  "updates": {
    "chapter_id": "chapter-uuid"
  }
}
```

**响应**：
```json
{
  "success": true,
  "message": "已更新 3 个单词"
}
```

### 8.4 批量删除单词

**接口**：`DELETE /api/words/batch`

**请求体**：
```json
{
  "word_ids": ["uuid-1", "uuid-2", "uuid-3"]
}
```

**响应**：
```json
{
  "success": true,
  "message": "已删除 3 个单词"
}
```

---

## 9. 键盘快捷键

### 9.1 导航快捷键

| 快捷键 | 功能 |
|--------|------|
| `↑` / `↓` | 移动到上一行/下一行 |
| `←` / `→` | 移动到上一列/下一列 |
| `Enter` | 保存当前字段，跳到下一行 |
| `Tab` | 跳到下一个可编辑字段 |
| `Shift + Tab` | 跳到上一个可编辑字段 |
| `Esc` | 取消编辑，恢复原值 |
| `Space` | 选中/取消当前行（checkbox） |

### 9.2 编辑快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl/Cmd + S` | 保存当前行的所有修改 |
| `Ctrl/Cmd + Z` | 撤销上一次修改（可选） |
| `Ctrl/Cmd + Enter` | 保存所有修改并退出编辑模式 |

### 9.3 快捷键提示

**显示位置**：
- 表格底部右侧
- 小字号灰色文字

**UI 设计**：
```
💡 快捷键：↑↓切换行 | Enter保存 | Esc取消
```

---

## 10. 用户体验优化

### 10.1 自动保存提示

**场景**：用户修改字段后，自动保存

**视觉反馈**：
- 保存中：显示loading图标（旋转的圆圈）
- 保存成功：绿色边框闪烁（200ms）
- 保存失败：红色边框 + 错误提示

**Toast 提示**：
```
┌─────────────────────────────────┐
│ ✓ 单词已保存        [撤销]      │
└─────────────────────────────────┘
```

### 10.2 离开提示

**场景**：用户有未保存的修改，试图离开页面

**确认对话框**：
```
┌─────────────────────────────────┐
│  ⚠️ 您有未保存的修改            │
│                                 │
│  确定要离开吗？未保存的修改将丢失│
│                                 │
│  [留在页面]    [离开页面]        │
└─────────────────────────────────┘
```

**触发条件**：
- 点击浏览器返回按钮
- 关闭浏览器标签
- 切换到其他页面

### 10.3 加载状态

**表格加载**：
- 显示骨架屏（Skeleton）
- 每行显示灰色的占位条

**保存加载**：
- 单元格显示loading图标
- 禁用该单元格的输入

### 10.4 错误处理

**网络错误**：
- 显示错误提示："保存失败，请检查网络连接"
- 提供重试按钮

**验证错误**：
- 输入框红色边框
- 下方显示错误文字

---

## 11. 性能优化

### 11.1 虚拟滚动

**场景**：当单词数超过500个时

**实现**：
- 使用 `react-window` 或 `react-virtualized`
- 只渲染可见区域的行
- 滚动时动态加载

### 11.2 防抖搜索

**场景**：搜索输入

**实现**：
- debounce 300ms
- 用户停止输入300ms后才执行搜索

### 11.3 批量保存优化

**场景**：用户快速修改多个字段

**实现**：
- 队列化保存请求
- 每秒最多保存5次
- 合并多次修改为一次请求

---

## 12. 移动端适配

### 12.1 卡片视图

**触发条件**：屏幕宽度 < 768px

**UI 设计**：
```
┌─────────────────────────────────────┐
│  ☑️  第1个单词                      │
│  ┌─────────────────────────────┐   │
│  │ 章节：默认章节               │   │
│  │ 单词：agenda                │   │
│  │ 音标：/əˈdʒendə/            │   │
│  │ 词性：n名词                 │   │
│  │ 释义：议程，日程表          │   │
│  │              [✏️ 编辑] [🗑️ 删除]│   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

**特点**：
- 每个单词一个卡片
- 垂直排列
- 点击卡片进入编辑模式

### 12.2 底部操作栏

**功能**：
- 固定在屏幕底部
- 显示已选数量
- 提供批量操作按钮

---

## 13. 开发计划

### 13.1 Phase 1: 基础表格视图（MVP）

- ✅ 视图模式切换（学习/编辑）
- ✅ 表格布局和样式
- ✅ 显示单词列表
- ✅ 分页功能
- ✅ 基本的筛选（章节、状态）

**工作量**：约3-4天

### 13.2 Phase 2: Inline 编辑

- ✅ 单元格直接编辑
- ✅ 失去焦点自动保存
- ✅ 字段验证
- ✅ 视觉反馈

**工作量**：约2-3天

### 13.3 Phase 3: 批量操作

- ✅ 多选功能
- ✅ 批量删除
- ✅ 批量移动章节
- ✅ 批量导入单词

**工作量**：约2-3天

### 13.4 Phase 4: 高级功能

- ⏸️ 键盘快捷键
- ⏸️ 搜索功能
- ⏸️ 虚拟滚动
- ⏸️ 移动端适配

**工作量**：约2-3天

**总计**：9-13天

---

## 14. 相关文件

### 前端文件

| 文件 | 说明 |
|------|------|
| `src/components/ViewModeToggle.tsx` | 新建：视图模式切换按钮 |
| `src/components/WordEditTable.tsx` | 新建：表格编辑视图主组件 |
| `src/components/WordTableRow.tsx` | 新建：表格行组件 |
| `src/components/WordTableCell.tsx` | 新建：可编辑单元格组件 |
| `src/components/BatchOperationBar.tsx` | 新建：批量操作栏 |
| `src/components/WordCardList.tsx` | 修改：仅在"学习模式"显示 |
| `src/app/api/books/[bookId]/words/route.ts` | 新建：获取单词列表 API |
| `src/app/api/words/[wordId]/route.ts` | 新建：更新/删除单词 API |
| `src/app/api/words/batch/route.ts` | 新建：批量操作 API |

---

## 15. 未来扩展方向

- 导出为 Excel：将词库导出为 .xlsx 文件
- 从 Excel 导入：支持从 Excel 文件批量导入
- 版本历史：记录单词的修改历史，支持回滚
- AI 辅助补全：自动补全释义、例句
- 单词去重：检测并处理重复单词
- 拖拽排序：拖拽行调整单词顺序

---

**文档版本**：v2.0
**最后更新**：2026-01-15

**主要变更**：
- ✅ 改用专门的表格视图，而不是在卡片上编辑
- ✅ 支持 inline 编辑（直接在表格里修改）
- ✅ 添加批量操作功能
- ✅ 添加键盘快捷键支持
- ✅ 分离学习模式和编辑模式
